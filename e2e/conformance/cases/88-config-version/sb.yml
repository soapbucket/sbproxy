# 88 - Removed Config Version: validation must reject origin config_version
# This fixture is intentionally invalid. run-tests.sh invokes `sbproxy validate`
# and asserts that the diagnostic names the removed key.
proxy:
  http_bind_port: 18080

origins:
  "cfgver.test":
    config_version: 2
    action:
      type: proxy
      url: https://test.sbproxy.dev
