{
  "name": "sbproxy-admin-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SBproxy built-in admin dashboard. Vue 3 + Vite single-page app built to ui/dist/ and embedded into the sbproxy binary behind the embed-admin-ui cargo feature.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest",
    "typecheck": "vue-tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "@fontsource/instrument-sans": "^5.2.8",
    "@fontsource/jetbrains-mono": "^5.2.8",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "^5.6.3",
    "vite": "^6.4.2",
    "vitest": "^3.2.4",
    "vue-tsc": "^2.2.0"
  }
}
