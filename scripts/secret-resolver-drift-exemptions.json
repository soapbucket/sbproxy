{
  "_comment": [
    "Recorded verdicts for findings scripts/check-secret-resolver-drift.py",
    "would otherwise report, in the same 'annotated entry with a reason'",
    "shape scripts/pub-item-verdicts.json uses for scan-pub-item-usage.py.",
    "This is not a bare allowlist of paths: every entry needs a non-empty",
    "'reason', and the checker refuses to run (exit 2) without one.",
    "",
    "Keys are '<repo-relative file>::<function name>::<shape>', where",
    "<shape> is 'adhoc-parse' (shape 1: a hand-rolled env:/file:/provider-URI",
    "scheme match outside crates/sbproxy-vault/src/) or",
    "'passthrough-fallback' (shape 2: a secret-shaped function whose",
    "guardless fallback hands a parameter back unchanged). The checker fails",
    "if a key names a finding that no longer exists, so this file cannot",
    "rot into a list of excuses for code that was already fixed.",
    "",
    "Most entries below are 'reviewed': a real, already-audited call site",
    "that is either the resolver's own public entry point or a documented",
    "fallback for when no resolver is installed (env:/file: only; a",
    "provider URI is refused rather than becoming the secret). One entry,",
    "signatures.rs::decode_secret, is 'escalated': the finding is real, not",
    "yet fixed, and out of scope for WOR-2287 itself. See its reason."
  ],
  "exemptions": {
    "crates/sbproxy-core/src/config_source.rs::resolve_secret_reference::adhoc-parse": {
      "verdict": "reviewed",
      "reason": "This IS the shared entry point for source: / config-authority secret references (WOR-1767/WOR-2282): it routes through sbproxy_vault::process_resolver() when one is installed, and its env:/file: handling here is the documented fallback for sbproxy validate / unit tests / a config with no proxy.secrets backends, where a provider URI is refused outright rather than resolved."
    },
    "crates/sbproxy-core/src/key_plane.rs::resolve_secret_material::adhoc-parse": {
      "verdict": "reviewed",
      "reason": "Delegates to sbproxy_vault::process_resolver() first (WOR-2285); the local env:/file: handling is the documented no-resolver-installed fallback for key_management.crypto material, and a provider URI without an installed resolver is refused via looks_like_secret_reference_uri rather than becoming the key (this is the exact WOR-1767/pepper incident the function's own doc comment cites)."
    },
    "crates/sbproxy-core/src/pipeline.rs::resolve_at_rest_key_material::adhoc-parse": {
      "verdict": "reviewed",
      "reason": "Same shape as key_plane::resolve_secret_material: resolves through sbproxy_vault::process_resolver() first, and the local file: handling is the documented no-resolver-installed fallback for at-rest encryption key material. A provider URI without a resolver is refused via looks_like_secret_reference_uri before the literal-bytes fallback, per its own doc comment."
    },
    "crates/sbproxy-core/src/cluster.rs::resolve_secret_material::adhoc-parse": {
      "verdict": "reviewed",
      "reason": "Resolves the cluster shared-key reference through sbproxy_vault::process_resolver() first (WOR-2285); the local env:/file: handling is the documented no-resolver-installed fallback, and every provider-URI scheme (not just vault://) is refused via looks_like_secret_reference_uri before falling through, per the WOR-2064-era incident documented at the call site."
    },
    "crates/sbproxy-core/src/server/action_dispatch.rs::mcp_secret_lookup::adhoc-parse": {
      "verdict": "reviewed",
      "reason": "Delegates to sbproxy_vault::process_resolver() when installed; the local env: handling is the documented no-resolver fallback, and the function's own doc comment records the deliberate third case (a bare, unprefixed variable name) this call site has always accepted as env: shorthand."
    },
    "crates/sbproxy-core/src/server/action_dispatch.rs::is_bare_credential_name::adhoc-parse": {
      "verdict": "reviewed",
      "reason": "The companion shape-guard for mcp_secret_lookup: decides whether a credential_ref carries none of the resolver's recognized forms (env:, file:, ${VAR}, or a provider-URI scheme), so it is a bare variable name rather than something the resolver would mis-resolve or reject. Documented at the definition; not a resolver reimplementation, a classifier for the one deliberately-scoped bare-name case."
    },
    "crates/sbproxy-config/src/cluster.rs::validate_shared_key_reference::adhoc-parse": {
      "verdict": "reviewed",
      "reason": "Validate-time (not resolve-time) shape check for security.shared_key, consistent with the runtime check in sbproxy-core's cluster::resolve_secret_material (WOR-2285): env:/file: are accepted as references to inject at runtime, and crate::types::is_secret_reference (the shared shape guard, reused rather than a hardcoded scheme allowlist) refuses every provider-URI scheme rather than just vault://, per the WOR-2064-era incident documented at the definition."
    },
    "crates/sbproxy-modules/src/auth/dpop_outbound.rs::validate_key_reference::adhoc-parse": {
      "verdict": "reviewed",
      "reason": "Validate-time shape check for a DPoP signing key reference, the same shape as sbproxy-config::cluster::validate_shared_key_reference: file: is accepted directly (the resolver has no bare guard for that form), and every other reference shape is delegated to sbproxy_vault::looks_like_secret_reference_uri rather than hand-matched. HTTP URLs and inline PEM are refused by the same check."
    },
    "crates/sbproxy/src/main.rs::is_secret_reference::adhoc-parse": {
      "verdict": "reviewed",
      "reason": "A display-time masking guard for `sbproxy plan`/doctor output (WOR-1869): decides whether a config string is a resolver reference (safe to print) or inline material (must be masked), never resolves anything. It duplicates the shape check sbproxy_config::types::is_secret_reference already performs because that one is pub(crate) and unreachable from the sbproxy binary crate; wiring the two together is a real cleanup but a separate, non-security-bearing change from this ticket's scope."
    },
    "crates/sbproxy-billing/src/types.rs::provider_label_for_key::passthrough-fallback": {
      "verdict": "false-positive",
      "reason": "`key` here is a payment-provider idempotency key for a rail's dedup label, not a config secret reference; `key.to_string()` in `Ok(key.to_string())` is the ordinary short-key case of a label-shortening helper, unrelated to sbproxy-vault. Flagged only because the function/parameter name matches the shape-2 term filter (\"key\"), which this checker's own docstring says is expected to happen for the generic terms it uses."
    },
    "crates/sbproxy-middleware/src/signatures.rs::decode_secret::passthrough-fallback": {
      "verdict": "escalated",
      "reason": "Real, currently-unfixed gap, not a reviewed exception: MessageSignaturesConfig.key (RFC 9421 HMAC/Ed25519 key material) reaches decode_secret() straight from sbproxy-core's cached_message_signature_verifier with no call through sbproxy_vault::process_resolver() anywhere on the path, so a vault://, awssm://, or env: (colon-form) reference that was never meant to resolve becomes the literal HMAC key via the as_bytes().to_vec() fallback -- the WOR-2283 bug class. (${VAR}-form references ARE safe today, resolved textually by sbproxy-config::compiler's interpolate_env_vars before this code ever runs.) Not fixed here: sbproxy-middleware does not currently depend on sbproxy-vault, and wiring this properly is a Cargo.toml + cross-crate design change outside WOR-2287's edit-the-checker-only scope. Needs a follow-up ticket under the WOR-2282 epic."
    }
  }
}
