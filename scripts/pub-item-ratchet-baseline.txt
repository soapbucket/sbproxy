# Why this file has no number in it
#
# The count lives beside it in scripts/pub-item-ratchet-baseline.count,
# alone on the only line of that file. This file is the log of why the
# count moved, and nothing else. Both are read by
# scripts/check-pub-item-ratchet.sh.
#
# The split is a merge fix (WOR-2252). While the number sat on line 221
# of a 289-line file, two branches that both bumped it wrote to
# non-adjacent lines, git saw two independent hunks, and took both. The
# result was a baseline file holding two numbers, which the old reader
# squashed into one impossible integer like "287286": a ceiling no real
# count can exceed, so the check passes forever while silently handing
# back every narrowing anyone lands. This file has hit eight conflicts.
#
# A one-line file cannot merge that way. Two branches that both change
# the count change the same line, so git stops and asks. That is the
# entire point of the arrangement, and the answer to such a conflict is
# always to recompute against the merged tree
#
#   python3 scripts/scan-pub-item-usage.py --count tests-only
#
# and never to keep either side's number, because neither side's number
# describes the merge.
#
# The notes below are append-only and conflict freely, which is fine: a
# conflict in prose is resolved by keeping both paragraphs, and that is
# the right resolution for a log.
#
# ---------------------------------------------------------------------
#
# Number of `pub` items whose only consumer outside their defining file
# is test code, as measured by scripts/scan-pub-item-usage.py.
#
# Enforced by scripts/check-pub-item-ratchet.sh. This number must never
# go up. Lower it whenever a cleanup lands; the script tells you to.
#
# Measured on WOR-1941, 2026-07-30, after inbound key validation and
# key-plane generation pinning gained their production consumers.
#
# Raised to 216 on WOR-2098, 2026-07-31: the RAG provider adapters ship
# wire-contract integration tests, and their injection seams (scripted
# DNS resolver, address policy, fixture-pinned client builder, escape
# helper, render probe) must be `pub` for `tests/` to reach them. All
# are `#[doc(hidden)]` and most also have in-crate production callers.
#
# Raised to 218 on WOR-2099, 2026-07-31: the distributed semantic cache
# added `SemanticNamespace::origin_prefix` and
# `SEMANTIC_ANONYMOUS_CREDENTIAL`, both in sbproxy-ai and both consumed
# from sbproxy-core's test modules (the Redis purge-scope tests and the
# credential-identity tests). Cross-crate test use requires `pub`, so
# neither can be narrowed, and both have in-file production callers.
#
# Raised to 319 on WOR-2100, 2026-07-31: the whole delta is the new
# `sbproxy-billing` crate, and it is the one case this ratchet's fourth
# option was written for. The crate is the durable settlement domain,
# and its production caller does not exist yet: the `ai_crawl` policy
# still emits the legacy per-rail challenge and redeems against the old
# ledger, so nothing in the request path yet names a payment
# requirement, a rail adapter, or a settlement receipt. Its 224 tests
# are its only consumers today.
#
# These are not narrowable. The crate's public surface is what
# `sbproxy-core` will call across a crate boundary, and the contract
# tests that pin the wire formats (x402 v2, Payment Auth draft-01,
# Stripe PaymentIntents, CLN, LND) live in `tests/` precisely so they
# exercise the same surface a caller gets rather than a private one.
#
# This number comes back down when the origin gate lands and the
# request path starts calling the service, the registry, and the
# requirement types. Do not raise it again for this crate.
#
# Raised to 321 on WOR-2139, 2026-08-01, for two items that cannot be
# narrowed without defeating what they are for.
#
# `run_scoped_label_gaps` (sbproxy-observe/src/metric_registry.rs) is
# the static guard that forbids a run, task, context, or trace id from
# becoming a Prometheus label. Its consumer is
# `tests/metric_drift.rs`, a separate integration-test crate, so `pub`
# is the only way to reach it. `tenant_label_gaps` sits beside it for
# exactly the same reason and has since it was written. The guard has
# to run against the same public table a caller sees; asserting against
# a private copy would prove nothing.
#
# `MAX_RUN_ID_BYTES` (sbproxy-ai/src/tracing_spans.rs) is the cap
# applied to a caller-supplied run identifier before it reaches a span
# attribute. It crosses into sbproxy-core, where the body phase caps at
# capture so the span and the access log agree on one value. A cap that
# two crates must agree on is a contract, not an implementation detail.
#
# Lowered to 305 on WOR-2143, 2026-08-01: the settlement origin gate
# landed and the request path now calls the sbproxy-billing surface the
# WOR-2100 note promised a caller for. Sixteen items gained production
# consumers: the service (authorize, prepare_requirement, the decision
# and problem types), derive_intent_id, the requirement and proof types,
# the x402 challenge renderer and header codecs, the Payment Auth
# challenge binder and credential selector, and the CLN challenge
# fields.
#
# Raised to 314 on WOR-2081, 2026-08-01, and this one is a measurement
# correction rather than new write-only code. Deleting the unwired
# `sbproxy-cache/src/two_tier.rs` removed five candidates, but all five
# carried a recorded `escalated` verdict, so none of them were ever
# counted in this number. What the deletion did change is the textual
# match this scan is built on: `two_tier.rs` defined its own
# `with_threshold`, and because the scan counts an identifier as
# referenced when the word appears in any non-test file, that unrelated
# definition was standing in as a production reference for
# `prompt_injection_v2::with_threshold` in sbproxy-modules. Both of that
# item's real callers are inside `#[cfg(test)]` blocks
# (`prompt_injection_v2/mod.rs` and `body_aware.rs`), so it has been
# tests-only all along and the scan simply could not see it.
#
# The number going up here means the scan got more accurate, not that
# the workspace got worse. Narrowing that builder is a separate change
# against sbproxy-modules and is not bundled into a cache deletion.

# Lowered to 312 on WOR-2179 / WOR-2164, 2026-08-02, and this is another
# measurement artifact rather than a real narrowing.
# `CelEngine::eval_bool_source` (sbproxy-extension/src/cel/mod.rs) gained
# no new Rust caller. The new `cel::compiled` module names it in a doc
# comment, where it explains that the compile-per-call helpers exist for
# request-supplied source and nothing else, and this scan counts an
# identifier as referenced when the word appears in any non-test file.
# So the item reads as production-referenced now because a paragraph
# mentions it. Recording the number the scan reports; do not read it as
# the helper having found a production caller. Its only Rust callers are
# still tests.

# Lowered to 289 on WOR-2065, 2026-08-02, working the triage tail. Ten of
# the twenty-three are a measurement fix and thirteen are real narrowings.
#
# The measurement fix is in `cfg_test_line_ranges`. The scan finds the
# extent of a `#[cfg(test)]` block by counting braces forward from the
# attribute, and `#[cfg(test)] mod test_env;` has no braces at all, so the
# count ran on until the next `{` anywhere below and swallowed whatever
# opened it. In `sbproxy-config/src/lib.rs` that was the
# `pub use config_authority::{...}` façade ten lines further down, so three
# publicly re-exported constants were filed as test-only. Ten candidates
# were wrong this way across five crates, and every one of them would have
# been a hard E0365 had anyone taken the scan's advice and narrowed it. The
# scan now stops at the first `;` it reaches before any brace.
#
# The thirteen narrowings are the whole of what the tests-only slice had
# left that could be narrowed at all. Six had a production caller inside
# their own file and became `pub(crate)`: ADRF_VERSION, start_with_cipher,
# mint_proof, as_legacy_str, resolved_headers, put_meta. Seven are helpers
# whose own rustdoc says they exist for tests, and they became
# `#[cfg(test)] pub(crate)` rather than plain `pub(crate)`, because a bare
# narrowing of an item with no production caller is the dead_code failure
# WOR-2065 already hit once on with_reverse_error: shadow_dropped_value,
# shadow_timeout_value, ratelimit_rejected_value, clear_resolved_base,
# set_applied_authority, clear_applied_bundle,
# reset_sink_dispatcher_for_test.
#
# What is left in the 289 is not a backlog of narrowings. 279 of them are
# reached by an integration test under `crates/<name>/tests/`, which
# compiles as its own crate, so `pub` is the only visibility that resolves
# and no cleanup can lower the number. The remaining 10 carry an
# `escalated` verdict in scripts/pub-item-verdicts.json, each with a
# question for whoever owns the subsystem, because the answer changes
# behaviour rather than visibility. Do not expect this number to fall much
# further without the settlement crate's origin gate work landing.

# Lowered to 288 on WOR-2211, 2026-08-02, answering one of those escalated
# questions. `Unit::provenance_is_consistent` (sbproxy-meter/src/event.rs)
# checked that a unit's declared source agrees with the evidence it
# carries, its own doc said the check was worth running on anything
# deserialised, and no decode path ran it. The escalation asked whether the
# wire decode should call it and reject on false. It should, and now does:
# `ReceiptUnit::provenance_is_consistent` lifts each wire unit back into
# the meter's vocabulary and asks this method, the ledger's replay and
# verification both ask the payload through
# `LedgerPayload::provenance_conflict`, and the two admin read paths ask
# the claims directly. This is the ratchet's first option, wiring the item
# to a production call site, rather than a narrowing.

# Lowered to 287 on WOR-2224, 2026-08-02, answering another one.
# `Router::set_provider_health` (sbproxy-ai/src/routing.rs) is the writer
# for the active-probe axis that `provider_eligible` intersects on every
# AI routing decision. The escalation asked whether the probe task was
# ever wired or whether passive ejection was the whole story. It was never
# wired: the axis, the `AiHealthCheckConfig` struct, and the
# `resilience.health_check` config block all landed together in the
# original AI-resilience commit and the task that was supposed to drive
# them was never written, so every provider sat at the `unknown` default
# while docs, `docs/configuration.md`, and `examples/ai-resilience` all
# described a working probe. The new `sbproxy-ai/src/health_probe.rs`
# spawns it from the pipeline's existing background-task seam. This is the
#
# Lowered to 287 on WOR-2223, 2026-08-02, answering another one.
# `LaneSplit::record_cloud` (sbproxy-model-host/src/hybrid.rs) is the
# cloud half of the hybrid savings split, and the escalation asked where
# the cloud-spill path should call it. The answer is the same place the
# local half is recorded: `ValueSink`, the usage sink that prices every
# finished completion of a model with a configured `reference`. It now
# reads the serving lane off the usage event instead of assuming local,
# so a completion that spilled to a cloud provider lands in
# cloud_completions and cloud_spent_micros rather than nowhere. The
# ratchet's first option again, a production call site rather than a
# narrowing.

# Raised on WOR-2203, 2026-08-03, for four items the extension bundle
# epic adds. None is narrowable and none is write-only code. The
# final number (then at the bottom of this file, now in
# scripts/pub-item-ratchet-baseline.count) was recomputed after merging
# main, and is neither this branch's count nor main's.
#
# `BUNDLE_API_VERSION` and `BUNDLE_KIND` (sbproxy-config/src/extensions.rs)
# are the manifest's `apiVersion` and `kind` literals. sbproxy-config is
# one of the three public crates, these two are the contract a bundle
# author writes at the top of every `bundle.yaml`, and the tests that pin
# them assert against the same constants a caller reads.
#
# `AiExtensionHookRegistration` (sbproxy-plugin/src/extensions.rs) is the
# link-time registration type for an AI hook. Its consumer is
# `crates/sbproxy-plugin/tests/ai_extension_contract.rs`, a separate
# integration-test crate, so `pub` is the only visibility that resolves.
# The AI chain reaches instances of it through
# `collect_linked_ai_extension_hooks` without naming the type, which is
# why the scan sees no production reference.
#
# `ExtensionHookDeclaration` (sbproxy-plugin/src/extensions.rs) is the
# same shape: production code walks `declaration.hooks` without spelling
# the element type, and the only cross-file mention is in
# sbproxy-core's inventory test module.
#
# The delta is +4 and not +5 because `SourceConfig::with_credential`
# (sbproxy-config/src/source.rs) gained a production caller in the same
# epic: private Git bundle sources now resolve credentials through it on
# the refresh path.
#
# Merged origin/main on 2026-08-03: main had lowered this to 282 while
# this branch had raised it to 287. Neither number is the answer. The
# count (then below, now in the .count file) is recomputed against the
# merged tree, because a baseline
# that keeps either side's number silently gives back whatever the other
# side changed. This is the seventh conflict in this file; the loud
# multi-line guard in check-pub-item-ratchet.sh landed with it.
#
# Raised to 288 on WOR-2245, 2026-08-03. Recomputed against the merged tree
# rather than taking either side of the conflict: this branch carried 290 from
# an older main and main carried 285, and neither described the merge. Eighth
# conflict in this file.
#
# The delta is the module-coverage API this branch adds to
# `sbproxy-capability/src/config_scan.rs`: `ModuleCoverageState`,
# `ModuleCoverage`, `verify_module_dispatch_coverage`, and
# `verify_config_readers_with_modules`. These are the surface the build guard
# and its own integration tests call, and `config_scan`'s tests live in a
# separate test crate, so `pub` is the only visibility that resolves. They
# cannot be narrowed for the same reason most of the items already in this
# number cannot.
#
#
# Lowered to 286 on WOR-2225, 2026-08-02, answering three more of the
# escalated questions. Each one asked the same thing: two implementations
# of one job exist and only one runs, so which is the real one.
#
# `parse_endpoint` (sbproxy-ai/src/api_routes.rs) is deleted, along with
# the `AiEndpoint` enum and `provider_supports_endpoint`. It was the
# predecessor of `classify_surface`, which the request path has always
# called and which recognises a strict superset of its vocabulary. Its
# capability matrix had also gone stale against the live one, advertising
# Google image, audio, and reranking surfaces that have no translator.
#
# `select_default` (sbproxy-core/src/compression_runtime.rs) is narrowed to
# `pub(crate)`. There were never two definitions of the route default:
# this was `select(On)` with an `expect` around it, and dispatch reaches
# the default through `select(On)` as one arm of the general selector path.
# `select` now delegates to it, so the two cannot come apart, and the
# `expect` is gone rather than pushed onto the request path.
#
# `install_key_plane` (sbproxy-core/src/key_plane.rs) is now
# `#[cfg(test)] pub(crate) install_key_plane_for_test`, and the dead
# `uninstall_key_plane` beside it is deleted. The production path,
# `activate_key_plane`, was not skipping an invariant this wrapper held:
# the wrapper was a bare slot write and `activate_key_plane` does the
# readiness retraction, the invalidation subscriber, and the shared budget
# handoff on top of it.
#
# 286 rather than 285 because the WOR-2225 regression test in
# `server/ai_dispatch.rs` compares behaviour fingerprints, and
# `SelectedCompressionRuntime::behavior_fingerprint` had no reference
# outside its own file at all before that. It moves from the wider
# unreferenced list into the tests-only slice without anything about it
# changing. It is worth a look on its own: the fingerprint is computed for
# every compiled pipeline and nothing in production reads it.
#
# Lowered to 286 on WOR-2225, 2026-08-02, answering two more of the
# escalated questions. Both are real, neither is a narrowing.
#
# `CardinalityLimiter::unique_count` (sbproxy-observe/src/cardinality.rs)
# gained a production caller. It reads how many unique values a label has
# accepted, which is the number that says whether the limiter is about to
# start folding values into `__other__`, and until now only the crate's own
# tests read it. In a multi-tenant deployment that collapse merges tenants
# into one series, so a per-tenant panel keeps drawing and starts answering
# a different question, and the only tell is spotting `__other__` in a
# query. `metrics::refresh_cardinality_gauges` now publishes it per label
# at scrape time as `sbproxy_label_cardinality_unique_values`, alongside
# `sbproxy_label_cardinality_budget` for the denominator.
#
# `metrics::record_request` (sbproxy-observe/src/metrics.rs) was deleted.
# It was the legacy request counter, a shim that called
# `record_request_with_labels` with `AgentLabels::unset()`, and the
# migration it was waiting on finished: no production caller was left. Its
# five test call sites moved to the labelled entry point passing
# `AgentLabels::unset()` explicitly, so no coverage went with it. The one
# assertion worth keeping, that an unresolved request lands on the
# empty-string sentinel series rather than a positive `human` or `unknown`
# verdict, is still made; it now names `AgentLabels::unset()` as the thing
# under test, which is what it was pinning all along.
#
# The two escalations left in scripts/pub-item-verdicts.json that this
# sweep also answered did not move the number.
# `EVICT_REASON_GRACEFUL_LEAVE` (sbproxy-mesh) and `decode_all_raw`
# (sbproxy-transport) are both still tests-only and both correctly so:
# the first reserves a label value for a feature that does not exist, the
# second is the response-direction frame walker for a bridge direction the
# proxy does not run. Their verdicts moved from `escalated` to `keep`.

# 2026-08-07 (WOR-2316): 288 -> 287. The RFC 8705 plumbing deleted the
# test-only `check_auth_with_tls` wrapper and gave the thumbprint path a
# production caller in `request_filter`, so one former tests-only pub
# item no longer exists.

# 2026-08-08 (WOR-2318): 287 -> 289. The span drift guard added two pub
# items whose only consumer is a test, and both have to exist before their
# caller does, for the same reason `METRICS` and `validate_metrics` do.
#
# `span_registry::SPANS` is the executable span registry: every span name
# this workspace opens or publishes, what emits it, and what we promise
# about the name. `validate_spans` is its table-only invariant check, the
# sibling of `validate_metrics`, which is tests-only for the same reason.
# A registry is consumed by the guard that enforces it; wiring either into
# production would mean the proxy reading its own capability table at
# runtime, which is not what it is for.
#
# Both are exactly the shape this ratchet is meant to allow through with a
# note rather than block: a table plus its checker, landing together, with
# the check running in CI on every commit.

# 2026-08-08 (WOR-2318): 289 -> 290. No new pub surface. `verify_references`
# in sbproxy-capability's scan.rs has been pub, and tests-only, since the
# guard was written; the scanner could not see it.
#
# `cfg_test_line_ranges` finds a `#[cfg(test)]` block by counting braces and
# says so in its own docstring: braces inside string literals throw the count
# off. scan.rs is full of them, because its tests carry Rust source fixtures
# as raw strings and its extractors build PromQL selectors with `format!`.
# On the parent commit the count never came back to zero, so the range that
# opened at the first `#[cfg(test)]` ran to end of file and every pub item
# below it was filed as test scaffolding. Adding the `label_values` extractor
# happened to rebalance the braces, the range now closes where the test
# module ends, and one item that was always a candidate became countable.
#
# It is not narrowable. Its consumer is
# `crates/sbproxy-observe/tests/metric_drift.rs`, a separate crate's
# integration test, which cannot see `pub(crate)`. It is the same shape as
# `validate_metrics` and `validate_spans` above: a checker whose caller is
# the guard that runs it.
#
# Worth knowing that the number can move again for this reason alone, in
# either direction, on any edit to scan.rs that changes its brace balance.
# The miscount is the conservative direction by design (an item looks more
# used, not less), so the drift it causes is a ratchet that was too loose
# getting tighter.
