{
  "type": "added",
  "message": "Read and access audit for credential resolution: `sbproxy_credential_read_total` counts every read unconditionally, and `key_management.read_audit` adds a rate-limited detail record per credential per window on the `key_audit` channel, with the credential id HMAC-hashed by default."
}
