{
  "type": "fixed",
  "message": "The `exposed_credentials` policy now refuses an unknown `provider` with a message saying `static` is the only provider that ships, instead of pointing the operator at a build that does not exist."
}
