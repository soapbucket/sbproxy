{
  "type": "added",
  "message": "**`/.well-known/openapi.json` and `.yaml` accept `version=` to emit OpenAPI 3.1.** `3.0`, `3.0.3`, `3.1`, and `3.1.0` are the accepted values. Omit the parameter and the document stays 3.0.3. An unknown value is 400 rather than a silent default. Admin `GET /api/openapi.json` stays 3.0.3."
}
