{
  "type": "changed",
  "message": "Pingora worker, blocking-pool and offload threads now get an 8 MiB stack instead of tokio's 2 MiB default, so a debug build of the request path no longer aborts with a stack overflow. New `SB_WORKER_STACK_BYTES` overrides it."
}
