{
  "type": "fixed",
  "message": "Refuse ONNX models that keep their tensors in a separate file. An operator-supplied model could name any path in an `external_data` reference and have the proxy read it (GHSA-h668-6x6g-f8r5); every ONNX loader now refuses such a model before opening anything, and translates the parsed model with no directory for the runtime to resolve against."
}
