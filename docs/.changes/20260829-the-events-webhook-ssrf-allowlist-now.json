{
  "type": "fixed",
  "message": "The events webhook SSRF allowlist now honors `egress.usage_sinks.hosts` when `allow_private` is true, so a private collector listed there can boot and deliver. Without that pair the guard still refuses private addresses."
}
