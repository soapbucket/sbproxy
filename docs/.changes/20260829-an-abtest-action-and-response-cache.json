{
  "type": "added",
  "message": "An `abtest` action and `response_cache` on the same origin are now refused at config load. The cache lookup runs before the variant is chosen and the variant is not part of the cache key, so a cache hit served one variant's body to clients assigned another."
}
