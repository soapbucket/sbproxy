{
  "type": "added",
  "message": "A config revision is promoted to last known good only after it survives a soak window judged on four signals: degraded subsystems, upstream circuit breakers, the request-outcome delta, and an operator probe. The verdict is three-way, so a window that measured nothing is inconclusive rather than a promotion. `POST /admin/config/confirm` closes the window early for a pipeline that ran its own smoke test."
}
