{
  "type": "added",
  "message": "Leased upstream credentials for cloud IAM: a credential can name a dynamic-secrets mount instead of storing anything static, and resolved material is never cached past the lease. Scoped to AWS, GCP, and Azure IAM and Vault-fronted database mounts; `lease` on an AI provider with no short-TTL issuance is refused with the limitation named."
}
