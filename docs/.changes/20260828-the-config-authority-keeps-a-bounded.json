{
  "type": "added",
  "message": "The config authority keeps a bounded archive of earlier revisions, so POST /admin/config-authority/rollback and sbproxy config authority rollback --to-revision N can return a fleet to a revision from further back than one step. proxy.config_authority.publish.archive_keep sets the depth and defaults to 20; zero keeps the one-step rollback exactly as it was."
}
