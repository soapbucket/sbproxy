{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ProviderConfig",
  "description": "Provider configuration from YAML/JSON.",
  "type": "object",
  "required": [
    "name"
  ],
  "properties": {
    "name": {
      "description": "Unique provider name used to reference this provider.",
      "type": "string"
    },
    "provider_type": {
      "description": "Optional provider type (e.g. \"openai\", \"anthropic\"); inferred from name if absent.",
      "default": null,
      "type": [
        "string",
        "null"
      ]
    },
    "deployment": {
      "description": "Canonical model-host deployment ID. This is required when `provider_type` is `managed_model` and rejected for every other provider.",
      "default": null,
      "type": [
        "string",
        "null"
      ]
    },
    "api_key": {
      "description": "API key used to authenticate with the upstream provider.",
      "type": [
        "string",
        "null"
      ]
    },
    "accept_native_credentials_for": {
      "description": "Canonical native-provider label whose caller-owned credential this exact provider destination may receive.\n\nThis is an explicit destination trust decision. It defaults to disabled and must match `provider_type` (or `name` when no type is configured). For example, `openai` permits an OpenAI-shaped native key to replace `api_key` only for this provider entry and its effective `base_url`.",
      "default": null,
      "type": [
        "string",
        "null"
      ]
    },
    "base_url": {
      "description": "Override the upstream base URL (defaults to the provider's well-known URL).",
      "default": null,
      "type": [
        "string",
        "null"
      ]
    },
    "models": {
      "description": "Models served by this provider; empty defers to the provider catalog.",
      "default": [],
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "default_model": {
      "description": "Default model used when the request omits an explicit model.",
      "default": null,
      "type": [
        "string",
        "null"
      ]
    },
    "model_map": {
      "description": "Per-provider mapping from logical model name to upstream model name.",
      "default": {},
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    },
    "weight": {
      "description": "Weight used by weighted routing strategies.",
      "default": 1,
      "type": "integer",
      "format": "uint32",
      "minimum": 0.0
    },
    "priority": {
      "description": "Priority used by priority-based routing (lower runs first).",
      "default": null,
      "type": [
        "integer",
        "null"
      ],
      "format": "uint32",
      "minimum": 0.0
    },
    "enabled": {
      "description": "Whether this provider is eligible for routing.",
      "default": true,
      "type": "boolean"
    },
    "max_retries": {
      "description": "Maximum retry attempts on transient upstream failures.",
      "default": null,
      "type": [
        "integer",
        "null"
      ],
      "format": "uint32",
      "minimum": 0.0
    },
    "timeout_ms": {
      "description": "Request timeout in milliseconds.",
      "default": null,
      "type": [
        "integer",
        "null"
      ],
      "format": "uint64",
      "minimum": 0.0
    },
    "organization": {
      "description": "Organization identifier (used by providers that scope keys per org).",
      "default": null,
      "type": [
        "string",
        "null"
      ]
    },
    "api_version": {
      "description": "API version header value (used by Anthropic and Azure OpenAI).",
      "default": null,
      "type": [
        "string",
        "null"
      ]
    },
    "host_override": {
      "description": "Override the `Host` header sent to the AI provider. Defaults to the provider's base URL hostname (api.openai.com, api.anthropic.com, ...). Set this when fronting the provider through a custom domain.",
      "default": null,
      "type": [
        "string",
        "null"
      ]
    },
    "disable_forwarded_host_header": {
      "description": "When true, suppress the `X-Forwarded-Host` header that the proxy would otherwise set to the client's original `Host` whenever it rewrites the upstream `Host`.",
      "default": false,
      "type": "boolean"
    },
    "allow_private_base_url": {
      "description": "Allow this provider's `base_url` to point at a private/loopback address. Defaults to `false`: a `base_url` resolving to a loopback, link-local, or RFC1918 target is rejected at config load as an SSRF risk. Set `true` for a local model server (Ollama, vLLM, LM Studio on `127.0.0.1`/LAN). Non-`http(s)` schemes (`file://`, ...) are always rejected regardless.",
      "default": false,
      "type": "boolean"
    },
    "no_prompt_training": {
      "description": "Operator's declaration that this provider does not retain or train on the prompts sent to it, per the provider's published data-handling agreement. There is no standardized per-request training opt-out header across providers, so SBproxy treats this as a deployment-level fact: a request that opts out of training (the `x-sbproxy-disallow-prompt-training` signal) is routed only to providers marked here. Defaults to `false`.",
      "default": false,
      "type": "boolean"
    },
    "data_posture": {
      "description": "Operator override of this destination's declared data-handling posture, consulted by the `data_posture:` routing eligibility filter. The provider catalog supplies the vendor default from its published terms; this block declares what holds for this deployment, for example `zdr: true` on an entry whose account has a signed zero-data-retention agreement. That declaration is the only thing that makes a vendor which retains by default eligible for `require_zdr`. Unset keeps the catalog's declaration. See `sbproxy_ai::data_posture`.",
      "anyOf": [
        {
          "$ref": "#/definitions/DataPostureOverride"
        },
        {
          "type": "null"
        }
      ]
    },
    "service_tier": {
      "description": "Upstream service tier this destination requests: `flex`, `standard`, or `priority`. Unset sends no tier field at all and the vendor serves on its own default.\n\nThis is the operator's decision, not the caller's. A caller that sets a tier on the request body has it replaced by this value, or removed when this is unset, because raising the tier raises the bill and the operator pays it.\n\nThe tier is a property of this entry, not of a request. To run two tiers of one vendor, declare two `providers[]` entries with the same `provider_type` and different tiers; the router then treats them as two candidates with independent weights, health, and realized latency.\n\nA tier the provider catalog does not record for this vendor is refused at config load rather than dropped at request time, so an entry that can never serve the tier you asked for never boots.",
      "default": null,
      "anyOf": [
        {
          "$ref": "#/definitions/ServiceTier"
        },
        {
          "type": "null"
        }
      ]
    },
    "serve": {
      "description": "WOR-1652: optional local model-serving block. When set, the gateway itself hosts the models (pull weights, fit an engine to the GPU, supervise it) and registers them as local providers ahead of any cloud fallback, instead of proxying `base_url` to an already-running server. `None` keeps the proxy-only behavior. The lifecycle half (spawn/evict) ships in phases of the model-host epic; this field is the config surface it reads.",
      "anyOf": [
        {
          "$ref": "#/definitions/ModelHostConfig"
        },
        {
          "type": "null"
        }
      ]
    },
    "aws_sigv4": {
      "description": "Sign this provider's requests with AWS Signature Version 4 instead of forwarding a static credential. Required by Bedrock and SageMaker, which do not accept a bearer token. Presence of this block is what selects the signer, so a provider entry either sets `api_key` or sets this, never both. `region` is required and is the credential scope; it is never inferred from `base_url`, matching the AWS SDKs, where an endpoint override leaves the signing region alone. When `base_url` is unset, `region` also fills the `{region}` placeholder in the provider catalog's default endpoint. See `sbproxy_ai::aws_sigv4`.",
      "anyOf": [
        {
          "$ref": "#/definitions/AwsSigV4Config"
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "definitions": {
    "DataPostureOverride": {
      "description": "Operator posture declaration on one provider entry, overriding the catalog's per-vendor default.\n\nThe catalog records what a vendor's published terms say about a stock account. A specific deployment can differ in both directions: an operator with a signed ZDR agreement declares `zdr: true` on their entry; an operator pointing an `openai`-typed entry at a retaining third-party endpoint declares `retains_data: true`.",
      "type": "object",
      "properties": {
        "retains_data": {
          "description": "Override the catalog's `retains_data` declaration for this entry. Unset keeps the catalog value, except that setting `zdr: true` alone implies `false`. Setting it `false` implies `zdr: true` unless you also set `zdr` explicitly, which is the same rule the catalog layer applies.",
          "default": null,
          "type": [
            "boolean",
            "null"
          ]
        },
        "zdr": {
          "description": "Declare that this deployment operates under a zero-data-retention arrangement with the vendor. This is what makes a provider whose stock terms retain prompt data eligible for `require_zdr`; the catalog's `zdr_available` records only that the vendor sells such an arrangement, never that you hold one.\n\nUnset, it follows `retains_data`: this entry's override if you set one, otherwise the catalog's declaration. A destination that stores nothing is zero data retention as it stands, so `retains_data: false` alone is enough and this key is for the case where the vendor does retain on a stock account and your agreement says otherwise.",
          "default": null,
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "ServiceTier": {
      "type": "string",
      "enum": [
        "flex",
        "standard",
        "priority"
      ]
    },
    "ModelHostConfig": {
      "description": "The `serve:` block: the local models plus host-wide policy.",
      "type": "object",
      "properties": {
        "models": {
          "description": "Models to serve locally. Registered as local providers ahead of any cloud fallback.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/ServeEntry"
          }
        },
        "catalog_file": {
          "description": "Support: preview. Optional path to an operator catalog file, replacing the built-in certified catalog for id resolution. Relative paths resolve from the directory containing the active `sb.yml`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "cache_dir": {
          "description": "Directory for the content-addressed weight cache, as an absolute path. `None` uses the platform default (`$HF_HOME` / `~/.cache/sbproxy/models`). A relative path is refused at config load: the engine subprocess that reads this cache has its own working directory and requires an absolute snapshot path, so a relative value fails only after a full model download.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "cache_budget_gib": {
          "description": "Support: preview. Disk budget in GiB for the weight cache before GC. `None` means unbounded (operator manages the disk). Collection runs after `models pull` and on demand through the authenticated `POST /admin/model-host/gc` route.",
          "default": null,
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "allow_unpinned_refs": {
          "description": "Support: preview. Permit unpinned raw `hf:` references on a node that holds the `worker` cluster role. Defaults to `false`.\n\nA raw reference runs the engine in repo mode, which gives up three protections a pinned catalog artifact gets: the container runs on the default bridge with DNS and external egress instead of an `--internal` network, the Hugging Face cache is mounted writable instead of read-only, and the launch-time trust and file-map checks are skipped because there are no local bytes to verify. No digest is ever computed for those weights, so the pull policy, offline mode, and digest-failure contract cover nothing on that path.\n\nThat trade is right for `sbproxy run <model>` on a workstation and for evaluating a model with no catalog entry yet. It is a poor fit for a long-lived fleet worker holding cluster identity, so the startup gate refuses it there unless this is set. Nodes without the worker role are unaffected either way.",
          "default": false,
          "type": "boolean"
        },
        "eviction": {
          "description": "What to do under VRAM pressure. Defaults to LRU eviction.",
          "default": "lru",
          "allOf": [
            {
              "$ref": "#/definitions/EvictionPolicy"
            }
          ]
        },
        "engines": {
          "description": "Support: preview. Per-engine provisioning (how to acquire each engine binary). Absent engines use the default (resolve from `PATH`).",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/EngineProvisioning"
          }
        },
        "max_concurrent_requests": {
          "description": "Cap on concurrently dispatched served-lane requests (WOR-1679). When set, the gateway holds an admission gate in front of the local engine: requests beyond the cap wait in a priority queue (a virtual key's `priority:` lane orders the queue, interactive first), and an interactive request with a cloud fallback in the same provider array spills to it instead of queuing. `None` (the default) disables the gate; the engine's internal queue is the only limit, exactly as before.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "queue_timeout_ms": {
          "description": "How long a queued request waits for a served-lane slot before the attempt fails over to the next provider (or errors when no fallback exists). Milliseconds; defaults to 30000. Read only when `max_concurrent_requests` is set.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        }
      }
    },
    "ServeEntry": {
      "description": "One model an operator wants the gateway to serve locally.",
      "type": "object",
      "required": [
        "model"
      ],
      "properties": {
        "model": {
          "description": "Catalog id (`qwen3-32b`) or an explicit `hf:Org/Repo:QUANT` reference. Resolved by [`crate::catalog`].",
          "type": "string"
        },
        "variant": {
          "description": "Exact catalog v2 artifact variant to run. When omitted, the runtime deterministically selects a compatible variant for the current worker. Pin this for reproducible deployments.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "The model id every other plane sees (WOR-1683): routing, `allowed_models`, rate limits, budgets, aliases. Defaults to the catalog id in `model`; **required** when `model` is a raw `hf:` reference (there is no catalog id to borrow). See [`Self::effective_name`].",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "engine": {
          "description": "Support: preview. Engine to serve it with. Defaults to `auto` (resolved per model at boot from the weight format and what is installed).",
          "default": "auto",
          "allOf": [
            {
              "$ref": "#/definitions/EngineChoice"
            }
          ]
        },
        "keep_alive": {
          "description": "Idle time before the engine is unloaded to free VRAM. Go duration syntax (`10m`, `1h`); `None` means never auto-unload.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "max_context": {
          "description": "Support: preview. Context length to plan VRAM for and pass to the engine. `None` uses the model's declared max (clamped by the fit planner to what actually fits).",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "extra_args": {
          "description": "Support: preview. Extra engine flags appended to the templated args. Values only, no shell: each entry is passed as one argv element.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "kv_quant": {
          "description": "Support: preview. KV-cache quantization. Defaults to `Auto` (the weight quant's default KV dtype).",
          "default": "auto",
          "allOf": [
            {
              "$ref": "#/definitions/KvCacheQuant"
            }
          ]
        },
        "enable_prefix_caching": {
          "description": "Support: preview. Enable vLLM's automatic prefix caching, reusing already-computed KV blocks across requests that share a prompt prefix. `None` leaves the engine default (off) in place.",
          "default": null,
          "type": [
            "boolean",
            "null"
          ]
        },
        "speculative": {
          "description": "Support: preview. Speculative decoding. The vLLM driver emits the engine's speculation flags; the managed reconcile path accepts n-gram speculation (needs no extra model) on a vLLM engine, but rejects draft-model speculation until its VRAM-headroom check (`fit::resolve_speculative_config`) is wired into a real prepare-time call site. `None` disables it.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/SpeculativeConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "chunked_prefill": {
          "description": "Support: preview. Chunked-prefill settings. `None` uses the engine default.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ChunkedPrefill"
            },
            {
              "type": "null"
            }
          ]
        },
        "lora_adapters": {
          "description": "Support: preview. LoRA adapters served over this base model on the vLLM engine (WOR-1945): each becomes a `--lora-modules <name>=<source>` alongside `--enable-lora`, so a client requests the adapter by name over one resident base. An `hf:Org/Repo` source is handed to vLLM as the repo id (resolved from the Hub or the mounted cache); a local path passes through. Requires `engine: vllm`.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/LoraAdapter"
          }
        },
        "pinned": {
          "description": "Support: preview. Keep this model resident: it is never evicted to make room for another (WOR-1672). Use for a latency-critical model that must stay hot. A set of pinned models is therefore never split. Admitting a new model that only fits by evicting a pinned one is rejected instead.",
          "default": false,
          "type": "boolean"
        },
        "tool_call_parser": {
          "description": "Support: preview. vLLM tool-call parser to enable auto tool-choice (WOR-1668), e.g. `hermes` (Qwen), `llama3_json`, `mistral`. When set, the engine launches with `--enable-auto-tool-choice --tool-call-parser <name>` so `tool_choice: auto` requests work; without it vLLM rejects auto tool-choice. `None` leaves tool calling off.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "swap_space_gib": {
          "description": "Support: preview. CPU swap pool size in GiB: vLLM's `--swap-space`. `None` uses the engine default.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "cpu_offload_gib": {
          "description": "Support: preview. GiB of model weights to keep in CPU RAM: vLLM's `--cpu-offload-gb`, trading PCIe bandwidth for VRAM so a model that does not fit can still load. `None` disables offload.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "max_loras": {
          "description": "Support: preview. Max LoRA adapters resident on the engine at once (WOR-1945): vLLM's `--max-loras`. When set below the number of configured `lora_adapters`, the engine loads adapters on demand and evicts the least-recently used past this cap (dynamic paging), rather than preloading all of them. `None` preloads every configured adapter (static), which suits a small, fixed adapter set.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "gguf_file": {
          "description": "Support: preview. For a llama.cpp GGUF model, the exact GGUF filename to serve from a multi-file repo (WOR-1656), e.g. `Qwen2.5-0.5B-Instruct-Q4_K_M.gguf`. With the `weights` feature sbproxy pre-fetches this file and passes llama.cpp a local `--model`, so it needs no curl-enabled build and never guesses the quant. Without pre-fetch it becomes `--hf-file` so llama.cpp downloads the right file. `None` lets llama.cpp resolve the repo default (fine only for a single-file repo).",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "reference": {
          "description": "The hosted model this local model displaces (WOR-1913). When set, every completion served locally is priced at what this reference would have charged and counted toward the dollars-saved value report (`GET /admin/model-host/value`). Omit it to make no savings claim: sbproxy never guesses a cloud reference price.",
          "anyOf": [
            {
              "$ref": "#/definitions/ReferenceModel"
            },
            {
              "type": "null"
            }
          ]
        },
        "modality": {
          "description": "The task this served model performs (WOR-1908 / WOR-1675). A catalog entry carries its own modality, but a raw `hf:Org/Repo` reference has none, so declare it here to serve an embedding or rerank model: it drives the engine's runtime-owned `--task` flag (vLLM `embed` / `score`) and zeroes the KV-cache term in the fit. Defaults to chat (autoregressive generation) when omitted. Support: preview.",
          "anyOf": [
            {
              "$ref": "#/definitions/Modality"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "EngineChoice": {
      "description": "The engine an operator asks for on a model (WOR-1684). Unlike [`EngineKind`] (the resolved identity), this includes `Auto`, which the runtime resolves per model at boot from the weight format and what is installed. `Auto` is the default so a minimal `serve:` block does not have to name an engine.",
      "oneOf": [
        {
          "description": "Resolve the engine from the weights and the environment.",
          "type": "string",
          "enum": [
            "auto"
          ]
        },
        {
          "description": "Force vLLM.",
          "type": "string",
          "enum": [
            "vllm"
          ]
        },
        {
          "description": "Force SGLang (WOR-1905). `Auto` never resolves to this: SGLang is an explicit opt-in that stays behind vLLM as the safetensors default until it is certified on real hardware.",
          "type": "string",
          "enum": [
            "sglang"
          ]
        },
        {
          "description": "Force llama.cpp.",
          "type": "string",
          "enum": [
            "llama_cpp"
          ]
        },
        {
          "description": "Force mistral.rs (WOR-1861). `Auto` never resolves to this: mistral.rs is an explicit opt-in that stays behind vLLM (safetensors) and llama.cpp (GGUF) until it has certification mileage, and the planner must not prefer it for large MoE prefill workloads.",
          "type": "string",
          "enum": [
            "mistralrs"
          ]
        }
      ]
    },
    "KvCacheQuant": {
      "description": "KV-cache quantization for a served model (WOR-1676). Quantizing the KV cache multiplies effective KV capacity, so a model can fit a longer context or a larger batch on the same VRAM at some quality cost. `Auto` keeps the cache at the weight quant's default (f16 for most, fp8 when serving FP8).",
      "oneOf": [
        {
          "description": "Follow the weight quant's default KV dtype.",
          "type": "string",
          "enum": [
            "auto"
          ]
        },
        {
          "description": "16-bit KV (the safe, full-quality default).",
          "type": "string",
          "enum": [
            "f16"
          ]
        },
        {
          "description": "8-bit float KV. Needs FP8-capable kernels.",
          "type": "string",
          "enum": [
            "fp8"
          ]
        },
        {
          "description": "8-bit integer KV.",
          "type": "string",
          "enum": [
            "int8"
          ]
        },
        {
          "description": "4-bit KV (maximum capacity, largest quality hit).",
          "type": "string",
          "enum": [
            "int4"
          ]
        }
      ]
    },
    "SpeculativeConfig": {
      "description": "Speculative-decoding settings for a served model.",
      "type": "object",
      "properties": {
        "method": {
          "description": "Which speculation method.",
          "default": "ngram",
          "allOf": [
            {
              "$ref": "#/definitions/SpecMethod"
            }
          ]
        },
        "draft_model": {
          "description": "Draft model repo/id, required for [`SpecMethod::DraftModel`].",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "num_speculative_tokens": {
          "description": "How many tokens to propose per step.",
          "default": 5,
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        }
      }
    },
    "SpecMethod": {
      "description": "How a model does speculative decoding (WOR-1674). Speculation wins when the batch is memory-bound (low load) and loses when it is compute-bound (full batch), so the runtime load-gates it; this is the config that says which method to use when it is on.",
      "oneOf": [
        {
          "description": "A separate small draft model proposes tokens.",
          "type": "string",
          "enum": [
            "draft_model"
          ]
        },
        {
          "description": "N-gram / prompt-lookup speculation (no draft model).",
          "type": "string",
          "enum": [
            "ngram"
          ]
        }
      ]
    },
    "ChunkedPrefill": {
      "description": "Chunked-prefill settings (WOR-1678). Chunked prefill trades a larger prefill chunk (higher throughput) against time-to-first-token (lower with smaller chunks). Set `max_batched_tokens` directly, or set `target_ttft_ms` to have the planner pick a chunk size.",
      "type": "object",
      "properties": {
        "max_batched_tokens": {
          "description": "Explicit prefill chunk size (engine `max-num-batched-tokens`). `None` leaves it to the auto-tune or the engine default.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "target_ttft_ms": {
          "description": "A TTFT SLO in milliseconds; when set (and no explicit chunk size), the planner chooses a chunk size to hold it.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        }
      }
    },
    "LoraAdapter": {
      "description": "A LoRA adapter served over a base model (WOR-1673). Clients request it by `name`; the runtime hot-loads it over the resident base with an LRU adapter cache rather than swapping the base model.",
      "type": "object",
      "required": [
        "name",
        "source"
      ],
      "properties": {
        "name": {
          "description": "The model name clients request to hit this adapter.",
          "type": "string"
        },
        "source": {
          "description": "Adapter source: an `hf:Org/Repo` reference or a local path.",
          "type": "string"
        }
      }
    },
    "ReferenceModel": {
      "description": "The explicitly-configured cloud model a local model displaces (WOR-1913). Its per-million-token prices value every local completion at what the equivalent hosted API would have charged, which is the dollars-saved number that justifies the GPU. There is no default and no guessing: without a `reference:` a served model makes no savings claim.",
      "type": "object",
      "required": [
        "completion_micros_per_mtok",
        "model",
        "prompt_micros_per_mtok"
      ],
      "properties": {
        "model": {
          "description": "The hosted model whose price this local model displaces, for display in the value report (e.g. `gpt-4o`). Not resolved against the catalog; it names the cloud API the saving is measured against.",
          "type": "string"
        },
        "prompt_micros_per_mtok": {
          "description": "Micro-USD per 1e6 prompt tokens the reference model charges.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "completion_micros_per_mtok": {
          "description": "Micro-USD per 1e6 completion tokens the reference model charges.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        }
      }
    },
    "Modality": {
      "description": "The task a served model performs (WOR-1908).\n\nA catalog is chat-only today, but the model host must be able to express an embedder, a reranker, and the other non-chat surfaces so the fit planner, engine launch, and capability negotiation branch on the actual task rather than assuming autoregressive chat. Only [`Modality::Chat`] decodes token by token and holds a KV cache; every other modality is a single forward pass. Defaults to `Chat`, so a pre-modality catalog entry parses and behaves exactly as before.",
      "oneOf": [
        {
          "description": "Autoregressive chat / text generation (the default). Decodes one token at a time and accumulates a KV cache.",
          "type": "string",
          "enum": [
            "chat"
          ]
        },
        {
          "description": "Text embeddings: one forward pass to a vector, no KV cache.",
          "type": "string",
          "enum": [
            "embedding"
          ]
        },
        {
          "description": "Reranking / scoring: one forward pass to a relevance score.",
          "type": "string",
          "enum": [
            "rerank"
          ]
        },
        {
          "description": "Speech to text (transcription).",
          "type": "string",
          "enum": [
            "speech_to_text"
          ]
        },
        {
          "description": "Text to speech (synthesis).",
          "type": "string",
          "enum": [
            "text_to_speech"
          ]
        },
        {
          "description": "Image generation.",
          "type": "string",
          "enum": [
            "image"
          ]
        }
      ]
    },
    "EvictionPolicy": {
      "description": "What to do when VRAM is needed and an idle model is resident.",
      "oneOf": [
        {
          "description": "Evict the least-recently-used idle model to make room (default).",
          "type": "string",
          "enum": [
            "lru"
          ]
        },
        {
          "description": "Never evict; reject a new model when VRAM is full. Predictable residency for a pinned single model.",
          "type": "string",
          "enum": [
            "never"
          ]
        }
      ]
    },
    "EngineProvisioning": {
      "description": "Provisioning for one engine (how to acquire it), keyed by engine in [`ModelHostConfig::engines`].",
      "type": "object",
      "properties": {
        "launch": {
          "description": "How the binary is acquired.",
          "default": "binary",
          "allOf": [
            {
              "$ref": "#/definitions/EngineLaunchMethod"
            }
          ]
        },
        "image": {
          "description": "Pinned container image (a tag or digest, never `:latest`) for [`EngineLaunchMethod::Container`]. Rejected at plan time if it ends in `:latest` or has no tag.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "acquire": {
          "description": "How to acquire the binary when [`EngineLaunchMethod::Binary`] (WOR-1801). `None` keeps the pre-WOR-1801 behaviour: resolve from `PATH` only.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/EngineAcquire"
            },
            {
              "type": "null"
            }
          ]
        },
        "shm_size_gib": {
          "description": "Shared-memory allocation in GiB for container launch.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        }
      }
    },
    "EngineLaunchMethod": {
      "description": "How an engine binary is acquired (WOR-1684). Engine *identity* stays the allowlisted [`EngineKind`]; only the acquisition method is configurable, so the config-spawn security posture (no arbitrary `cmd:`) is unchanged.",
      "oneOf": [
        {
          "description": "Resolve the engine binary from `PATH`.",
          "type": "string",
          "enum": [
            "binary"
          ]
        },
        {
          "description": "Run the engine from a pinned container image.",
          "type": "string",
          "enum": [
            "container"
          ]
        },
        {
          "description": "Run the engine from a managed uv/venv (vLLM's Python path).",
          "type": "string",
          "enum": [
            "venv"
          ]
        }
      ]
    },
    "EngineAcquire": {
      "description": "The `acquire:` block on a binary engine's provisioning (WOR-1801): how to obtain the binary. Engine *identity* stays the allowlisted [`EngineKind`]; only the acquisition method is configurable, so the config-spawn posture (no arbitrary `cmd:`) is unchanged.",
      "type": "object",
      "properties": {
        "source": {
          "description": "Where the binary comes from. Defaults to a pinned release.",
          "default": "release",
          "allOf": [
            {
              "$ref": "#/definitions/AcquireSource"
            }
          ]
        },
        "version": {
          "description": "Release or source tag to pin; `None` uses the built-in default. Never `latest`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "accel": {
          "description": "Acceleration flavour to acquire.",
          "default": "auto",
          "allOf": [
            {
              "$ref": "#/definitions/EngineAccel"
            }
          ]
        },
        "path": {
          "description": "Explicit binary path (required for `path`).",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "sha256": {
          "description": "Expected SHA-256 of the release asset or source archive. A `release` fetch without it logs a warning; CUDA source builds require it unless using the built-in source pin.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "AcquireSource": {
      "description": "How a *binary* engine (llama.cpp today) is obtained when it is not already on `PATH` (WOR-1801). Container / venv acquisition is a separate [`EngineLaunchMethod`]; this covers the single-binary path.",
      "oneOf": [
        {
          "description": "A pinned prebuilt release for the platform + accel, sha256 verified when a digest is pinned (default). PATH is always tried first.",
          "type": "string",
          "enum": [
            "release"
          ]
        },
        {
          "description": "An operator-installed binary at an explicit `path`.",
          "type": "string",
          "enum": [
            "path"
          ]
        },
        {
          "description": "Provision via `uvx` (`uv tool run`): fetch the `uv` single binary and run the engine in a cached, ephemeral environment that uv sets up on first use, bringing its own Python if needed. This is the acquisition path for vLLM, which is a Python package rather than a single-binary release. `version` pins the *engine package* version (the vLLM version); the `uv` version is pinned internally.",
          "type": "string",
          "enum": [
            "uvx"
          ]
        },
        {
          "description": "Build a digest-pinned llama.cpp source archive with fixed CMake CUDA flags.",
          "type": "string",
          "enum": [
            "source_build"
          ]
        }
      ]
    },
    "EngineAccel": {
      "description": "Hardware-acceleration flavour to acquire a binary engine build for (WOR-1801). `Auto` picks from the host: Apple gets the Metal build, a compatible NVIDIA Linux host may build CUDA from pinned source, and other hosts use the platform release.",
      "oneOf": [
        {
          "description": "Detect from the host GPU.",
          "type": "string",
          "enum": [
            "auto"
          ]
        },
        {
          "description": "CUDA. llama.cpp uses the digest-pinned Linux source-build path because upstream does not publish a CUDA Linux release asset.",
          "type": "string",
          "enum": [
            "cuda"
          ]
        },
        {
          "description": "Vulkan (a portable GPU path on Linux).",
          "type": "string",
          "enum": [
            "vulkan"
          ]
        },
        {
          "description": "Apple Metal (built into the macOS asset).",
          "type": "string",
          "enum": [
            "metal"
          ]
        },
        {
          "description": "CPU-only.",
          "type": "string",
          "enum": [
            "cpu"
          ]
        }
      ]
    },
    "AwsSigV4Config": {
      "description": "Sign this provider's requests with AWS Signature Version 4.\n\nPresence of this block is what selects the signer. Absent it, the provider's `api_key` is forwarded verbatim in the catalog's auth header, which is the pre-existing bearer-token behavior and never reaches this code at all. The two are mutually exclusive: a provider entry sets `api_key` or `aws_sigv4`, never both.",
      "type": "object",
      "required": [
        "region"
      ],
      "properties": {
        "region": {
          "description": "AWS region used for the credential scope, for example `us-east-1`. Required, and independent of `base_url`: pointing `base_url` at a VPC endpoint or a private hostname does not change the region a signature is scoped to, exactly as an AWS SDK's `endpoint_url` override does not. When `base_url` is unset, this value also fills the `{region}` placeholder in the provider catalog's default endpoint.",
          "type": "string"
        },
        "service": {
          "description": "Signing service name in the credential scope. Defaults to `bedrock` for a `bedrock` provider and `sagemaker` for a `sagemaker` provider; set it explicitly for any other AWS service fronted through a provider entry.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "credentials": {
          "description": "How to obtain the AWS credentials. Unset uses the standard AWS credential provider chain.",
          "anyOf": [
            {
              "$ref": "#/definitions/AwsCredentialsConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "refresh_margin_secs": {
          "description": "Seconds before expiry at which a short-lived credential is refreshed. Defaults to 900. A refresh that fails inside this window is retried on the next request and logged, and the cached credential keeps serving until 600 seconds remain, at which point a refresh failure fails the request.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "AwsCredentialsConfig": {
      "description": "How one provider's AWS credentials are obtained.",
      "type": "object",
      "properties": {
        "source": {
          "description": "Which credential source backs this provider. Defaults to the standard AWS provider chain.",
          "default": "default_chain",
          "allOf": [
            {
              "$ref": "#/definitions/AwsCredentialSource"
            }
          ]
        },
        "access_key_id": {
          "description": "AWS access key ID. Required by, and only read by, `static`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "secret_access_key": {
          "description": "AWS secret access key. Required by, and only read by, `static`. Resolve it through `${VAR}`, `vault://`, or `awssm://` rather than writing it into the config file.",
          "type": [
            "string",
            "null"
          ]
        },
        "session_token": {
          "description": "Session token accompanying an already-issued short-lived key pair. Only read by `static`. A token supplied this way expires on AWS's schedule and SBproxy has no way to renew it.",
          "type": [
            "string",
            "null"
          ]
        },
        "role_arn": {
          "description": "Role ARN to assume. Required by, and only read by, `assume_role`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "external_id": {
          "description": "External ID demanded by the role's trust policy. Only read by `assume_role`. Held as a credential, so no SBproxy code path formats it, but note that the process-wide log redactor keys off a fixed list of credential field names that does not include this one (`external_id` is also a non-secret payment identifier elsewhere in the product, so widening that list would mask reconciliation IDs). Supply it as a `${VAR}` or `vault://` reference so the literal never sits in the config file.",
          "type": [
            "string",
            "null"
          ]
        },
        "session_name": {
          "description": "Role session name recorded in CloudTrail. Only read by `assume_role`; defaults to `sbproxy`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "session_duration_secs": {
          "description": "Requested role session length in seconds. Only read by `assume_role`; unset takes the role's own default (one hour on most roles).",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "profile": {
          "description": "Named profile in the shared AWS config files. Read by `default_chain` and by the base identity `assume_role` starts from.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "AwsCredentialSource": {
      "description": "Where a provider's AWS credentials come from.",
      "oneOf": [
        {
          "description": "The standard AWS credential provider chain: environment variables, the shared config and credentials files, an OIDC web identity token (EKS IRSA), the ECS task role, and finally the EC2 instance profile. This is the default and the right answer for anything running inside AWS, because the chain refreshes short-lived credentials on its own.",
          "type": "string",
          "enum": [
            "default_chain"
          ]
        },
        {
          "description": "A long-lived access key pair supplied by config. `session_token` may carry an already-issued short-lived credential, but SBproxy cannot renew one it was handed: use `assume_role` or `default_chain` for credentials that expire.",
          "type": "string",
          "enum": [
            "static"
          ]
        },
        {
          "description": "STS `AssumeRole`, with the base identity coming from the default chain. The session is renewed before it expires, so a role session is the supported way to run on short-lived credentials.",
          "type": "string",
          "enum": [
            "assume_role"
          ]
        }
      ]
    }
  }
}
