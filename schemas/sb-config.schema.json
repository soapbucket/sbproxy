{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ConfigFile",
  "description": "Top-level config file structure (sb.yml).\n\nThis is the one container in the schema without `#[serde(deny_unknown_fields)]` (WOR-1140). Every nested container rejects an unknown key outright, which turns a typo in a server, security, or origin block into a boot error rather than a silent drop to the field's default. The root stays permissive so that an unknown top-level key reaches the `serde_ignored` pass in [`crate::compile_config`] as a diagnosable condition instead of dying as an untyped parse error, and that pass then decides what it is worth.\n\nIt decides on one question: does dropping the key change behavior? A descriptive leftover (`id`, `config_version`, `workspace_id`) warns and compiles. A flat schema-v1 key that carries origin behavior (`hostname`, `action`, `authentication`, `policies`, ...) is refused, because this type has no field for any of them and never translated them into `origins`: a file in that shape used to compile into a proxy with no origin at all. Go compatibility is deprecated; see `MIGRATION.md` and `tests/v1_compat.rs`.",
  "type": "object",
  "properties": {
    "source": {
      "description": "Optional source descriptor.\n\nWhen set, the config compiler resolves the listed source(s) before parsing the rest of the file. The remaining fields on `ConfigFile` are still honored: the file the source resolves to is itself a `ConfigFile`. When unset (the historical default), the file is treated as inline config.",
      "default": null,
      "anyOf": [
        {
          "$ref": "#/definitions/ConfigSource"
        },
        {
          "type": "null"
        }
      ]
    },
    "extensions": {
      "description": "Extension bundle discovery sources. Paths stay unresolved in this parsed representation and are interpreted only by the runtime loader.",
      "default": {
        "bundles_dir": null,
        "sources": [],
        "grants": {}
      },
      "allOf": [
        {
          "$ref": "#/definitions/ExtensionBundlesConfig"
        }
      ]
    },
    "proxy": {
      "description": "Server-wide settings parsed from the top-level `proxy:` block.",
      "default": {
        "http_bind_port": 8080,
        "bind_address": null,
        "zone": null,
        "http2_cleartext": false,
        "https_bind_port": null,
        "tls_cert_file": null,
        "tls_key_file": null,
        "acme": null,
        "http3": null,
        "metrics": null,
        "observability": null,
        "alerting": null,
        "admin": null,
        "model_host": null,
        "classifier_hooks": null,
        "ai_toolkit": null,
        "cluster": null,
        "config_authority": null,
        "secrets": null,
        "key_management": null,
        "l2_cache_settings": null,
        "anomaly": null,
        "cache_reserve": null,
        "response_cache_store": null,
        "compression_state": null,
        "config_history": null,
        "messenger_settings": null,
        "trusted_proxies": [],
        "correlation_id": {
          "enabled": true,
          "header": "X-Request-Id",
          "echo_response": true
        },
        "mtls": null,
        "ai_providers_file": null,
        "device_parser_file": null,
        "synthetic_probe": null,
        "agent_registry": null,
        "notifications": null,
        "scripting": {
          "lua": {
            "sandbox": {
              "max_execution_ms": 100,
              "max_memory_mb": 8,
              "allow_patterns": true
            }
          },
          "javascript": {
            "sandbox": {
              "budget_ms": 100,
              "memory_mb": 16,
              "stack_kb": 1024
            }
          }
        },
        "extensions": {},
        "http_client_timeouts": {
          "forward_auth_client_secs": 30,
          "forward_auth_request_secs": 5,
          "bot_auth_directory_client_secs": 5,
          "swr_client_secs": 30,
          "callback_client_secs": 10
        },
        "web_bot_auth": null,
        "attestation": null,
        "tenants": [],
        "credentials": [],
        "federation": null,
        "payments": null
      },
      "allOf": [
        {
          "$ref": "#/definitions/ProxyServerConfig"
        }
      ]
    },
    "origins": {
      "description": "Map of hostname to per-origin configuration.",
      "default": {},
      "type": "object",
      "additionalProperties": {
        "$ref": "#/definitions/RawOriginConfig"
      }
    },
    "access_log": {
      "description": "Optional structured-JSON access-log emission, off by default. When enabled, every completed request emits one JSON line via the tracing `access_log` target. See [`AccessLogConfig`] for filtering and sampling controls.",
      "default": null,
      "anyOf": [
        {
          "$ref": "#/definitions/AccessLogConfig"
        },
        {
          "type": "null"
        }
      ]
    },
    "agent_classes": {
      "description": "Top-level agent-class catalog selection and resolver tuning. When unset, the binary constructs a resolver from the embedded default catalog (so per-agent metric labels keep firing); operators set this block to provide an inline catalog or change the rDNS / bot-auth / cache settings. Hosted-feed fields remain parseable for compatibility but are not fetched by the OSS runtime.",
      "default": null,
      "anyOf": [
        {
          "$ref": "#/definitions/AgentClassesConfig"
        },
        {
          "type": "null"
        }
      ]
    },
    "rate_limits": {
      "description": "WOR-1130: top-level workspace rate-limit budget + auto-suspend escalation (the R2.3 / A2.5 contract). Distinct from the per-origin `rate_limits` policy: this is a workspace-wide ceiling with a soft / throttle / auto-suspend state machine.",
      "default": null,
      "anyOf": [
        {
          "$ref": "#/definitions/RateLimitsConfig"
        },
        {
          "type": "null"
        }
      ]
    },
    "audit": {
      "description": "Durable form of the audit trail. Absent, or present with the default `sink: memory`, keeps every audit channel in the bounded in-memory ring and on its tracing target, both of which die with the process. `sink: chain` additionally appends every `security_audit` event to a hash-chained, signed file.",
      "default": null,
      "anyOf": [
        {
          "$ref": "#/definitions/AuditConfig"
        },
        {
          "type": "null"
        }
      ]
    },
    "egress": {
      "description": "Operator-authored egress allowlists that arm the AiProvider, UsageSink, ModelArtifact, TokenExchange, and Telemetry purposes (WOR-2476, WOR-2481). Absent, or a sub-block omitted within it, keeps the corresponding purpose exactly as ungated as it was before this section existed. See [`EgressTopLevelConfig`].",
      "default": null,
      "anyOf": [
        {
          "$ref": "#/definitions/EgressTopLevelConfig"
        },
        {
          "type": "null"
        }
      ]
    },
    "session_ledger": {
      "description": "WOR-1186: emit the canonical session ledger (per-tool-call run records) from the live MCP `tools/call` path. Off unless this block is present and `enabled: true`.",
      "default": null,
      "anyOf": [
        {
          "$ref": "#/definitions/SessionLedgerConfig"
        },
        {
          "type": "null"
        }
      ]
    },
    "request_events": {
      "description": "Where completed request events go. Absent, or present with the default `sink: none`, keeps the dispatch on the request path a no-op and every event is discarded.",
      "default": null,
      "anyOf": [
        {
          "$ref": "#/definitions/RequestEventsConfig"
        },
        {
          "type": "null"
        }
      ]
    },
    "events": {
      "description": "Where typed proxy events go. Absent, or present with the default `sink: none`, means the eighteen event types stay in-process and nothing leaves the proxy.",
      "default": null,
      "anyOf": [
        {
          "$ref": "#/definitions/EventsConfig"
        },
        {
          "type": "null"
        }
      ]
    },
    "flags": {
      "description": "Process-wide feature flags available to CEL through `flag_enabled(name, key)`. An absent or empty list installs an empty runtime store, including on hot reload.",
      "default": [],
      "type": "array",
      "items": {
        "$ref": "#/definitions/FeatureFlagConfig"
      }
    },
    "update": {
      "description": "WOR-1804: how `sbproxy update` behaves for the binary and the managed inference engines. Optional; an absent block is the same as the defaults (stable channel, no background check).",
      "default": {
        "channel": "stable",
        "auto": false,
        "check_interval_secs": 86400
      },
      "allOf": [
        {
          "$ref": "#/definitions/UpdateConfig"
        }
      ]
    },
    "origin_defaults": {
      "description": "WOR-2436: what an origin is before any project has an opinion about it.\n\nThe same shape as one entry under `origins:`, held untyped because the composition resolver merges `policies:`, `transforms:`, `request_modifiers:` and `response_modifiers:` by a `name:` key that the typed modifier structs reject (`RequestModifierConfig` is `deny_unknown_fields` and has no `name` field). Every entry in those four lists must carry a `name:`, because a default has to be addressable to be overridable, and each may carry `locked: true` to refuse a project override. See [`crate::origin_profile`].\n\nAuthority-writable on purpose: this block is the platform setting a security floor, which is the whole reason it exists. Its sibling `origin_sources` is not, and is on [`crate::AUTHORITY_DENIED_PATHS`].",
      "type": [
        "object",
        "null"
      ],
      "additionalProperties": true
    },
    "origin_sources": {
      "description": "WOR-2436: which project repositories the aggregator pulls, what hosts each one answers on, and the environment tier the pinning rule is judged against. See [`OriginSourcesConfig`].",
      "anyOf": [
        {
          "$ref": "#/definitions/OriginSourcesConfig"
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "definitions": {
    "ConfigSource": {
      "description": "Where to load a `sb.yml` config text from.\n\nThe default (no `source:` field at all) means the file is the config: the surrounding `ConfigFile` is treated as inline content and consumed directly. When `source:` is present, the compiler resolves it to a config text string before parsing.\n\nThree kinds are recognized today:\n\n* `local` keeps the historical behavior - the inline file is the config. This is the form that round-trips when an operator writes `source: { kind: local }` explicitly. * `git` points at a remote git repository, an optional revision (branch, tag, or commit), and a path within the repository to the actual config file. * `git_overlay` composes one base source with one or more overlay sources, merging each in order. A `db` form is reserved for a later iteration but is intentionally not part of this primitive yet.\n\nA git source is transport trust: HTTPS plus whatever the git host authenticated, and nothing more. Pin `revision` to a full commit sha and set `verify_signature` to close most of that gap; see [`crate::source`] for the resolution contract.",
      "oneOf": [
        {
          "description": "The inline file is the config; nothing is fetched. This is the historical behavior and the implied default when `source:` is omitted.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "local"
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Clone a git repository and read a single file inside it as the config text.",
          "type": "object",
          "required": [
            "kind",
            "path",
            "repo"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "git"
              ]
            },
            "repo": {
              "description": "Repository URL (https, ssh, or any URL `git clone` accepts).",
              "type": "string"
            },
            "revision": {
              "description": "Optional branch, tag, or full commit sha. When `None`, the default branch is used. A full commit sha is a pin: the loader verifies the resolved `HEAD` equals it, so a branch moving underneath the node cannot be followed silently.",
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "path": {
              "description": "Path inside the repository to the config file, relative to the repository root.",
              "type": "string"
            },
            "credential": {
              "description": "Optional credential reference for a private repository, as `env:NAME`, `${NAME}`, `file:/path`, or `secret://backend/name`. An inline literal is refused: a token in a config file is a token in every copy of that file.",
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "verify_signature": {
              "description": "Require a valid signature on the resolved tag or commit. Off by default, because most repositories are not signed.",
              "default": false,
              "type": "boolean"
            },
            "confine": {
              "description": "Treat the fetched document as externally authored: it may not carry a host-backed secret reference (`env:NAME`, `file:PATH`, `vault://env/NAME`) and may not name a host path the proxy opens (WOR-2433).\n\nOff by default, and deliberately: turning it on for everybody would be a fail-closed upgrade on running fleets. A GitOps repository that names a host path anywhere would refuse its own config on the release that changed the default, and a node that boots into a refusal serves nothing. The host-path half also has no substitute spelling, because a path still has to be a path on this host, so the only place to move one is a layer this node owns.\n\nIt is not silent while it is off: a document that reaches for this host logs one warning naming the **first** finding the check reaches, at boot and again whenever a refresh brings a revision this process has not already checked, naming the source and the key and never the value. First rather than every, because the check stops at the first thing it refuses: a document naming both a host path and an `env:` reference reports one of them now and the other once you fix it.\n\nPer source leaf, not per tree. A `git_overlay` resolves each `kind: git` node with its own `confine`, so an overlay left unconfined keeps its own powers and warns on its own.\n\nTurn it on when the repository is written by somebody other than whoever runs this proxy; the document then gets the same treatment a config-authority bundle gets. A secret still has a spelling under it: `${VAR}` survives confinement and is substituted before the parse, which is what `proxy.cluster.security.shared_key` and every other secret-bearing field can use. See the `Confined fragments` section of docs/configuration.md.",
              "default": false,
              "type": "boolean"
            },
            "timeout_secs": {
              "description": "Hard timeout for one fetch, in seconds. The child `git` process is killed when it expires; a config-load path with no timeout hangs startup.",
              "default": 60,
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "refresh_interval_secs": {
              "description": "How often to re-resolve this source while the proxy runs, in seconds. `0` disables refresh, so the document is resolved once at boot and on every ordinary reload.",
              "default": 60,
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Compose a base source with one or more overlays. Each overlay is merged onto the accumulated result in the order it appears in the list.",
          "type": "object",
          "required": [
            "base",
            "kind",
            "overlays"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "git_overlay"
              ]
            },
            "base": {
              "description": "The base source the overlays are layered on top of.",
              "allOf": [
                {
                  "$ref": "#/definitions/ConfigSource"
                }
              ]
            },
            "overlays": {
              "description": "Overlays applied in order; each is itself a `ConfigSource` so overlays can chain arbitrarily deep (subject to the recursion cap enforced by the loader).",
              "type": "array",
              "items": {
                "$ref": "#/definitions/ConfigSource"
              }
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "ExtensionBundlesConfig": {
      "description": "Top-level extension bundle discovery configuration from `sb.yml`.",
      "type": "object",
      "properties": {
        "bundles_dir": {
          "description": "Short local-directory form. Relative paths remain relative here and are interpreted by the runtime that loads the configuration.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "sources": {
          "description": "Additional bundle sources, consumed in declaration order.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/BundleSourceConfig"
          }
        },
        "grants": {
          "description": "Capability grants, keyed by bundle manifest `name`.\n\nA bundle's hooks may declare capabilities in their manifest (`net:outbound=<scheme>://<host>[:port]`); nothing is granted by declaration alone. The operator grants here, with the same entry syntax, and a candidate whose declared set exceeds its granted set refuses at load naming both sets. An empty or absent grant list means the bundle gets no capabilities, which is the default every bundle had before this key existed.",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      },
      "additionalProperties": false
    },
    "BundleSourceConfig": {
      "description": "One ordered source of extension bundle directories.",
      "oneOf": [
        {
          "description": "A directory on the local filesystem.",
          "type": "object",
          "required": [
            "path",
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "directory"
              ]
            },
            "path": {
              "description": "Directory containing bundle subdirectories.",
              "type": "string"
            }
          },
          "additionalProperties": false
        },
        {
          "description": "A directory materialized from a pinned or signed Git revision.",
          "type": "object",
          "required": [
            "path",
            "repo",
            "revision",
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "git"
              ]
            },
            "repo": {
              "description": "Repository URL accepted by the system Git client.",
              "type": "string"
            },
            "revision": {
              "description": "Full commit SHA or signed tag selected for this source.",
              "type": "string"
            },
            "path": {
              "description": "Bundle directory inside the repository.",
              "type": "string"
            },
            "credential": {
              "description": "Optional secret reference used to authenticate the fetch.",
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "verify_signature": {
              "description": "Require a valid signature on the selected tag or commit.",
              "default": false,
              "type": "boolean"
            },
            "timeout_secs": {
              "description": "Hard timeout for one fetch, in seconds.",
              "default": 60,
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "refresh_interval_secs": {
              "description": "Refresh cadence in seconds. Zero resolves only during ordinary startup and reload operations.",
              "default": 60,
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "ProxyServerConfig": {
      "description": "Server-level proxy configuration parsed from the top-level `proxy:` block of sb.yml.\n\nThis is the composite home for every server-wide knob the request path reads before routing reaches an origin: listener ports, TLS / ACME sources, optional metrics and alerting, the admin API, secrets resolution, and the optional shared-state backends (L2 cache + messenger). Out-of-tree top-level blocks live in [`Self::extensions`] and are ignored by the compiler.",
      "type": "object",
      "properties": {
        "http_bind_port": {
          "description": "HTTP listener port. Defaults to 8080.",
          "default": 8080,
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "bind_address": {
          "description": "Address the public listeners bind. Defaults to `0.0.0.0`, every interface.\n\nApplies to both [`Self::http_bind_port`] and [`Self::https_bind_port`], deliberately. Two fields would let an operator lock down HTTP, leave HTTPS on every interface, and believe the box was closed; one field cannot be half-applied.\n\nA server deployment wants the default, which is why it is the default. Set `127.0.0.1` when the proxy serves only processes on the same machine, or a specific interface address to pick one NIC. `sbproxy run` and `sbproxy service install` generate `127.0.0.1` (WOR-2199): they configure one machine for itself, and the address they print has to be the address they bound.\n\nThis is not a substitute for authentication. It limits who can reach the listener, not what they may do once they reach it.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "zone": {
          "description": "The availability zone this proxy considers itself in, e.g. `\"us-east-1a\"` (WOR-2328).\n\nLoad balancer targets whose `targets[].zone` label matches are preferred by target selection, with per-request spillover across zones when no same-zone target is healthy. When unset, the `SB_ZONE` environment variable fills in at pipeline compile time (see [`Self::resolve_zone`]), which is how a Kubernetes deployment feeds in the node's `topology.kubernetes.io/zone` label. Config wins over the environment so a deployment that states its zone here can never be re-zoned by a stray variable. With neither set, target selection ignores zone labels entirely and warns at boot when labels are authored anyway.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "http2_cleartext": {
          "description": "Enable HTTP/2 cleartext (h2c) on the plain HTTP listener.\n\nWhen `true`, the proxy detects the HTTP/2 connection preface on the unencrypted port and serves the connection as HTTP/2. This is required for plaintext gRPC clients that do not negotiate TLS+ALPN. When `false` (the default), the listener speaks HTTP/1.1 only and rejects raw h2 prefaces as malformed requests. TLS-fronted HTTP/2 is unaffected and continues to work via ALPN on `https_bind_port`.",
          "default": false,
          "type": "boolean"
        },
        "https_bind_port": {
          "description": "Optional HTTPS listener port. When set, `tls_cert_file` + `tls_key_file` (or `acme`) must be configured too.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint16",
          "minimum": 0.0
        },
        "tls_cert_file": {
          "description": "Path to a PEM-encoded TLS certificate. Ignored when `acme` is configured.",
          "type": [
            "string",
            "null"
          ]
        },
        "tls_key_file": {
          "description": "Path to the PEM-encoded TLS key matching `tls_cert_file`.",
          "type": [
            "string",
            "null"
          ]
        },
        "acme": {
          "description": "ACME (Let's Encrypt-style) certificate management. When set, overrides the manual `tls_cert_file` / `tls_key_file` pair.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/AcmeConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "http3": {
          "description": "Reserved HTTP/3 (QUIC) listener configuration.\n\nThe block remains in the schema for forward compatibility. Omission or `enabled: false` compiles, but `enabled: true` is rejected because this build does not serve HTTP/3. Native support is tracked in WOR-1969.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/Http3Config"
            },
            {
              "type": "null"
            }
          ]
        },
        "metrics": {
          "description": "Metrics collection settings, including cardinality limiting.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/MetricsConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "observability": {
          "description": "Top-level observability block: live log sinks, redaction and custom fields plus the OTLP exporter and durable usage rollups. The parent log level, format, and sampling fields are compatibility-only; process tracing uses CLI/environment selection and built-in sampling defaults.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ObservabilityConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "alerting": {
          "description": "Alert notification channel configuration.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/AlertingConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "admin": {
          "description": "Embedded admin/stats API server configuration.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/AdminConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "model_host": {
          "description": "Canonical desired state and lifecycle policy for models hosted by SBproxy.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ModelHostControlConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "classifier_hooks": {
          "description": "Optional classifier-sidecar hooks for intent and quality routing.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ClassifierHooksConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "ai_toolkit": {
          "description": "Optional bounded AI workflow, evaluation, and rollout runtime.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/AiToolkitConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "cluster": {
          "description": "Optional shared cluster substrate for keys, metrics, and managed models.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ClusterConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "config_authority": {
          "description": "Optional config-authority participation: subscribe to signed configuration bundles published by an upstream authority.\n\nThe whole block sits on [`crate::config_merge::AUTHORITY_DENIED_PATHS`], so a bundle can never repoint a subscriber at a different authority or relax its verification.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ConfigAuthorityConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "secrets": {
          "description": "Secrets management configuration.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/SecretsConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "key_management": {
          "description": "Dynamic key-management configuration (WOR-1546): the mutable key store, policy cache, at-rest crypto, OIDC claim mapping, and an optional declarative seed. Distinct from the static `credentials:` block, which keeps working and lowers into the same store as config-sourced records.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/KeyManagementConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "l2_cache_settings": {
          "description": "Optional L2 cache / shared-state backend. When set with `driver: redis`, rate limit counters and response cache entries are stored in the external backend so multiple proxy replicas share state.\n\nAccepted under either `l2_cache` (canonical) or `l2_cache_settings` (alias).",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/L2CacheConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "anomaly": {
          "description": "WOR-2666: optional behavioral anomaly detection.\n\nWhen `enabled`, the proxy keeps a rolling per-agent-class histogram of the categorical signals it already collects (TLS fingerprint, ML classification, headless-library detection) plus a per-IP request rate, and flags observations that sit in the long tail. Absent or disabled, no histogram is built and the detector hook is not installed.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/AnomalyConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "cache_reserve": {
          "description": "Optional Cache Reserve (long-tail cold tier) configuration.\n\nWhen `enabled`, response-cache entries that pass the admission filter are mirrored to the configured backend: memory, filesystem, Redis, object storage (S3, Google Cloud Storage, Azure Blob, or a local directory, with optional at-rest sealing), or S3 with AWS KMS envelope encryption. On a hot miss the proxy consults the reserve before falling through to origin and promotes the entry back into the hot tier on hit.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/CacheReserveConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "response_cache_store": {
          "description": "Optional selection of the shared response-cache backing store.\n\nOne store serves every origin: the cache key already carries workspace, hostname, method, and path, so origins never collide. When this block is absent, the store is chosen the way it always was, Redis if `l2_cache` is set and an in-process map otherwise, so an existing config keeps the backend it has today.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ResponseCacheStoreConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "compression_state": {
          "description": "Process-owned path configuration for durable Local compression state.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/CompressionStateRuntimeConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "config_history": {
          "description": "Durable last-known-good config history (WOR-2456): the ring of every config this proxy has applied, kept on local disk so a rollback has somewhere to restore from. Absent (the default) keeps the proxy's existing behavior exactly; no ring is written. See [`ConfigHistoryConfig`].",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ConfigHistoryConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "messenger_settings": {
          "description": "Shared message bus. Not supported in this build: setting it fails config compile (WOR-2166). The block still parses so the failure is an explanatory diagnostic rather than an unknown-key error, and so an archived schema-v1 document reaches that diagnostic.\n\nIt was never wired to anything. Nothing subscribed to a topic and nothing published on one, so an accepted bus moved no events. Config distribution across replicas is `proxy.config_authority`; cache invalidation is `POST /admin/cache/purge` against a shared Redis tier configured through `proxy.l2_cache`.\n\nYAML key: `messenger_settings`.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/MessengerSettings"
            },
            {
              "type": "null"
            }
          ]
        },
        "trusted_proxies": {
          "description": "CIDR ranges (or bare IPs) whose `X-Forwarded-For` / `X-Real-IP` / `Forwarded` headers the proxy will trust. When SBProxy is itself behind a load balancer or CDN (Cloudflare, ALB, Fly.io, ...), set this to the upstream proxy's source range so the real client IP can be recovered from the forwarding chain. Connections from any peer outside this list have their inbound forwarding headers stripped before processing, so they cannot be spoofed.\n\nEmpty by default. The TCP peer is treated as the client and no inbound forwarding metadata is honored.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "correlation_id": {
          "description": "Correlation-ID propagation policy. By default, the proxy honors any inbound `X-Request-Id` header, generates one if absent, forwards the value to the upstream, and echoes it in the downstream response. Set fields here to change the header name or disable.",
          "default": {
            "enabled": true,
            "header": "X-Request-Id",
            "echo_response": true
          },
          "allOf": [
            {
              "$ref": "#/definitions/CorrelationIdConfig"
            }
          ]
        },
        "mtls": {
          "description": "Optional mTLS (mutual TLS) client certificate verification on the HTTPS listener. When set, the TLS handshake requires (or optionally accepts) a client certificate signed by the configured CA bundle. Cert metadata that Pingora exposes (organization, serial number, fingerprint) is forwarded to the upstream as `X-Client-Cert-*` headers. Requires `https_bind_port` to be set.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/MtlsListenerConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "ai_providers_file": {
          "description": "Optional override for the embedded AI provider catalog. When set, the AI gateway loads provider metadata (auth header, auth prefix, base URL, wire format, capabilities) from this YAML file at startup instead of the version compiled into the binary.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "device_parser_file": {
          "description": "Refused at config compile. The device parser in this build matches on compiled-in rules and has no code path that loads a regex catalog from disk, so a path here named a file the proxy never opened. Compare [`Self::ai_providers_file`] just above, which is the same idea for the provider catalog and is read at startup.\n\nRetained as a parseable field so the failure explains itself rather than reading as an unknown key.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "synthetic_probe": {
          "description": "Optional synthetic-transaction probe driving an in-process request through the compiled handler chain on a fixed cadence and reporting the verdict on `/readyz`. Disabled by default; opt in for deployments that want `/readyz` to fail when the proxy is unable to service its own requests.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/SyntheticProbeConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "agent_registry": {
          "description": "Optional agent registry: a signed catalog of known agents plus an owner-approval queue for agents that ask to register themselves. Disabled by default. State lives in one embedded redb file named by `store_path`; nothing here needs a database or a sidecar.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/AgentRegistryConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "notifications": {
          "description": "Optional outbound webhook notifications: many subscriptions, each with its own filter and signing key, bounded retries, and a durable deadletter queue with replay. Disabled by default. State lives in one embedded redb file named by `store_path`.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/NotificationsConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "scripting": {
          "description": "Scripting runtime limits. Today this block carries the Lua sandbox knobs (execution-time budget, memory budget, pattern API gating); other languages (CEL, JavaScript, WebAssembly) keep their own knobs elsewhere until they have similar enforcement surfaces. When omitted, the documented defaults are applied (see [`LuaSandboxConfig::default`]).",
          "default": {
            "lua": {
              "sandbox": {
                "max_execution_ms": 100,
                "max_memory_mb": 8,
                "allow_patterns": true
              }
            },
            "javascript": {
              "sandbox": {
                "budget_ms": 100,
                "memory_mb": 16,
                "stack_kb": 1024
              }
            }
          },
          "allOf": [
            {
              "$ref": "#/definitions/ScriptingConfig"
            }
          ]
        },
        "extensions": {
          "description": "Opaque extensions for out-of-tree top-level config blocks. The compiler never parses these; extension consumers read their own keys.",
          "default": {},
          "type": "object",
          "additionalProperties": true
        },
        "http_client_timeouts": {
          "description": "Tunable client-side timeouts for the proxy's outbound HTTP helpers (forward-auth, callbacks, mirrors, SWR refreshes, bot- auth directory). Defaults match the prior hardcoded literals so existing configs see no behavior change. See [`HttpClientTimeoutsConfig`] for the field list.",
          "default": {
            "forward_auth_client_secs": 30,
            "forward_auth_request_secs": 5,
            "bot_auth_directory_client_secs": 5,
            "swr_client_secs": 30,
            "callback_client_secs": 10
          },
          "allOf": [
            {
              "$ref": "#/definitions/HttpClientTimeoutsConfig"
            }
          ]
        },
        "web_bot_auth": {
          "description": "Web Bot Auth signing identity (WOR-805). When set, the proxy publishes the derived Ed25519 public key as an HTTP Message Signatures directory at `/.well-known/http-message-signatures-directory` so verifiers (including SBproxy's own inbound `bot_auth` directory client) can check the Web Bot Auth signatures the proxy produces. The 32-byte seed is also the key the proxy signs its outbound requests with. Absent keeps the endpoint off so existing configs are unaffected.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/WebBotAuthConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "attestation": {
          "description": "Consumption attestation (WOR-2127): whether this proxy asserts what a call is going to cost, records what it actually consumed, and what it charges for. Absent leaves the whole mechanism off, so an existing config is unaffected. See [`AttestationConfig`].",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/AttestationConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "tenants": {
          "description": "WOR-1053: declared tenants. Each entry carries an `id` referenced by `origin.tenant_id`. Future PRs add per-tenant `credentials`, `policies`, and `vault` blocks; PR1 lands the scope so the rest of the credentials epic can land against a stable tenant resolver.\n\nWhen empty, every origin resolves to the synthetic `__default__` tenant. Existing single-tenant configs see no behavior change. An origin that names a tenant not declared here fails config compile.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/ProxyTenantConfig"
          }
        },
        "credentials": {
          "description": "Canonical credentials block at proxy scope. The full schema lives in [`CredentialBlock`]. Tenant and origin scopes carry matching `credentials:` fields; resolution at request time walks origin -> tenant -> proxy, with most-restrictive policies winning across the merged set.\n\nThe legacy `virtual_keys:` YAML key under `origins[].action.providers` is rejected at config compile; operators migrate to the canonical block per `docs/migration-credentials.md`.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/CredentialBlock"
          }
        },
        "federation": {
          "description": "OpenID Federation entity-statement serving for this proxy process. Absent, or present with `enabled: false`, leaves the well-known endpoint unmounted.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/FederationConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "payments": {
          "description": "Durable payment settlement. When absent, the proxy keeps its existing non-settlement crawl-ledger behavior exactly. When present, a paid request reaches the origin only after its durable intent has committed `Succeeded`.\n\nThe block is always parsed, on every build, so `sbproxy validate` reads the same document everywhere. The consumer compares [`crate::payments::PaymentsConfig::required_features`] against its own compiled feature set and fails startup naming the missing feature, so a configured rail that was not compiled in never reaches a first request.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/PaymentsConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "AcmeConfig": {
      "description": "ACME (Automatic Certificate Management Environment) configuration for automatic TLS.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Master switch for ACME-managed TLS certificates.",
          "default": false,
          "type": "boolean"
        },
        "email": {
          "description": "Account contact email registered with the ACME directory.",
          "default": "",
          "type": "string"
        },
        "directory_url": {
          "description": "ACME directory URL. Defaults to Let's Encrypt production.",
          "default": "https://acme-v02.api.letsencrypt.org/directory",
          "type": "string"
        },
        "challenge_types": {
          "description": "Allowed ACME challenge types in priority order. Defaults to `[http-01]`, the only type the proxy currently drives; `tls-alpn-01` is accepted in the list but is not yet served.",
          "default": [
            "http-01"
          ],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "storage_backend": {
          "description": "Backing store for issued certificates: `redb`, `sqlite`, `file`, `redis`, `s3`, `gcs`, `azure`, or `memory`. Anything else is rejected.",
          "default": "redb",
          "type": "string"
        },
        "storage_path": {
          "description": "Where the certificate store lives. The meaning depends on `storage_backend`: a filesystem directory for `redb`, `sqlite`, and `file`, a `host:port` for `redis`, and a bucket URL such as `s3://bucket/prefix` for `s3`, `gcs`, and `azure`. Ignored by the `memory` backend.",
          "default": "/var/lib/sbproxy/certs",
          "type": "string"
        },
        "renew_before_days": {
          "description": "Number of days before expiry to attempt renewal.",
          "default": 30,
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "ca_root": {
          "description": "PEM file holding a trusted root for the **ACME directory endpoint**, for a CA that is not in the system trust store.\n\nThis is about trusting the ACME server you talk to, not the certificates it issues. A private or test CA (step-ca, Pebble, an internal ACME server) presents a directory endpoint signed by a root the host does not know, and without this the client refuses the connection before any order begins.\n\nNamed and shaped after Caddy's `acme_ca_root`, deliberately. Verification stays **on**: the root is added to the trust store rather than the check being skipped, so a misconfigured or intercepted directory is still refused. There is no \"skip verification\" knob here and there should not be one.\n\nRead at issuance time rather than cached, so a rotated test CA does not need a restart. A missing or unparseable file refuses the issuance with an error naming the path; it never falls back to system roots, because falling back would silently restore the verification failure this setting exists to fix.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "Http3Config": {
      "description": "HTTP/3 (QUIC) configuration.\n\nThe shape is reserved for forward compatibility. The config compiler accepts an omitted or disabled block, but rejects `enabled: true` because this build does not serve HTTP/3. Native support is tracked in WOR-1969.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Whether to enable the HTTP/3 (QUIC) listener.\n\nMust remain `false` in this build. Setting it to `true` fails config compilation because HTTP/3 is not served.",
          "default": false,
          "type": "boolean"
        },
        "max_streams": {
          "description": "Maximum number of concurrent QUIC streams per connection.",
          "default": 100,
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "idle_timeout_secs": {
          "description": "Idle timeout for QUIC connections, in seconds.",
          "default": 30,
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "MetricsConfig": {
      "description": "Metrics collection configuration.",
      "type": "object",
      "properties": {
        "max_cardinality_per_label": {
          "description": "Max unique label values allowed per metric label before new values are collapsed to `__other__`. Defaults to 1 000.",
          "default": 1000,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "cardinality": {
          "description": "Per-label cardinality overrides.",
          "default": {
            "hostname_cap": null
          },
          "allOf": [
            {
              "$ref": "#/definitions/MetricsCardinalityConfig"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "MetricsCardinalityConfig": {
      "description": "Per-label metrics cardinality overrides.",
      "type": "object",
      "properties": {
        "hostname_cap": {
          "description": "Optional override for the `hostname` label cap.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "ObservabilityConfig": {
      "description": "Top-level observability block grouping the log surfaces, telemetry, and durable usage rollups. The process-logger `level` and `format` under `log` are installed, below the CLI flags and `RUST_LOG`; `sampling` is not.",
      "type": "object",
      "properties": {
        "log": {
          "description": "Log sinks, redaction, custom fields, and the process logger's level and format.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ObservabilityLogConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "telemetry": {
          "description": "OTLP exporter configuration. When `enabled = true`, the configured endpoint receives traces and (optionally) metrics.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ObservabilityTelemetryConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "usage_rollups": {
          "description": "Durable windowed usage rollups. On by default; omit the block to accept the defaults.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/UsageRollupsConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "ObservabilityLogConfig": {
      "description": "Subset of `sbproxy-observe::LoggingConfig` that lands in the public config schema. Kept in `sbproxy-config` so the YAML round-trips through serde without dragging a serde dependency back into the observe crate.",
      "type": "object",
      "properties": {
        "level": {
          "description": "Process log level: a bare level (`trace`, `debug`, `info`, `warn`, `error`) or any `tracing-subscriber` per-target directive such as `sbproxy_ai=debug,h2=warn`.\n\nThird in precedence, behind `--log-level` (with its `SB_LOG_LEVEL` env form) and `RUST_LOG`, and ahead of the `info` default. A process started with either of those two keeps them for its whole life; this value is what a deployment that sets neither gets. Applied again on every config reload, so SIGHUP picks up an edit without a restart.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "format": {
          "description": "Process output format: `compact`, `pretty`, or `json`. Behind `--log-format` (with its `SB_LOG_FORMAT` env form) and ahead of the `compact` default. Any other value is reported on stderr at startup and falls back rather than being silently accepted.\n\nRestart-only, unlike `level`: the output layer is built once and the runtime reload handle covers the filter alone. A reload logs the new value nowhere and keeps rendering in the old format. Sinks under `sinks:` carry their own independent `format`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "sampling": {
          "description": "Per-level sampling rates for the process logger. Parsed, validated, and inert: no emitter consults them, so the process logger drops no line at any rate. Per-request access-log sampling is a different key, `access_log.sample_rate:`, and that one is live.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ObservabilitySamplingConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "redact": {
          "description": "Operator-extensible redaction block. `fields` extends the built-in field-key denylist; `patterns` adds regex masks that run after the field-key pass. The built-in baseline (the hard-coded denylist in `sbproxy-observe::logging::apply_redaction`) always runs first and is not disable-able from YAML.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ObservabilityRedactConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "sinks": {
          "description": "WOR-1045: log sink fan-out (`stdout`, `stderr`, `file`, ...). When empty, the legacy single-tracing-subscriber path keeps driving stdout. Each declared sink has a unique `name` within this scope; duplicates fail config compilation. Tenant + origin sink scopes are blocked on the WOR-1051 credentials epic.\n\nDispatch wiring (writing each emitted line to each matching sink) lands in PR2. PR1 parses + validates the schema so an operator's e2e fixture (`e2e/tests/redaction.rs`) no longer errors at parse time.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/ObservabilitySinkConfig"
          }
        },
        "custom_fields": {
          "description": "Operator-defined custom access-log fields. Each entry adds a key to the access line's `custom` object, computed per request from either a static value with `${...}` variable interpolation or a script (CEL / Lua / JS) evaluated against the request context. Lets operators pivot logs on dimensions the built-in schema does not carry (region, deployment, a derived risk score, a hashed account id, ...) without forking the binary. Configurable at proxy, tenant, and origin scope; the sets compose per request as proxy then tenant then origin, with a more-specific scope's field overriding a less-specific field of the same `name`.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/CustomLogFieldConfig"
          }
        },
        "decision_audit": {
          "description": "Decision-event audit publication. Absent means off, which is also what `enabled: false` means: no decision event publishes an audit record at this scope.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/DecisionAuditConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "ObservabilitySamplingConfig": {
      "description": "Per-level sample rates for the structured-log emitter.\n\nEvery rate here is inert. The emitter has no sampling call site at all: it renders, redacts, and writes every record, so setting `debug: 0.0` drops nothing and setting `1.0` restores nothing. Documented rather than removed because the rates round-trip through existing configs; the surface that does throttle logs today is `access_log.sample_rate:`.",
      "type": "object",
      "properties": {
        "info": {
          "description": "Fraction of `info` lines that would be emitted. Inert.",
          "default": null,
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "debug": {
          "description": "Fraction of `debug` lines that would be emitted. Inert.",
          "default": null,
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "trace": {
          "description": "Fraction of `trace` lines that would be emitted. Inert.",
          "default": null,
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        }
      },
      "additionalProperties": false
    },
    "ObservabilityRedactConfig": {
      "description": "Operator-extensible redaction config. Sits under `proxy.observability.log.redact:` (today) and will surface at tenant and origin scopes once multi-tenant scaffolding lands.",
      "type": "object",
      "properties": {
        "fields": {
          "description": "Additional JSON field keys whose values are replaced with `[REDACTED:<NAME>]`. Matched case-insensitively against the keys produced by `serde_json`'s renderer. Always additive on top of the built-in denylist; tenants and origins cannot disable the baseline.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "patterns": {
          "description": "Regex masks applied to the rendered JSON after the field-key pass. Each pattern is compiled at config-load; invalid regex is a `compile_config` error.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/ObservabilityRedactPattern"
          }
        },
        "pii": {
          "description": "Optional rule-driven PII redactor. When enabled, the global `sbproxy-security::pii::PiiRedactor` runs as a fourth pass after the value pattern scrubber, the field denylist + operator fields, and the operator regex patterns. Rules are looked up in `sbproxy-security::pii::default_rules()` by name (`email`, `credit_card`, `us_ssn`, `phone_us`, `ipv4`, `openai_key`, `anthropic_key`, `aws_access`, `github_token`, `slack_token`, `iban`).",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ObservabilityPiiConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "ObservabilityRedactPattern": {
      "description": "One named regex mask. `name` is reported on cardinality / counter metrics; `replacement` defaults to `[REDACTED:<NAME>]` when empty.",
      "type": "object",
      "required": [
        "name",
        "pattern"
      ],
      "properties": {
        "name": {
          "description": "Operator-supplied label; appears in metrics + the marker.",
          "type": "string"
        },
        "pattern": {
          "description": "PCRE-style regex (Rust `regex` crate flavour).",
          "type": "string"
        },
        "replacement": {
          "description": "Replacement string. Defaults to `[REDACTED:<NAME_UPPER>]` when empty; can include `$1` backrefs if the pattern has groups.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "ObservabilityPiiConfig": {
      "description": "Operator-controlled PII redaction at the log layer. Mirrors the per-origin `PiiConfig` used by the AI handler but applies to every emitted log line, regardless of origin.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Master switch. When `Some(false)`, the redactor is never built and the pipeline shorts the PII pass at this scope (and any more-specific scope that inherits without overriding). When `None`, the scope inherits its parent's `enabled` flag (proxy default is \"off\"); `Some(true)` turns the pass on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "rules": {
          "description": "Names of the built-in rules to enable. When empty at the proxy scope, all default rules are enabled (the spirit of \"PII redaction on\" is the least-surprising default). At a tenant or origin scope the list is ADDED to the parent's resolved set, not replaced.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "disable": {
          "description": "Names of built-in rules to opt out of even when included by `rules:` or by the default-all behavior. The matching name is case-sensitive. At a tenant or origin scope the list is SUBTRACTED from the resolved set (parent inheritance plus this scope's `rules:` additions).",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "ObservabilitySinkConfig": {
      "description": "WOR-1045: one declared log sink. Multiple sinks fan out from a single emit; a tenant-scoped sink only receives lines whose resolved `Principal.tenant_id` matches the tenant scope. PR1 lands the schema and uniqueness validation; dispatch wiring lands in PR2.\n\n## Field schema\n\n* `name` is unique within the declaring scope. The same name may appear once at proxy scope and once at tenant scope; cross-scope collisions are intentional (a tenant `acme-loki` sink is a different thing from the proxy `acme-loki` sink). * `target` selects which internal channel feeds this sink: `access_log`, `error_log`, `audit_log`, `trace_exporter`, `external_log`. The channel maps 1:1 onto the existing `sbproxy_observe::logging::Sink` enum. * `format` is the wire shape: `compact | pretty | json`. When omitted the parent `proxy.observability.log.format` decides. * `output` is the where: `stdout | stderr | file`. `otlp` lands under WOR-1046; `syslog` is a planned follow-up. * `profile` is the redaction shape: `internal` keeps JA3/JA4 and raw query strings; `external` strips them. Tenant-scoped sinks default to `external` because the operator usually does not control the downstream backend.",
      "type": "object",
      "required": [
        "name",
        "output",
        "target"
      ],
      "properties": {
        "name": {
          "description": "Unique name within the declaring scope (proxy / tenant / origin). Duplicates within a scope are rejected at config compile.",
          "type": "string"
        },
        "target": {
          "description": "Which internal channel feeds this sink. One of `access_log | error_log | audit_log | trace_exporter | external_log`. Unknown values fail compilation.",
          "type": "string"
        },
        "format": {
          "description": "Wire format. One of `compact | pretty | json`. Defaults to the parent `observability.log.format` when omitted.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "output": {
          "description": "Where the line goes. `output: { type: stdout }` keeps the legacy stdout behavior; `file` reuses the access-log rotation stack; `otlp` lands under WOR-1046.",
          "allOf": [
            {
              "$ref": "#/definitions/ObservabilitySinkOutput"
            }
          ]
        },
        "profile": {
          "description": "Redaction profile applied to this sink's lines. One of `internal | external`. `external` strips JA3/JA4 fingerprints and raw query strings in addition to the standard redactions. Tenant-scoped sinks default to `external`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "ObservabilitySinkOutput": {
      "description": "WOR-1045 + WOR-1046: tagged-union of supported sink output types. Each variant carries its own configuration.\n\nVariants: `stdout`, `stderr`, `file`, `otlp`. `syslog` remains a planned follow-up. Unknown `type:` values fail compilation.",
      "oneOf": [
        {
          "description": "Write to process stdout. The default for a freshly-installed proxy.",
          "type": "object",
          "required": [
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "stdout"
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Write to process stderr. Useful for routing the audit channel separately from access on systemd-journald.",
          "type": "object",
          "required": [
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "stderr"
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Append to a file with optional rotation. Reuses the access-log rotation stack (`sbproxy_observe::access_log` rotation + gzip path).",
          "type": "object",
          "required": [
            "path",
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "file"
              ]
            },
            "path": {
              "description": "Absolute path to the output file. The parent directory must exist; the file is created on first write.",
              "type": "string"
            },
            "max_size_mb": {
              "description": "Maximum file size before rotation. Defaults to 100 MiB.",
              "default": null,
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0.0
            },
            "max_backups": {
              "description": "Number of rotated backups to keep. Defaults to 7.",
              "default": null,
              "type": [
                "integer",
                "null"
              ],
              "format": "uint32",
              "minimum": 0.0
            },
            "compress": {
              "description": "Whether to gzip rotated backups. Defaults to true.",
              "default": null,
              "type": [
                "boolean",
                "null"
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "WOR-1046: forward the rendered structured-log line to an OTLP log collector. The exporter wraps `opentelemetry_otlp::LogExporter` and ships records through a `BatchLogProcessor`. When `transport` and `timeout_secs` are omitted the sink inherits the values already declared on the top-level `telemetry:` block so a single operator config does not have to repeat collector coordinates.",
          "type": "object",
          "required": [
            "endpoint",
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "otlp"
              ]
            },
            "endpoint": {
              "description": "OTLP collector endpoint (e.g. `http://otel-collector:4318/v1/logs` for HTTP/proto, `http://otel-collector:4317` for gRPC). The path component is honored for HTTP transport; the gRPC variant uses the host:port only.",
              "type": "string"
            },
            "transport": {
              "description": "Transport selector: `http` or `grpc`. Defaults to whatever the top-level `telemetry.transport` declares; `grpc` when that block is absent.",
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "timeout_secs": {
              "description": "Per-export timeout in seconds. Defaults to 10 seconds when omitted; honored by the underlying OTLP exporter's HTTP / gRPC client.",
              "default": null,
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0.0
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "CustomLogFieldConfig": {
      "description": "One operator-defined custom access-log field.\n\nExactly one value source must be set: either `value` (a static string with `${...}` variable interpolation) or `source` together with `engine` (a script). Supplying both, or neither, is a config error. `engine` must be one of `cel`, `lua`, `js`. (`wasm` is rejected: it is a compiled module, not inline source.)",
      "type": "object",
      "required": [
        "name"
      ],
      "properties": {
        "name": {
          "description": "Key the computed value lands under in the access line's `custom` object. Must be unique within the scope.",
          "type": "string"
        },
        "value": {
          "description": "Static value with `${...}` variable interpolation. Mutually exclusive with `source` / `engine`. Supported variables include `${env.NAME}`, `${tenant_id}`, `${method}`, `${path}`, `${host}`, `${status}`, `${provider}`, `${model}`, `${request.header.NAME}`, and `${attribution.KEY}`.",
          "type": [
            "string",
            "null"
          ]
        },
        "engine": {
          "description": "Scripting engine for `source`. One of `cel`, `lua`, `js`. Required when `source` is set.",
          "type": [
            "string",
            "null"
          ]
        },
        "source": {
          "description": "Script source evaluated against the request context; its result is stringified into the field. Mutually exclusive with `value`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "DecisionAuditConfig": {
      "description": "Operator control over which decision events publish an audit record.\n\nEvery decision point in the pipeline (cache admission, cache-key derivation, routing, auth, policy) already produces a `reason` string saying what it decided and why. This block is what turns that rationale into an OCSF audit record instead of letting it be decoded and dropped.\n\n```yaml observability: log: decision_audit: enabled: true events: cache.admit: true ```\n\nProxy scope only in this release. The tenant and origin log blocks deny unknown fields and carry no `decision_audit` key, so a block written under either one fails config load rather than composing or being ignored. When scoping does land it will compose the way `custom_fields` does, per key rather than per block: a more-specific scope naming one event must not wipe the entries a less-specific scope set for the others.\n\n# This block is the only thing that decides what publishes\n\nNo event carries a default of its own. Every label is off until a setting here says otherwise, and there is nowhere else to look. An earlier draft kept a second per-event default outside the config, which is how an operator ends up believing a feed is on: the two disagreed, and the one nothing consulted was the one that read as authoritative.\n\nThe security ordering behind that draft is worth keeping, as the shape a later release should default on once the events have emitters. `auth`, `policy`, `rate_limit`, `waf`, both AI guardrail events, `ai.tool_call`, `mcp.tool`, and `payment.lifecycle` are the security-relevant ones, and an operator who can afford only part of the feed wants those. `cache.key` is security-relevant and also the densest event in the set, once per cacheable request, so it stays an explicit opt-in rather than riding a default. `route.decide` is worth publishing when a decision crosses a provider or data-residency boundary and is noise otherwise, which is a question about the decision rather than about the event, so it needs the routing event config to carry that predicate before any default for it means anything.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Master switch for the block. `Some(true)` publishes every event the `events` map does not name, except `ai.stream.event`, which is never published by any setting; `Some(false)` and `None` are both **off**, as is an absent block.\n\nAccepted at proxy, tenant, and origin scope. A tenant or origin block composes over the proxy one per event label, so naming `route.decide` under a tenant inherits the proxy's `cache.admit` rather than replacing the map; see [`DecisionAuditScopes`].\n\n`None` reads as off rather than as inherit, because a scope that wrote no block asked for nothing and the composition already gives a wider scope its say.\n\nOff is the default because the decision events differ by orders of magnitude in how often they fire. `cache.key` runs once per cacheable request, so a master switch with permissive defaults would hand an operator a per-request SIEM feed on their busiest origin the moment they flipped it. That is an ingest bill rather than a control, and the usual answer to a feed nobody can afford is to turn the whole thing off, which takes the security-relevant events with it. Opting in per event costs one line and keeps that choice available.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "events": {
          "description": "Per-event override of the master switch, keyed by the event's stable label ([`sbproxy_observe::decision::DecisionEvent::as_label`]): `cache.admit`, `cache.key`, `route.decide`, `auth`, `policy`, and the rest. `true` publishes that event's records, `false` silences it. An event this map does not name falls back to the master switch.\n\nA key naming no known event is refused at config load rather than ignored, because a typo'd label is a feed the operator believes they turned on and nobody is watching. `ai.stream.event: true` is refused by value for the same reason the block defaults off: that event fires once per streamed chunk. `ai.close` carries the stream's summary instead. That one label is also the single exception to the fallback above: it never publishes, master switch or not.\n\nA `BTreeMap` rather than a `HashMap`, matching `custom_fields`: the composed set is iterated to resolve the emission policy, and a hash order would make two proxies holding identical config walk it differently.",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "boolean"
          }
        },
        "policy_record_format": {
          "description": "Which wire shape a `policy` decision publishes (WOR-2448).\n\nPolicy decisions have always published a `PolicyVerdictEvent` under the `policy_verdict_event:` prefix, serialized through its serde derive. Every other decision publishes a `DecisionAudit` rendered as OCSF under `decision_audit_event:`. Same bus, same class of event, two formats, so an analyst reconstructing every control decision on one request parses both and joins them by hand.\n\nThis selects between them for the `policy` event, and nothing else. Exactly one record is published either way: emitting both during a migration would double volume on the densest event in the system and give an analyst two rows for one decision.\n\n`legacy` is the default this release, so an upgrade changes nothing your filters depend on and a startup warning names the deprecation instead. Set `decision` once your consumer reads the shared shape. The default changes in the next major release.",
          "anyOf": [
            {
              "$ref": "#/definitions/PolicyRecordFormat"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "PolicyRecordFormat": {
      "description": "Wire shape for `policy` decision records (WOR-2448).\n\nA deprecation dial rather than a preference. The two variants are the before and after of one migration, and `legacy` exists to give operators a release in which to move their consumers rather than to be a supported alternative.",
      "oneOf": [
        {
          "description": "`policy_verdict_event:` carrying a serde-serialized `PolicyVerdictEvent`. The shape shipped since the audit bus landed, and the default this release.\n\nIts known gap is that it carries no free-text reason, so the most security-relevant event in the system is the one that cannot say why it decided. That gap is not fixable in this shape without changing it, which is what the migration is for.",
          "type": "string",
          "enum": [
            "legacy"
          ]
        },
        {
          "description": "`decision_audit_event:` carrying a `DecisionAudit` rendered as OCSF API Activity 6003, with the policy id, surface, verdict, and latency as selectable fields under `unmapped`, plus the reason the legacy shape has no room for.\n\nOne shape for every control decision, which is the only version of this surface an analyst can query as a whole.",
          "type": "string",
          "enum": [
            "decision"
          ]
        }
      ]
    },
    "ObservabilityTelemetryConfig": {
      "description": "Subset of `sbproxy-observe::TelemetryConfig` exposed in the YAML.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Whether OTLP export is enabled.",
          "default": false,
          "type": "boolean"
        },
        "endpoint": {
          "description": "OTLP collector endpoint URL.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "transport": {
          "description": "Transport: `http` or `grpc`. Default `grpc`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "service_name": {
          "description": "`service.name` resource attribute. Default `sbproxy`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "sample_rate": {
          "description": "Head-based sampling probability for unsampled roots. Default 0.1.",
          "default": null,
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "always_sample_errors": {
          "description": "Always-sample errors / policy blocks / ledger denials. Default true.",
          "default": null,
          "type": [
            "boolean",
            "null"
          ]
        },
        "keep_over_budget_usd": {
          "description": "Keep any completed trace at or above this derived USD cost.",
          "default": null,
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "keep_slower_than_secs": {
          "description": "Keep any completed trace at or above this wall-clock latency.",
          "default": null,
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "propagation": {
          "description": "Propagation format. Only `w3c` (the default) is wired; the binary refuses to start with any other value.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "resource_attrs": {
          "description": "Free-form resource attributes attached to every span.",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "export_metrics": {
          "description": "Mirror metrics over OTLP in addition to the Prometheus scrape.",
          "default": false,
          "type": "boolean"
        },
        "metrics_interval_secs": {
          "description": "Period for the OTLP metric exporter, seconds. Default 30s.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "headers": {
          "description": "Additional headers sent with every OTLP export request (traces, metrics, and any OTLP log sink). Values may be literals or secret references (`${VAR}`, `file:`, `vault://`, `secret://`, and the other backend URI schemes); references resolve at boot and the proxy refuses to start when one cannot be resolved, so a raw reference never reaches the collector. Hosted backends (Grafana Cloud, Honeycomb, Langfuse Cloud, Datadog) authenticate with these headers.",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "UsageRollupsConfig": {
      "description": "Durable spend-rollup configuration (hour and day usage buckets in an embedded database, so the admin spend API serves windowed history that survives restarts). Buckets are keyed by provider, model, tenant, team, credential id, and project, and aggregate request counts, tokens, cost, and an outcome split. Rows carry no prompt content and no raw key material, so the file is safe to back up. Aggregation is deterministic.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Whether rollups are recorded. Defaults to `true`. When the store path cannot be opened the proxy logs a warning and runs with rollups off instead of failing boot.",
          "default": true,
          "type": "boolean"
        },
        "path": {
          "description": "Rollup database file path. Default `/var/lib/sbproxy/usage-rollups.redb`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "retention_hourly_days": {
          "description": "Days of hourly buckets to keep before compacting into daily buckets. Default 90.",
          "default": 90,
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "retention_daily_days": {
          "description": "Days of daily buckets to keep. Default 395 (about 13 months).",
          "default": 395,
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "AlertingConfig": {
      "description": "Top-level alerting configuration block.",
      "type": "object",
      "properties": {
        "channels": {
          "description": "List of notification channels to fire alerts to.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/AlertChannelConfig"
          }
        }
      },
      "additionalProperties": false
    },
    "AlertChannelConfig": {
      "description": "Configuration for a single alert notification channel.",
      "type": "object",
      "required": [
        "type"
      ],
      "properties": {
        "type": {
          "description": "Channel type: `\"webhook\"`, `\"slack\"`, `\"pagerduty\"`, or `\"log\"`.",
          "type": "string"
        },
        "url": {
          "description": "Webhook URL. Required for `webhook` (any receiver) and `slack` (the incoming-webhook URL); unused by `pagerduty` and `log`.",
          "type": [
            "string",
            "null"
          ]
        },
        "headers": {
          "description": "Additional HTTP headers for webhook delivery.",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "routing_key": {
          "description": "PagerDuty Events API v2 routing key (required when `channel_type == \"pagerduty\"`). Accepts secret references.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "AdminConfig": {
      "description": "Configuration for the embedded read-only admin/stats API server.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Whether the admin server is enabled. Defaults to false.",
          "default": false,
          "type": "boolean"
        },
        "port": {
          "description": "Port to listen on. Defaults to 9090.",
          "default": 9090,
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "username": {
          "description": "HTTP Basic Auth username. Defaults to \"admin\".",
          "default": "admin",
          "type": "string"
        },
        "password": {
          "description": "HTTP Basic Auth password. Defaults to \"changeme\".",
          "default": "changeme",
          "type": "string"
        },
        "max_log_entries": {
          "description": "Maximum number of recent requests to retain in the log ring buffer. Defaults to 1000.",
          "default": 1000,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "rate_limit_per_minute": {
          "description": "Maximum admin API requests per client IP per minute. The global cap across all clients is ten times this value. Must be between 1 and 100000; the limiter cannot be turned off. Defaults to 240.",
          "default": 240,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "prompt_persistence_path": {
          "description": "WOR-800 PR5: filesystem path to a redb file that persists the prompt-store runtime overlay. When set, every successful `POST /admin/prompts/.../versions` and `PUT /admin/prompts/.../pin` also writes through to the file, and the file's existing contents are hydrated into the in-memory overlay at boot. Absent means PR3-style ephemeral mutations.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "prompt_persistence_encryption": {
          "description": "At-rest encryption for [`Self::prompt_persistence_path`]. Absent or disabled stores prompt records as plaintext JSON, which is the pre-existing behavior and stays the default so an upgrade cannot orphan an existing file.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/PromptPersistenceEncryptionConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "trace_url_template": {
          "description": "URL template for trace deep-links in the admin UI. The literal `{trace_id}` is replaced with the request's trace id, e.g. `https://jaeger.internal/trace/{trace_id}`. Unset renders trace ids as plain text (no broken default link).",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "tls": {
          "description": "Optional TLS for the admin server (WOR-1717). When set, the admin endpoint and the built-in UI are served over HTTPS using the PEM certificate and key at the configured paths, instead of plaintext HTTP. Leave unset to serve plaintext (loopback default).",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/AdminTlsConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "bind": {
          "description": "WOR-1717: address the admin server binds. Defaults to `127.0.0.1` (loopback only). Set to `0.0.0.0` or a specific interface for remote admin, and pair it with `allow_ips` and `tls`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "allow_ips": {
          "description": "WOR-1717: IP / CIDR allowlist for admin clients. Empty means loopback-only (`127.0.0.1`, `::1`), the safe default. List CIDRs to permit remote admin from known networks.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "cors_origins": {
          "description": "WOR-1717: allowed CORS origins for the admin API, so a separately hosted SPA or dev server can call it cross-origin with credentials. Empty means no CORS headers are emitted (same-origin only).",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "operators": {
          "description": "WOR-1716: additional admin operators with roles, for RBAC and an attributable audit trail. The top-level `username` / `password` is the implicit full-access `admin` operator; each entry here adds a read-only or admin identity that logs in with its own credentials.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/AdminOperator"
          }
        }
      },
      "additionalProperties": false
    },
    "PromptPersistenceEncryptionConfig": {
      "description": "At-rest encryption settings for the prompt-persistence redb file.\n\nPersisted `NamedPrompt` records are sealed with AES-256-GCM before they reach redb. Keys stay readable because hydration is a prefix scan over `prompts:<host>:<name>`, and the key is authenticated as associated data so a sealed value cannot be moved to another host or prompt name.\n\nSame reference syntax and the same no-plaintext-fallback rule as [`ResponseCacheEncryptionConfig`]: a key that is missing, unresolvable, or shorter than 16 bytes aborts startup. That is deliberately stricter than the surrounding prompt-persistence behavior, where an unreadable file only degrades to ephemeral mutations. An unreadable file loses saved prompts; a key the operator asked for and cannot supply would silently write secrets in the clear.\n\nDerived under its own HKDF purpose, so pointing this and the response cache at one operator secret still yields two unrelated keys.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Master switch. Defaults to `false`.",
          "default": false,
          "type": "boolean"
        },
        "key": {
          "description": "Secret reference for the active key. Used to seal new records and to open records sealed under it.\n\nResolved through the same mechanism as every other config secret: a provider URI (`secret://backend/name`, `vault://...`) against a backend declared under `proxy.secrets.backends`, a `file:/path` reference, or a whole-value `${ENV_VAR}`. Required when [`Self::enabled`] is `true`.\n\nThe resolved value should be 32 random bytes (base64 or hex encoded) rather than a human-chosen passphrase.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "previous_keys": {
          "description": "Retired keys, used only to open records sealed before a rotation. Same reference syntax as [`Self::key`].\n\nTo rotate: move the current `key` into this list and name the new one as `key`. Records reseal under the active key the next time they are written.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "AdminTlsConfig": {
      "description": "TLS material for the admin server (WOR-1717): filesystem paths to a PEM certificate chain and its matching private key. Both are required together; supplying `tls` makes the admin server, including the built-in UI, serve HTTPS instead of plaintext HTTP.",
      "type": "object",
      "required": [
        "cert",
        "key"
      ],
      "properties": {
        "cert": {
          "description": "Path to the PEM certificate chain file.",
          "type": "string"
        },
        "key": {
          "description": "Path to the PEM private key file (PKCS#8 or RSA).",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "AdminOperator": {
      "description": "An admin operator identity with a role, for RBAC (WOR-1716).",
      "type": "object",
      "required": [
        "password_hash",
        "username"
      ],
      "properties": {
        "username": {
          "description": "Login username.",
          "type": "string"
        },
        "password_hash": {
          "description": "HMAC-SHA256 hash of the login password, hex-encoded, using the same pepper as the inbound key plane (sbproxy-keystore::crypto). Compute with `sbproxy admin hash-password`.",
          "type": "string"
        },
        "role": {
          "description": "Role governing which admin actions this operator may perform.",
          "default": "admin",
          "allOf": [
            {
              "$ref": "#/definitions/AdminRole"
            }
          ]
        },
        "tenant": {
          "description": "Billing tenant whose metered consumption this operator may read (WOR-2131). Absent means the whole deployment, which is what every operator written before this field existed keeps getting.\n\nA receipt names one buyer's traffic, so the meter routes treat a cross-tenant read as a disclosure rather than a reporting mistake: naming a tenant here narrows `/api/meter/*` to that tenant and refuses a request for any other. The scope is read from this document on every request rather than carried in the session token, so revoking it is a config reload rather than a wait for tokens to expire.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "AdminRole": {
      "description": "Admin RBAC role (WOR-1716). `read_only` may call read (GET) endpoints only; `admin` may call every admin route.",
      "oneOf": [
        {
          "description": "Read-only: GET / read endpoints only; state-changing routes 403.",
          "type": "string",
          "enum": [
            "read_only"
          ]
        },
        {
          "description": "Full admin: every admin route.",
          "type": "string",
          "enum": [
            "admin"
          ]
        }
      ]
    },
    "ModelHostControlConfig": {
      "description": "Canonical model-host desired state under `proxy.model_host`.",
      "type": "object",
      "properties": {
        "authority": {
          "description": "System authoritative for desired-state mutations.",
          "default": "file_managed",
          "allOf": [
            {
              "$ref": "#/definitions/ModelHostAuthority"
            }
          ]
        },
        "store_path": {
          "description": "Revision store path required by admin-managed authority.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "catalog_file": {
          "description": "Optional operator catalog file replacing the built-in model catalog.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "max_parallel_prepares": {
          "description": "Maximum artifact and engine preparations that may run concurrently.",
          "default": 2,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "safety_margin": {
          "description": "Fraction of each device's usable memory reserved as headroom.",
          "default": 0.1,
          "type": "number",
          "format": "double"
        },
        "shutdown_deadline_ms": {
          "description": "Maximum graceful drain time during shutdown.",
          "default": 30000,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "handoff_timeout_ms": {
          "description": "Maximum rolling placement handoff time while prior replicas are retained.",
          "default": 60000,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "cache": {
          "description": "Artifact cache and residency policy.",
          "default": {
            "directory": null,
            "budget_gib": null,
            "max_resident_models": null
          },
          "allOf": [
            {
              "$ref": "#/definitions/ModelHostCacheConfig"
            }
          ]
        },
        "engines": {
          "description": "Per-engine provisioning policy.",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/ManagedEngineConfig"
          }
        },
        "deployments": {
          "description": "Deployment ID to desired local model.",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/ManagedDeploymentConfig"
          }
        }
      },
      "additionalProperties": false
    },
    "ModelHostAuthority": {
      "description": "System that owns the model-host desired state.",
      "oneOf": [
        {
          "description": "The active `sb.yml` file is authoritative.",
          "type": "string",
          "enum": [
            "file_managed"
          ]
        },
        {
          "description": "The authenticated admin API and its revision store are authoritative.",
          "type": "string",
          "enum": [
            "admin_managed"
          ]
        },
        {
          "description": "A configured cluster authority publishes the desired-state revision.",
          "type": "string",
          "enum": [
            "cluster_authority"
          ]
        }
      ]
    },
    "ModelHostCacheConfig": {
      "description": "Content-addressed model cache policy.",
      "type": "object",
      "properties": {
        "directory": {
          "description": "Cache directory, as an absolute path. Omission uses the platform default: `$HF_HOME`, then `/var/lib/sbproxy/models` when this process can write there, then `~/.cache/sbproxy/models`.\n\nA relative path is refused at load. The engine subprocess that reads this cache has its own working directory and requires an absolute snapshot path, and in a split-role cluster each node reads the key from its own file, so a relative value names a different directory per process. Interpolate a variable such as `${HOME}` when the location is per-host.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "budget_gib": {
          "description": "Disk budget in GiB. Omission leaves capacity operator-managed.",
          "default": null,
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "max_resident_models": {
          "description": "Maximum simultaneously resident models.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "ManagedEngineConfig": {
      "description": "Provisioning policy for one managed inference engine.",
      "type": "object",
      "properties": {
        "launch": {
          "description": "Launch mechanism.",
          "default": "binary",
          "allOf": [
            {
              "$ref": "#/definitions/ManagedEngineLaunch"
            }
          ]
        },
        "image": {
          "description": "Immutable OCI image reference for container launch.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "path": {
          "description": "Explicit allowlisted engine binary path.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "version": {
          "description": "Pinned engine or release version.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "sha256": {
          "description": "Expected SHA-256 for a downloaded binary or source archive.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "acceleration": {
          "description": "Requested acceleration backend.",
          "default": "auto",
          "allOf": [
            {
              "$ref": "#/definitions/ManagedEngineAcceleration"
            }
          ]
        },
        "shm_size_gib": {
          "description": "Shared-memory size in GiB for container launch.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "ManagedEngineLaunch": {
      "description": "How a managed engine process is provisioned and launched.",
      "oneOf": [
        {
          "description": "Resolve an installed binary or acquire a pinned binary release.",
          "type": "string",
          "enum": [
            "binary"
          ]
        },
        {
          "description": "Run a digest-pinned OCI container.",
          "type": "string",
          "enum": [
            "container"
          ]
        },
        {
          "description": "Run vLLM from a managed, version-pinned uv environment.",
          "type": "string",
          "enum": [
            "uv"
          ]
        }
      ]
    },
    "ManagedEngineAcceleration": {
      "description": "Hardware acceleration requested for a managed engine.",
      "oneOf": [
        {
          "description": "Select from the detected worker hardware.",
          "type": "string",
          "enum": [
            "auto"
          ]
        },
        {
          "description": "NVIDIA CUDA.",
          "type": "string",
          "enum": [
            "cuda"
          ]
        },
        {
          "description": "Apple Metal.",
          "type": "string",
          "enum": [
            "metal"
          ]
        },
        {
          "description": "Vulkan.",
          "type": "string",
          "enum": [
            "vulkan"
          ]
        },
        {
          "description": "CPU-only execution.",
          "type": "string",
          "enum": [
            "cpu"
          ]
        }
      ]
    },
    "ManagedDeploymentConfig": {
      "description": "One desired local model deployment.",
      "type": "object",
      "required": [
        "model"
      ],
      "properties": {
        "model": {
          "description": "Logical ID from the certified model catalog.",
          "type": "string"
        },
        "variant": {
          "description": "Exact artifact variant pin. Omission enables worker-compatible selection.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "heterogeneous_variants": {
          "description": "Allow replicas to choose different compatible artifact variants.",
          "default": false,
          "type": "boolean"
        },
        "replicas": {
          "description": "Desired local replica count.",
          "default": 1,
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "tensor_parallel": {
          "description": "Fixed tensor-parallel degree per replica: the exact number of devices each replica spans. Omit to let the fit planner pick the smallest degree that fits. When set, `replicas * tensor_parallel` devices are needed, since each replica claims a distinct device set.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0.0
        },
        "required_labels": {
          "description": "Worker labels required by this deployment.",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "spread_by": {
          "description": "Ordered failure-domain label keys used to spread replicas.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "pull": {
          "description": "Artifact download policy.",
          "default": "on_demand",
          "allOf": [
            {
              "$ref": "#/definitions/ManagedPullPolicy"
            }
          ]
        },
        "warm": {
          "description": "Prepare the deployment during startup or reload.",
          "default": false,
          "type": "boolean"
        },
        "cold_start": {
          "description": "Behavior when no current replica is ready. Omission uses the security-profile default.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ManagedColdStartPolicy"
            },
            {
              "type": "null"
            }
          ]
        },
        "keep_alive_secs": {
          "description": "Idle lifetime in seconds after the last completed request.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "max_concurrency": {
          "description": "Per-replica in-flight request cap.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0.0
        },
        "max_queue_depth": {
          "description": "Maximum requests waiting behind active capacity.",
          "default": 128,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "queue_timeout_ms": {
          "description": "Maximum queue wait in milliseconds.",
          "default": 30000,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "engine": {
          "description": "Inference engine selection.",
          "default": "auto",
          "allOf": [
            {
              "$ref": "#/definitions/ManagedEngineChoice"
            }
          ]
        },
        "rollout": {
          "description": "Replica replacement behavior.",
          "default": "rolling",
          "allOf": [
            {
              "$ref": "#/definitions/ManagedRolloutPolicy"
            }
          ]
        },
        "extra_args": {
          "description": "Extra engine command-line arguments appended after the runtime's own. Validated against the resolved engine.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "chunked_prefill": {
          "description": "Chunked-prefill settings (vLLM). Omit to use the engine default.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ManagedChunkedPrefill"
            },
            {
              "type": "null"
            }
          ]
        },
        "tool_call_parser": {
          "description": "vLLM tool-call parser enabling auto tool-choice, e.g. `hermes`, `llama3_json`, `mistral`. Omit to leave tool calling off.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "swap_space_gib": {
          "description": "CPU swap pool size in GiB (vLLM `--swap-space`). Omit to use the engine default.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "cpu_offload_gib": {
          "description": "GiB of model weights to keep in CPU RAM (vLLM `--cpu-offload-gb`). Omit to disable offload.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "engine_version": {
          "description": "Per-deployment engine version pin, overriding the node-wide `engines:` policy so one model can run a different backend version than another. Never `latest`. Omit to inherit the node policy or the built-in default.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "engine_image": {
          "description": "Per-deployment engine container image, overriding the node policy. Must be tag- or digest-pinned. Omit to inherit the node policy.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "engine_sha256": {
          "description": "Expected SHA-256 for the pinned engine binary or image digest. Omit to inherit the node policy.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "ManagedPullPolicy": {
      "description": "Model artifact download policy.",
      "oneOf": [
        {
          "description": "Pull and verify artifacts during startup reconciliation.",
          "type": "string",
          "enum": [
            "on_boot"
          ]
        },
        {
          "description": "Pull and verify artifacts when the first request needs them.",
          "type": "string",
          "enum": [
            "on_demand"
          ]
        },
        {
          "description": "Never pull automatically; require an explicit lifecycle command.",
          "type": "string",
          "enum": [
            "manual"
          ]
        }
      ]
    },
    "ManagedColdStartPolicy": {
      "description": "Request behavior when a managed deployment has no ready replica.",
      "oneOf": [
        {
          "description": "Queue on an assigned replica and share its coordinated engine start.",
          "type": "string",
          "enum": [
            "wait"
          ]
        },
        {
          "description": "Return a retryable refusal without starting an engine.",
          "type": "string",
          "enum": [
            "reject"
          ]
        },
        {
          "description": "Advance to another configured provider without starting an engine.",
          "type": "string",
          "enum": [
            "fallback"
          ]
        }
      ]
    },
    "ManagedEngineChoice": {
      "description": "Engine selection for one managed deployment.",
      "oneOf": [
        {
          "description": "Select a compatible engine from the artifact format and worker.",
          "type": "string",
          "enum": [
            "auto"
          ]
        },
        {
          "description": "Use vLLM.",
          "type": "string",
          "enum": [
            "vllm"
          ]
        },
        {
          "description": "Use SGLang (WOR-1905). An explicit opt-in that `Auto` never selects; it serves the same safetensors weights as vLLM on a CUDA worker.",
          "type": "string",
          "enum": [
            "sglang"
          ]
        },
        {
          "description": "Use llama.cpp.",
          "type": "string",
          "enum": [
            "llama_cpp"
          ]
        },
        {
          "description": "Use mistral.rs (WOR-1861). An explicit opt-in that `Auto` never selects; the pure-Rust subprocess lane over the upstream pinned prebuilt binary, serving safetensors weights.",
          "type": "string",
          "enum": [
            "mistralrs"
          ]
        }
      ]
    },
    "ManagedRolloutPolicy": {
      "description": "Replacement behavior when a deployment changes.",
      "oneOf": [
        {
          "description": "Prepare the replacement before draining the prior generation.",
          "type": "string",
          "enum": [
            "rolling"
          ]
        },
        {
          "description": "Drain the prior generation before preparing the replacement.",
          "type": "string",
          "enum": [
            "recreate"
          ]
        }
      ]
    },
    "ManagedChunkedPrefill": {
      "description": "Chunked-prefill tuning for a managed deployment.",
      "type": "object",
      "properties": {
        "max_batched_tokens": {
          "description": "Explicit prefill chunk size (the engine's `max-num-batched-tokens`). Omit to leave it to the engine default.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "target_ttft_ms": {
          "description": "A time-to-first-token target in milliseconds; when set with no explicit chunk size, the planner chooses a chunk size to hold it.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "ClassifierHooksConfig": {
      "description": "Classifier-sidecar hooks installed into the stock proxy runtime.",
      "type": "object",
      "required": [
        "endpoint"
      ],
      "properties": {
        "endpoint": {
          "description": "HTTP(S) gRPC endpoint of the minimal or rich classifier sidecar.",
          "type": "string"
        },
        "timeout_ms": {
          "description": "End-to-end deadline for one hook decision.",
          "default": 500,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "tls": {
          "description": "Optional transport-level TLS configuration for HTTPS endpoints.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ClassifierHooksTlsConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "authentication": {
          "description": "Optional request authentication presented to the classifier service.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ClassifierHooksAuthenticationConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "intent": {
          "description": "Optional classifier-backed prompt intent detection.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ClassifierIntentHookConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "quality": {
          "description": "Optional classifier-backed provider quality routing.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ClassifierQualityHookConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "ClassifierHooksTlsConfig": {
      "description": "TLS material for a classifier-hook gRPC endpoint.",
      "type": "object",
      "properties": {
        "ca_pem": {
          "description": "Optional CA bundle used to verify the remote classifier.\n\nResolved through the process secret resolver so the value may be a provider URI, `${ENV}`, `env:NAME`, or `file:/path`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "server_name": {
          "description": "Override for the TLS server name / SNI. Defaults to the endpoint host.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "client_identity": {
          "description": "Optional client certificate presented to the classifier for mTLS.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ClassifierHooksClientIdentityConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "ClassifierHooksClientIdentityConfig": {
      "description": "Client certificate + private key for classifier-hook mTLS.",
      "type": "object",
      "required": [
        "cert_pem",
        "key_pem"
      ],
      "properties": {
        "cert_pem": {
          "description": "Client certificate chain in PEM format, supplied via a secret reference.",
          "type": "string"
        },
        "key_pem": {
          "description": "Client private key in PEM format, supplied via a secret reference.",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "ClassifierHooksAuthenticationConfig": {
      "description": "Request authentication presented to the classifier hook service.",
      "oneOf": [
        {
          "description": "Bearer-style metadata authentication on every gRPC request.",
          "type": "object",
          "required": [
            "credential",
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "bearer"
              ]
            },
            "credential": {
              "description": "Secret reference for the bearer token value.",
              "type": "string"
            },
            "header": {
              "description": "Metadata key that carries the token. Defaults to `authorization`.",
              "default": "authorization",
              "type": "string"
            },
            "scheme": {
              "description": "Optional value prefix. Defaults to `Bearer`.",
              "default": "Bearer",
              "type": "string"
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "ClassifierIntentHookConfig": {
      "description": "Model used to classify a prompt into the five stock intent labels.",
      "type": "object",
      "properties": {
        "model": {
          "description": "Logical classifier model id loaded by the sidecar.",
          "default": "intent",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "ClassifierQualityHookConfig": {
      "description": "Classifier-backed provider scorer for prompt-aware routing.",
      "type": "object",
      "properties": {
        "minimum_score": {
          "description": "Minimum positive-label score eligible to win provider selection.",
          "default": 0.75,
          "type": "number",
          "format": "double"
        },
        "provider_models": {
          "description": "Per-provider classifier model and positive-label contract.",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/ClassifierProviderModelConfig"
          }
        }
      },
      "additionalProperties": false
    },
    "ClassifierProviderModelConfig": {
      "description": "One provider's prompt-quality classifier contract.",
      "type": "object",
      "required": [
        "label",
        "model"
      ],
      "properties": {
        "model": {
          "description": "Logical classifier model id loaded by the sidecar.",
          "type": "string"
        },
        "label": {
          "description": "Label whose score represents this provider's suitability.",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "AiToolkitConfig": {
      "description": "Bounded, generation-pinned AI workflow and evaluation configuration.\n\nThe runtime resolves agent credentials only while constructing a candidate pipeline. This parsed form therefore retains secret references, never the referenced secret material itself.",
      "type": "object",
      "properties": {
        "limits": {
          "description": "Optional overrides for the runtime's conservative public limits.",
          "default": {
            "max_agents": null,
            "max_capabilities_per_agent": null,
            "max_workflows": null,
            "max_datasets": null,
            "max_dataset_versions": null,
            "max_dataset_versions_total": null,
            "max_dataset_entries": null,
            "max_dataset_bytes_total": null,
            "max_rollouts": null,
            "max_rollout_versions": null,
            "max_retained_operations": null,
            "max_request_bytes": null,
            "max_response_bytes": null,
            "max_identifier_bytes": null,
            "max_description_bytes": null,
            "max_schema_bytes": null,
            "max_secret_bytes": null,
            "max_evaluation_cases": null,
            "max_metrics": null,
            "max_judge_criteria": null,
            "agent_concurrency": null,
            "evaluation_concurrency": null,
            "default_workflow_timeout_ms": null,
            "max_workflow_timeout_ms": null
          },
          "allOf": [
            {
              "$ref": "#/definitions/AiToolkitLimitsConfig"
            }
          ]
        },
        "agents": {
          "description": "Governed agent endpoints available to workflows.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/AiToolkitAgentConfig"
          }
        },
        "workflows": {
          "description": "Finite-state workflows compiled into this pipeline generation.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/AiToolkitWorkflowConfig"
          }
        },
        "datasets": {
          "description": "Immutable evaluation dataset versions seeded at publication.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/AiToolkitDatasetConfig"
          }
        },
        "prompt_rollouts": {
          "description": "Stable weighted prompt rollouts selected on the live request path.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/AiToolkitPromptRolloutConfig"
          }
        }
      },
      "additionalProperties": false
    },
    "AiToolkitLimitsConfig": {
      "description": "Optional overrides for AI toolkit resource and deadline limits.\n\nAn omitted field inherits the runtime default. Keeping these overrides optional avoids copying runtime defaults into the config compiler, where the two sets of values could drift.",
      "type": "object",
      "properties": {
        "max_agents": {
          "description": "Maximum configured agents.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "max_capabilities_per_agent": {
          "description": "Maximum capabilities advertised by one agent.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "max_workflows": {
          "description": "Maximum configured workflows.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "max_datasets": {
          "description": "Maximum distinct dataset names retained per scope.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "max_dataset_versions": {
          "description": "Maximum immutable versions retained per dataset.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "max_dataset_versions_total": {
          "description": "Maximum immutable dataset versions retained across all scopes (hard maximum 16,384).",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "max_dataset_entries": {
          "description": "Maximum entries in one dataset version.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "max_dataset_bytes_total": {
          "description": "Maximum serialized dataset-entry bytes retained across all scopes (hard maximum 512 MiB).",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "max_rollouts": {
          "description": "Maximum configured prompt rollouts.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "max_rollout_versions": {
          "description": "Maximum versions in one prompt rollout.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "max_retained_operations": {
          "description": "Maximum recent operation summaries retained per scope.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "max_request_bytes": {
          "description": "Maximum serialized request bytes accepted by a toolkit operation.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "max_response_bytes": {
          "description": "Maximum serialized response bytes accepted from an agent or retained.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "max_identifier_bytes": {
          "description": "Maximum bytes in a public identifier.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "max_description_bytes": {
          "description": "Maximum bytes in a public description.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "max_schema_bytes": {
          "description": "Maximum bytes in one serialized JSON schema.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "max_secret_bytes": {
          "description": "Maximum bytes resolved from one agent credential reference.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "max_evaluation_cases": {
          "description": "Maximum cases evaluated by one run.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "max_metrics": {
          "description": "Maximum custom metrics evaluated by one run.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "max_judge_criteria": {
          "description": "Maximum offline-judge criteria evaluated by one run.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "agent_concurrency": {
          "description": "Maximum concurrent governed agent calls.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "evaluation_concurrency": {
          "description": "Maximum concurrent evaluation cases.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "default_workflow_timeout_ms": {
          "description": "Default workflow deadline when a run does not supply one.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "max_workflow_timeout_ms": {
          "description": "Hard ceiling for every workflow deadline.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "AiToolkitAgentConfig": {
      "description": "One governed agent endpoint available within an origin's tenant scope.",
      "type": "object",
      "required": [
        "auth",
        "endpoint",
        "id",
        "origin"
      ],
      "properties": {
        "origin": {
          "description": "Stable configured origin id (or hostname) that owns this agent.",
          "type": "string"
        },
        "id": {
          "description": "Stable agent id within the origin scope.",
          "type": "string"
        },
        "endpoint": {
          "description": "HTTP endpoint invoked through the agent-orchestration egress gate.",
          "type": "string"
        },
        "auth": {
          "description": "Authentication material expressed only as a secret reference.",
          "allOf": [
            {
              "$ref": "#/definitions/AiToolkitAgentAuthConfig"
            }
          ]
        },
        "capabilities": {
          "description": "Capabilities advertised by this agent.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/AiToolkitCapabilityConfig"
          }
        }
      },
      "additionalProperties": false
    },
    "AiToolkitAgentAuthConfig": {
      "description": "Agent authentication configuration retained as an unresolved reference.",
      "type": "object",
      "required": [
        "shared_secret"
      ],
      "properties": {
        "shared_secret": {
          "description": "Secret reference used to derive the agent bearer credential.",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "AiToolkitCapabilityConfig": {
      "description": "One discoverable agent capability and its request/response schemas.",
      "type": "object",
      "required": [
        "input_schema",
        "name",
        "output_schema"
      ],
      "properties": {
        "name": {
          "description": "Stable capability name used by workflow actions.",
          "type": "string"
        },
        "description": {
          "description": "Bounded operator-facing description.",
          "default": "",
          "type": "string"
        },
        "input_schema": {
          "description": "JSON Schema for the agent request body."
        },
        "output_schema": {
          "description": "JSON Schema for the agent response body."
        }
      },
      "additionalProperties": false
    },
    "AiToolkitWorkflowConfig": {
      "description": "One finite-state workflow owned by an origin scope.",
      "type": "object",
      "required": [
        "initial_state",
        "max_steps",
        "name",
        "origin",
        "states",
        "timeout_ms"
      ],
      "properties": {
        "origin": {
          "description": "Stable configured origin id (or hostname) that owns this workflow.",
          "type": "string"
        },
        "name": {
          "description": "Stable workflow name within the origin scope.",
          "type": "string"
        },
        "initial_state": {
          "description": "State entered first.",
          "type": "string"
        },
        "max_steps": {
          "description": "Maximum transitions in one execution.",
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "timeout_ms": {
          "description": "Whole-workflow deadline in milliseconds.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "states": {
          "description": "Bounded workflow graph.",
          "type": "array",
          "items": {
            "$ref": "#/definitions/AiToolkitWorkflowStateConfig"
          }
        }
      },
      "additionalProperties": false
    },
    "AiToolkitWorkflowStateConfig": {
      "description": "One state in a configured AI toolkit workflow.",
      "type": "object",
      "required": [
        "action",
        "name"
      ],
      "properties": {
        "name": {
          "description": "Stable state name.",
          "type": "string"
        },
        "action": {
          "description": "Capability name discovered and invoked when this state runs.",
          "type": "string"
        },
        "transitions": {
          "description": "Outcome label to next-state name. An absent match completes the run.",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "AiToolkitDatasetConfig": {
      "description": "One immutable evaluation dataset version seeded from configuration.",
      "type": "object",
      "required": [
        "name",
        "origin",
        "version"
      ],
      "properties": {
        "origin": {
          "description": "Stable configured origin id (or hostname) that owns this dataset.",
          "type": "string"
        },
        "name": {
          "description": "Dataset name within the origin scope.",
          "type": "string"
        },
        "version": {
          "description": "Explicit immutable version; zero is invalid.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "entries": {
          "description": "Bounded evaluation cases.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/AiToolkitDatasetEntryConfig"
          }
        }
      },
      "additionalProperties": false
    },
    "AiToolkitDatasetEntryConfig": {
      "description": "One input/expected-output pair in a configured evaluation dataset.",
      "type": "object",
      "required": [
        "input"
      ],
      "properties": {
        "input": {
          "description": "Input supplied to the evaluated model or offline response set.",
          "type": "string"
        },
        "expected_output": {
          "description": "Optional expected output used by correctness metrics.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "metadata": {
          "description": "Bounded caller metadata retained with the case.",
          "default": null
        }
      },
      "additionalProperties": false
    },
    "AiToolkitPromptRolloutConfig": {
      "description": "One stable weighted prompt rollout owned by an origin scope.",
      "type": "object",
      "required": [
        "name",
        "origin",
        "salt",
        "versions"
      ],
      "properties": {
        "origin": {
          "description": "Stable configured origin id (or hostname) that owns this rollout.",
          "type": "string"
        },
        "name": {
          "description": "Prompt name used by bare request references.",
          "type": "string"
        },
        "salt": {
          "description": "Stable operator-controlled cohort salt.",
          "type": "string"
        },
        "versions": {
          "description": "Weighted immutable prompt versions.",
          "type": "array",
          "items": {
            "$ref": "#/definitions/AiToolkitPromptRolloutVersionConfig"
          }
        }
      },
      "additionalProperties": false
    },
    "AiToolkitPromptRolloutVersionConfig": {
      "description": "One immutable member of a weighted prompt rollout.",
      "type": "object",
      "required": [
        "content",
        "version",
        "weight"
      ],
      "properties": {
        "version": {
          "description": "Positive numeric prompt version.",
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "content": {
          "description": "Prompt template/content selected for this version.",
          "type": "string"
        },
        "weight": {
          "description": "Finite non-negative relative weight.",
          "type": "number",
          "format": "double"
        }
      },
      "additionalProperties": false
    },
    "ClusterConfig": {
      "description": "Stable `proxy.cluster` configuration.",
      "type": "object",
      "required": [
        "cluster_id",
        "security"
      ],
      "properties": {
        "cluster_id": {
          "description": "Logical cluster identity. Every member must use the same value.",
          "type": "string"
        },
        "node_id": {
          "description": "Stable unique node identity.",
          "default": "",
          "type": "string"
        },
        "roles": {
          "description": "Roles this process performs.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/ClusterRole"
          },
          "uniqueItems": true
        },
        "labels": {
          "description": "Bounded labels used by placement and failure-domain spreading.",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "seeds": {
          "description": "Static gossip seed addresses in `host:port` form.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "gossip_port": {
          "description": "UDP SWIM gossip listener port.",
          "default": 7946,
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "transport_port": {
          "description": "TCP typed-state and cache transport port.",
          "default": 8946,
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "advertise_addr": {
          "description": "Address advertised to peers in `host:port` form.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "transport_advertise_addr": {
          "description": "Typed-state transport address advertised to peers in `host:port` form. Defaults to the gossip-advertised host and `transport_port`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "model_bind": {
          "description": "Private model-plane listener in IP:port form.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "model_endpoint": {
          "description": "Private model-plane endpoint advertised by worker nodes.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "state_dir": {
          "description": "Writable durable state stored with this installed node identity.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "security": {
          "description": "Explicit peer-security policy.",
          "allOf": [
            {
              "$ref": "#/definitions/ClusterSecurityConfig"
            }
          ]
        },
        "snapshot_ttl_secs": {
          "description": "Lifetime of a published node model snapshot.",
          "default": 30,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "publish_interval_secs": {
          "description": "Worker snapshot publication cadence.",
          "default": 5,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "dead_peer_gc_secs": {
          "description": "Time to retain dead peers in the routing membership table before GC. The operator model directory retains a longer-lived tombstone.",
          "default": 300,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "enrollment": {
          "description": "Optional one-time enrollment authority.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ClusterEnrollmentConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "deployment_authority": {
          "description": "Optional signed model deployment authority.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ClusterDeploymentAuthorityConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "replication": {
          "description": "Optional replicated durable state substrate (WOR-1947). Absent keeps the single-owner in-memory typed-state cache semantics.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ClusterReplicationConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "ClusterRole": {
      "description": "Stable role assigned to one cluster node.",
      "oneOf": [
        {
          "description": "Accept public gateway traffic and apply caller policy.",
          "type": "string",
          "enum": [
            "gateway"
          ]
        },
        {
          "description": "Host managed model replicas.",
          "type": "string",
          "enum": [
            "worker"
          ]
        },
        {
          "description": "Enroll nodes or publish signed deployment revisions.",
          "type": "string",
          "enum": [
            "authority"
          ]
        }
      ]
    },
    "ClusterSecurityConfig": {
      "description": "Canonical peer-security configuration.",
      "type": "object",
      "required": [
        "mode"
      ],
      "properties": {
        "mode": {
          "description": "Selected peer-security mode.",
          "allOf": [
            {
              "$ref": "#/definitions/ClusterSecurityMode"
            }
          ]
        },
        "development": {
          "description": "Explicitly acknowledge that shared-key mode is for development.",
          "default": false,
          "type": "boolean"
        },
        "shared_key": {
          "description": "Shared secret reference for authenticated UDP gossip and development transport.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "cert_file": {
          "description": "This node's PEM certificate chain for mTLS.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "key_file": {
          "description": "This node's PEM private key for mTLS.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "ca_file": {
          "description": "PEM CA certificate used to verify every peer.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "server_name": {
          "description": "Cluster SAN bound into every enrolled peer identity.",
          "default": "sbproxy-mesh",
          "type": "string"
        },
        "key_derivation": {
          "description": "How `shared_key` becomes the AES-256-GCM wire key.\n\nDefaults to `sha256`, which is what every existing cluster runs, so an upgrade never changes the key a node seals under. Nodes open under both derivations, so a cluster can be flipped one node at a time. See `docs/mesh-replication.md`.",
          "default": "sha256",
          "allOf": [
            {
              "$ref": "#/definitions/MeshKeyDerivation"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "ClusterSecurityMode": {
      "description": "Peer-security mode for the cluster substrate.",
      "oneOf": [
        {
          "description": "Mutually authenticated TLS with operator or enrollment-issued material.",
          "type": "string",
          "enum": [
            "mtls"
          ]
        },
        {
          "description": "AES-GCM wire encryption using one shared development secret.",
          "type": "string",
          "enum": [
            "shared_key"
          ]
        }
      ]
    },
    "MeshKeyDerivation": {
      "description": "How the mesh derives its AES-256-GCM wire key from the shared secret.\n\nBoth derivations are always accepted on the receive side, so this only selects what a node seals under and a cluster can be migrated one node at a time.",
      "oneOf": [
        {
          "description": "`SHA-256(secret)`. The original scheme and the default, so an upgrade never changes an existing cluster's key.",
          "type": "string",
          "enum": [
            "sha256"
          ]
        },
        {
          "description": "HKDF-SHA256 under a mesh-specific purpose, matching how every other key in this workspace is derived.",
          "type": "string",
          "enum": [
            "hkdf"
          ]
        }
      ]
    },
    "ClusterEnrollmentConfig": {
      "description": "Optional one-time enrollment authority configuration.",
      "type": "object",
      "required": [
        "authority_dir"
      ],
      "properties": {
        "authority_dir": {
          "description": "Durable authority directory created by `sbproxy cluster init`.",
          "type": "string"
        },
        "allow_insecure_http": {
          "description": "Permit plaintext HTTP enrollment for local development only.",
          "default": false,
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "ClusterDeploymentAuthorityConfig": {
      "description": "Signed deployment-authority configuration.",
      "type": "object",
      "required": [
        "verifying_key_file"
      ],
      "properties": {
        "signing_key_file": {
          "description": "Ed25519 private signing-key file, valid only on an authority node.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "verifying_key_file": {
          "description": "Ed25519 public verification-key file installed on every node.",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "ClusterReplicationConfig": {
      "description": "Replicated durable state substrate configuration (WOR-1947).\n\nEnabling this block requires `state_dir`: the replica shard persists to `<state_dir>/replicated-state.redb` so a restarted node serves its committed records from disk.",
      "type": "object",
      "properties": {
        "factor": {
          "description": "Copies kept per record. Clamped to cluster size at placement time.",
          "default": 2,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "write_consistency": {
          "description": "Acknowledgements required before a write reports success.",
          "default": "quorum",
          "allOf": [
            {
              "$ref": "#/definitions/ClusterConsistencyLevel"
            }
          ]
        },
        "read_consistency": {
          "description": "Replicas consulted before a read reports its winner.",
          "default": "quorum",
          "allOf": [
            {
              "$ref": "#/definitions/ClusterConsistencyLevel"
            }
          ]
        },
        "anti_entropy_interval_secs": {
          "description": "Cadence of the maintenance loop (anti-entropy, handoff, GC).",
          "default": 30,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "tombstone_gc_grace_secs": {
          "description": "Age a tombstone must reach, with every replica confirming, before it is physically collected. Also bounds how long a node may be offline before its stored records are quarantined on rejoin.",
          "default": 86400,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "max_entries": {
          "description": "Maximum records (live plus tombstones) per node shard.",
          "default": 65536,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "max_value_bytes": {
          "description": "Maximum decoded record value size in bytes.",
          "default": 1048576,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "ClusterConsistencyLevel": {
      "description": "Consistency level for replicated state reads and writes.",
      "oneOf": [
        {
          "description": "One replica acknowledgement (the coordinator's own shard counts).",
          "type": "string",
          "enum": [
            "one"
          ]
        },
        {
          "description": "A majority of the key's replica set.",
          "type": "string",
          "enum": [
            "quorum"
          ]
        },
        {
          "description": "Every replica in the key's replica set.",
          "type": "string",
          "enum": [
            "all"
          ]
        }
      ]
    },
    "ConfigAuthorityConfig": {
      "description": "`proxy.config_authority`: how this node takes part in config authority.\n\nBoth halves live here, and they are mutually exclusive. [`Self::upstream`] makes this node a subscriber: it pulls signed bundles from an authority, verifies them, merges them over the local document, and applies the result through the ordinary reload transaction. [`Self::publish`] makes it an authority: it validates, signs, and serves bundles to subscribers of its own.",
      "type": "object",
      "properties": {
        "upstream": {
          "description": "Upstream authority this node subscribes to. Absent means this node pulls no remote configuration.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ConfigAuthorityUpstreamConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "publish": {
          "description": "Publication settings that make this node an authority. Absent means this node serves no bundles.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ConfigAuthorityPublishConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "ConfigAuthorityUpstreamConfig": {
      "description": "`proxy.config_authority.upstream`: the authority this node pulls signed configuration bundles from.\n\n```yaml proxy: config_authority: upstream: url: https://control.example.com mode: overlay subscriber_id: edge-01 credential: env:SB_CONFIG_TOKEN verifying_keys_file: /etc/sbproxy/authority-keys.json poll_interval: 30s cache_path: /var/lib/sbproxy/config-bundle.json max_staleness: 24h require_bundle_on_boot: false ```",
      "type": "object",
      "required": [
        "cache_path",
        "mode",
        "subscriber_id",
        "url",
        "verifying_keys_file"
      ],
      "properties": {
        "url": {
          "description": "Absolute base URL of the authority, for example `https://control.example.com`. The subscriber appends its own path; a path on this URL is kept as a prefix.\n\nMust be `https` unless [`Self::allow_insecure_http`] is set.",
          "type": "string"
        },
        "mode": {
          "description": "Whether a bundle merges over the local document or replaces it.\n\nRequired rather than defaulted: the answer decides whether the local file still describes what this node serves, which is not a question to answer by omission.",
          "allOf": [
            {
              "$ref": "#/definitions/BundleMode"
            }
          ]
        },
        "subscriber_id": {
          "description": "Stable identity this node presents to the authority. Sent on every fetch so an authority can scope what it publishes.",
          "type": "string"
        },
        "credential": {
          "description": "Reference to the bearer credential presented to the authority.\n\nResolved through the process secret resolver, so the accepted forms are `env:NAME`, `${NAME}`, `file:/path`, and a provider-URI reference such as `secret://backend/name`. An inline literal is refused: a token committed to a config file is a token in every git history that ever held it.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "verifying_keys_file": {
          "description": "Path to the JSON file naming every key this subscriber trusts. See `VerifyingKeySet::from_file` for the file shape.",
          "type": "string"
        },
        "poll_interval": {
          "description": "How often the subscriber polls the authority, in seconds. Accepts a humanized duration (`30s`, `5m`) or bare seconds.\n\nThe real interval carries jitter, so a fleet restarting together does not synchronize onto the authority.",
          "default": 30,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "cache_path": {
          "description": "Where the verified bundle is cached so the node can boot on the last known configuration when the authority is unreachable. The anti-replay cursor is stored beside it.",
          "type": "string"
        },
        "max_staleness": {
          "description": "How old a cached bundle may be and still be used at boot, in seconds. Accepts a humanized duration (`24h`, `7d`) or bare seconds.\n\nA running node that exceeds this window keeps serving and logs at error level every cycle; the window is a boot-time gate, not a kill switch on a node that is already up.",
          "default": 86400,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "require_bundle_on_boot": {
          "description": "Whether the node refuses to start without a usable bundle.\n\nAbsent means `false` under `mode: overlay` and `true` under `mode: replace`. An explicit `false` under `mode: replace` is a config error rather than a silently overridden value: under replace the local document is not a servable configuration, so there would be nothing to boot on.",
          "default": null,
          "type": [
            "boolean",
            "null"
          ]
        },
        "allow_insecure_http": {
          "description": "Permit a plaintext `http://` authority URL. Development only.\n\nBundle signatures are checked either way, so this does not let an attacker forge a configuration, but it does expose the credential this node presents and reveals the whole configuration to anyone on the path.",
          "default": false,
          "type": "boolean"
        },
        "allow_shared_secret_keys": {
          "description": "Acknowledge that `hmac_sha256` entries in the verifying-key file may verify bundles. Development only.\n\nA shared secret is symmetric: every subscriber holding it can forge a bundle for every other subscriber. Off by default, and verification refuses those bundles until it is on.",
          "default": false,
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "BundleMode": {
      "description": "How a subscriber applies a verified bundle to its local configuration.\n\nDerives `JsonSchema` because `proxy.config_authority.upstream.mode` reuses this enum, and every type reachable from `ConfigFile` feeds the generated editor schema.",
      "oneOf": [
        {
          "description": "Merge the bundle over the subscriber's own configuration, leaving anything the bundle does not mention untouched.",
          "type": "string",
          "enum": [
            "overlay"
          ]
        },
        {
          "description": "Replace the subscriber's configuration wholesale. Anything absent from the bundle is dropped.",
          "type": "string",
          "enum": [
            "replace"
          ]
        }
      ]
    },
    "ConfigAuthorityPublishConfig": {
      "description": "`proxy.config_authority.publish`: this node signs and serves configuration bundles to subscribers.\n\n```yaml proxy: config_authority: publish: authority_id: control-plane-eu key_id: authority-2026-07 signing_key_file: /etc/sbproxy/authority-signing.key store_dir: /var/lib/sbproxy/config-authority bind: 0.0.0.0:9443 tls: cert_file: /etc/sbproxy/authority.pem key_file: /etc/sbproxy/authority-key.pem ```\n\nThe bundle endpoint gets its own listener on [`Self::bind`], separate from the admin server, and serves exactly one path. Publication, status, and subscriber management are admin routes on the admin listener, because those are operator actions authenticated with operator credentials, while a bundle fetch is a fleet action authenticated with a per-subscriber credential.",
      "type": "object",
      "required": [
        "authority_id",
        "bind",
        "key_id",
        "signing_key_file",
        "store_dir"
      ],
      "properties": {
        "authority_id": {
          "description": "Stable identifier stamped into every bundle this node signs. Subscribers read it as `authority_id` in the envelope.",
          "type": "string"
        },
        "key_id": {
          "description": "Key ID stamped into every envelope, selecting which entry of a subscriber's `verifying_keys_file` verifies the signature.\n\nRotation publishes under a new ID while subscribers still trust the old one, so this changes without a synchronized fleet restart.",
          "type": "string"
        },
        "signing_key_file": {
          "description": "Path to the Ed25519 signing key: one standard-base64 32-byte seed.\n\nMust be owner-only on unix. A node with `publish` configured and no readable signing key refuses to start, because an authority that cannot sign cannot serve, and finding that out at the first publish attempt means finding it out during a change window.",
          "type": "string"
        },
        "store_dir": {
          "description": "Directory holding the durable revision counter, the current and previous signed bundles, and the subscriber registry.",
          "type": "string"
        },
        "bind": {
          "description": "`host:port` for the bundle listener, for example `0.0.0.0:9443`.\n\nIts own listener, not the admin port: subscribers authenticate with a per-subscriber credential rather than operator credentials, and nothing on this listener answers `/admin/*`, `/metrics`, or the UI.",
          "type": "string"
        },
        "tls": {
          "description": "TLS material for the bundle listener.\n\nRequired whenever [`Self::bind`] is not a loopback address. The admin listener leaves TLS optional on a remote bind; this one does not, because the credential a subscriber presents here is a long-lived fleet credential and the payload is the whole configuration.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ConfigAuthorityPublishTlsConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "rate_limit_per_subscriber_per_minute": {
          "description": "Requests one subscriber may make per minute before the listener answers `429`.",
          "default": 30,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "rate_limit_total_per_minute": {
          "description": "Requests the listener serves per minute across the whole fleet before it answers `429`.",
          "default": 1200,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "archive_keep": {
          "description": "How many earlier revisions the authority keeps so a rollback can name one of them.\n\nThe authority has always kept the current bundle and the one before it, which is enough to undo the last publish. This bounds the ring beside those two, which is what lets `POST /admin/config-authority/rollback` accept a `to_revision` from further back than one step.\n\nThis counts revisions a rollback can name, so the revision currently being served does not use up a slot and the ring holds `archive_keep + 1` files.\n\nZero keeps no ring and leaves the one-step rollback exactly as it was. The maximum is 200. At the maximum, and with a configuration document at the 4 MiB wire limit, the ring is bounded at 1.58 GiB of disk; a real document makes the default ring cost kilobytes.",
          "default": 20,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "ConfigAuthorityPublishTlsConfig": {
      "description": "TLS material for the config-authority bundle listener.\n\nBoth fields are required together. Unlike the admin listener's TLS block, this one is mandatory on any non-loopback bind.",
      "type": "object",
      "required": [
        "cert_file",
        "key_file"
      ],
      "properties": {
        "cert_file": {
          "description": "Path to the PEM certificate chain, leaf first.",
          "type": "string"
        },
        "key_file": {
          "description": "Path to the PEM private key (PKCS#8 or RSA).",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "SecretsConfig": {
      "description": "Top-level secrets management configuration.\n\nThe live surface is [`SecretsConfig::backends`], selected by provider URI references. The legacy single-backend and rotation fields remain parseable for compatibility but are not consumed by the OSS runtime.",
      "type": "object",
      "properties": {
        "backend": {
          "description": "Legacy single-backend selector. Use [`SecretsConfig::backends`].",
          "default": "env",
          "type": "string"
        },
        "hashicorp": {
          "description": "Legacy HashiCorp block. Declare a named `hashicorp` backend instead.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/HashiCorpSecretsConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "map": {
          "description": "Logical name to vault path mapping. INERT since the removal of the `secret:<name>` colon form it served (WOR-1785); still parsed for schema-v1 compatibility, and boot warns when set. Use `secret://<backend>/<name>` references instead.",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "rotation": {
          "description": "How resolved upstream credentials are re-read and how a failed re-read behaves. Consumed by the key plane (WOR-2327).",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/RotationConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "fallback": {
          "description": "Legacy fallback selector; provider URI resolution fails loudly and does not consult this value.",
          "default": "cache",
          "type": "string"
        },
        "backends": {
          "description": "Named secret backends that provider-URI references resolve against (WOR-1767). A `secret://<name>/<key>` reference resolves against the `local` backend named `<name>`; `secretfile://<name>/<key>` against the `file` backend named `<name>`. An unresolved reference in an `api_key` or `client_secret` fails startup rather than reaching the wire verbatim.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/SecretBackendConfig"
          }
        }
      },
      "additionalProperties": false
    },
    "HashiCorpSecretsConfig": {
      "description": "Legacy HashiCorp Vault connection settings.\n\nThe OSS resolver consumes the `hashicorp` variant in [`SecretsConfig::backends`], not this compatibility block.",
      "type": "object",
      "required": [
        "addr"
      ],
      "properties": {
        "addr": {
          "description": "Vault server address (e.g. `\"https://vault.example.com:8200\"`).",
          "type": "string"
        },
        "token": {
          "description": "Vault token. When omitted the `VAULT_TOKEN` environment variable is used.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "mount": {
          "description": "KV secrets engine mount path. Defaults to `\"secret\"`.",
          "default": "secret",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "RotationConfig": {
      "description": "Secret-rotation behavior for credentials the proxy resolves from a backend and presents upstream.\n\nRead at boot into `sbproxy_vault::RotationPolicy` and applied by the key plane. Process-owned like the rest of `proxy.secrets`: a reload that changes it is refused with a restart message.",
      "type": "object",
      "properties": {
        "grace_period_secs": {
          "description": "How long a resolved credential may still be served after re-resolution has **failed**, measured from the end of the re-resolve window. Defaults to 300 (5 minutes).\n\nThis is availability, not credential overlap: the proxy presents upstream credentials rather than validating them, so it has no old-value acceptance window to honor. What this prevents is a briefly unreachable backend turning every request carrying a bound credential into a 503. A deleted or revoked credential is never served out of this window.",
          "default": 300,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "re_resolve_interval_secs": {
          "description": "How long a resolved credential is served before the backend is consulted again. Defaults to 60, which is the value the key plane hardcoded before this block was consumed.",
          "default": 60,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "SecretBackendConfig": {
      "description": "One named secret backend for provider-URI resolution (WOR-1767).\n\nConfig-native (does not depend on the vault crate). The binary builds a vault manager from these at boot.",
      "oneOf": [
        {
          "description": "In-config secrets, referenced as `secret://<name>/<key>`. Entry values may themselves be `${ENV}` so real secrets stay out of YAML.",
          "type": "object",
          "required": [
            "name",
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "local"
              ]
            },
            "name": {
              "description": "Backend name used in the `secret://<name>/...` reference.",
              "type": "string"
            },
            "entries": {
              "description": "Key to value map.",
              "default": {},
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "A YAML/JSON secrets file, referenced as `secretfile://<name>/<key>`.",
          "type": "object",
          "required": [
            "name",
            "path",
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "file"
              ]
            },
            "name": {
              "description": "Backend name used in the `secretfile://<name>/...` reference.",
              "type": "string"
            },
            "path": {
              "description": "Path to the secrets file.",
              "type": "string"
            },
            "format": {
              "description": "File format.",
              "default": "yaml",
              "allOf": [
                {
                  "$ref": "#/definitions/SecretFileFormat"
                }
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "HashiCorp Vault KV, referenced as `vault://<name>/<path>`.",
          "type": "object",
          "required": [
            "addr",
            "auth",
            "name",
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "hashicorp"
              ]
            },
            "name": {
              "description": "Backend name used in the `vault://<name>/...` reference.",
              "type": "string"
            },
            "addr": {
              "description": "Vault server URL, e.g. `https://vault.example/v1`.",
              "type": "string"
            },
            "mount": {
              "description": "KV mount path.",
              "default": "secret",
              "type": "string"
            },
            "engine": {
              "description": "KV engine version.",
              "default": "v2",
              "allOf": [
                {
                  "$ref": "#/definitions/SecretKvEngine"
                }
              ]
            },
            "cache_ttl_secs": {
              "description": "Cache TTL in seconds for resolved reads.",
              "default": null,
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0.0
            },
            "namespace": {
              "description": "Optional Vault Enterprise namespace.",
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "auth": {
              "description": "Authentication method.",
              "allOf": [
                {
                  "$ref": "#/definitions/HashiCorpBackendAuth"
                }
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "AWS Secrets Manager, referenced as `awssm://<name>/<secret-id>`.",
          "type": "object",
          "required": [
            "auth",
            "mount_prefix",
            "name",
            "region",
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "aws"
              ]
            },
            "name": {
              "description": "Backend name used in the `awssm://<name>/...` reference.",
              "type": "string"
            },
            "region": {
              "description": "AWS region.",
              "type": "string"
            },
            "mount_prefix": {
              "description": "Path prefix every read must stay inside.",
              "type": "string"
            },
            "cache_ttl_secs": {
              "description": "Cache TTL in seconds for resolved reads.",
              "default": null,
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0.0
            },
            "auth": {
              "description": "Authentication method.",
              "allOf": [
                {
                  "$ref": "#/definitions/AwsBackendAuth"
                }
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "GCP Secret Manager, referenced as `gcpsm://<name>/<secret>`.",
          "type": "object",
          "required": [
            "name",
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "gcp"
              ]
            },
            "name": {
              "description": "Backend name used in the `gcpsm://<name>/...` reference.",
              "type": "string"
            },
            "project_id": {
              "description": "Default GCP project id for short references.",
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "endpoint": {
              "description": "Secret Manager API endpoint override.",
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "cache_ttl_secs": {
              "description": "Cache TTL in seconds for resolved reads.",
              "default": null,
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0.0
            },
            "auth": {
              "description": "Authentication method (defaults to Application Default Credentials).",
              "default": "application_default",
              "allOf": [
                {
                  "$ref": "#/definitions/GcpBackendAuth"
                }
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Azure Key Vault, referenced as `azurekv://<name>/<secret>`.",
          "type": "object",
          "required": [
            "name",
            "type",
            "vault_url"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "azure"
              ]
            },
            "name": {
              "description": "Backend name used in the `azurekv://<name>/...` reference.",
              "type": "string"
            },
            "vault_url": {
              "description": "Key Vault URL, e.g. `https://acme-prod.vault.azure.net`.",
              "type": "string"
            },
            "cache_ttl_secs": {
              "description": "Cache TTL in seconds for resolved reads.",
              "default": null,
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0.0
            },
            "auth": {
              "description": "Authentication method (defaults to managed identity).",
              "default": "managed_identity",
              "allOf": [
                {
                  "$ref": "#/definitions/AzureBackendAuth"
                }
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Kubernetes Secrets, referenced as `k8ssecret://<name>/<secret>/<key>`.",
          "type": "object",
          "required": [
            "auth",
            "name",
            "namespace",
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "k8s"
              ]
            },
            "name": {
              "description": "Backend name used in the `k8ssecret://<name>/...` reference.",
              "type": "string"
            },
            "namespace": {
              "description": "Namespace the backend reads Secret objects from.",
              "type": "string"
            },
            "cache_ttl_secs": {
              "description": "Cache TTL in seconds for resolved reads.",
              "default": null,
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0.0
            },
            "auth": {
              "description": "Authentication method.",
              "allOf": [
                {
                  "$ref": "#/definitions/K8sBackendAuth"
                }
              ]
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "SecretFileFormat": {
      "description": "Format of a `file` secret backend's contents (WOR-1767).",
      "oneOf": [
        {
          "description": "YAML (default).",
          "type": "string",
          "enum": [
            "yaml"
          ]
        },
        {
          "description": "JSON.",
          "type": "string",
          "enum": [
            "json"
          ]
        }
      ]
    },
    "SecretKvEngine": {
      "description": "HashiCorp KV engine version for a `hashicorp` secret backend (WOR-1767).",
      "oneOf": [
        {
          "description": "KV version 1.",
          "type": "string",
          "enum": [
            "v1"
          ]
        },
        {
          "description": "KV version 2 (default).",
          "type": "string",
          "enum": [
            "v2"
          ]
        }
      ]
    },
    "HashiCorpBackendAuth": {
      "description": "Authentication for a `hashicorp` secret backend (WOR-1767).",
      "oneOf": [
        {
          "description": "Static token.",
          "type": "object",
          "required": [
            "token",
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "token"
              ]
            },
            "token": {
              "description": "Vault token (may be `${ENV}`).",
              "type": "string"
            }
          },
          "additionalProperties": false
        },
        {
          "description": "AppRole role_id + secret_id.",
          "type": "object",
          "required": [
            "role_id",
            "secret_id",
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "approle"
              ]
            },
            "role_id": {
              "description": "AppRole role id.",
              "type": "string"
            },
            "secret_id": {
              "description": "AppRole secret id (may be `${ENV}`).",
              "type": "string"
            },
            "mount": {
              "description": "AppRole auth mount.",
              "default": null,
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Kubernetes service-account JWT exchange.",
          "type": "object",
          "required": [
            "role",
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "kubernetes"
              ]
            },
            "role": {
              "description": "Vault role bound to the service account.",
              "type": "string"
            },
            "jwt_path": {
              "description": "Path to the service-account JWT.",
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "mount": {
              "description": "Kubernetes auth mount.",
              "default": null,
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "AwsBackendAuth": {
      "description": "Authentication for an `aws` secret backend (WOR-1767).",
      "oneOf": [
        {
          "description": "Static access keys.",
          "type": "object",
          "required": [
            "access_key_id",
            "secret_access_key",
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "static_keys"
              ]
            },
            "access_key_id": {
              "description": "Access key id (may be `${ENV}`).",
              "type": "string"
            },
            "secret_access_key": {
              "description": "Secret access key (may be `${ENV}`).",
              "type": "string"
            },
            "session_token": {
              "description": "Optional session token (may be `${ENV}`).",
              "default": null,
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "The AWS default credential chain (env, instance profile, ...).",
          "type": "object",
          "required": [
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "default_chain"
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Assume an IAM role for cross-account access.",
          "type": "object",
          "required": [
            "role_arn",
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "assumed_role"
              ]
            },
            "role_arn": {
              "description": "Role ARN to assume.",
              "type": "string"
            },
            "external_id": {
              "description": "Optional external id from the trust policy.",
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "session_name": {
              "description": "Optional session name.",
              "default": null,
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "GcpBackendAuth": {
      "description": "Authentication for a `gcp` secret backend (WOR-1767). Externally tagged to match the bare-string `application_default` default.",
      "oneOf": [
        {
          "description": "Application Default Credentials (default).",
          "type": "string",
          "enum": [
            "application_default"
          ]
        },
        {
          "description": "A service-account key file on disk.",
          "type": "object",
          "required": [
            "service_account_key_file"
          ],
          "properties": {
            "service_account_key_file": {
              "type": "object",
              "required": [
                "path"
              ],
              "properties": {
                "path": {
                  "description": "Path to the key file.",
                  "type": "string"
                }
              },
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Inline service-account key JSON (may be `${ENV}`).",
          "type": "object",
          "required": [
            "service_account_key_json"
          ],
          "properties": {
            "service_account_key_json": {
              "type": "object",
              "required": [
                "json"
              ],
              "properties": {
                "json": {
                  "description": "The key JSON.",
                  "type": "string"
                }
              },
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        },
        {
          "description": "An external-account (Workload Identity Federation) file.",
          "type": "object",
          "required": [
            "external_account_file"
          ],
          "properties": {
            "external_account_file": {
              "type": "object",
              "required": [
                "path"
              ],
              "properties": {
                "path": {
                  "description": "Path to the external-account file.",
                  "type": "string"
                }
              },
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "AzureBackendAuth": {
      "description": "Authentication for an `azure` secret backend. Externally tagged to match the bare-string `managed_identity` default.",
      "oneOf": [
        {
          "description": "System-assigned managed identity (default).",
          "type": "string",
          "enum": [
            "managed_identity"
          ]
        },
        {
          "description": "User-assigned managed identity, selected by client id.",
          "type": "object",
          "required": [
            "user_assigned_identity"
          ],
          "properties": {
            "user_assigned_identity": {
              "type": "object",
              "required": [
                "client_id"
              ],
              "properties": {
                "client_id": {
                  "description": "Client id of the user-assigned identity.",
                  "type": "string"
                }
              },
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Service-principal client credentials.",
          "type": "object",
          "required": [
            "service_principal"
          ],
          "properties": {
            "service_principal": {
              "type": "object",
              "required": [
                "client_id",
                "client_secret",
                "tenant_id"
              ],
              "properties": {
                "tenant_id": {
                  "description": "Microsoft Entra tenant id.",
                  "type": "string"
                },
                "client_id": {
                  "description": "App registration client id.",
                  "type": "string"
                },
                "client_secret": {
                  "description": "App registration client secret (may be `${ENV}`).",
                  "type": "string"
                },
                "authority": {
                  "description": "Optional authority host override for sovereign clouds.",
                  "default": null,
                  "type": [
                    "string",
                    "null"
                  ]
                }
              },
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        },
        {
          "description": "The logged-in Azure CLI (`az account get-access-token`).",
          "type": "string",
          "enum": [
            "azure_cli"
          ]
        }
      ]
    },
    "K8sBackendAuth": {
      "description": "Authentication for a `k8s` secret backend (WOR-1767).",
      "oneOf": [
        {
          "description": "In-cluster service-account credentials.",
          "type": "object",
          "required": [
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "in_cluster"
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "A kubeconfig file.",
          "type": "object",
          "required": [
            "path",
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "kubeconfig"
              ]
            },
            "path": {
              "description": "Path to the kubeconfig.",
              "type": "string"
            },
            "context": {
              "description": "Optional context name.",
              "default": null,
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "KeyManagementConfig": {
      "description": "Top-level `key_management:` block: the runtime key plane (mutable store, policy cache, governance, at-rest crypto, OIDC claim map, declarative seed).",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Turn the dynamic key plane on. When false (default), inbound auth keeps using the compiled virtual-key registry and this block is inert.",
          "default": false,
          "type": "boolean"
        },
        "store": {
          "description": "Store backend (system of record).",
          "default": {
            "backend": "embedded",
            "path": "/var/lib/sbproxy/keystore.redb",
            "url": null,
            "redis_source_of_truth": false,
            "prefix": "sbproxy/keystore",
            "secrets_manager": {
              "provider": "local",
              "address": null,
              "mount": null,
              "kv_v2": true,
              "token_env": "VAULT_TOKEN",
              "namespace": null,
              "region": null
            }
          },
          "allOf": [
            {
              "$ref": "#/definitions/KeyStoreConfig"
            }
          ]
        },
        "cache": {
          "description": "In-memory policy cache in front of the store.",
          "default": {
            "ttl_secs": 60,
            "negative_ttl_secs": 5,
            "max_entries": 10000,
            "tier": "none",
            "redis_url": null,
            "mesh_node_id": null,
            "mesh": null
          },
          "allOf": [
            {
              "$ref": "#/definitions/KeyCacheConfig"
            }
          ]
        },
        "governance": {
          "description": "Governed key admission, accounting, and authenticated introspection.",
          "default": {
            "consistency": "approximate",
            "backend": null,
            "lease_ttl_secs": 120,
            "terminal_retention_secs": 300,
            "failure_mode": "closed",
            "missing_rate": "zero_cost",
            "key_introspection": false,
            "require_governed_key": false
          },
          "allOf": [
            {
              "$ref": "#/definitions/KeyGovernanceConfig"
            }
          ]
        },
        "crypto": {
          "description": "At-rest crypto material.",
          "default": {
            "pepper": null,
            "master_key": null,
            "allow_ephemeral_secrets": false,
            "rotation": {
              "inbound_key_days": 90,
              "credential_days": 90,
              "master_key_days": 365,
              "credential_grace_secs": 300
            }
          },
          "allOf": [
            {
              "$ref": "#/definitions/KeyCryptoConfig"
            }
          ]
        },
        "inbound": {
          "description": "Which inbound headers carry a minted key, and whether one is required.",
          "default": {
            "headers": [
              {
                "name": "authorization",
                "scheme": "Bearer "
              },
              {
                "name": "x-api-key",
                "scheme": ""
              },
              {
                "name": "x-sb-api",
                "scheme": ""
              }
            ],
            "require": false,
            "provider_hints": [
              {
                "provider": "anthropic",
                "header": "x-api-key",
                "scheme": "",
                "value_prefix": "sk-ant-",
                "also_header": null
              },
              {
                "provider": "anthropic",
                "header": "authorization",
                "scheme": "Bearer ",
                "value_prefix": "sk-ant-",
                "also_header": null
              },
              {
                "provider": "anthropic",
                "header": "x-api-key",
                "scheme": "",
                "value_prefix": "",
                "also_header": "anthropic-version"
              },
              {
                "provider": "openrouter",
                "header": "authorization",
                "scheme": "Bearer ",
                "value_prefix": "sk-or-",
                "also_header": null
              },
              {
                "provider": "gemini",
                "header": "x-goog-api-key",
                "scheme": "",
                "value_prefix": "",
                "also_header": null
              },
              {
                "provider": "azure",
                "header": "api-key",
                "scheme": "",
                "value_prefix": "",
                "also_header": null
              },
              {
                "provider": "openai",
                "header": "authorization",
                "scheme": "Bearer ",
                "value_prefix": "sk-",
                "also_header": null
              }
            ],
            "native_key_policy": null
          },
          "allOf": [
            {
              "$ref": "#/definitions/KeyInboundConfig"
            }
          ]
        },
        "allow_api_override": {
          "description": "Allow the admin API to override config-seeded records on reload. When false (default), config-seeded records are authoritative and re-asserted on every reload.",
          "default": false,
          "type": "boolean"
        },
        "failure_mode_allow": {
          "description": "Superseded by `failure_posture`. When the store is unreachable, allow the request through in a degraded mode. Default false: fail closed (deny).\n\nNote the polarity this field is named into: `true` here means ALLOW, that is, fail open. Other booleans in this config carry the opposite sense, which is the inconsistency [`FailureMode`] exists to retire.\n\nStill parsed, and still the value used when `failure_posture` is absent, so an existing config keeps behaving exactly as it did. Nothing in the runtime reads this field directly any more: every store-outage decision goes through [`KeyManagementConfig::failure_posture`].",
          "default": false,
          "type": "boolean"
        },
        "failure_posture": {
          "description": "Failure posture for a key-store outage, in the shared [`FailureMode`] vocabulary.\n\nSet this in preference to `failure_mode_allow`. When present it wins; when absent the legacy boolean is converted (`false` becomes `closed`, `true` becomes `degraded`). It is `Option` on purpose, so \"the operator said nothing\" stays distinguishable from \"the operator explicitly asked for the default\".\n\n`closed` refuses with 503. `degraded` and `open` both let the request fall through to the origin's own configured auth, which is what `failure_mode_allow: true` has always done: it is not a blanket admit. `degraded` is the honest label for it, because the request proceeds with no per-key policy, no budget, and no attribution, and that fact is recorded rather than passed over in silence. `observe` is meaningless here and is rejected at config-compile time: an unreachable store produced no counterfactual verdict to record.",
          "anyOf": [
            {
              "$ref": "#/definitions/FailureMode"
            },
            {
              "type": "null"
            }
          ]
        },
        "oidc_claim_map": {
          "description": "Optional OIDC/JWT claim to virtual-key mapping.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/OidcClaimMapConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "seed": {
          "description": "Optional declarative seed of keys and credentials.",
          "default": {
            "keys": [],
            "credentials": []
          },
          "allOf": [
            {
              "$ref": "#/definitions/KeySeedConfig"
            }
          ]
        },
        "read_audit": {
          "description": "Read and access audit for key and credential resolution (WOR-2570).",
          "default": {
            "enabled": false,
            "detail_window_secs": 300,
            "hash_identifiers": true
          },
          "allOf": [
            {
              "$ref": "#/definitions/KeyReadAuditConfig"
            }
          ]
        },
        "break_glass": {
          "description": "Break-glass emergency access to the key and credential admin API (WOR-2573).",
          "default": {
            "enabled": false,
            "approvers": [],
            "quorum": 2,
            "max_ttl_secs": 3600,
            "review_window_secs": 86400
          },
          "allOf": [
            {
              "$ref": "#/definitions/BreakGlassConfig"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "KeyStoreConfig": {
      "description": "`key_management.store:` block.",
      "type": "object",
      "properties": {
        "backend": {
          "description": "Backend selector.",
          "default": "embedded",
          "allOf": [
            {
              "$ref": "#/definitions/KeyStoreBackend"
            }
          ]
        },
        "path": {
          "description": "Embedded redb file path (backend `embedded`).",
          "default": "/var/lib/sbproxy/keystore.redb",
          "type": "string"
        },
        "url": {
          "description": "Redis connection URL (backend `redis`).",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "redis_source_of_truth": {
          "description": "Legacy compatibility switch. Selecting `backend: redis` already makes Redis the key store; this value does not change runtime behavior.",
          "default": false,
          "type": "boolean"
        },
        "prefix": {
          "description": "Secret-reference namespace prefix (backend `secrets_manager`).",
          "default": "sbproxy/keystore",
          "type": "string"
        },
        "secrets_manager": {
          "description": "External secrets-manager connection (backend `secrets_manager`).",
          "default": {
            "provider": "local",
            "address": null,
            "mount": null,
            "kv_v2": true,
            "token_env": "VAULT_TOKEN",
            "namespace": null,
            "region": null
          },
          "allOf": [
            {
              "$ref": "#/definitions/SecretsManagerStoreConfig"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "KeyStoreBackend": {
      "description": "Which store backend backs the key plane.",
      "oneOf": [
        {
          "description": "redb embedded store (default).",
          "type": "string",
          "enum": [
            "embedded"
          ]
        },
        {
          "description": "Redis store / coherence tier.",
          "type": "string",
          "enum": [
            "redis"
          ]
        },
        {
          "description": "Secrets-manager-direct: a configured vault backend is the system of record.",
          "type": "string",
          "enum": [
            "secrets_manager"
          ]
        },
        {
          "description": "Cluster mesh replicated store (WOR-2064): records live on the durable replicated state substrate configured by `proxy.cluster.replication`, so a key minted on one node resolves on its peers with no external store. Requires `proxy.cluster` with a `replication` block. Consistency is pinned by the backend (quorum writes, quorum reads, revocation written at one) and is not operator-configurable; the replication factor comes from the cluster's replication block.",
          "type": "string",
          "enum": [
            "mesh"
          ]
        }
      ]
    },
    "SecretsManagerStoreConfig": {
      "description": "`key_management.store.secrets_manager:` connection block.",
      "type": "object",
      "properties": {
        "provider": {
          "description": "Which external manager.",
          "default": "local",
          "allOf": [
            {
              "$ref": "#/definitions/SecretsManagerProvider"
            }
          ]
        },
        "address": {
          "description": "HashiCorp Vault address (provider `hashicorp`), e.g. `https://vault.example/v1`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "mount": {
          "description": "KV mount path (provider `hashicorp`) or path prefix (provider `aws`).",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "kv_v2": {
          "description": "Use KV engine v2 (provider `hashicorp`). Default true.",
          "default": true,
          "type": "boolean"
        },
        "token_env": {
          "description": "Environment variable holding the Vault token (provider `hashicorp`). Default `VAULT_TOKEN`.",
          "default": "VAULT_TOKEN",
          "type": "string"
        },
        "namespace": {
          "description": "Optional `X-Vault-Namespace` (provider `hashicorp`, Vault Enterprise).",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "region": {
          "description": "AWS region (provider `aws`), e.g. `us-east-1`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "SecretsManagerProvider": {
      "description": "External secrets manager backing the `secrets_manager` store backend. Only writable managers are supported (HashiCorp Vault, AWS Secrets Manager, and an in-memory `local` store for dev/tests).",
      "oneOf": [
        {
          "description": "In-memory, non-persistent. Dev and tests only.",
          "type": "string",
          "enum": [
            "local"
          ]
        },
        {
          "description": "HashiCorp Vault KV (token auth, token read from `token_env`).",
          "type": "string",
          "enum": [
            "hashicorp"
          ]
        },
        {
          "description": "AWS Secrets Manager via the default credential chain.",
          "type": "string",
          "enum": [
            "aws"
          ]
        }
      ]
    },
    "KeyCacheConfig": {
      "description": "`key_management.cache:` block.",
      "type": "object",
      "properties": {
        "ttl_secs": {
          "description": "Positive-entry TTL in seconds (default 60).",
          "default": 60,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "negative_ttl_secs": {
          "description": "Negative (known-absent) entry TTL in seconds (default 5).",
          "default": 5,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "max_entries": {
          "description": "Soft cap on cached entries per kind (default 10000).",
          "default": 10000,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "tier": {
          "description": "Optional second cache tier.",
          "default": "none",
          "allOf": [
            {
              "$ref": "#/definitions/KeyCacheTier"
            }
          ]
        },
        "redis_url": {
          "description": "Redis URL for the redis cache tier (when `tier: redis`). Falls back to the store URL when unset.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "mesh_node_id": {
          "description": "Node id for the mesh cache tier (when `tier: mesh`). Defaults to the machine hostname.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "mesh": {
          "description": "Mesh cluster bootstrap for the mesh cache tier. When set, the node joins a gossip cluster and the cache routes by consistent hash, so a key cached on one replica is reachable from the others. When absent, the mesh tier runs single-node.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/MeshClusterConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "KeyCacheTier": {
      "description": "Which optional second cache tier sits behind the in-memory L1.",
      "oneOf": [
        {
          "description": "L1 only (default).",
          "type": "string",
          "enum": [
            "none"
          ]
        },
        {
          "description": "Redis L2 tier.",
          "type": "string",
          "enum": [
            "redis"
          ]
        },
        {
          "description": "Mesh distributed-cache tier.",
          "type": "string",
          "enum": [
            "mesh"
          ]
        }
      ]
    },
    "MeshClusterConfig": {
      "description": "`key_management.cache.mesh:` cluster bootstrap for the mesh cache tier.",
      "type": "object",
      "properties": {
        "seeds": {
          "description": "Static seed peers (`host:port`) to join. An empty list bootstraps a single-node cluster.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "gossip_port": {
          "description": "UDP port for the gossip protocol.",
          "default": 7946,
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "transport_port": {
          "description": "TCP port for the cross-node cache RPC transport. `0` requests an OS-assigned ephemeral port.",
          "default": 8946,
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "advertise_addr": {
          "description": "Address this node advertises to peers (`host:port`). Defaults to the gossip bind when unset.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "transport_advertise_addr": {
          "description": "Address this node advertises for typed-state transport (`host:port`). Defaults to the gossip-advertised host and `transport_port`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "shared_key": {
          "description": "Optional cluster-wide shared secret (AES-256-GCM) for the gossip and transport wire. Accepts an inline value or `env:NAME`. Plaintext when unset.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "key_derivation": {
          "description": "How `shared_key` becomes the AES-256-GCM wire key.\n\nDefaults to `sha256`, which is what every cluster runs today, so an upgrade never changes the key a node seals under. `hkdf` moves the mesh onto the same purpose-separated derivation every other key in this workspace uses.\n\nNodes open under both derivations regardless of this setting, so a cluster can be flipped one node at a time without partitioning. See `docs/mesh-replication.md`.",
          "default": "sha256",
          "allOf": [
            {
              "$ref": "#/definitions/MeshKeyDerivation"
            }
          ]
        },
        "peer_tls": {
          "description": "Optional peer mTLS (mutually-authenticated TLS) for the mesh transport. When set, inbound connections must present a CA-signed client certificate and outbound connections present this node's certificate, all verified against the configured CA. Plaintext when unset.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/MeshPeerTlsConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "MeshPeerTlsConfig": {
      "description": "`key_management.cache.mesh.peer_tls:` mutual-TLS material (file paths) for the mesh peer transport.",
      "type": "object",
      "required": [
        "ca_file",
        "cert_file",
        "key_file"
      ],
      "properties": {
        "cert_file": {
          "description": "Path to this node's PEM certificate chain (leaf first).",
          "type": "string"
        },
        "key_file": {
          "description": "Path to this node's PEM private key.",
          "type": "string"
        },
        "ca_file": {
          "description": "Path to the PEM CA certificate that signs every peer.",
          "type": "string"
        },
        "server_name": {
          "description": "Logical server name the peer certificates are issued for (their SAN); outbound connections verify peers against it. Defaults to `sbproxy-mesh`.",
          "default": "sbproxy-mesh",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "KeyGovernanceConfig": {
      "description": "`key_management.governance:` admission, accounting, and introspection controls for governed virtual keys.",
      "type": "object",
      "properties": {
        "consistency": {
          "description": "Accounting consistency. Approximate is the backward-compatible default.",
          "default": "approximate",
          "allOf": [
            {
              "$ref": "#/definitions/GovernanceConsistency"
            }
          ]
        },
        "backend": {
          "description": "Dedicated backend for strict accounting.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/GovernanceBackendConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "lease_ttl_secs": {
          "description": "Reservation lease duration. Expired reservations are released when a gateway exits before settling or releasing them.",
          "default": 120,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "terminal_retention_secs": {
          "description": "Retention for settled, released, and expired reservation outcomes. Must be at least the lease duration so retries remain idempotent.",
          "default": 300,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "failure_mode": {
          "description": "Superseded by `failure_posture`. Behavior when the governance backend cannot serve a reserve call at request time (`GovernanceError::BackendUnavailable`). The default denies the request (fail closed): governed limits must not be silently bypassed by a backend outage. Setting `allow_unreserved` admits the request instead, but every such decision is always audited on the `security_audit` channel and counted on `sbproxy_governance_fail_open_total`.\n\nStill parsed, and still the value used when `failure_posture` is absent, so an existing config keeps behaving exactly as it did. Nothing in the runtime reads this field directly any more: the read path goes through [`KeyGovernanceConfig::failure_posture`], which reports `allow_unreserved` as `degraded` because the call proceeds without the reservation this control exists to make.",
          "default": "closed",
          "allOf": [
            {
              "$ref": "#/definitions/GovernanceFailureMode"
            }
          ]
        },
        "failure_posture": {
          "description": "Failure posture for a governance backend outage, in the shared [`FailureMode`] vocabulary.\n\nSet this in preference to `failure_mode`. When present it wins; when absent the legacy `failure_mode` value is converted (`closed` stays `closed`, `allow_unreserved` becomes `degraded`). It is `Option` on purpose, so \"the operator said nothing\" stays distinguishable from \"the operator explicitly asked for the default\".\n\n`closed` denies with 503. `degraded` admits without a reservation and records that fact on the `security_audit` channel and on `sbproxy_governance_fail_open_total`. `open` also admits but records neither, which is why `degraded` is the honest spelling of the old `allow_unreserved`. `observe` is meaningless here and is rejected at config-compile time: a reserve call that could not reach its backend produced no counterfactual verdict to record.",
          "anyOf": [
            {
              "$ref": "#/definitions/FailureMode"
            },
            {
              "type": "null"
            }
          ]
        },
        "missing_rate": {
          "description": "Behavior when a governed key carries a `total_micro_usd` limit but the resolved model has no rate to estimate a pre-request cost ceiling from. The default (`zero_cost`) admits with no monetary pre-gate; `require_rate` denies instead, since a limit that cannot be pre-gated must not be silently treated as unlimited.",
          "default": "zero_cost",
          "allOf": [
            {
              "$ref": "#/definitions/GovernanceMissingRatePolicy"
            }
          ]
        },
        "key_introspection": {
          "description": "Reserved caller-introspection switch. The OSS runtime does not install `GET /api/v1/key`; retained for config compatibility.",
          "default": false,
          "type": "boolean"
        },
        "require_governed_key": {
          "description": "Require AI requests to resolve to a governed key instead of accepting origin credentials or anonymous access.",
          "default": false,
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "GovernanceConsistency": {
      "description": "Consistency guarantee for governed key admission and accounting.",
      "oneOf": [
        {
          "description": "Process-local atomic counters. Fast and safe on one gateway, but totals across multiple gateways are approximate.",
          "type": "string",
          "enum": [
            "approximate"
          ]
        },
        {
          "description": "Cluster-wide atomic reservations backed by Redis scripts.",
          "type": "string",
          "enum": [
            "strict"
          ]
        }
      ]
    },
    "GovernanceBackendConfig": {
      "description": "Shared backend used for strict governed key accounting.",
      "oneOf": [
        {
          "description": "A dedicated Redis connection. This is intentionally explicit and is not inherited from the key store or cache configuration.",
          "type": "object",
          "required": [
            "type",
            "url"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "redis"
              ]
            },
            "url": {
              "description": "Redis or TLS-enabled Redis connection URL.",
              "type": "string"
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "GovernanceFailureMode": {
      "description": "Superseded by [`FailureMode`]. Behavior when the governance backend cannot serve a reserve call (`sbproxy_ai::governance::GovernanceStore::reserve`).\n\nApplies only to a reserve call that fails with `GovernanceError::BackendUnavailable`. Every other reserve error (invalid request shape, a reservation id reused with different input, a hit against a real governed limit, arithmetic overflow) is unrelated to backend availability and is not affected by this setting.\n\nKept because `failure_mode: closed | allow_unreserved` is pinned by test and by shipped configs. Read it through [`KeyGovernanceConfig::failure_posture`], never directly.",
      "oneOf": [
        {
          "description": "Deny the request (`503`) when the governance backend is unavailable.",
          "type": "string",
          "enum": [
            "closed"
          ]
        },
        {
          "description": "Admit the request without a governance reservation when the backend is unavailable. Always emits a `security_audit` event and increments `sbproxy_governance_fail_open_total` so the decision stays observable.",
          "type": "string",
          "enum": [
            "allow_unreserved"
          ]
        }
      ]
    },
    "FailureMode": {
      "description": "What a control does when it cannot reach a decision.\n\nA \"control\" here is anything that gates a request and can itself fail: a policy whose backend is unreachable, a guardrail whose provider times out, a detector that never engaged, a store that cannot be read. The question is always the same, so the knob is too, and it is spelled `failure_posture` everywhere it appears:\n\n```yaml failure_posture: closed     # refuse the request failure_posture: open       # admit it failure_posture: degraded   # admit it, but record that the guarantee was not made failure_posture: observe    # admit it, and record what would have happened ```\n\n# The four postures\n\nOnly [`Closed`](Self::Closed) refuses. The other three all admit the request and differ in what they leave behind, which is the part that matters six months later when someone asks whether a control was actually protecting anything:\n\n- **`open`** admits and claims nothing. Cheapest, and the least recoverable after the fact. - **`degraded`** admits while explicitly marking the guarantee as not made. This is the posture behind the existing `AllowUnreserved` modes: the request proceeds, but no quota was reserved and no governance decision was recorded, and that fact is itself counted so it can be alerted on. - **`observe`** admits and records the decision the control *would* have taken. For rolling a control out against live traffic before letting it refuse anything.\n\n# Relationship to `test_mode` and tag actions\n\n`observe` is deliberately close in spirit to the WAF's `test_mode` and the prompt-injection `Tag` action, and the overlap is worth naming so the two do not drift into meaning different things. They are not the same axis:\n\n- `test_mode` / `Tag` describe what the control does when it **works** and finds a hit. - `failure_posture: observe` describes what it does when it **cannot run at all**.\n\nA control can legitimately be in `test_mode` and still need a failure posture, because \"the detector matched\" and \"the detector was unreachable\" are different events. Where a site already has `test_mode`, leave it alone and let `failure_posture` govern only the cannot-decide path.\n\n# Why this type exists\n\nThe same decision was previously spelled six different ways across the config surface: `fail_open: bool`, `fail_closed: bool`, `failure_mode_allow: bool`, two separately-declared `failure_mode` enums, an `on_failure` enum, and an unvalidated `on_error: String`. Two of those booleans carry **opposite** polarity, so `true` means \"admit\" in one struct and \"refuse\" in another. An operator had to re-derive the meaning at every site, and a reviewer had to check the field name before they could read a diff.\n\nNew and migrated controls take `failure_posture: FailureMode`. The name is deliberately not `failure_mode`: two blocks already declare a field by that exact name carrying a narrower enum, and a test pins that `failure_mode: open` must fail to parse there. One new word that works at every site beats one that collides at two.\n\nThe legacy fields still parse, because [`schema-v1` compatibility] is pinned by test, and each site's `failure_posture()` accessor converts from them when the new key is absent. They carry no `#[deprecated]` attribute on purpose: `-D warnings` would then turn every remaining read into a build failure, including the conversion itself. They are deprecated in prose and by having no other reader left.\n\n# Choosing a default\n\nDefault closed for anything that enforces a security boundary: a control that silently admits traffic when it breaks is worse than no control, because the config still advertises protection and the dashboard still reads green.\n\nDefault open only where refusing would take the gateway down over a non-security concern, and say so at the site. A policy-expression bug should not black-hole every request; an unreachable authorization backend should.\n\n[`schema-v1` compatibility]: https://github.com/soapbucket/sbproxy",
      "oneOf": [
        {
          "description": "Refuse the request. The safe default for anything enforcing a security boundary: a control that silently admits traffic when it breaks is worse than no control, because the config still advertises protection and the dashboard still reads green.",
          "type": "string",
          "enum": [
            "closed"
          ]
        },
        {
          "description": "Admit the request and claim nothing. Cheapest, and the least recoverable after the fact.",
          "type": "string",
          "enum": [
            "open"
          ]
        },
        {
          "description": "Admit the request while explicitly marking the guarantee as not made. The posture behind the legacy `AllowUnreserved` modes: the call proceeds, but no quota was reserved and no governance decision was recorded, and that fact is counted so it can be alerted on.",
          "type": "string",
          "enum": [
            "degraded"
          ]
        },
        {
          "description": "Admit the request and record the decision the control would have taken. For rolling a control out against live traffic before letting it refuse anything.",
          "type": "string",
          "enum": [
            "observe"
          ]
        }
      ]
    },
    "GovernanceMissingRatePolicy": {
      "description": "Behavior when a governed key's monetary limit cannot be pre-gated because the resolved model has no rate to estimate a cost ceiling from.",
      "oneOf": [
        {
          "description": "Treat the estimated cost as zero. No monetary pre-gate applies to this request; a key's `total_micro_usd` limit is still enforced at settlement, from actually billed usage.",
          "type": "string",
          "enum": [
            "zero_cost"
          ]
        },
        {
          "description": "Deny the request when the key carries a `total_micro_usd` limit but the resolved model has no rate: a limit that cannot be pre-gated must not be silently treated as unlimited.",
          "type": "string",
          "enum": [
            "require_rate"
          ]
        }
      ]
    },
    "KeyCryptoConfig": {
      "description": "`key_management.crypto:` block. Both values accept a secret reference (`vault://`, `env:`, `file:`, ...) resolved at boot, or an inline value (discouraged outside tests).",
      "type": "object",
      "properties": {
        "pepper": {
          "description": "Server pepper for inbound virtual-key hashing. When unset, a process-ephemeral pepper is generated, so stored hashes do not survive a restart; set this in production.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "master_key": {
          "description": "Master key for upstream-credential envelope encryption. Required to store encrypted upstream credentials; vault-ref credentials do not need it.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "allow_ephemeral_secrets": {
          "description": "Let the process mint an ephemeral `pepper` or `master_key` when the operator pinned neither (WOR-2567).\n\nDefault false, and the default is the change: a restart with no pinned pepper silently invalidates every stored key hash, and the failure surfaces as a flood of 401s rather than as a boot refusal. Vault and comparable products refuse to start without a resolvable root key rather than minting one, and that is now the behavior here. Set true for a single-process local development run, where a key plane whose hashes do not outlive the process is exactly what is wanted.",
          "default": false,
          "type": "boolean"
        },
        "root_of_trust": {
          "description": "Customer-managed root of trust for the upstream-credential envelope (WOR-2568). Absent means sbproxy's own `master_key` is the root.",
          "anyOf": [
            {
              "$ref": "#/definitions/RootOfTrustConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "rotation": {
          "description": "Named crypto periods and the credential rotation grace window (WOR-2567).",
          "default": {
            "inbound_key_days": 90,
            "credential_days": 90,
            "master_key_days": 365,
            "credential_grace_secs": 300
          },
          "allOf": [
            {
              "$ref": "#/definitions/KeyRotationCadenceConfig"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "RootOfTrustConfig": {
      "description": "`key_management.crypto.root_of_trust:` block (WOR-2568). Present means the customer holds the root of trust for the upstream-credential envelope; absent means sbproxy's own `master_key` does.\n\nOne field answers \"is our root of trust customer-held right now\", which is deliberate: before this block existed the only way to answer it was to audit which reference `master_key` happened to carry, and every answer that audit could give was \"no\", because a resolved reference is a copy.",
      "type": "object",
      "required": [
        "address",
        "key_name",
        "provider",
        "token"
      ],
      "properties": {
        "provider": {
          "description": "Which external key service performs the wrap and unwrap.",
          "allOf": [
            {
              "$ref": "#/definitions/RootOfTrustProvider"
            }
          ]
        },
        "address": {
          "description": "Base address of the key service, for example `https://vault.internal:8200`.",
          "type": "string"
        },
        "mount": {
          "description": "Transit mount path. Defaults to `transit`, matching Vault's own default mount.",
          "default": "transit",
          "type": "string"
        },
        "key_name": {
          "description": "Name of the Transit key that wraps sbproxy's data keys. Created and owned by the customer; sbproxy never creates it.",
          "type": "string"
        },
        "token": {
          "description": "Secret reference for the token sbproxy authenticates with (`env:`, `file:`, `vault://`, ...). Resolved once at boot. Losing this token is a second, independent way for the customer to cut sbproxy off.",
          "type": "string"
        },
        "namespace": {
          "description": "Optional Vault Enterprise namespace header.",
          "type": [
            "string",
            "null"
          ]
        },
        "unwrap_cache_ttl_secs": {
          "description": "How long an unwrapped data key may be reused before the external service is consulted again.\n\nThis number *is* the deployment's revocation-latency bound and is reported verbatim on `GET /admin/crypto/root-of-trust`. Larger trades a longer window in which a revoked grant still works for fewer calls to the key service. The resolved-credential cache is clamped to this value for customer-managed envelopes, so raising `proxy.secrets.rotation.re_resolve_interval_secs` cannot quietly extend it.",
          "default": 60,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "liveness_interval_secs": {
          "description": "How often to probe the key service for reachability and continued authorization, in seconds. Feeds the admin surface's last-successful-check timestamp and the liveness metric. Zero disables the background probe; the on-demand path still fails closed.",
          "default": 30,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "RootOfTrustProvider": {
      "description": "External key services that can hold the root of trust.",
      "oneOf": [
        {
          "description": "HashiCorp Vault's Transit secrets engine (encryption as a service). The caller never receives the key, only ciphertext or plaintext.",
          "type": "string",
          "enum": [
            "vault_transit"
          ]
        }
      ]
    },
    "KeyRotationCadenceConfig": {
      "description": "`key_management.crypto.rotation:` block (WOR-2567): the named crypto period for each class of key material, and the grace window a rotated upstream credential keeps its previous material usable for.\n\nNIST SP 800-57 Part 1 Rev 5 frames a key's life as generation, activation, active use, rotation, destruction, and expects a deployment to *state* its crypto period rather than leave \"rotate periodically\" as the whole policy. These defaults are that statement; the runtime reads them to compute rotation age and to warn when a record is past due.",
      "type": "object",
      "properties": {
        "inbound_key_days": {
          "description": "Crypto period for inbound virtual keys, in days. Default 90.",
          "default": 90,
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "credential_days": {
          "description": "Crypto period for upstream provider credentials, in days. Default 90: a provider API key is a bearer credential with no binding to the caller, so it sits at the short end of NIST's range rather than the one to two years a TLS key gets.",
          "default": 90,
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "master_key_days": {
          "description": "Crypto period for the envelope master key, in days. Default 365, the symmetric data-encryption-key end of NIST's range. Under a customer-managed root this is the customer's Transit key rotation cadence, not sbproxy's.",
          "default": 365,
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "credential_grace_secs": {
          "description": "How long a rotated upstream credential keeps serving its previous material when the new material cannot be resolved, in seconds. Default 300. Mirrors the dual-validity window `rotate_key` already gives inbound keys; zero disables the overlap.",
          "default": 300,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "KeyInboundConfig": {
      "description": "`key_management.inbound:` block. Controls which request headers are swept for a minted key, and whether a route refuses requests that carry none.\n\nThe header a key arrives in is a property of the calling tool, not of the key: to know which header holds the key you would have to have resolved it already. So extraction is configured per route here rather than per key.",
      "type": "object",
      "properties": {
        "headers": {
          "description": "Ordered candidate headers. One well-shaped minted token resolves; two distinct tokens are ambiguous and fail closed. An empty list disables the sweep and leaves the legacy `authorization` path as the only front door.",
          "default": [
            {
              "name": "authorization",
              "scheme": "Bearer "
            },
            {
              "name": "x-api-key",
              "scheme": ""
            },
            {
              "name": "x-sb-api",
              "scheme": ""
            }
          ],
          "type": "array",
          "items": {
            "$ref": "#/definitions/InboundHeaderConfig"
          }
        },
        "require": {
          "description": "Deny with 401 when no minted key resolved. Off by default, so an upgrade changes nothing. Set per origin to make the proxy the only door on a route that has no other auth provider.",
          "default": false,
          "type": "boolean"
        },
        "provider_hints": {
          "description": "Ordered rules attributing a native (non-minted) inbound credential to a provider. First matching hint wins, so more specific value prefixes belong before general ones. A matching hint then enters native-key policy admission; a credential matching no hint remains unattributed.",
          "default": [
            {
              "provider": "anthropic",
              "header": "x-api-key",
              "scheme": "",
              "value_prefix": "sk-ant-",
              "also_header": null
            },
            {
              "provider": "anthropic",
              "header": "authorization",
              "scheme": "Bearer ",
              "value_prefix": "sk-ant-",
              "also_header": null
            },
            {
              "provider": "anthropic",
              "header": "x-api-key",
              "scheme": "",
              "value_prefix": "",
              "also_header": "anthropic-version"
            },
            {
              "provider": "openrouter",
              "header": "authorization",
              "scheme": "Bearer ",
              "value_prefix": "sk-or-",
              "also_header": null
            },
            {
              "provider": "gemini",
              "header": "x-goog-api-key",
              "scheme": "",
              "value_prefix": "",
              "also_header": null
            },
            {
              "provider": "azure",
              "header": "api-key",
              "scheme": "",
              "value_prefix": "",
              "also_header": null
            },
            {
              "provider": "openai",
              "header": "authorization",
              "scheme": "Bearer ",
              "value_prefix": "sk-",
              "also_header": null
            }
          ],
          "type": "array",
          "items": {
            "$ref": "#/definitions/ProviderHintConfig"
          }
        },
        "native_key_policy": {
          "description": "Explicit default policy for recognized caller-owned native provider keys. Absent by default, which fails closed if a provider hint matches.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/NativeKeyPolicyConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "InboundHeaderConfig": {
      "description": "One entry in `key_management.inbound.headers:`.",
      "type": "object",
      "required": [
        "name"
      ],
      "properties": {
        "name": {
          "description": "Header name, matched case-insensitively.",
          "type": "string"
        },
        "scheme": {
          "description": "Prefix stripped from the value before the token shape is tested, matched case-insensitively. Empty for raw-value headers such as `x-api-key`.",
          "default": "",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "ProviderHintConfig": {
      "description": "One rule mapping an inbound credential's shape to a provider label, for attribution of native (non-minted) keys.",
      "type": "object",
      "required": [
        "header",
        "provider"
      ],
      "properties": {
        "provider": {
          "description": "Provider label stamped on the request when this rule matches (canonical AI-provider spelling: `anthropic`, `openai`, ...).",
          "type": "string"
        },
        "header": {
          "description": "Header the credential arrives in, matched case-insensitively.",
          "type": "string"
        },
        "scheme": {
          "description": "Scheme prefix stripped before the value test, matched case-insensitively. Empty for raw-value headers.",
          "default": "",
          "type": "string"
        },
        "value_prefix": {
          "description": "Prefix the credential value must start with (`sk-ant-`). Empty matches any non-empty value.",
          "default": "",
          "type": "string"
        },
        "also_header": {
          "description": "A second header that must also be present for this rule to match (`anthropic-version`). `None` requires nothing extra.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "NativeKeyPolicyConfig": {
      "description": "Default admission policy for caller-owned native provider keys.\n\nNative keys cannot carry an SBproxy policy record because the caller, not the operator, owns their secret. This block supplies the equivalent default policy for every native key recognized by `provider_hints`. Leaving it absent fails closed for recognized native-key traffic.",
      "type": "object",
      "required": [
        "allowed_providers"
      ],
      "properties": {
        "allowed_providers": {
          "description": "Canonical provider labels admitted to use caller-owned credentials.\n\nMatching is case-insensitive and ignores surrounding whitespace. The list must be non-empty and may not contain duplicates.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "max_requests_per_minute": {
          "description": "Max requests per minute for each origin/provider native-key bucket.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "max_tokens_per_minute": {
          "description": "Max input and output tokens per minute.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "max_budget_tokens": {
          "description": "Max total tokens for the native-key budget window.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "max_budget_usd": {
          "description": "Max total cost in USD for the native-key budget window.",
          "default": null,
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "allowed_models": {
          "description": "Models native-key traffic may use (empty = all).",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "blocked_models": {
          "description": "Models native-key traffic may not use.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "require_pii_redaction": {
          "description": "Named PII redaction rules that must be active before dispatch.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "OidcClaimMapConfig": {
      "description": "`key_management.oidc_claim_map:` block.",
      "type": "object",
      "required": [
        "claim_field"
      ],
      "properties": {
        "claim_field": {
          "description": "The verified JWT/OIDC claim whose value names the virtual-key record to resolve, so the bearer-token and OIDC front doors converge on one record.",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "KeySeedConfig": {
      "description": "`key_management.seed:` block: declarative records applied at boot.",
      "type": "object",
      "properties": {
        "keys": {
          "description": "Inbound virtual keys.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/SeedKeyConfig"
          }
        },
        "credentials": {
          "description": "Upstream provider credentials.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/SeedCredentialConfig"
          }
        }
      },
      "additionalProperties": false
    },
    "SeedKeyConfig": {
      "description": "A seeded inbound virtual key.",
      "type": "object",
      "required": [
        "key_id"
      ],
      "properties": {
        "key_id": {
          "description": "Stable public id and token prefix.",
          "type": "string"
        },
        "secret": {
          "description": "Plaintext secret hashed at boot. Mutually exclusive with `secret_hash`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "secret_hash": {
          "description": "Precomputed HMAC-SHA256 hash (hex) when the operator hashed offline.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "Human-readable name, surfaced on access logs.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "max_requests_per_minute": {
          "description": "Max requests per minute.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "max_tokens_per_minute": {
          "description": "Max input and output tokens per minute.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "priority": {
          "description": "Served-model admission lane (`interactive`, `standard`, or `batch`).",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "max_budget_tokens": {
          "description": "Max total tokens for this key's budget window.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "max_budget_usd": {
          "description": "Max total cost in USD for this key's budget window.",
          "default": null,
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "allowed_models": {
          "description": "Models this key may use (empty = all).",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "blocked_models": {
          "description": "Models this key may not use.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "allowed_providers": {
          "description": "Providers this key may use (empty = all).",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "blocked_providers": {
          "description": "Providers this key may not use. Blocks override allows.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "allowed_tools": {
          "description": "Caller-supplied tool allowlist. `None` is unrestricted and an empty list denies every caller-supplied tool.",
          "default": null,
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        },
        "require_pii_redaction": {
          "description": "Named PII redaction rules that must be active before this key can dispatch upstream (empty = none required).",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "principal_selectors": {
          "description": "Inbound principal selectors allowed to present this key (empty = any). Each entry is a principal-selector object (virtual_key, team, project, user, role, claim).",
          "default": [],
          "type": "array",
          "items": true
        },
        "route_to_model": {
          "description": "Pin a model for requests on this key; the gateway overwrites the request body `model` before routing.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "compression_profile": {
          "description": "Route-local compression selector (`on`, `off`, or a named profile).",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "inject_tools": {
          "description": "Provider tool definitions injected into the request when this key authenticates, replacing any client-supplied tools.",
          "default": [],
          "type": "array",
          "items": true
        },
        "inject_mcp": {
          "description": "Federated MCP catalog reference injected for this key.",
          "default": null
        },
        "bypass_prompt_injection": {
          "description": "Skip the body-aware prompt-injection scan for this key. Default false.",
          "default": false,
          "type": "boolean"
        },
        "allow_content_capture": {
          "description": "Consent to the origin's opt-in redacted content capture for console inspection. Default false. A sample is retained only when the AI origin also sets `capture_content: true`, so both the operator and the key owner must opt in.",
          "default": false,
          "type": "boolean"
        },
        "project": {
          "description": "Project attribution.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "user": {
          "description": "User attribution.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "tags": {
          "description": "Free-form grouping tags.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "metadata": {
          "description": "Free-form string metadata for audit and usage attribution.",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "tenant": {
          "description": "Owning tenant.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "expires_at": {
          "description": "RFC 3339 expiry instant; past it the key is unusable.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "SeedCredentialConfig": {
      "description": "A seeded upstream credential.",
      "type": "object",
      "required": [
        "id"
      ],
      "properties": {
        "id": {
          "description": "Stable id.",
          "type": "string"
        },
        "name": {
          "description": "Operator-facing name.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "provider": {
          "description": "Provider this credential authenticates to.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "kind": {
          "description": "Credential kind (default `ai_provider`).",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "vault_ref": {
          "description": "A secret reference (`vault://`, `awssm://`, ...). Stored as vault-ref material and resolved at use.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "secret": {
          "description": "A plaintext secret to envelope-encrypt at boot (needs `crypto.master_key`).",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "tenant": {
          "description": "Owning tenant.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "KeyReadAuditConfig": {
      "description": "`key_management.read_audit:` block (WOR-2570): the read half of the key audit trail.\n\n`audit.key_path` records who *changed* a key or credential. This records who *resolved* one for use, which is the question a breach investigation actually asks and a different question from the first.\n\nCost-bounded on purpose, following the shape Vault's audit devices take for volume versus detail: the counter moves on every resolution, and the chained detail record fires at most once per credential per window.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Emit chained detail records for credential resolutions. The volume counter is unconditional and is not gated by this.",
          "default": false,
          "type": "boolean"
        },
        "detail_window_secs": {
          "description": "Minimum seconds between detail records for the same credential. Default 300. The first resolution in each window emits; the rest are counted and not recorded, so cost scales with credential count rather than with request rate.",
          "default": 300,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "hash_identifiers": {
          "description": "HMAC the credential id in the detail record, so a chain handed to an auditor does not enumerate which credentials exist while still letting an investigator confirm a specific id with `sbproxy audit hash`. Default true, matching Vault's audit-device posture of hashing sensitive string fields and passing timestamps, outcomes, and other non-identifying fields through in the clear.",
          "default": true,
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "BreakGlassConfig": {
      "description": "`key_management.break_glass:` block (WOR-2573): the pre-staged, time-boxed, quorum-approved emergency path into the key and credential admin API.\n\nEvery vault product surveyed converges on the same shape, and the shape is the point: a break-glass grant should be expensive to use quietly and cheap to review afterwards.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Turn the break-glass endpoints on. Off by default: an emergency path nobody configured is an emergency path nobody reviews.",
          "default": false,
          "type": "boolean"
        },
        "approvers": {
          "description": "Operators who may approve a grant, by admin username. A requester is never counted among their own approvers even when listed here.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "quorum": {
          "description": "How many distinct approvers a grant needs before it activates. Default 2. Must be at least 1 and no greater than the number of configured approvers, or config compile refuses the block.",
          "default": 2,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "max_ttl_secs": {
          "description": "Hard cap on a grant's requested TTL, in seconds. Default 3600. A request naming more is refused rather than silently clamped, so the requester finds out at request time instead of at expiry.",
          "default": 3600,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "review_window_secs": {
          "description": "How long after expiry a grant with no reviewer sign-off stays on the review queue, in seconds. Default 86400, the 24-hour post-access review window the surveyed products converge on. Grants past this are still listed and still flagged; the number drives the overdue marker, not deletion.",
          "default": 86400,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "L2CacheConfig": {
      "description": "Top-level shared-state / L2 cache backend configuration.\n\nTurns rate-limit buckets and response-cache entries into cluster-wide shared state so multiple proxy replicas coordinate against the same counters and cache pool. YAML key: `l2_cache_settings`.",
      "type": "object",
      "required": [
        "driver"
      ],
      "properties": {
        "driver": {
          "description": "Backend driver. Currently only `\"redis\"` is supported.",
          "type": "string"
        },
        "params": {
          "description": "Driver-specific parameters.",
          "default": {
            "dsn": "",
            "ca_file": null,
            "cert_file": null,
            "key_file": null
          },
          "allOf": [
            {
              "$ref": "#/definitions/L2CacheParams"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "L2CacheParams": {
      "description": "Driver-specific parameters for the [`L2CacheConfig`].\n\nKept separate from `L2CacheConfig` so future drivers can add fields (auth, pool size) without churning the parent struct.",
      "type": "object",
      "properties": {
        "dsn": {
          "description": "Redis connection DSN. Supports `redis://`, `rediss://`, credentials, bracketed IPv6 addresses, and a non-negative logical database.",
          "default": "",
          "type": "string"
        },
        "ca_file": {
          "description": "Optional path to PEM-encoded Redis trust anchors for a private CA.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "cert_file": {
          "description": "Optional path to a PEM-encoded Redis client certificate chain. Must be configured together with `key_file` and requires `rediss://`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "key_file": {
          "description": "Optional path to the PEM-encoded Redis client private key. Must be configured together with `cert_file` and requires `rediss://`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "AnomalyConfig": {
      "description": "Behavioral anomaly detection over the signals the proxy already collects.\n\nThe detector is comparative, not a rule set: it learns what a given agent class normally looks like and flags what does not fit. That makes it useful exactly where a signature list is not, and it also means it says nothing at all until it has a baseline, which [`Self::min_observations`] is the floor for.\n\n# What it cannot survive\n\nThe histogram is in memory and has no persistence option. A restart empties the window, and the detector is silent again until it has re-learned a baseline. That is a deliberate consequence of the rule that nothing here may require an external store: the alternative is a database the proxy cannot start without. Operators running short deployments, or restarting often, should expect the detector to spend a meaningful fraction of its life below `min_observations` and read `sbproxy_anomaly_detected_total` accordingly.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Master switch. When `false`, no histogram is built and no detector hook is installed.",
          "default": false,
          "type": "boolean"
        },
        "min_observations": {
          "description": "Observations a dimension needs before the detector will call anything an outlier.\n\nBelow this the histogram cannot tell \"rare\" from \"first time ever\", and a detector that flags every first sighting is a detector nobody reads.",
          "default": 50,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "outlier_frequency": {
          "description": "Relative frequency below which an observed value is an outlier. Defaults to `0.01`: a fingerprint has to have been under 1% of the window's traffic for its class before it is flagged.",
          "default": 0.01,
          "type": "number",
          "format": "double"
        },
        "rate_spike_multiplier": {
          "description": "Multiple of the per-IP mean at which today's request count for one IP is a rate spike. Defaults to `10.0`.",
          "default": 10.0,
          "type": "number",
          "format": "double"
        },
        "rate_spike_min_mean": {
          "description": "Mean per-IP rate below which the rate-spike check does not engage, so a single burst against an idle class is not a spike.",
          "default": 5.0,
          "type": "number",
          "format": "double"
        },
        "reputation": {
          "description": "What admission does with the reputation score. Both thresholds are unset by default, which leaves the score advisory.",
          "default": {},
          "allOf": [
            {
              "$ref": "#/definitions/AnomalyReputationConfig"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "AnomalyReputationConfig": {
      "description": "Admission thresholds on the reputation score (WOR-2666).\n\nThe score is published whether or not anything acts on it, and nothing acts on it until an operator names a number. That split is deliberate and it is the same one Cloudflare's threat score has: the gateway computes it always, and a rule decides what it means. An operator watches the gauge for a while, sees what their own traffic scores, and only then writes a floor.\n\n# Read this before setting one\n\nThe score is keyed on the agent class the resolver produced, and that class is a *claim* unless the resolver source was a verified one (`bot_auth`, `kya`, `rdns`, `tls_fingerprint`). Anyone can send GPTBot's `User-Agent`, be resolved into the `gptbot` class, and misbehave there, which moves the score the real GPTBot is then admitted against. The decision record carries the resolver source for exactly this reason, so a rule written after the fact can tell the two apart.\n\nThe class `unknown` is a single shared bucket for everything the resolver did not recognize. A floor that catches `unknown` catches most of the unclassified web with it.",
      "type": "object",
      "properties": {
        "deny_below": {
          "description": "Score below which admission refuses the request with a `403`.\n\nUnset by default. `0.0` to `1.0`, where 1.0 is a class that has produced no anomalies in the window.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "challenge_below": {
          "description": "Score below which admission answers `429` instead of proxying.\n\nUnset by default. Set above `deny_below` to get a two-step posture; `deny_below` wins when a score is under both.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        }
      },
      "additionalProperties": false
    },
    "CacheReserveConfig": {
      "description": "Top-level Cache Reserve configuration.\n\nCache Reserve is a long-tail cold tier sitting under the per-origin response cache. Items evicted from the hot cache are admitted into the reserve subject to a sample rate and size threshold; on a hot miss the proxy consults the reserve before going to origin and promotes the entry back into the hot tier on hit.\n\nBackend selection is open-ended via [`CacheReserveBackendConfig`] so the in-tree memory / filesystem / redis / object-storage backends can be extended without touching this schema.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Master switch. When `false`, the reserve is not built and the hot cache behaves exactly as it does without this block.",
          "default": false,
          "type": "boolean"
        },
        "backend": {
          "description": "Backend wiring. Required when `enabled == true`.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/CacheReserveBackendConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "sample_rate": {
          "description": "Fraction (0.0 to 1.0) of hot-cache writes that are mirrored to the reserve. Defaults to `0.1`. The reserve is meant for long-tail content; sampling controls reserve write amplification and (on object-store backends) per-request operation cost.",
          "default": 0.1,
          "type": "number",
          "format": "double"
        },
        "min_ttl": {
          "description": "Skip mirroring entries whose TTL is below this threshold. Items that won't outlive a typical hot-cache eviction window aren't worth carrying in the reserve. Defaults to 3600 seconds.",
          "default": 3600,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "max_size_bytes": {
          "description": "Skip oversize objects. Defaults to 1 MiB. Set to `0` to disable the upper bound (not recommended for object-store backends).",
          "default": 1048576,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "CacheReserveBackendConfig": {
      "description": "Backend selector for [`CacheReserveConfig`].\n\nTagged externally on `type`. The built-in variants are listed below; out-of-tree builds may register additional types via their own startup path (the in-tree pipeline ignores unknown types after logging a warning).",
      "oneOf": [
        {
          "description": "In-process map. For tests and ephemeral single-replica setups.",
          "type": "object",
          "required": [
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "memory"
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Filesystem-backed reserve. One file per key under `path`.",
          "type": "object",
          "required": [
            "path",
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "filesystem"
              ]
            },
            "path": {
              "description": "Root directory under which entries are written.",
              "type": "string"
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Redis-backed reserve.",
          "type": "object",
          "required": [
            "redis_url",
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "redis"
              ]
            },
            "redis_url": {
              "description": "Redis connection URL (`redis://host:port[/db]`).",
              "type": "string"
            },
            "key_prefix": {
              "description": "Optional key prefix override. Defaults to `\"sbproxy:reserve:\"`.",
              "default": null,
              "type": [
                "string",
                "null"
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Retired (WOR-2673): the AWS-SDK S3 backend with KMS envelope encryption. [`Self::ObjectStore`] with `backend: s3` replaces it and reaches the same buckets.\n\nThis variant still deserializes, and the config compiler still refuses it by name, on purpose. This enum carries `#[serde(other)]`, so deleting the variant outright would have made an existing `type: s3` block parse as \"a backend registered out of tree\": the config would load, the proxy would serve, the startup log would carry one `warn!`, and the cold tier would be gone. Every field is optional here because none of them is read for behavior; they exist so a real operator config parses far enough to reach a refusal that names its replacement.\n\nSee `docs/cache-reserve.md` for the field-by-field migration, including the one behavior that does not carry over: KMS-wrapped per-object data keys. The replacement seals locally with AES-256-GCM, or leaves sealing to the bucket's own SSE-KMS.",
          "type": "object",
          "required": [
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "s3"
              ]
            },
            "bucket": {
              "description": "Ignored. Was the source S3 bucket.",
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "region": {
              "description": "Ignored. Was the AWS region the bucket lives in.",
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "kms_key_id": {
              "description": "Ignored, and the field whose behavior does not carry over. Was the KMS key that wrapped each object's data key.",
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "prefix": {
              "description": "Ignored. Was the key prefix prepended to every object.",
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "replication_target_bucket": {
              "description": "Ignored. Was a diagnostics-only hint; cross-region replication was always configured at the bucket level.",
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "sse_kms_bucket_default": {
              "description": "Ignored. Was the switch between local envelope encryption and S3 bucket-default SSE-KMS.",
              "default": null,
              "type": [
                "boolean",
                "null"
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "WOR-2673: object storage. One variant covers S3, Google Cloud Storage, Azure Blob Storage, and a local directory, because they all reach the proxy through the same `object_store` trait. The field names are the `storage` action's, so an operator who has configured that already knows this.",
          "type": "object",
          "required": [
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "object_store"
              ]
            },
            "backend": {
              "description": "`s3`, `gcs`, `azure`, or `local`.",
              "default": "s3",
              "type": "string"
            },
            "bucket": {
              "description": "Bucket name, or the container name on Azure. Required for every backend but `local`.",
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "path": {
              "description": "Root directory for the `local` backend. Required there and refused elsewhere.",
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "region": {
              "description": "Region, for `s3`. Falls back to the provider's own environment discovery (`AWS_REGION`) when omitted.",
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "endpoint": {
              "description": "Endpoint override, for `s3`. Set this for MinIO, Cloudflare R2, Backblaze B2, or any other S3-compatible store.",
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "prefix": {
              "description": "Key prefix inside the bucket. Defaults to `sbproxy/reserve/`, so the reserve can share a bucket without colliding with anything else in it.",
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "encryption": {
              "description": "Optional at-rest sealing, applied before an entry leaves the process. Absent or `enabled: false` writes payloads as the cache produced them.",
              "default": null,
              "anyOf": [
                {
                  "$ref": "#/definitions/CacheReserveEncryptionConfig"
                },
                {
                  "type": "null"
                }
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Catch-all for backends registered out-of-tree. The in-tree pipeline ignores these with a warning; an out-of-tree startup hook intercepts the variant before the warning fires.",
          "type": "object",
          "required": [
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "other"
              ]
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "CacheReserveEncryptionConfig": {
      "description": "At-rest sealing for the object-storage cache reserve (WOR-2673).\n\nThe same reference syntax, the same rotation shape, and the same no-plaintext-fallback rule as [`ResponseCacheEncryptionConfig`]: a key that is missing, unresolvable, or shorter than 16 bytes aborts startup rather than being used verbatim or silently skipped.\n\nDerived under its own HKDF purpose, so pointing this and the response cache at one operator secret still yields two unrelated keys.\n\nThis is deliberately not a cloud KMS integration. A KMS call to unwrap a data key would put a network round trip on the read path of a tier whose purpose is to be cheaper than the origin, and would make a reachable KMS a hard requirement for reading the cache at all. Bucket-level SSE-KMS is configured on the bucket and composes with this setting rather than competing with it.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Master switch. Defaults to `false`.",
          "default": false,
          "type": "boolean"
        },
        "key": {
          "description": "Secret reference for the active key, used to seal new entries and to open entries sealed under it. Required when `enabled` is `true`.\n\nResolved like every other config secret: a provider URI (`secret://backend/name`, `vault://...`), a `file:/path` reference, or a whole-value `${ENV_VAR}`. The resolved value should be 32 random bytes (base64 or hex encoded), not a passphrase.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "previous_keys": {
          "description": "Retired keys, used only to open entries sealed before a rotation. Same reference syntax as [`Self::key`].\n\nTo rotate: move the current `key` into this list and name the new one as `key`. Entries reseal under the active key as they are rewritten; entries whose key leaves this list stop opening and are treated as misses.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "ResponseCacheStoreConfig": {
      "description": "Top-level selection of the response cache's backing store.\n\nThe response cache is process-wide: `CompiledPipeline` builds one store and every origin whose `response_cache.enabled` is true shares it. Origins do not collide because the cache key already includes workspace, hostname, method, path, canonical query, and the Vary fingerprint.\n\nYAML key: `proxy.response_cache_store`.",
      "type": "object",
      "properties": {
        "backend": {
          "description": "Which store holds cached responses. Defaults to the in-process map.",
          "default": {
            "type": "memory"
          },
          "allOf": [
            {
              "$ref": "#/definitions/ResponseCacheBackendConfig"
            }
          ]
        },
        "encryption": {
          "description": "Optional encryption of cached payloads at rest. Absent or `enabled: false` stores payloads as the backend receives them.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ResponseCacheEncryptionConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "ResponseCacheBackendConfig": {
      "description": "Backend selector for [`ResponseCacheStoreConfig`].\n\nTagged externally on `type`. This is a closed set: unlike `cache_reserve`, the response-cache store has no out-of-tree registration path, so an unrecognized `type` is a parse error rather than a silent fallback.",
      "oneOf": [
        {
          "description": "In-process map, capped by the largest per-origin `response_cache.max_size`. Per-replica, so a multi-replica deployment caches independently. The default.",
          "type": "object",
          "required": [
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "memory"
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "One file per entry under `path`, named by the SHA-256 of the cache key. Survives a restart and is shared by every process pointed at the same directory. Prefix purge is unavailable because keys are hashed into filenames, so `invalidate_on_mutation` is a no-op on this backend and entries fall out by TTL.",
          "type": "object",
          "required": [
            "path",
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "file"
              ]
            },
            "path": {
              "description": "Directory holding the cache files. Created at startup if it does not exist; startup fails if it cannot be created.",
              "type": "string"
            },
            "max_size_mb": {
              "description": "Ceiling on the total size of the directory, in megabytes. `0`, the default, means no ceiling. When set, each write walks the directory to measure it, so leave it at `0` unless the disk budget is real.",
              "default": 0,
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Memcached over the ASCII protocol. Shared across replicas. Stale-while-revalidate and prefix purge are both unavailable: memcached expires items server-side and offers no key scan.",
          "type": "object",
          "required": [
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "memcached"
              ]
            },
            "host": {
              "description": "Server hostname or IP.",
              "default": "127.0.0.1",
              "type": "string"
            },
            "port": {
              "description": "Server port.",
              "default": 11211,
              "type": "integer",
              "format": "uint16",
              "minimum": 0.0
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Redis, reusing the connection configured under `proxy.l2_cache`. Selecting this without an `l2_cache` block is a startup error.",
          "type": "object",
          "required": [
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "redis"
              ]
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "ResponseCacheEncryptionConfig": {
      "description": "At-rest encryption settings for [`ResponseCacheStoreConfig`].\n\nCached response headers and bodies are sealed with AES-256-GCM before they reach the backing store. Status, cache time, and TTL stay readable because the file and memcached backends need them to compute expiry; status is authenticated so it cannot be altered.\n\nThere is no plaintext fallback. A key that is missing, unresolvable, or shorter than 16 bytes aborts startup.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Master switch. Defaults to `false`.",
          "default": false,
          "type": "boolean"
        },
        "key": {
          "description": "Secret reference for the active key. Used to seal new entries and to open entries sealed under it.\n\nResolved through the same mechanism as every other config secret: a provider URI (`secret://backend/name`, `vault://...`) against a backend declared under `proxy.secrets.backends`, a `file:/path` reference, or a whole-value `${ENV_VAR}`. An unresolvable reference aborts startup rather than being used as key material verbatim. Required when `enabled` is `true`.\n\nThe resolved value should be 32 random bytes (base64 or hex encoded), not a human-chosen passphrase: the logged key fingerprint is a weak offline oracle against a short passphrase, but not against 256 bits of real entropy.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "previous_keys": {
          "description": "Retired keys, used only to open entries sealed before a rotation. Same reference syntax as [`Self::key`].\n\nTo rotate: move the current `key` into this list and name the new one as `key`. Entries reseal under the active key as they are rewritten. Removing a reference from this list retires its entries; they are evicted the next time they are read.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "per_origin_keys": {
          "description": "What happens when an origin that caches does not declare its own key under `origins.<host>.response_cache.encryption`.\n\nDefaults to [`PerOriginKeyMode::Inherit`], which is the backwards-compatible behavior and is safe on its own terms: the origin is bound into the associated data either way, so an entry sealed for one origin never opens as another even when both inherit this key. Set [`PerOriginKeyMode::Required`] when the deployment's threat model needs every tenant to hold key material nobody else holds.",
          "default": "inherit",
          "allOf": [
            {
              "$ref": "#/definitions/PerOriginKeyMode"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "PerOriginKeyMode": {
      "description": "How the response cache treats an origin that declares no key of its own.\n\nBoth modes bind the origin into the AEAD associated data, so cross-origin isolation is cryptographic in both. The difference is whether tenants share master key material.",
      "oneOf": [
        {
          "description": "An origin with no key of its own uses the store-wide key. The default, and what every config written before per-origin keys existed gets.\n\nCross-origin isolation still holds, because the origin id is authenticated in every envelope. What an operator does *not* get is key separation: one leaked store-wide key opens every tenant's entries.",
          "type": "string",
          "enum": [
            "inherit"
          ]
        },
        {
          "description": "Every origin with `response_cache.enabled: true` must declare its own `encryption.key`. Startup fails, naming each origin that does not, rather than quietly sealing that tenant under shared material.",
          "type": "string",
          "enum": [
            "required"
          ]
        }
      ]
    },
    "CompressionStateRuntimeConfig": {
      "description": "Process-owned settings for the embedded compression-state database.\n\nThis block controls where the process opens its durable Local backend. It is intentionally independent of route-level compression policy.",
      "type": "object",
      "properties": {
        "local_path": {
          "description": "Explicit absolute path to the redb database file.\n\nWhen omitted, startup selects the first suitable platform state directory. Validation checks only the string contract; filesystem availability is a startup concern.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "ConfigHistoryConfig": {
      "description": "Durable last-known-good config history (WOR-2456): every config this proxy applies is kept as a content-addressed entry on local disk, so a rollback can restore a prior revision without depending on git history or any other external system being reachable at the moment it is needed.\n\nDisabled by default. An example elsewhere may show `enabled: true`, but that is the example, not this build's shipping default; every other opt-in proxy-level block in this schema defaults the same way.\n\nOnce enabled, the block names process-owned local storage, which is why [`crate::config_merge::AUTHORITY_DENIED_PATHS`] carries `proxy.config_history` for the same reason it carries `proxy.compression_state`: the ring's directory is a fact about this machine, not something a fleet-wide authority document should be able to repoint.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Master switch. Disabled by default so an existing deployment does not start writing config revisions to local disk without an explicit opt-in.",
          "default": false,
          "type": "boolean"
        },
        "dir": {
          "description": "Directory the revision ring lives in. Defaults to `/var/lib/sbproxy/config-history`.",
          "default": "/var/lib/sbproxy/config-history",
          "type": "string"
        },
        "keep": {
          "description": "Number of applied entries the ring retains, beyond whichever entry the last-known-good pointer names (that entry is never evicted). Must be at least 1; see [`Self::validate`]. Defaults to 20.",
          "default": 20,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "keep_rejected": {
          "description": "Accepted, not yet wired. Nothing writes the ring's `rejected/` directory in this release, so this field has no observable effect today; a config that fails to apply is not recorded anywhere. Parsed and stored for forward compatibility: once the writer ships, it bounds how many rejected candidates the ring retains for operator inspection. Defaults to 10.",
          "default": 10,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "soak": {
          "description": "The soak window a newly applied revision must survive before it is promoted to last known good (WOR-2458). Defaults to a soak that is on; see the `soak` block's own documentation for why an opt-in block defaults its soak on.",
          "default": {
            "enabled": true,
            "window_secs": 120,
            "min_requests": 50,
            "max_error_rate_delta": 0.05,
            "require_no_degraded_subsystems": true,
            "require_upstream_health": true,
            "probe": null,
            "auto_revert": false
          },
          "allOf": [
            {
              "$ref": "#/definitions/ConfigSoakConfig"
            }
          ]
        },
        "boot": {
          "description": "What this node does when the config it was told to boot on does not work (WOR-2459). Defaults to today's behavior: exit.",
          "default": {
            "fallback": "off",
            "max_attempts": 3,
            "success_secs": 30
          },
          "allOf": [
            {
              "$ref": "#/definitions/ConfigBootConfig"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "ConfigSoakConfig": {
      "description": "The soak window a newly applied revision must survive before it is promoted to last known good (WOR-2458).\n\nCompiling is not evidence that a config works. A dead upstream URL, a rate limit of 10 that should have been 10000, an auth block that rejects the caller carrying most of the traffic, and a WAF rule that matches everything all compile cleanly. So a committed reload arms a window, four signals report into it, and only a window that closes on a passing verdict promotes the revision to last known good.\n\n# The verdict is three-way\n\nModeled on Argo Rollouts' analysis runs, which complete `Successful`, `Failed`, or `Inconclusive`, with `Inconclusive` pausing a rollout rather than promoting or aborting it. A node that took four requests overnight and 500'd one of them has a 25% error rate and no information: the request-outcome signal abstains below `min_requests` rather than reporting a failure. One abstaining signal never fails a soak, but a window where *every* signal abstained is `Inconclusive` and does not promote, because promoting on a soak that measured nothing is promote-on-apply wearing a timer.\n\n# Enabled by default once history is\n\n`proxy.config_history` is itself opt-in and disabled by default. A node that opted into the ring but not into the soak would record revisions whose `lkg` pointer never moves, which leaves the boot fallback (`proxy.config_history.boot`) with nothing to boot from. So this block defaults on inside a block that defaults off.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Master switch. On by default; see the type's own documentation for why an opt-in block defaults its soak on.",
          "default": true,
          "type": "boolean"
        },
        "window_secs": {
          "description": "How long a newly applied revision must survive before the window closes and a verdict is reached. Defaults to 120 seconds.\n\n`POST /admin/config/confirm` short-circuits it, which is the Junos `commit confirmed` ergonomic: a deployment pipeline that ran its own smoke test calls that instead of sleeping out the window.",
          "default": 120,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "min_requests": {
          "description": "Fewest requests the window must observe before the request-outcome signal reports anything but `abstain`. Defaults to 50.",
          "default": 50,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "max_error_rate_delta": {
          "description": "How much the error rate may rise, as a fraction between 0 and 1, against the rate measured when the window armed, before the request-outcome signal fails. Defaults to 0.05.",
          "default": 0.05,
          "type": "number",
          "format": "double"
        },
        "require_no_degraded_subsystems": {
          "description": "Whether a reload that published while any subsystem stayed on prior state fails its soak. On by default, and when it fails it fails immediately rather than waiting the window out: the evidence is already in hand and no traffic is needed to confirm it.",
          "default": true,
          "type": "boolean"
        },
        "require_upstream_health": {
          "description": "Whether an unhealthy upstream, an open circuit breaker, or an ejected outlier fails the soak. On by default. This is the signal that catches a config which repointed an origin at a dead address on a node with almost no traffic.",
          "default": true,
          "type": "boolean"
        },
        "probe": {
          "description": "An optional operator-declared probe. Absent by default: the other three signals need no configuration, and this one is for whatever the operator knows and the proxy does not.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ConfigSoakProbeConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "auto_revert": {
          "description": "Whether a failed soak re-applies the last known good revision on its own. **Off by default**, and the only key in this block that is (WOR-2461).\n\n# Why this one defaults off\n\nA node that undoes an operator's change without being asked is surprising in a way that costs trust, and the failure mode is asymmetric: a flapping upstream during a deploy window reverts a good config, the operator re-applies it, it reverts again, and the safety feature is now the incident. With it off the soak still runs and still promotes, so the operator gets a correct last-known-good pointer, a metric, an event, and an alert, with none of that risk. Calibrate the thresholds against real traffic with this off before arming it.\n\nJunos `commit confirmed` is the closest prior art and it is deliberately not what this is: there the operator opts in per commit and the rollback timer is armed for that one change. Here the opt-in is per node and standing, which is a bigger promise, so it is off until someone makes it.\n\n# It arms only for a diff an arc-swap can undo\n\nA `Restart` or `Breaking` diff (listener ports, admin block, cluster identity, an origin's action or auth type) cannot be undone by swapping the pipeline pointer back, and half-reverting would leave the process in a state neither config describes. Those get boot fallback and `POST /admin/config/rollback` instead. The stored `blast_radius` on the ring entry decides it.",
          "default": false,
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "ConfigSoakProbeConfig": {
      "description": "An operator-declared HTTP probe the soak window runs alongside its other three signals (WOR-2458).\n\n# This dials whatever URL you name, with no allowlist\n\nThe probe client is a plain HTTP client. It does **not** route through the egress guard that screens the proxy's other outbound dials, so `url` is a config-reachable fetch to any address this host can reach, loopback and link-local included. What keeps that bounded is that `proxy.config_history` sits on `AUTHORITY_DENIED_PATHS`: a config-authority document cannot set this key, so the only writer is an operator with write access to the node's own configuration file, who can already point an origin anywhere. Treat it the way you treat an origin URL, and prefer a loopback health endpoint on this node.\n\nDeliberately separate from `proxy.synthetic_probe`, which the soak also reads. The synthetic driver fires an in-process request through the compiled handler chain against a non-network origin, so a passing run proves the chain executes and proves nothing about whether any upstream is reachable. An operator who wants a real upstream exercised declares this instead, and both can be on at once.",
      "type": "object",
      "required": [
        "url"
      ],
      "properties": {
        "url": {
          "description": "URL to `GET` on each probe tick.",
          "type": "string"
        },
        "expect_status": {
          "description": "Status the response must carry. Defaults to 200.",
          "default": 200,
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "interval_secs": {
          "description": "Seconds between probe ticks. Defaults to 10.",
          "default": 10,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "timeout_ms": {
          "description": "Per-request timeout in milliseconds. Defaults to 2000. A probe that times out fails the soak; it does not abstain.",
          "default": 2000,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "ConfigBootConfig": {
      "description": "How a node boots when its own config file does not work (WOR-2459).\n\n# Why a boot counter\n\nAn entry that was good in October need not construct after an upgrade that tightened validation. Borrowed from systemd-boot's boot counting: `boot_attempts` on the entry being tried is incremented on disk *before* the attempt and cleared once the process has bound its listeners and served for `success_secs`. An entry that fails `max_attempts` times is retired as unbootable and the walk continues down the ring. The ring is finite, so the walk terminates.",
      "type": "object",
      "properties": {
        "fallback": {
          "description": "What to do when the configured document does not boot. Defaults to `off`, so today's behavior is unchanged unless asked for. `--config-fallback` and `SB_CONFIG_FALLBACK` override this, and deliberately win: a rescue boot must not depend on the file being right.",
          "default": "off",
          "allOf": [
            {
              "$ref": "#/definitions/BootFallbackMode"
            }
          ]
        },
        "max_attempts": {
          "description": "How many times one ring entry may be tried before it is retired as unbootable. Defaults to 3.",
          "default": 3,
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "success_secs": {
          "description": "How long a booted process must serve before its entry's boot counter is cleared. Defaults to 30 seconds.",
          "default": 30,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "BootFallbackMode": {
      "description": "What a node does when the config it was told to boot on does not work (WOR-2459).",
      "oneOf": [
        {
          "description": "Exit, the way every release before this one did. The default, so enabling the ring does not silently change how a broken config behaves.",
          "type": "string",
          "enum": [
            "off"
          ]
        },
        {
          "description": "Walk the ring for a revision that boots, starting from the entry the last-known-good pointer names.",
          "type": "string",
          "enum": [
            "last_known_good"
          ]
        }
      ]
    },
    "MessengerSettings": {
      "description": "Shape of the `proxy.messenger_settings` block, retained only so the compiler can refuse it with an explanation (WOR-2166).\n\nNo value of this type is ever turned into a running bus. `compile_config` rejects the config the moment the block is present, whatever the driver says, because this build has no runtime consumer for a message bus: no production code subscribes to a topic and none publishes on one. The driver string is read for the diagnostic and nothing else.",
      "type": "object",
      "required": [
        "driver"
      ],
      "properties": {
        "driver": {
          "description": "Backend driver name. Quoted back in the rejection diagnostic so an operator sees which bus they configured; no backend is constructed.",
          "type": "string"
        },
        "params": {
          "description": "Free-form string parameters the driver factory used to consume. Nothing reads them: the block is refused before any driver runs.",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "CorrelationIdConfig": {
      "description": "Correlation-ID propagation policy.\n\nThe proxy mints a per-request correlation identifier early in the request lifecycle. With the default policy:\n\n1. If the inbound request carries `header` (default `X-Request-Id`), its value is adopted as the request's correlation ID. This lets upstream callers (a frontend, an API client, another proxy) correlate their traces with ours. 2. Otherwise the proxy generates a 32-hex-character UUID v4 and uses that. 3. The chosen value is set on the upstream request (under the same header name) so the upstream sees the same correlation ID the proxy used in its logs / webhooks. 4. The chosen value is echoed back to the client on the response, unless `echo_response` is `false`.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Master switch. Default: `true`.",
          "default": true,
          "type": "boolean"
        },
        "header": {
          "description": "Header name to read on ingress, set on the upstream, and echo on the response. Default: `X-Request-Id`.",
          "default": "X-Request-Id",
          "type": "string"
        },
        "echo_response": {
          "description": "Whether to echo the correlation ID on the downstream response. Default: `true`.",
          "default": true,
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "MtlsListenerConfig": {
      "description": "mTLS client certificate verification on the HTTPS listener.\n\nWhen set, the proxy configures the OpenSSL `SslAcceptor` underneath Pingora's `add_tls_with_settings` to verify the client certificate against the configured CA bundle.\n\nWhat we expose to the upstream after a successful handshake: * `X-Client-Cert-Verified: 1` * `X-Client-Cert-Organization: <Subject's O field, when present>` * `X-Client-Cert-Serial: <hex serial>` * `X-Client-Cert-Fingerprint: <hex sha256 of the cert>`\n\nCN and SAN extraction is a follow-up because Pingora 0.8's `SslDigest` does not expose the parsed Subject CN directly. When `require: true`, requests without a valid client cert are rejected during the TLS handshake and never reach `request_filter`.",
      "type": "object",
      "required": [
        "client_ca_file"
      ],
      "properties": {
        "client_ca_file": {
          "description": "Path to a PEM-encoded CA bundle used to verify client certs.",
          "type": "string"
        },
        "require": {
          "description": "When `true` (default), the TLS handshake fails if the client does not present a certificate. When `false`, the handshake succeeds without a cert and `X-Client-Cert-Verified: 0` is set (so upstreams can choose whether to reject anonymous traffic).",
          "default": true,
          "type": "boolean"
        },
        "allowed_cn_patterns": {
          "description": "Optional allowlist of regex patterns the client certificate's Common Name must match. When non-empty, a certificate that passes CA-chain validation is still rejected during the handshake if its CN matches none of these patterns. Empty (the default) accepts any CN signed by the configured CA (WOR-1155).",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "SyntheticProbeConfig": {
      "description": "Configuration for the in-process synthetic-transaction probe.\n\nWhen enabled, a background task fires a request through the compiled pipeline against the configured `hostname` on a fixed cadence. The request never leaves the process: the synthetic origin is required to use a non-network action (typically `static`, `mock`, `echo`, or `noop`) so `/readyz` can verify the handler chain end to end without making the readiness check dependent on a real upstream.\n\nThe probe verdict is reported as a `synthetic_pipeline` component in the `/readyz` body and increments `sbproxy_synthetic_probe_failures_total{reason}` whenever the driver records a failure.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Master switch. Disabled by default so operators with strict request-cost budgets do not pay for a synthetic transaction they did not opt into.",
          "default": false,
          "type": "boolean"
        },
        "hostname": {
          "description": "Sentinel hostname routed to the synthetic origin. Defaults to `__synthetic.local` per the synthetic-probe convention; pick another value if it collides with an existing origin in your deployment.",
          "default": "__synthetic.local",
          "type": "string"
        },
        "path": {
          "description": "Path issued on the synthetic request. Defaults to `/readyz/synthetic`.",
          "default": "/readyz/synthetic",
          "type": "string"
        },
        "interval_secs": {
          "description": "Cadence between synthetic runs.",
          "default": 30,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "timeout_ms": {
          "description": "Per-run timeout budget. The driver records a `timeout` failure if a single synthetic round trip exceeds this.",
          "default": 1000,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "stale_after_secs": {
          "description": "Maximum age (in seconds) the cached probe outcome can have before the readiness probe reports `Unhealthy`. Set this to roughly 3x `interval_secs`. Defaults to `interval_secs * 3` when zero.",
          "default": 0,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "AgentRegistryConfig": {
      "description": "Agent registry: the signed catalog subscriber plus the owner-approval queue for agent self-registration.\n\nBoth halves keep their state in one embedded redb file at `store_path`. The catalog is refreshed from `feed_path`, verified against `key_directory_path`, which is itself verified against `bootstrap_keys`. A registry with no feed configured still runs: the approval queue is useful on its own, and the catalog then serves whatever the store last cached.",
      "type": "object",
      "required": [
        "store_path"
      ],
      "properties": {
        "enabled": {
          "description": "Master switch. Disabled by default, so a config that names the block without turning it on opens no store file.",
          "default": false,
          "type": "boolean"
        },
        "store_path": {
          "description": "Path to the embedded store file holding the catalog cache and the registration queue. Created owner-only if absent.",
          "type": "string"
        },
        "feed_path": {
          "description": "Path to the signed catalog feed. Absent means no refresh is possible and `POST /admin/agent-registry/refresh` says so.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "key_directory_path": {
          "description": "Path to the signed key directory that names the feed signing keys.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "bootstrap_keys": {
          "description": "Bootstrap public keys, keyed by the key id the directory's signature names, valued as base64 of the raw 32-byte Ed25519 public key.\n\nBootstrap keys vouch for the feed publisher's key directory, which in turn vouches for the per-period keys that sign individual feeds. Only public material appears here, so this block belongs in version control with the rest of the config.\n\nAn empty map means no key directory can be trusted and therefore no feed can be applied. The registry refuses rather than falling back to a key shipped in the binary, because a build carrying a known public key is a build where whoever holds the private half signs directories.",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "stale_grace_secs": {
          "description": "How far past its own `expires_at` a feed may still be applied. Zero, the default, means the publisher's expiry is honored exactly.",
          "default": 0,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "duplicate_window_secs": {
          "description": "How long an identical resubmission is treated as a retry of the pending one rather than a new registration. One hour by default.",
          "default": 3600,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "rotation_grace_secs": {
          "description": "How long a rotated-away client secret keeps authenticating. Thirty days by default, so a fleet can pick up a new secret without a synchronized restart.",
          "default": 2592000,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "NotificationsConfig": {
      "description": "Outbound webhook notifications.\n\nDistinct from `events:`, which is one collector for the SIEM feed. This is the customer-facing side: several destinations, each with its own event-type filter and its own signing key, managed at runtime through the admin API rather than by editing this file.",
      "type": "object",
      "required": [
        "store_path"
      ],
      "properties": {
        "enabled": {
          "description": "Master switch. Disabled by default, so a config that names the block without turning it on opens no store file.",
          "default": false,
          "type": "boolean"
        },
        "store_path": {
          "description": "Path to the embedded store file holding the subscriptions and the deadletter queue. Created owner-only.\n\nThe file holds live HMAC signing secrets, which unlike an inbound API key cannot be stored as a one-way hash: the notifier has to re-derive a signature on every delivery. Put it on the volume you already trust with the rest of your configuration.",
          "type": "string"
        },
        "queue_capacity": {
          "description": "Bound on the hand-off queue between the request path and the delivery worker. A full queue drops the incoming event and counts the drop rather than making a request wait on a customer's endpoint.",
          "default": 4096,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "ScriptingConfig": {
      "description": "Per-engine scripting sandbox limits, exposed under the `proxy.scripting:` block of sb.yml.\n\nBoth the Lua and the JavaScript sub-block are installed into their live engines at boot and refreshed on reload. CEL and WebAssembly manage their own budgets separately.",
      "type": "object",
      "properties": {
        "lua": {
          "description": "Lua sandbox limits. Always populated, even when the operator omitted the block, so callers never have to special-case `None`.",
          "default": {
            "sandbox": {
              "max_execution_ms": 100,
              "max_memory_mb": 8,
              "allow_patterns": true
            }
          },
          "allOf": [
            {
              "$ref": "#/definitions/LuaScriptingConfig"
            }
          ]
        },
        "javascript": {
          "description": "JavaScript sandbox limits. Always populated, even when the operator omitted the block, so callers never have to special-case `None`.",
          "default": {
            "sandbox": {
              "budget_ms": 100,
              "memory_mb": 16,
              "stack_kb": 1024
            }
          },
          "allOf": [
            {
              "$ref": "#/definitions/JsScriptingConfig"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "LuaScriptingConfig": {
      "description": "Lua scripting runtime configuration. Wraps the sandbox limits so future Lua-specific tunables (preloaded libraries, request-binding budgets, etc.) have a stable home.",
      "type": "object",
      "properties": {
        "sandbox": {
          "description": "Per-script execution limits.",
          "default": {
            "max_execution_ms": 100,
            "max_memory_mb": 8,
            "allow_patterns": true
          },
          "allOf": [
            {
              "$ref": "#/definitions/LuaSandboxConfig"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "LuaSandboxConfig": {
      "description": "Sandbox configuration applied to every Lua script invocation.\n\nThree knobs:\n\n* `max_execution_ms` is a wall-clock budget enforced through the Luau interrupt callback. Once exceeded, the script is aborted with an `Error::external` propagated back to the caller. * `max_memory_mb` caps the Lua VM's total allocator footprint. Allocations past the limit fail the script with `Error::MemoryError`, which is far cheaper than letting a runaway script OOM the proxy process. * `allow_patterns` gates the Lua pattern API (`string.find`, `string.match`, `string.gmatch`, `string.gsub`). The pattern engine has known pathological inputs that can lock a worker, and `max_execution_ms` cannot preempt one because the matcher runs inside the C string library where the interrupt never fires. So operators who do not need patterns can drop them entirely.\n\nThe on-the-wire field uses `max_memory_mb` (megabytes) because that is the unit operators reason about; the engine converts to bytes internally.",
      "type": "object",
      "properties": {
        "max_execution_ms": {
          "description": "Wall-clock execution budget per Lua invocation, in milliseconds. Default: 100 ms.",
          "default": 100,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "max_memory_mb": {
          "description": "Hard cap on the Lua VM's allocator footprint, in megabytes. Default: 8 MB.",
          "default": 8,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "allow_patterns": {
          "description": "Whether to expose the Lua pattern API (`string.find`, `string.match`, `string.gmatch`, `string.gsub`). Those four are the whole pattern-taking surface of the `string` table; the rest of it (`upper`, `len`, `sub`, `rep`, and the others) stays available either way. Default: `true` for back compatibility; flip to `false` to disable pattern matching.",
          "default": true,
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "JsScriptingConfig": {
      "description": "JavaScript engine config block (`proxy.scripting.javascript:`).\n\nThe boot and reload paths install this sandbox into the process-wide handle every `JsEngine::new` reads, so an operator override reaches the live QuickJS engines.",
      "type": "object",
      "properties": {
        "sandbox": {
          "description": "Sandbox limits: CPU time budget, heap memory cap, and native stack cap. See [`JsSandboxConfig`].",
          "default": {
            "budget_ms": 100,
            "memory_mb": 16,
            "stack_kb": 1024
          },
          "allOf": [
            {
              "$ref": "#/definitions/JsSandboxConfig"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "JsSandboxConfig": {
      "description": "JavaScript sandbox limits enforced on every script execution.\n\nThe `budget_ms` field is the CPU time budget for a single `execute` / `call_function` / `match_request` / `waf_match` call. QuickJS calls the engine's interrupt handler periodically during evaluation; when the elapsed wall-clock time exceeds `budget_ms` the interrupt handler returns `true`, which aborts the script with an uncatchable exception that surfaces in Rust as a structured timeout error.\n\nThe `memory_mb` and `stack_kb` fields are passed through to `Runtime::set_memory_limit` and `Runtime::set_max_stack_size` respectively. They guard against runaway allocations and deeply recursive scripts in the same way the CPU budget guards against `while (true) {}`.",
      "type": "object",
      "properties": {
        "budget_ms": {
          "description": "Wall-clock CPU budget per script execution. Defaults to 100 ms, which is comfortably above any reasonable transform / matcher script but well under the per-request timeout budget of a typical request.",
          "default": 100,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "memory_mb": {
          "description": "Maximum heap memory the QuickJS runtime is allowed to allocate for the lifetime of this engine instance. Defaults to 16 MB.",
          "default": 16,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "stack_kb": {
          "description": "Maximum native stack size for the QuickJS runtime, in kilobytes. Defaults to 1024 KB (1 MB).",
          "default": 1024,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "HttpClientTimeoutsConfig": {
      "description": "Tunable client-side timeouts for the proxy's outbound HTTP helpers.\n\nSeveral internal code paths build pooled `reqwest::Client` instances to call out to operator-controlled services: forward-auth services, callback / webhook receivers, mirror destinations, stale-while- revalidate refreshes against origin upstreams, and Web Bot Auth directory lookups. Each helper used to bake a `Duration::from_secs` literal into a `LazyLock`-built client, which meant operators had to fork the binary to extend a timeout for a slow auth service or shorten one for an aggressive deadline budget.\n\nAll fields default to the prior hardcoded values so existing configs see no behavior change. Operators only set a field here to nudge a specific timeout.\n\nExample:\n\n```yaml proxy: http_client_timeouts: forward_auth_client_secs: 60 callback_client_secs: 15 ```",
      "type": "object",
      "properties": {
        "forward_auth_client_secs": {
          "description": "Outer client-level timeout for the shared forward-auth `reqwest::Client`. The per-request timeout from each `forward_auth.timeout` field still applies on top. Defaults to 30s.",
          "default": 30,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "forward_auth_request_secs": {
          "description": "Per-request fallback timeout for a forward-auth subrequest when the auth provider's own `timeout` field is unset. Defaults to 5s.",
          "default": 5,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "bot_auth_directory_client_secs": {
          "description": "Client-level timeout for the Web Bot Auth directory client that fetches signed directories from agent operators. Defaults to 5s.",
          "default": 5,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "swr_client_secs": {
          "description": "Client-level timeout for the stale-while-revalidate refresh client that re-fetches expired cache entries in the background. Defaults to 30s to match the conservative outer ceiling the rest of the proxy uses for outbound HTTP.",
          "default": 30,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "callback_client_secs": {
          "description": "Client-level timeout for the callback / webhook client that fires audit-mode webhooks and other fire-and-forget POSTs. Defaults to 10s.",
          "default": 10,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "WebBotAuthConfig": {
      "description": "Web Bot Auth signing identity for the proxy. See the [`ProxyServerConfig::web_bot_auth`] field.\n\nThe proxy holds one Ed25519 keypair, identified by `key_id`. Its public half is published in the hosted signatures directory; its private seed signs outbound requests to upstreams that require Web Bot Auth. Treat `ed25519_seed_hex` as a secret (source it via an env interpolation rather than committing it).",
      "type": "object",
      "required": [
        "ed25519_seed_hex",
        "key_id"
      ],
      "properties": {
        "key_id": {
          "description": "Key id advertised as the JWK `kid` and the RFC 9421 `keyid`. Must be non-empty.",
          "type": "string"
        },
        "ed25519_seed_hex": {
          "description": "Ed25519 private seed as 64 hex characters (32 bytes). The public key is derived and published; the seed never leaves the proxy. Validated at config-compile time.",
          "type": "string"
        },
        "directory_url": {
          "description": "Absolute URL of this proxy's published key directory (`/.well-known/http-message-signatures-directory`). When set, an origin that opts into outbound Web Bot Auth signing stamps a `Signature-Agent` header with this value so the upstream verifier can fetch the key. Optional: omitting it still signs, just without the discovery pointer.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "AttestationConfig": {
      "description": "`proxy.attestation`: the proxy-wide consumption attestation block.\n\nOff unless [`Self::role`] says otherwise, and inert in every config that does not mention it. When a role is set, the queue, the ledger, and a complete [`AttestationBillableConfig`] all become required, because a role with any of them missing is a proxy that claims to meter and does not.",
      "type": "object",
      "properties": {
        "role": {
          "description": "Which halves of attestation this proxy performs. See [`AttestationRole`]. Individual origins may narrow or widen this through `origins.<host>.attestation.role`.",
          "default": "off",
          "allOf": [
            {
              "$ref": "#/definitions/AttestationRole"
            }
          ]
        },
        "failure_mode": {
          "description": "What happens when attestation itself cannot run: the queue is full, the ledger will not accept an append, the signing identity is unusable.\n\nDefaults to [`FailureMode::Degraded`], which departs from the `closed` default the rest of this config surface takes, and the departure is the point. Fail-closed is right for a control that enforces a security boundary, because a control that silently admits traffic when it breaks is worse than no control at all. Billing is not a security boundary. A full ledger disk taking the whole API down is a worse outcome than a provable hole in the record, and `degraded` is precisely the posture that leaves the hole detectable: the call proceeds, the guarantee is marked as not made, and the gap is countable so an operator can alert on it and reconcile afterwards. An operator who genuinely cannot serve unbilled traffic sets `closed` and means it.",
          "default": "degraded",
          "allOf": [
            {
              "$ref": "#/definitions/FailureMode"
            }
          ]
        },
        "enforcement_mode": {
          "description": "What happens when attestation *does* reach a verdict and that verdict is \"refuse\": a claim that exceeds an agreement's ceiling, for instance. [`EnforcementMode::Observe`] is the rollout posture, and it is a different question from [`Self::failure_mode`]: a control can reasonably observe while it is being tuned and still need to fail closed when its backend disappears.\n\nThe only verdict attestation reaches is a claim measured against an agreement's ceiling, and the claim half is not implemented in this build, so this key records a posture nothing acts on yet.",
          "default": "block",
          "allOf": [
            {
              "$ref": "#/definitions/EnforcementMode"
            }
          ]
        },
        "sign_with": {
          "description": "Which existing signing identity signs receipts, as the config path that declares it. The only accepted value today is [`ATTESTATION_SIGN_WITH_WEB_BOT_AUTH`], and that block must actually be configured. Required whenever the role writes receipts.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "queue": {
          "description": "Where unsettled claims wait. Required when the role is not [`AttestationRole::Off`].",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/AttestationQueueConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "ledger": {
          "description": "Where settled records are chained. Required when the role is not [`AttestationRole::Off`].",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/AttestationLedgerConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "billable": {
          "description": "What the operator charges for. Required, and required complete, when the role is not [`AttestationRole::Off`].",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/AttestationBillableConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "measured": {
          "description": "Units this proxy counted for itself. See [`AttestationMeasuredConfig`].\n\nListed first because it is the resolver that needs nothing from anybody, and a receipt is easier to argue about when an unarguable line is sitting next to the contested ones.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/AttestationMeasuredConfig"
          }
        },
        "route_weights": {
          "description": "Routes priced in this document. See [`AttestationRouteWeightConfig`].\n\nA sibling list rather than a variant of one `units:` block, and the same goes for [`Self::measured`] and [`Self::origin_headers`]. Each resolver has its own provenance and its own way of being wrong, so each declares itself in its own vocabulary and none of them can be mistaken for another when a receipt is read back. It also means the expression resolver arrives later as a fourth list rather than as a variant every existing entry has to be retrofitted into.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/AttestationRouteWeightConfig"
          }
        },
        "origin_headers": {
          "description": "Counts this proxy reads back from its upstreams. See [`AttestationOriginHeaderConfig`].",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/AttestationOriginHeaderConfig"
          }
        }
      },
      "additionalProperties": false
    },
    "AttestationRole": {
      "description": "What part this proxy plays in attesting to consumption.\n\nThe two halves answer the two halves of a billing dispute and are independently useful, which is why this is four values rather than a boolean. A claim is made before the call and says what it is going to cost; a receipt is written after it and says what it actually consumed. A gateway in front of somebody else's metered API wants [`Self::Claim`] alone. A proxy selling its own upstream wants [`Self::Receipt`] alone. An operator reselling metered capacity wants [`Self::Both`], because they have to answer to a buyer and a supplier at once.\n\nThis build implements the receipt half only. `compile_config` refuses [`Self::Claim`] and [`Self::Both`], at the proxy block and at a per-origin override alike, because nothing writes a claim before a call is served, nothing reads [`AttestationConfig::queue`], and no ceiling is computed for [`AttestationConfig::enforcement_mode`] to act on. Both variants stay in the vocabulary so the refusal can name what is missing instead of reporting an unknown value.",
      "oneOf": [
        {
          "description": "Attest to nothing. The default, and what every config that does not mention the block gets.",
          "type": "string",
          "enum": [
            "off"
          ]
        },
        {
          "description": "Assert what a call is going to cost, before it is served. Not implemented in this build: a config that names it is refused at compile.",
          "type": "string",
          "enum": [
            "claim"
          ]
        },
        {
          "description": "Record what a call actually consumed, after it is served.",
          "type": "string",
          "enum": [
            "receipt"
          ]
        },
        {
          "description": "Both halves. The posture for reselling metered capacity. Not implemented in this build either, because the claim half is not: a config that names it is refused at compile.",
          "type": "string",
          "enum": [
            "both"
          ]
        }
      ]
    },
    "EnforcementMode": {
      "description": "What a control does when it *does* reach a decision and that decision is \"refuse\".\n\nThis is the second of two axes, and keeping them apart is the point. [`FailureMode`] answers \"the control could not run, now what\". `EnforcementMode` answers \"the control ran, it matched, now what\". Those are different events and an operator needs both: a detector can reasonably be in `observe` while it is being tuned, and still need to fail closed when its backend disappears.\n\nThis type replaces the ad-hoc spellings of the same idea that grew per policy: the WAF's `test_mode: bool`, the prompt-injection `Tag` action, and similar. They all meant \"match, but do not block\". `observe` is spelled the same here as in [`FailureMode`] on purpose, so one word means one thing across the config surface.",
      "oneOf": [
        {
          "description": "Refuse the request when the control matches.",
          "type": "string",
          "enum": [
            "block"
          ]
        },
        {
          "description": "Admit the request but record the match. The rollout posture.",
          "type": "string",
          "enum": [
            "observe"
          ]
        }
      ]
    },
    "AttestationQueueConfig": {
      "description": "`proxy.attestation.queue`: where claims wait until they settle.\n\nA claim is written when the call starts and settled when it finishes, and the gap between those is where a crash loses money. The queue is that gap made durable.\n\nNothing writes this queue today. The claim half is not implemented in this build and [`AttestationRole::Claim`] is refused at config compile, so the block is validated and its path resolved, and no file or directory is created for it.",
      "type": "object",
      "required": [
        "path"
      ],
      "properties": {
        "path": {
          "description": "Filesystem path of the queue. Required: an attestation role with nowhere to hold unsettled claims silently drops them on restart, which is the failure the whole mechanism exists to prevent.",
          "type": "string"
        },
        "max_entries": {
          "description": "How many unsettled claims to hold before the configured [`AttestationConfig::failure_mode`] applies. Defaults to 100,000, which is roughly a minute of unsettled work at a thousand requests a second.",
          "default": 100000,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "AttestationLedgerConfig": {
      "description": "`proxy.attestation.ledger`: where settled records are chained.\n\nThe ledger answers the half of a billing dispute a signature cannot: \"I made calls you never credited\". Each record is hash-chained to the one before it, so a gap is visible rather than merely absent.",
      "type": "object",
      "required": [
        "path"
      ],
      "properties": {
        "path": {
          "description": "Filesystem path of the append-only ledger file.",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "AttestationBillableConfig": {
      "description": "`proxy.attestation.billable`: what the operator charges for.\n\nEvery field is `Option` so that an incomplete block is a config error this crate can describe rather than a serde error that names one missing field at a time. The operator owes an answer for all eight, and [`Self::missing_outcomes`] hands them the whole list in one message. Nothing is defaulted: an unstated billing rule still runs, it just runs as whatever the code happened to do, and nobody discovers what that was until a buyer asks.\n\n`cache_hit` is the case that proves the rule. A vendor selling compute can argue a cache hit cost them nothing; a vendor selling answers can argue the answer is what was bought and where it came from is their business. Both are positions real companies hold, so this surface holds neither.",
      "type": "object",
      "properties": {
        "delivered": {
          "description": "The response was served to the client in full.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/BillableRule"
            },
            {
              "type": "null"
            }
          ]
        },
        "client_disconnected": {
          "description": "The client went away before the response finished.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/BillableRule"
            },
            {
              "type": "null"
            }
          ]
        },
        "origin_4xx": {
          "description": "The origin rejected the request as the caller's fault.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/BillableRule"
            },
            {
              "type": "null"
            }
          ]
        },
        "origin_5xx": {
          "description": "The origin failed.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/BillableRule"
            },
            {
              "type": "null"
            }
          ]
        },
        "policy_blocked": {
          "description": "A policy refused the call before it reached the origin.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/BillableRule"
            },
            {
              "type": "null"
            }
          ]
        },
        "rate_limited": {
          "description": "A rate limit rejected the call.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/BillableRule"
            },
            {
              "type": "null"
            }
          ]
        },
        "cache_hit": {
          "description": "The response was served from cache without touching the origin.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/BillableRule"
            },
            {
              "type": "null"
            }
          ]
        },
        "retry": {
          "description": "One attempt of a call that was retried.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/BillableRule"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "BillableRule": {
      "description": "The billing answer for one outcome, in the configuration vocabulary.\n\nA deliberate mirror of `sbproxy_meter::Billable` rather than a re-export. The meter crate depends on no other crate in this workspace, which is what lets an operator metering a plain REST API compile it without the AI stack; deriving [`schemars::JsonSchema`] on its types would end that. So the wire vocabulary lives here, the billing vocabulary lives there, and `sbproxy-core` converts between them. The two are kept in step by `sbproxy_meter::BillableOutcome::ALL`: adding an outcome there stops the conversion compiling until this surface answers for it.",
      "oneOf": [
        {
          "description": "Bill every unit the call produced.",
          "type": "string",
          "enum": [
            "yes"
          ]
        },
        {
          "description": "Bill nothing. The call is still recorded, because a receipt that omits the free calls cannot be reconciled against a request log.",
          "type": "string",
          "enum": [
            "no"
          ]
        },
        {
          "description": "Bill the work the origin actually performed, even though the request was cut short.",
          "type": "string",
          "enum": [
            "partial"
          ]
        },
        {
          "description": "Fold this attempt into the invoice line its claim names, so a flaky origin costs the buyer once rather than once per attempt.",
          "type": "string",
          "enum": [
            "collapse"
          ]
        }
      ]
    },
    "AttestationMeasuredConfig": {
      "description": "`proxy.attestation.measured[]`: one unit the proxy counted itself.\n\nThe only unit source with nothing outside the process in it. The proxy saw the bytes and held the clock, so nobody else contributed to the number and there is no third party whose word has to be taken. That is why it is the resolver to reach for first: a route weight is only as honest as the document it was read from, and an origin header is only as honest as the party being paid for it, whereas this one is arithmetic over things that demonstrably happened.\n\nA partial unit is billed as a whole one. Twelve thousand and forty-three bytes against a kibibyte rule is twelve units, not eleven and a bit, because the operator delivered those bytes and there is no fraction of a kibibyte to hand back. See `sbproxy_meter::MeasuredRule`.",
      "type": "object",
      "required": [
        "name",
        "quantity"
      ],
      "properties": {
        "name": {
          "description": "Unit name that appears on the invoice line, for example `egress_kib`. Unique across every resolver: two units sharing a name on one receipt is an invoice line that cannot be read.",
          "type": "string"
        },
        "quantity": {
          "description": "The observed quantity this entry counts. See [`AttestationMeasuredQuantity`].",
          "allOf": [
            {
              "$ref": "#/definitions/AttestationMeasuredQuantity"
            }
          ]
        },
        "per": {
          "description": "How much of the raw quantity makes one unit.\n\nThis is the key that turns bytes into kibibytes: `1024` against [`AttestationMeasuredQuantity::BytesOut`] bills one unit per kibibyte written. A divisor rather than a multiplier because the raw quantity is always the smaller currency and the invoice line is always the larger one. Nobody sells thousandths of a request, but plenty of people sell kibibytes and compute-seconds, and writing `per: 1000` against `duration_ms` says \"a second\" in the units the proxy actually observed rather than asking the operator to express a rate as a fraction.\n\nDefaults to `1`, which bills one unit per observed item and is the only sensible reading of an entry that omits it. Zero is rejected at config compile: a divisor of zero has no answer to fall back on, and the request path is the wrong place to find that out.",
          "default": 1,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "AttestationMeasuredQuantity": {
      "description": "Which observed quantity a measured unit counts, in the configuration vocabulary.\n\nA deliberate mirror of `sbproxy_meter::MeasuredQuantity` rather than a re-export, for the same reason [`BillableRule`] mirrors `sbproxy_meter::Billable`. The meter crate depends on no other crate in this workspace, which is what lets an operator metering a plain REST API compile it without the gateway around it; deriving [`schemars::JsonSchema`] on its types would end that. So the config vocabulary lives here, the metering vocabulary lives there, and `sbproxy-core` converts between them. Both spell the variants the same way on the wire, so a config written against one reads the same as a receipt written by the other.",
      "oneOf": [
        {
          "description": "The request itself, which is always one. What a flat per-call charge is built from.",
          "type": "string",
          "enum": [
            "requests"
          ]
        },
        {
          "description": "Request bytes received from the client.",
          "type": "string",
          "enum": [
            "bytes_in"
          ]
        },
        {
          "description": "Response bytes written to the client. What was actually written, so a response that was cut short bills what crossed the wire.",
          "type": "string",
          "enum": [
            "bytes_out"
          ]
        },
        {
          "description": "Wall-clock milliseconds the request was in flight.",
          "type": "string",
          "enum": [
            "duration_ms"
          ]
        }
      ]
    },
    "AttestationRouteWeightConfig": {
      "description": "`proxy.attestation.route_weights[]`: one route the operator priced.\n\nThe simplest thing an operator can say about what a call costs, and the only one that needs nothing from anybody: the weight is written down, so the number is a pure function of the route and the document it was read from. That document is already signed, so naming its revision on the receipt is all it takes for a buyer to check the price themselves. See `sbproxy_meter::RouteWeightTable`.",
      "type": "object",
      "required": [
        "name",
        "path",
        "weight"
      ],
      "properties": {
        "name": {
          "description": "Unit name that appears on the invoice line, for example `search_call`. Repeating a name across entries is how one line gets different prices on different routes; see [`Self::path`].",
          "type": "string"
        },
        "method": {
          "description": "HTTP method this entry prices, or absent for any method. Matched case-insensitively.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "path": {
          "description": "The path this entry prices: either exact (`/v1/search`) or a prefix ending in `/*` (`/v1/search/*`), which covers everything below that segment and deliberately not the segment itself.\n\nWhen several entries share a [`Self::name`] and all match, the most specific wins: a named method beats an unnamed one, an exact path beats a prefix, and a longer prefix beats a shorter one. One name still bills one line.",
          "type": "string"
        },
        "weight": {
          "description": "What one matching call costs.\n\nZero is allowed and means the route is metered and free, which is not the same as having no entry for it. No entry means this line does not price the route at all, and the receipt then carries no unit rather than a zero.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "AttestationOriginHeaderConfig": {
      "description": "`proxy.attestation.origin_headers[]`: one count the upstream reports.\n\nThe only unit source that can be wrong without the proxy being wrong, because the party supplying the number is the party being paid for it. That is not a reason to refuse it: an API selling result rows has to bill result rows, and only the origin knows how many there were. What the proxy does instead is attest rather than vouch. The receipt records the header name and the value exactly as it arrived, so the claim on the invoice is \"the origin sent this\", which is a claim the proxy can actually stand behind.\n\nThere is deliberately no knob for what to do with a value that will not parse. Substituting a number the proxy counted would put the proxy's provenance on the origin's claim, and a receipt that cannot separate \"the origin lied\" from \"the proxy miscounted\" is worthless in the dispute it exists for. A value that does not parse bills zero and goes on the receipt verbatim. See `sbproxy_meter::OriginHeaderRule`.",
      "type": "object",
      "required": [
        "header",
        "name"
      ],
      "properties": {
        "name": {
          "description": "Unit name that appears on the invoice line, for example `result_row`. Unique across every resolver: two units sharing a name on one receipt is an invoice line that cannot be read.",
          "type": "string"
        },
        "header": {
          "description": "Response header the count is read from. Matched case-insensitively, and quoted back on the receipt in the spelling written here.\n\nA header rather than a body path. Reading a JSON body means buffering one, and what that costs a streaming response is its own decision rather than a side effect of a metering key.",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "ProxyTenantConfig": {
      "description": "WOR-1053: declared tenant. PR1 only carries the `id`; PR2+ adds per-tenant `credentials`, `policies`, `vault`, and `observability` blocks alongside the multi-tenant inheritance fan-out.\n\nA reserved tenant id of `__default__` is the synthetic default every origin resolves to when `origin.tenant_id` is absent. The operator never declares `__default__` explicitly; doing so fails config compile.",
      "type": "object",
      "required": [
        "id"
      ],
      "properties": {
        "id": {
          "description": "Operator-supplied stable identifier. Referenced from `origin.tenant_id` and stamped on every request the origin serves. Length capped to 256 ASCII characters at compile.",
          "type": "string"
        },
        "credentials": {
          "description": "Tenant-scoped credentials block. Inherits proxy-scope credentials of the same name unless overridden here.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/CredentialBlock"
          }
        },
        "observability": {
          "description": "Tenant-scoped observability block. Today the only nested surface is `log.redact.pii`, which composes against the proxy-scope `observability.log.redact.pii` block (see [`ObservabilityPiiConfig`]). Origin-scope and proxy-scope values compose in the same shape; resolution at emit time walks origin -> tenant -> proxy with most-specific-wins on `enabled` and a rules set that inherits + extends + disables. Absent leaves the tenant inheriting whatever proxy scope declared (or no PII pass at all).",
          "anyOf": [
            {
              "$ref": "#/definitions/TenantObservabilityConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "CredentialBlock": {
      "description": "Canonical credentials block. Sits under `proxy.credentials`, `tenants[].credentials`, or `origins[].credentials`. A request resolves matching credentials by walking origin -> tenant -> proxy scopes; the first scope that produces a match for the request's principal serves the credential.\n\nThe credential carries:\n\n* Which provider produces it (`type`, `provider`). * Where the secret material lives (`key`, a provider-specific secret reference such as `vault://`, `awssm://`, `gcpsm://`, `azurekv://`, `k8ssecret://`, `secretfile://`, or `secret://`, or a legacy `${ENV}` / `file:` reference). * Which inbound principals can use it (`principals` selectors). * Per-credential attribution metadata (`attrs`). * Allow / deny model lists that stack on top of the origin-level allowlist (most-restrictive wins). * Per-credential sub-policies (rate limit, PII redaction, ...).",
      "type": "object",
      "required": [
        "name",
        "type"
      ],
      "properties": {
        "name": {
          "description": "Operator-supplied stable name. Unique within the declaring scope. Used to identify the credential in metrics and logs.",
          "type": "string"
        },
        "type": {
          "description": "Credential kind. Closed enum (`ai_provider`, `bearer`, `api_key`, `jwt`, `basic`, `oidc_client`, `outbound_token_exchange`, `outbound_client_credentials`).",
          "type": "string"
        },
        "provider": {
          "description": "Provider name for `type: ai_provider` credentials. Matches an entry in the origin's `providers:` list. Ignored for non-AI credential kinds.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "key": {
          "description": "Secret material reference. Provider-specific schemes include `vault://`, `awssm://`, `gcpsm://`, `azurekv://`, `k8ssecret://`, `secretfile://`, and `secret://`; legacy `${ENV}` and `file:` forms also remain valid. The removed `secret:<name>` form is rejected. The resolver dispatches at runtime; the config parser carries the value as a string.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "principals": {
          "description": "Principal selectors that match this credential to inbound principals. An empty list matches every principal; downstream resolution then uses the first credential whose selectors match the request.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/PrincipalSelector"
          }
        },
        "attrs": {
          "description": "Attribution attributes lowered onto matched principals. Individual compatibility-only fields are documented on their definitions.",
          "default": {
            "project": null,
            "user": null,
            "team": null,
            "cost_center": null,
            "tags": [],
            "metadata": {},
            "budget": null
          },
          "allOf": [
            {
              "$ref": "#/definitions/CredentialAttrs"
            }
          ]
        },
        "models": {
          "description": "Model allow / deny lists. Stacks on top of the origin-level allowlist (most-restrictive wins).",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/CredentialModels"
            },
            {
              "type": "null"
            }
          ]
        },
        "policies": {
          "description": "Sub-policies that only fire when this credential matches.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/CredentialPolicy"
          }
        },
        "route_to_model": {
          "description": "Pin the upstream `model` field. When set, the AI dispatch rewrites the request's `model` before sending it to the provider; the client-supplied value is ignored. Mirrors the `route_to_model` field on the underlying `VirtualKeyConfig`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "compression_profile": {
          "description": "Select `on`, `off`, or a named route-local compression profile.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "inject_tools": {
          "description": "Replace the request's `tools` array with these entries. The shape is provider-native (`function` objects today); the AI dispatch forwards the array verbatim. Empty == no injection. Mirrors `inject_tools` on the underlying `VirtualKeyConfig`.",
          "default": [],
          "type": "array",
          "items": true
        },
        "allowed_tools": {
          "description": "Caller-supplied tool allowlist for this credential. Omitted is unrestricted and an explicit empty list denies every caller tool.",
          "default": null,
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        },
        "inject_mcp": {
          "description": "WOR-1646: inject a federated MCP gateway's live catalog as this credential's tool surface. Raw passthrough of the `InjectMcpRef` shape (`{ref, format, filter}`) on the underlying `VirtualKeyConfig`; resolved at request time."
        }
      },
      "additionalProperties": false
    },
    "PrincipalSelector": {
      "description": "Selector matching an inbound principal to a credential. At least one field must be set; an entirely empty selector is rejected at compile.",
      "type": "object",
      "properties": {
        "virtual_key": {
          "description": "Glob matching `Principal.virtual_key.name`. `*` matches any virtual key. `vk_frontend_*` matches every key with that prefix.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "team": {
          "description": "Match `Principal.attrs.team`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "project": {
          "description": "Match `Principal.attrs.project`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "user": {
          "description": "Match `Principal.attrs.user`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "role": {
          "description": "Match any of the principal's `attrs.roles`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "claim": {
          "description": "Match an exact key=value entry on `Principal.attrs.claims`. Serialized as a flat map for readability.",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "CredentialAttrs": {
      "description": "Attribution attributes copied onto matched principals.",
      "type": "object",
      "properties": {
        "project": {
          "description": "Project the credential's spend rolls up to.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "user": {
          "description": "User the credential is owned by (independent of who the inbound request authenticates as).",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "team": {
          "description": "Team the credential's spend rolls up to. Copied onto `Principal.attrs.team`, which feeds the access log's `team` column, the attribution tag set behind the `team` metric label, and the usage rollup dimension. This is the write end of the same dimension `principals[].team` selects on.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "cost_center": {
          "description": "Cost center. Lifted onto `Principal.attrs.metadata` under the `cost_center` key for back-compat with the existing access-log surface.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "tags": {
          "description": "Operator-supplied tags. Each tag becomes a separate attribution row.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "metadata": {
          "description": "Free-form metadata copied verbatim onto `Principal.attrs.metadata`.",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "budget": {
          "description": "Per-credential budget. Sits inside `attrs:` because budget is an attribution-side concern; the budget enforcer reads the matched principal's attrs to apply caps.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/CredentialBudget"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "CredentialBudget": {
      "description": "Per-credential budget. The token and cost caps are lowered into the live credential registry. `reset` remains a reserved, compatibility-only field and does not install a reset schedule.",
      "type": "object",
      "properties": {
        "max_tokens": {
          "description": "Maximum input + output tokens enforced for this credential.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "max_cost_usd": {
          "description": "Maximum USD spend enforced for this credential.",
          "default": null,
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "reset": {
          "description": "Reserved reset-window hint. It is accepted but not parsed or enforced by the OSS runtime.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "CredentialModels": {
      "description": "Model allow / deny lists scoped to this credential. Stacks on top of the origin-level allowlist. Most-restrictive wins.",
      "type": "object",
      "properties": {
        "allow": {
          "description": "Models this credential is allowed to use. Empty allows all origin-allowed models.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "deny": {
          "description": "Models this credential is explicitly denied. Stacks on top of `allow`: a model that is in `allow` but also in `deny` is denied.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "CredentialPolicy": {
      "description": "Sub-policy attached to a credential. Closed enum; out-of-tree policies plug in through the existing plugin registry rather than widening this enum.",
      "oneOf": [
        {
          "description": "Per-credential rate limit. Stacks on top of the origin-level rate limit (most-restrictive wins).",
          "type": "object",
          "required": [
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "rate_limit"
              ]
            },
            "rpm": {
              "description": "Requests per minute cap.",
              "default": null,
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0.0
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Require PII redaction for the named rule set on every request served by this credential. The names match `sbproxy_security::pii::default_rules`.",
          "type": "object",
          "required": [
            "rules",
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "require_pii_redaction"
              ]
            },
            "rules": {
              "description": "Rule names that MUST run on every request.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "TenantObservabilityConfig": {
      "description": "Tenant-scope observability sub-tree. The `log.redact` block (PII rules, patterns, and field denylist), `log.sinks` (tenant-scoped fan-out, filtered by `Principal.tenant_id`), `log.custom_fields` (tenant-scoped access-log fields that override proxy-scope fields of the same name), and `cardinality` (per-tenant metric label budget) are all consumed at runtime.",
      "type": "object",
      "properties": {
        "log": {
          "description": "Tenant-scoped log block. See [`TenantObservabilityLogConfig`].",
          "default": {
            "redact": {},
            "sinks": [],
            "custom_fields": []
          },
          "allOf": [
            {
              "$ref": "#/definitions/TenantObservabilityLogConfig"
            }
          ]
        },
        "cardinality": {
          "description": "WOR-1067: per-tenant cardinality budget. Caps the unique label value count across `sbproxy_requests_total` and friends for just this tenant so a noisy tenant cannot demote labels for every other tenant. Omitting the block leaves this tenant on the proxy-wide budget (today's behavior).",
          "anyOf": [
            {
              "$ref": "#/definitions/TenantCardinalityConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "TenantObservabilityLogConfig": {
      "description": "Tenant-scope `log:` sub-block. Mirrors the proxy-scope `ObservabilityLogConfig`; today exposes the redaction leaf plus the tenant-scoped sinks fan-out (WOR-1045 PR2). The dispatcher routes every record whose resolved `Principal.tenant_id` matches this tenant into each declared sink; cross-tenant records never reach here.",
      "type": "object",
      "properties": {
        "decision_audit": {
          "description": "Scoped `decision_audit:` additions, composed per event label against the wider scopes rather than replacing them.\n\nThe same shape as the proxy-scope block. Composition is per key, matching `custom_fields:`: a scope naming one event must not silence the events a wider scope turned on, because that would make enabling one tenant's routing audit quietly disable its cache audit. Precedence for a given event is origin, then tenant, then proxy, and an event no scope names is off.\n\n`enabled:` composes the same way: the most specific scope that sets it wins for the events nobody names explicitly.",
          "anyOf": [
            {
              "$ref": "#/definitions/DecisionAuditConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "redact": {
          "description": "Tenant-scope `redact:` sub-block. See [`TenantObservabilityRedactConfig`].",
          "default": {},
          "allOf": [
            {
              "$ref": "#/definitions/TenantObservabilityRedactConfig"
            }
          ]
        },
        "sinks": {
          "description": "Tenant-scoped log sinks. Each sink's `name` is unique within this tenant; the same name may also appear at proxy scope (they are different sinks). Sinks at this scope default to the `external` redaction profile because the downstream backend is usually outside the operator's trust boundary.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/ObservabilitySinkConfig"
          }
        },
        "custom_fields": {
          "description": "Tenant-scoped custom access-log fields. Same shape as the proxy-scope `custom_fields:`. A field defined here overrides a proxy-scope field with the same `name` for requests resolved to this tenant; an origin-scope field overrides both.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/CustomLogFieldConfig"
          }
        }
      },
      "additionalProperties": false
    },
    "TenantObservabilityRedactConfig": {
      "description": "Tenant-scope `redact:` sub-block. Today only `pii:` is honored; the field-key and pattern overrides remain proxy-scope only because they touch the rendered JSON, which is tenant-agnostic in the emitter.",
      "type": "object",
      "properties": {
        "fields": {
          "description": "WOR-1042: tenant-scope additions to the field-key denylist. Additive only; a tenant CANNOT disable a proxy-level field denylist entry because the security baseline always applies.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "patterns": {
          "description": "WOR-1042: tenant-scope additions to the regex pattern set. Additive on top of the proxy-scope patterns. Use `disable:` (below) to opt out of a more-general proxy-scope pattern by name (e.g. a healthcare tenant disabling a `phone_us` mask).",
          "type": "array",
          "items": {
            "$ref": "#/definitions/ObservabilityRedactPattern"
          }
        },
        "disable": {
          "description": "WOR-1042: names of proxy-scope `patterns:` entries to opt out of at this tenant. Targets only the operator-supplied regex pass; the built-in field-key denylist is never disable-able.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "pii": {
          "description": "Tenant-scope override for the proxy-scope PII pass. Resolution rules: the tenant inherits the proxy-scope `enabled` flag and the proxy-scope rule set, then ADDS its own `rules:` entries and SUBTRACTS its own `disable:` entries. An explicit `enabled: false` opts the tenant out even when proxy-scope enables PII. See [`ObservabilityPiiConfig`].",
          "anyOf": [
            {
              "$ref": "#/definitions/ObservabilityPiiConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "TenantCardinalityConfig": {
      "description": "WOR-1067: per-tenant cardinality budget. The runtime installs one dedicated label-value tracker per declared tenant; overflows on tenant B do not touch tenant A's accepted-value set. The `__default__` tenant continues to use the proxy-wide `CardinalityLimiter` (in `sbproxy-observe`) so single-tenant deployments stay bit-for-bit identical to pre-WOR-1067 behavior.",
      "type": "object",
      "properties": {
        "max_series": {
          "description": "Maximum unique label values per metric, per label name, for requests resolving to this tenant. When omitted the observability stack falls back to its per-tenant default cap ([`crate::types::TENANT_CARDINALITY_DEFAULT_MAX_SERIES`]) so an operator can declare an `observability.cardinality:` block with no fields to opt this tenant in to the default per-tenant budget without having to pick a number.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "FederationConfig": {
      "description": "`proxy.federation`: the signing identity served by the normal proxy listener at `/.well-known/openid-federation`.",
      "type": "object",
      "required": [
        "entity_id",
        "published_jwks",
        "signing_key"
      ],
      "properties": {
        "enabled": {
          "description": "Whether the process mounts the well-known entity-statement route.",
          "default": false,
          "type": "boolean"
        },
        "entity_id": {
          "description": "Externally visible HTTPS entity identifier used for `iss` and `sub`.",
          "type": "string"
        },
        "signing_key": {
          "description": "Private signing-key location and JWS header selection.",
          "allOf": [
            {
              "$ref": "#/definitions/FederationSigningKeyConfig"
            }
          ]
        },
        "published_jwks": {
          "description": "Public JWKS embedded in the signed entity statement."
        },
        "lifetime_secs": {
          "description": "Lifetime of each generated entity statement.",
          "default": 3600,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "refresh_margin_secs": {
          "description": "Time before expiry at which the cached statement is regenerated.",
          "default": 300,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "authority_hints": {
          "description": "Entity URLs of this entity's superiors, published in the entity statement as `authority_hints`.\n\nOpenID Federation 1.0 s3 makes this required for every entity that is not itself a Trust Anchor, and a peer's resolver walks it: `compose_trust_chain` reads `authority_hints` from the leaf it fetched and returns no chain when the array is empty. A statement published without it is anchor-shaped, so no peer can chain this proxy to anything.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "peer_trust": {
          "description": "Inbound peer verification. Absent leaves the proxy publishing its own identity and verifying nobody.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/FederationPeerTrustConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "FederationSigningKeyConfig": {
      "description": "Private-key reference and protected JWS header fields for federation entity-statement signing.",
      "type": "object",
      "required": [
        "algorithm",
        "kid",
        "pem_file"
      ],
      "properties": {
        "pem_file": {
          "description": "PEM file read only when a runtime pipeline is constructed.",
          "type": "string"
        },
        "algorithm": {
          "description": "Asymmetric JWS algorithm name, such as `ES256`.",
          "type": "string"
        },
        "kid": {
          "description": "Key identifier stamped into the protected JWS header.",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "FederationPeerTrustConfig": {
      "description": "`proxy.federation.peer_trust`: verify the entity a request claims to come from against pinned trust anchors, on the request path.\n\nA peer names itself in a header. The proxy fetches that entity's self-signed configuration, walks its `authority_hints` up to one of the anchors below through the governed `egress.federation` client, validates every signature and linkage in the chain, checks any required trust marks, and either stamps the verified entity id onto the request or refuses it. Every step publishes the decision events and metrics `docs/federation.md` documents.",
      "type": "object",
      "required": [
        "trust_anchors"
      ],
      "properties": {
        "required": {
          "description": "Refuse a request that names no peer, or whose peer does not resolve to an anchor.\n\n`false` still refuses a request whose named peer fails to resolve: an unverifiable claim is worse than no claim. What it permits is a request that makes no claim at all, which is the shape for a proxy that federates with some callers and serves ordinary traffic from the rest.",
          "default": false,
          "type": "boolean"
        },
        "header": {
          "description": "Request header the peer names itself in.",
          "default": "x-federation-entity-id",
          "type": "string"
        },
        "trust_anchors": {
          "description": "Pinned trust anchors. At least one is required.",
          "type": "array",
          "items": {
            "$ref": "#/definitions/FederationTrustAnchorConfig"
          }
        },
        "required_trust_marks": {
          "description": "Trust-mark identifiers a verified peer must additionally carry, each signed by an anchor above and published in the peer's own entity configuration.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "max_chain_depth": {
          "description": "Maximum statements in an accepted chain.",
          "default": 5,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "cache_ttl_secs": {
          "description": "How long a resolved peer decision is reused before the chain is walked again. A chain walk is several outbound HTTPS fetches, so re-running it per request would put a peer's availability on this proxy's request path.",
          "default": 600,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "max_chain_fetches": {
          "description": "Total outbound fetches one chain walk may spend.\n\nThis is the bound that matters, and it is not `max_chain_depth`. `authority_hints` is an array: one entity naming five thousand superiors costs five thousand fetches at depth 1, and a depth cap never fires. A well-formed chain spends about two fetches per level, so the default covers a four-level federation that has to try a second superior at one level.",
          "default": 16,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "max_chain_bytes": {
          "description": "Total bytes one chain walk may read across every fetch.\n\nEach fetch is separately capped at 1 MiB by the fetcher; this bounds the sum, so a peer serving a maximum-size document at every hop cannot make one request hold the product of the two.",
          "default": 2097152,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "max_chain_duration_ms": {
          "description": "Wall-clock budget for one chain walk, in milliseconds.\n\nEvery fetch has its own connect and read timeout. Without an aggregate deadline, a peer that answers just inside the per-fetch timeout at every hop holds the request open for the product of the two. The walk stops here whatever it has found.",
          "default": 5000,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "max_authority_hints": {
          "description": "Most `authority_hints` a single entity configuration may publish before the walk refuses the document.\n\nRefused rather than truncated: silently ignoring an operator's superiors turns a configuration error into an unexplained refusal further down.",
          "default": 8,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "walks_per_minute": {
          "description": "Chain walks one source address may start per minute.\n\nThe decision cache is keyed on the peer address and the claimed entity id, so a caller that rotates the entity id it claims misses the cache every time. This is what stops that from becoming one walk per request.",
          "default": 30,
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "FederationTrustAnchorConfig": {
      "description": "One pinned OpenID Federation trust anchor.",
      "type": "object",
      "required": [
        "entity_id",
        "jwks"
      ],
      "properties": {
        "entity_id": {
          "description": "The anchor's entity URL.",
          "type": "string"
        },
        "jwks": {
          "description": "The anchor's published JWKS, as the `{\"keys\": [...]}` object. This is the pin: every chain is verified against these keys, so they come from the operator rather than from the network."
        }
      },
      "additionalProperties": false
    },
    "PaymentsConfig": {
      "description": "`proxy.payments`: durable settlement for paid requests.\n\nAbsent means the proxy keeps its existing non-settlement ledger behaviour exactly. Present means a paid request reaches the origin only after a durable intent has committed `Succeeded`.",
      "type": "object",
      "required": [
        "challenge_binding_key",
        "state_path"
      ],
      "properties": {
        "state_path": {
          "description": "Absolute path to the SQLite database that owns intents, attempts, proofs, and receipts. Authoritative for this release: a request is authorized because a row in this file says so.",
          "type": "string"
        },
        "challenge_binding_key": {
          "description": "Secret reference to the key that binds a challenge to its issuing proxy. Never an inline key.",
          "type": "string"
        },
        "authorization_timeout_ms": {
          "description": "Total budget for the one synchronous provider interaction a paid request is allowed, in milliseconds. Capped at [`MAX_AUTHORIZATION_TIMEOUT_MS`].",
          "default": 2000,
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "max_body_bytes": {
          "description": "Largest request body the payment path buffers, in bytes. Bounded by [`MAX_PAYMENT_BODY_BYTES`]; a larger body is answered 413 before any challenge or provider work.",
          "default": 1048576,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "failure_mode": {
          "description": "What happens to a payable request when settlement infrastructure cannot answer: the durable store errors, a challenge cannot be prepared, or the signer refuses.\n\nThis posture governs infrastructure failures only. A payment refusal is never subject to it: a rejected, expired, replayed, or unsettled payment always keeps the request away from the origin, whatever this is set to. Defaults to `closed`, which refuses the request, because settlement enforces a revenue boundary and a control that silently admits traffic when it breaks is worse than no control.",
          "default": "closed",
          "allOf": [
            {
              "$ref": "#/definitions/FailureMode"
            }
          ]
        },
        "recovery_encryption": {
          "description": "Encryption for the write-ahead recovery envelopes that make a crashed Stripe dispatch recoverable. Required whenever the Stripe settlement rail is enabled.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/RecoveryEncryptionConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "worker": {
          "description": "Recovery-only background worker. It never performs the settlement that authorizes a request.",
          "default": {
            "reconcile_interval_ms": 1000,
            "max_reconcile_batch": 32,
            "shutdown_timeout_ms": 5000
          },
          "allOf": [
            {
              "$ref": "#/definitions/PaymentsWorkerConfig"
            }
          ]
        },
        "protocols": {
          "description": "Wire protocols offered on top of the rails.",
          "default": {
            "payment_auth": null
          },
          "allOf": [
            {
              "$ref": "#/definitions/PaymentProtocolsConfig"
            }
          ]
        },
        "rails": {
          "description": "Settlement rails and their provider credentials.",
          "default": {
            "x402": null,
            "stripe": null,
            "lightning_cln": null,
            "lightning_lnd": null,
            "lightning_backend": null
          },
          "allOf": [
            {
              "$ref": "#/definitions/PaymentRailsConfig"
            }
          ]
        },
        "usage_reporters": {
          "description": "Usage reporters. A reporter accounts for consumption and can never authorize a request or produce a settlement receipt.",
          "default": {
            "stripe_meter": null
          },
          "allOf": [
            {
              "$ref": "#/definitions/UsageReportersConfig"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "RecoveryEncryptionConfig": {
      "description": "AES-256-GCM material for the Stripe write-ahead recovery envelope.\n\nThe envelope holds the exact bytes of a Stripe create request so a process that crashed between stamping dispatch and recording the response can replay the byte-equivalent POST under the same idempotency key and recover the original response. It never holds the API key or any authorization header.",
      "type": "object",
      "required": [
        "key",
        "key_id"
      ],
      "properties": {
        "key_id": {
          "description": "Identifier stored beside each ciphertext so a rotation can tell which key decrypts which envelope. Outstanding envelopes must be drained or expired before their key id is retired.",
          "type": "string"
        },
        "key": {
          "description": "Secret reference to the 32-byte key. Structural validation checks only that this names a secret; runtime resolution requires exactly 32 decoded bytes.",
          "type": "string"
        },
        "max_age_hours": {
          "description": "Hard expiry for an envelope, in hours. Bounded by [`RECOVERY_MAX_AGE_HOURS_RANGE`].",
          "default": 23,
          "type": "integer",
          "format": "uint8",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "PaymentsWorkerConfig": {
      "description": "Cadence and shutdown budget for the recovery-only worker.",
      "type": "object",
      "properties": {
        "reconcile_interval_ms": {
          "description": "Delay between reconciliation sweeps, in milliseconds.",
          "default": 1000,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "max_reconcile_batch": {
          "description": "Largest number of records claimed in one sweep.",
          "default": 32,
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "shutdown_timeout_ms": {
          "description": "Time the worker is given to stop claiming and drain in-flight queries at shutdown, in milliseconds.",
          "default": 5000,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "PaymentProtocolsConfig": {
      "description": "Wire protocols the proxy speaks on top of its settlement rails.",
      "type": "object",
      "properties": {
        "payment_auth": {
          "description": "Payment HTTP Authentication. Absent means the proxy never emits a `WWW-Authenticate: Payment` challenge.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/PaymentAuthProtocolConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "PaymentAuthProtocolConfig": {
      "description": "Payment HTTP Authentication, pinned to one draft, one method, and one intent.\n\nThe core draft's intent registry is empty, so the method-specific registration supplies the exact request, credential, and receipt semantics. This release implements only the registered `stripe` plus `charge` pair; it does not invent a generic provider schema.",
      "type": "object",
      "required": [
        "realm"
      ],
      "properties": {
        "draft": {
          "description": "Draft identifier. Must equal [`PAYMENT_AUTH_DRAFT`]. Declared rather than assumed so a configuration written against a different draft fails loudly instead of producing bytes the client cannot parse.",
          "default": "draft-ryan-httpauth-payment-01",
          "type": "string"
        },
        "realm": {
          "description": "Protection space echoed in every challenge and credential. It is the first slot of the challenge binding, so changing it invalidates outstanding challenges.",
          "type": "string"
        },
        "method": {
          "description": "Payment method token. Must equal [`PAYMENT_AUTH_METHOD`].",
          "default": "stripe",
          "type": "string"
        },
        "intent": {
          "description": "Intent token. Must equal [`PAYMENT_AUTH_INTENT`].",
          "default": "charge",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "PaymentRailsConfig": {
      "description": "Settlement rails and their provider credentials.\n\nEvery block is optional, but an advertised rail with no enabled block, or a Lightning advertisement with two enabled backends and no [`Self::lightning_backend`] selector, fails at load rather than at first request.",
      "type": "object",
      "properties": {
        "x402": {
          "description": "x402 v2 `exact`.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/X402RailConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "stripe": {
          "description": "Stripe PaymentIntents, used both directly and as the settlement half of Payment Auth `stripe` plus `charge`.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/StripeRailConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "lightning_cln": {
          "description": "Core Lightning.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/LightningClnRailConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "lightning_lnd": {
          "description": "LND.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/LightningLndRailConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "lightning_backend": {
          "description": "Which backend an advertised Lightning rail resolves to. Required when both backends are configured; inferred when exactly one is.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/LightningBackend"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "X402RailConfig": {
      "description": "x402 v2 `exact` rail.",
      "type": "object",
      "required": [
        "asset",
        "asset_decimals",
        "facilitators",
        "max_timeout_seconds",
        "network",
        "pay_to",
        "quote_currency"
      ],
      "properties": {
        "scheme": {
          "description": "Scheme name. Must equal [`X402_SCHEME`].",
          "default": "exact",
          "type": "string"
        },
        "network": {
          "description": "CAIP-2 chain identifier, for example `eip155:84532`. A short chain nickname is rejected: an authoritative payment requirement must not depend on a translation table.",
          "type": "string"
        },
        "asset": {
          "description": "Asset contract address or identifier on `network`.",
          "type": "string"
        },
        "quote_currency": {
          "description": "ISO-4217 code the quote is priced in. Every rail a single route advertises must share this value; the proxy performs no currency conversion.",
          "type": "string"
        },
        "asset_decimals": {
          "description": "Decimal places in the asset's atomic unit. Used for the exact checked conversion from quote micros.",
          "type": "integer",
          "format": "uint8",
          "minimum": 0.0
        },
        "pay_to": {
          "description": "Recipient address on `network`.",
          "type": "string"
        },
        "max_timeout_seconds": {
          "description": "Value copied into the requirement's `maxTimeoutSeconds`. Bounded by [`X402_MAX_TIMEOUT_SECONDS_RANGE`].",
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "extra": {
          "description": "Scheme-specific extra object copied verbatim into the requirement. Its JCS encoding is bounded by [`MAX_X402_EXTRA_JCS_BYTES`] and is byte-stable, so the client can echo it and the proxy can compare it exactly.",
          "default": {},
          "type": "object",
          "additionalProperties": true
        },
        "facilitators": {
          "description": "Ordered facilitator candidates. Fallback is allowed only by issuing a fresh challenge; a signed requirement is bound to the one facilitator it selected.",
          "type": "array",
          "items": {
            "$ref": "#/definitions/X402FacilitatorConfig"
          }
        },
        "verify_timeout_ms": {
          "description": "Budget for the `verify` call, in milliseconds.",
          "default": 700,
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "settle_timeout_ms": {
          "description": "Budget for the `settle` call, in milliseconds.",
          "default": 1200,
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "breaker": {
          "description": "Breaker around the facilitator.",
          "default": {
            "failure_threshold": 3,
            "open_ms": 5000,
            "half_open_max": 1
          },
          "allOf": [
            {
              "$ref": "#/definitions/BreakerConfig"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "X402FacilitatorConfig": {
      "description": "One facilitator candidate for the x402 rail.\n\nThe URL is the facilitator's complete API root. The adapter preserves every configured path segment, strips only trailing slashes, and appends exactly `/verify` or `/settle`; it never injects a version or protocol segment of its own.",
      "type": "object",
      "required": [
        "facilitator_url"
      ],
      "properties": {
        "facilitator_url": {
          "description": "Absolute HTTPS API root. No userinfo, query, or fragment, and no endpoint suffix.",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "BreakerConfig": {
      "description": "Bounded circuit breaker around one provider endpoint.",
      "type": "object",
      "properties": {
        "failure_threshold": {
          "description": "Consecutive transport failures that open the breaker.",
          "default": 3,
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "open_ms": {
          "description": "How long the breaker stays open, in milliseconds. Bounded by [`BREAKER_OPEN_MS_RANGE`].",
          "default": 5000,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "half_open_max": {
          "description": "Probes admitted while half open. Exactly 1 in this release: a second concurrent probe against an unhealthy facilitator can dispatch a settlement nobody is waiting to record.",
          "default": 1,
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "StripeRailConfig": {
      "description": "Stripe settlement rail.",
      "type": "object",
      "required": [
        "api_key",
        "business_network_id",
        "currency_decimals",
        "payment_method_types",
        "quote_currency"
      ],
      "properties": {
        "api_key": {
          "description": "Secret reference to the Stripe secret key. Never inline.",
          "type": "string"
        },
        "api_version": {
          "description": "Stripe API version. Must equal [`STRIPE_API_VERSION`].",
          "default": "2026-06-24.dahlia",
          "type": "string"
        },
        "account_context": {
          "description": "Account routing. Only `platform` is accepted in this release, and no `Stripe-Account` header is sent. Connect routing is deliberately not reachable from a challenge or a credential: a client-controlled destination account would let a payer choose who gets paid.",
          "default": "platform",
          "type": "string"
        },
        "business_network_id": {
          "description": "Business network identifier copied into the method details of a Payment Auth request.",
          "type": "string"
        },
        "quote_currency": {
          "description": "ISO-4217 code the quote is priced in.",
          "type": "string"
        },
        "currency_decimals": {
          "description": "Declared minor-unit decimals for `quote_currency`. Checked against the built-in ISO-4217 table so a mis-declared currency cannot silently charge a hundred times the quoted price.",
          "type": "integer",
          "format": "uint8",
          "minimum": 0.0
        },
        "payment_method_types": {
          "description": "Payment method types offered on the PaymentIntent.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "direct_payment_intent": {
          "description": "Optional direct PaymentIntent mode.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/DirectPaymentIntentConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "DirectPaymentIntentConfig": {
      "description": "Direct Stripe PaymentIntent mode.\n\nThe challenge creates a manual-capture PaymentIntent and the credential retry captures it. Preparation must never capture funds or deliver the resource, so `automatic` capture is not accepted.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Whether the direct PaymentIntent mode is offered.",
          "default": false,
          "type": "boolean"
        },
        "capture_method": {
          "description": "Capture mode. Only `manual` is accepted.",
          "default": "manual",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "LightningClnRailConfig": {
      "description": "Core Lightning rail, reached over its Unix-socket JSON-RPC.",
      "type": "object",
      "required": [
        "quote_currency",
        "rune",
        "settlement_decimals",
        "socket_path"
      ],
      "properties": {
        "socket_path": {
          "description": "Absolute path to the `lightning-rpc` Unix socket.",
          "type": "string"
        },
        "rune": {
          "description": "Secret reference to the rune that authorizes the RPC calls.",
          "type": "string"
        },
        "minimum_version": {
          "description": "Smallest accepted node version as `major.minor`. Cannot be set below [`MIN_CLN_VERSION`], whose `xpay` label and `listinvoices` status this adapter depends on. Startup checks the live version through `getinfo`.",
          "default": "26.06",
          "type": "string"
        },
        "quote_currency": {
          "description": "ISO-4217 or asset code the quote is priced in.",
          "type": "string"
        },
        "settlement_decimals": {
          "description": "Decimal places in the settlement unit. `11` prices BTC in millisatoshis.",
          "type": "integer",
          "format": "uint8",
          "minimum": 0.0
        },
        "invoice_expiry_seconds": {
          "description": "Invoice lifetime, in seconds. Bounded by [`INVOICE_EXPIRY_SECONDS_RANGE`].",
          "default": 300,
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "LightningLndRailConfig": {
      "description": "LND rail, reached over gRPC.",
      "type": "object",
      "required": [
        "endpoint",
        "macaroon",
        "quote_currency",
        "settlement_decimals",
        "tls_certificate_path"
      ],
      "properties": {
        "endpoint": {
          "description": "gRPC endpoint URL. Must be absolute and TLS-bearing.",
          "type": "string"
        },
        "tls_certificate_path": {
          "description": "Absolute path to the node's TLS certificate.",
          "type": "string"
        },
        "macaroon": {
          "description": "Secret reference to the hex-encoded macaroon.",
          "type": "string"
        },
        "quote_currency": {
          "description": "ISO-4217 or asset code the quote is priced in.",
          "type": "string"
        },
        "settlement_decimals": {
          "description": "Decimal places in the settlement unit.",
          "type": "integer",
          "format": "uint8",
          "minimum": 0.0
        },
        "invoice_expiry_seconds": {
          "description": "Invoice lifetime, in seconds. Bounded by [`INVOICE_EXPIRY_SECONDS_RANGE`].",
          "default": 300,
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "LightningBackend": {
      "description": "Which settlement backend serves an advertised Lightning rail.",
      "oneOf": [
        {
          "description": "Core Lightning over its Unix-socket JSON-RPC.",
          "type": "string",
          "enum": [
            "cln"
          ]
        },
        {
          "description": "LND over gRPC.",
          "type": "string",
          "enum": [
            "lnd"
          ]
        }
      ]
    },
    "UsageReportersConfig": {
      "description": "Usage reporters. Accounting only.",
      "type": "object",
      "properties": {
        "stripe_meter": {
          "description": "Stripe Meter Events. Meter events record consumption; they never prove that a payment settled and can never move an access intent to `Succeeded`.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/StripeMeterReporterConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "StripeMeterReporterConfig": {
      "description": "Stripe Meter Events reporter.",
      "type": "object",
      "required": [
        "customer_field",
        "event_name",
        "source",
        "unit"
      ],
      "properties": {
        "event_name": {
          "description": "Meter event name registered in the Stripe dashboard.",
          "type": "string"
        },
        "customer_field": {
          "description": "Field on the request context that carries the Stripe customer identifier.\n\nResolved against the authenticated principal's metadata map, so the identifier travels with the credential rather than with the request. A request whose principal carries no such entry is not reported: a meter event with nobody to bill is refused by Stripe anyway, and queueing one would turn a missing credential attribute into a permanently failing durable row.",
          "type": "string"
        },
        "source": {
          "description": "Which request-path record is authoritative for this meter event.\n\nRequired, with no default. See [`UsageSource`].",
          "allOf": [
            {
              "$ref": "#/definitions/UsageSource"
            }
          ]
        },
        "unit": {
          "description": "The unit this meter event bills.\n\nFor [`UsageSource::Http`] this is one of the operator's own `proxy.attestation` unit names, and only billable units carrying that name are reported. For the other two sources it is one of [`UsageSource::known_units`].\n\nOne unit per meter event rather than a list, because a Stripe meter event carries one quantity. Two units summed into one number is exactly the unfalsifiable figure the metering vocabulary exists to prevent.",
          "type": "string"
        },
        "failure_posture": {
          "description": "What the request path does when the durable enqueue fails.\n\nDefaults to [`crate::types::FailureMode::Degraded`] rather than the surface-wide `closed`, for the same reason `proxy.attestation.failure_mode` does: billing is not a security boundary, and a settlement database that will not accept a write must not take the API down. `degraded` is what keeps the resulting hole provable rather than silent.",
          "default": "degraded",
          "allOf": [
            {
              "$ref": "#/definitions/FailureMode"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "UsageSource": {
      "description": "Which request-path record is authoritative for one meter event.\n\nOne meter event, one source. A single request can produce a request receipt, an AI usage record, and one record per MCP tool call, and two of those can easily describe the same commercial unit: an AI request served through the gateway is also an HTTP request the meter priced. Reporting both against one Stripe meter charges the customer twice for one sale.\n\nThe defence is that the operator names the authoritative source per meter event and the proxy never guesses. There is deliberately no `Default` and no serde default: an unstated answer here is the double-charge, and a default would be this crate picking a side of a commercial argument on the operator's behalf.\n\nAn operator who genuinely wants to bill both dimensions configures two meter events, which is a decision they made rather than one the proxy made for them.",
      "oneOf": [
        {
          "description": "The request receipt the `proxy.attestation` meter settled.\n\nUnits come from that block's `measured`, `route_weights`, and `origin_headers` resolvers, after the `billable` outcome table has decided which of them survive this request's outcome.",
          "type": "string",
          "enum": [
            "http"
          ]
        },
        {
          "description": "The AI usage record for a request that reached a model.",
          "type": "string",
          "enum": [
            "ai"
          ]
        },
        {
          "description": "One record per MCP tool call the request dispatched.",
          "type": "string",
          "enum": [
            "mcp"
          ]
        }
      ]
    },
    "RawOriginConfig": {
      "description": "A single origin config as it appears in YAML. Plugin-specific fields are kept as `serde_json::Value` for deferred parsing.",
      "type": "object",
      "required": [
        "action"
      ],
      "properties": {
        "action": {
          "description": "Action describing what the origin does (proxy, redirect, static, etc.)."
        },
        "tenant_id": {
          "description": "WOR-1053: declared tenant for this origin. Must match an `id` under `proxy.tenants[]`; absent resolves to the synthetic `__default__` tenant so existing single-tenant configs keep working unchanged.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "credentials": {
          "description": "Canonical credentials block at origin scope. Overrides + adds to the tenant + proxy scopes. See [`CredentialBlock`].",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/CredentialBlock"
          }
        },
        "authentication": {
          "description": "Authentication block (also accepted under YAML alias `auth`). Either a single provider mapping (`type: api_key`, ...) or a list of two or more provider mappings tried in declared order with OR semantics: the first provider that accepts the request wins, and a request every provider rejects gets the first provider's denial with every provider's challenge merged onto it.",
          "default": null
        },
        "policies": {
          "description": "Policy entries (rate limit, WAF, IP filter, etc.) evaluated in order.",
          "default": [],
          "type": "array",
          "items": true
        },
        "transforms": {
          "description": "Transform pipeline applied to request and response bodies.",
          "default": [],
          "type": "array",
          "items": true
        },
        "filters": {
          "description": "Proxy-Wasm HTTP filters evaluated in declaration order.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/ProxyWasmFilterAttachment"
          }
        },
        "request_modifiers": {
          "description": "Request modifiers (header rewrites, path edits, etc.).",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/RequestModifierConfig"
          }
        },
        "response_modifiers": {
          "description": "Response modifiers applied after the action returns.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/ResponseModifierConfig"
          }
        },
        "cors": {
          "description": "Optional CORS configuration.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/CorsConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "hsts": {
          "description": "Optional HSTS configuration.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/HstsConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "compression": {
          "description": "Optional response compression configuration.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/CompressionConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "session": {
          "description": "Optional session cookie configuration (also accepted as `session_config`).",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/SessionConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "properties": {
          "description": "Per-origin custom-properties capture. Mirrors [`sbproxy_observe::PropertiesConfig`]; absent means the proxy uses the type's `Default` (capture on, no echo, no redaction).",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/PropertiesConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "sessions": {
          "description": "Per-origin session-id capture. Mirrors [`sbproxy_observe::SessionsConfig`]; absent means the proxy uses the type's `Default` (capture on, `Anonymous` auto-generate).",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/SessionsConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "user": {
          "description": "Per-origin user-id capture. Mirrors [`sbproxy_observe::UserConfig`]; absent means the proxy uses the type's `Default` (capture on, 256-byte cap).",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/UserConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "force_ssl": {
          "description": "When true, redirect plain HTTP requests to HTTPS.",
          "default": false,
          "type": "boolean"
        },
        "allowed_methods": {
          "description": "Whitelist of HTTP methods this origin accepts; empty allows all.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "forward_rules": {
          "description": "Path-based forward rules to inline child origins.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/RawForwardRule"
          }
        },
        "fallback_origin": {
          "description": "Origin used when the primary upstream fails.",
          "default": null
        },
        "response_cache": {
          "description": "Per-origin response-cache configuration (raw, compiled later).",
          "default": null
        },
        "variables": {
          "description": "Static variables available for template interpolation.",
          "default": {},
          "type": "object",
          "additionalProperties": true
        },
        "on_request": {
          "description": "Hooks invoked when a request enters the origin.",
          "default": [],
          "type": "array",
          "items": true
        },
        "on_response": {
          "description": "Hooks invoked when a response is being returned.",
          "default": [],
          "type": "array",
          "items": true
        },
        "bot_detection": {
          "description": "Bot detection configuration.",
          "default": null
        },
        "threat_protection": {
          "description": "Threat protection (IP reputation, blocklist) configuration.",
          "default": null
        },
        "error_pages": {
          "description": "Per-status custom error response bodies. Each entry covers one or more HTTP status codes and contributes a content-typed body the proxy substitutes when it generates the matching status. Multiple entries for the same status are content-negotiated against the inbound request's `Accept` header.",
          "default": null,
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/definitions/ErrorPageEntry"
          }
        },
        "problem_details": {
          "description": "RFC 9457 `application/problem+json` default-renderer configuration. When enabled, proxy-generated errors that are not matched by an [`ErrorPageEntry`] render as a structured problem-details body. Composes with `error_pages`: custom pages still win when authored. See [`ProblemDetailsConfig`].",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ProblemDetailsConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "proxy_status": {
          "description": "RFC 9209 `Proxy-Status` response header configuration. When enabled, the proxy stamps a structured `Proxy-Status` header on every non-2xx response so downstream clients can diagnose forwarding errors without scraping the body. The identity token defaults to `sbproxy` and can be overridden for fleet- wide branding (e.g. `acme-edge`).",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ProxyStatusConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "deprecation": {
          "description": "RFC 9745 / RFC 8594 API deprecation announcement for the whole origin. Matching responses carry `Deprecation`, `Sunset`, and the `successor-version` / `deprecation` Link relations per what is configured; `/.well-known/openapi.json` marks the origin's operations `deprecated: true`. A forward rule's own `deprecation:` block overrides this one for the requests it matches. See [`DeprecationConfig`].",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/DeprecationConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "traffic_capture": {
          "description": "Refused at config compile. Nothing ever read this block, and because it is an untyped value nothing ever validated it either: a typo inside it was indistinguishable from a correct setting. Live request mirroring is [`MirrorConfig`] under `mirror`.\n\nRetained as a parseable field so the failure names the replacement rather than reading as an unknown key.",
          "default": null
        },
        "mirror": {
          "description": "Shadow traffic mirror, fire-and-forget copy of each request to a separate upstream. See [`MirrorConfig`].",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/MirrorConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "message_signatures": {
          "description": "HTTP message signatures configuration (RFC 9421).",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/MessageSignaturesConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "olp": {
          "description": "WOR-808 PR7: RSL Open License Protocol issuer configuration. When set, the proxy serves `/.well-known/olp/token` (issuance) and `/.well-known/olp/key` (JWK publication) on the origin so crawlers following a `WWW-Authenticate: License` challenge can obtain and verify license tokens.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/OlpConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "comp": {
          "description": "WOR-2673: IAB Content Authorization Marketplace Protocol (CoMP) bridge. When set, the proxy serves `/.well-known/iab-comp/{manifest.json,quote,redeem}` on this origin, so an AI buyer can read the licensing catalog, get a signed price, and redeem a paid acceptance for an OLP license token without any publisher-specific integration.\n\nRequires this origin's [`Self::olp`] block: the bridge mints with that issuer's signing key, so the token it hands back is one this same origin's `/.well-known/olp/introspect` verifies.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/CompMarketplaceConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "web_bot_auth_publish": {
          "description": "WOR-805 AC#4: opt in to publishing SBproxy's own Web Bot Auth signing-key directory at `/.well-known/http-message-signatures-directory` and the Signature Agent Card discovery doc. Verifiers that fetch the directory can then verify the signatures SBproxy attaches to outbound requests when the corresponding `MessageSignatureSigner` runs upstream of the proxy.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/WebBotAuthPublishConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "idempotency": {
          "description": "`Idempotency-Key` middleware, per `draft-ietf-httpapi-idempotency-key-header`. Opt in per origin to have the proxy short-circuit retries of POST/PUT/PATCH (or any configured method) carrying a repeated `Idempotency-Key` header. See [`IdempotencyConfig`].",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/IdempotencyConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "connection_pool": {
          "description": "Compatibility-only per-origin connection-pool shape, except for `idle_timeout_secs`, which is the legacy spelling of `timeouts.idle_ms`. Pingora's built-in pool settings apply regardless of the other values.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ConnectionPoolConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "timeouts": {
          "description": "Per-origin upstream transport deadlines (connect, read, write, idle). Absent fields resolve to the built-in defaults at config compile time. See [`UpstreamTimeoutsConfig`].",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/UpstreamTimeoutsConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "extensions": {
          "description": "Opaque per-origin extensions for out-of-tree config blocks.\n\nThe compiler never parses these values. Extension consumers (e.g. a semantic-cache hook) read their own nested keys by name. Mirrors the server-level `proxy.extensions` pattern so the schema stays neutral.",
          "default": {},
          "type": "object",
          "additionalProperties": true
        },
        "expose_openapi": {
          "description": "When true, the gateway exposes a per-host OpenAPI document at `/.well-known/openapi.json` (and `.yaml`) for this origin. Off by default: emission is opt-in so origins do not leak route shape without the operator's consent.",
          "default": false,
          "type": "boolean"
        },
        "stream_safety": {
          "description": "Per-origin streaming safety rule identifiers. Forwarded to the stream-safety hook so each origin can enforce its own subset (e.g. `[\"pii\", \"toxicity\"]`). Empty disables streaming safety for the origin even when the hook is wired.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "default_content_shape": {
          "description": "Per-origin default content shape used when the agent's `Accept` header is `*/*` or absent. Threaded into the synthesised `auto_content_negotiate` config by [`crate::compile_origin`]. Recognized values: `markdown`, `json`, `html`, `pdf`, `other`. Unset falls back to `html`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "content_signal": {
          "description": "Per-origin `Content-Signal` response header value. Closed enum (validated at compile time): `ai-train`, `search`, `ai-input`. When set, the proxy stamps `Content-Signal: <value>` on 200 responses for this origin and the projection cache (`licenses.xml`, `tdmrep.json`) reflects the same signal. An unset value means \"no signal asserted\" and the proxy stamps `TDM-Reservation: 1` instead.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "token_bytes_ratio": {
          "description": "Per-origin override for the Markdown projection's tokens-per-byte ratio. Threads into the synthesised `html_to_markdown` transform's `token_bytes_ratio` field and the projection fallback path so the `x-markdown-tokens` response header and the JSON envelope's `token_estimate` field both honor the override. Unset falls back to `DEFAULT_TOKEN_BYTES_RATIO` (0.25).",
          "default": null,
          "type": [
            "number",
            "null"
          ],
          "format": "float"
        },
        "agent_skills": {
          "description": "Per-origin Agent Skills v0.2.0 advertisement. When non-empty, the proxy serves `GET /.well-known/agent-skills/index.json` for this origin and re-hosts each path-absolute or relative artifact at the URL declared in the entry. Empty (or absent) keeps the well-known endpoint disabled for the origin so v1 configs compile unchanged.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/AgentSkillEntry"
          }
        },
        "agents_md": {
          "description": "Per-origin `/AGENTS.md` body (WOR-809). When set, the proxy serves it verbatim at `GET /AGENTS.md` (content type `text/markdown`) per the AGENTS.md agent-instructions convention. Independent of `ai_crawl_control`. Absent keeps the endpoint off.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "ai_txt": {
          "description": "Per-origin `/ai.txt` body (WOR-809). When set, the proxy serves it verbatim at `GET /ai.txt` per the Spawning ai.txt convention. Independent of `ai_crawl_control`. Absent keeps the endpoint off.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "agents_json": {
          "description": "Per-origin agents.json manifest (WOR-820). When set, the proxy serves `GET /.well-known/agents.json` (the Wildcard agents.json v0.1 spec): operator-authored `info` + `flows`, with `sources` defaulting to the origin's emitted OpenAPI document. Independent of `ai_crawl_control`. Absent keeps the endpoint off.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/AgentsJsonConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "outbound_credential": {
          "description": "Per-origin outbound credential resolver (WOR-802). When set, the proxy mints/resolves a credential and stamps it on the request it sends upstream (RFC 8693 token exchange, OAuth client-credentials, or a vault-resolved secret). Kept as JSON for deferred compilation in `sbproxy-core` (the typed enum lives in `sbproxy-modules`). Secret fields use the standard `${ENV}` interpolation, resolved at config load.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/OutboundCredentialSchema"
            },
            {
              "type": "null"
            }
          ]
        },
        "outbound_web_bot_auth": {
          "description": "Opt this origin into outbound Web Bot Auth signing (WOR-805). When `true` and `proxy.web_bot_auth` is configured, the proxy signs the request it sends upstream with the proxy's Ed25519 key (RFC 9421, `tag=web-bot-auth`), so an upstream that demands Web Bot Auth accepts SBproxy as a verified agent. Default `false`.",
          "default": false,
          "type": "boolean"
        },
        "attestation": {
          "description": "Per-origin consumption attestation overrides (WOR-2127). Absent leaves the origin on the proxy-wide role with no agreement named. Authoring this block without a `proxy.attestation` block fails config compile: a per-origin role with no queue, ledger, or billing table behind it can never produce a record. See [`OriginAttestationConfig`].",
          "anyOf": [
            {
              "$ref": "#/definitions/OriginAttestationConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "observability": {
          "description": "Origin-scope observability block. Today the only nested surface is `log.redact.pii`, which composes against the tenant-scope block (or proxy-scope when the origin has no tenant). See [`OriginObservabilityConfig`].",
          "anyOf": [
            {
              "$ref": "#/definitions/OriginObservabilityConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "ProxyWasmFilterAttachment": {
      "description": "One Proxy-Wasm HTTP filter attached to an origin.",
      "type": "object",
      "required": [
        "type"
      ],
      "properties": {
        "type": {
          "description": "Stable hook type declared by an installed Proxy-Wasm bundle.",
          "type": "string"
        },
        "config": {
          "description": "Plugin configuration exposed through `proxy_get_buffer_bytes`.",
          "default": {}
        },
        "failure_posture": {
          "description": "Optional origin-specific override for the bundle failure posture.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/FailureMode"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "RequestModifierConfig": {
      "description": "Request modifier entry.\n\nEach modifier entry can contain one or more of: `headers`, `url`, `query`, `method`, `body`, `lua_script`, `js_script`, or `rego_module` / `rego_module_path`. Multiple modifier entries in the list are applied in order.",
      "type": "object",
      "properties": {
        "headers": {
          "description": "Header set/add/remove operations.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/HeaderModifiers"
            },
            {
              "type": "null"
            }
          ]
        },
        "url": {
          "description": "URL path rewrite rules.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/UrlModifier"
            },
            {
              "type": "null"
            }
          ]
        },
        "query": {
          "description": "Query parameter modifications.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/QueryModifier"
            },
            {
              "type": "null"
            }
          ]
        },
        "method": {
          "description": "Override the HTTP method (e.g., \"POST\", \"PUT\").",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "body": {
          "description": "Body replacement.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/BodyModifier"
            },
            {
              "type": "null"
            }
          ]
        },
        "lua_script": {
          "description": "Optional Lua script for dynamic request modification.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "js_script": {
          "description": "Optional JavaScript script for dynamic request modification.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "rego_module": {
          "description": "Optional inline Rego module for dynamic request modification (WOR-2482). The module's `data.sbproxy.modify_request` rule evaluates against the same document `lua_script` / `js_script` receive as `req` and `ctx`, merged into one `input`, and returns `{\"set_headers\": {...}}`, the same shape those scripts return. Mutually exclusive with `rego_module_path`. See `docs/scripting.md`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "rego_module_path": {
          "description": "Filesystem path to a `.rego` file, read once when the config compiles (and again on every reload), in place of an inline `rego_module`. Mutually exclusive with `rego_module`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "rego_budget_ms": {
          "description": "Rego only: evaluation budget in milliseconds. Defaults to 50, the same bound `policy: rego` and `ai_routing_policy`'s Rego form use. Must be greater than zero; a zero budget is refused at config compile.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "rego_v0": {
          "description": "Parse `rego_module` (or the file at `rego_module_path`) as pre-OPA-1.0 Rego v0 (no `if`/`contains` required) instead of the v1 default. A compatibility escape hatch for a module pasted from an older OPA install.",
          "default": false,
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "HeaderModifiers": {
      "description": "Header modification operations (set, add, remove).",
      "type": "object",
      "properties": {
        "set": {
          "description": "Headers to set, replacing any existing value.",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "add": {
          "description": "Headers to append (preserves existing values).",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "remove": {
          "description": "Headers to remove. Alias: `delete`.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "UrlModifier": {
      "description": "URL path rewrite configuration.",
      "type": "object",
      "properties": {
        "path": {
          "description": "Path rewrite rules.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/PathRewrite"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "PathRewrite": {
      "description": "Path rewrite: replace a substring in the path.",
      "type": "object",
      "properties": {
        "replace": {
          "description": "Replace a substring in the path.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/PathReplace"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "PathReplace": {
      "description": "A simple string-replace operation on the URL path.",
      "type": "object",
      "required": [
        "new",
        "old"
      ],
      "properties": {
        "old": {
          "description": "The substring to search for.",
          "type": "string"
        },
        "new": {
          "description": "The replacement string.",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "QueryModifier": {
      "description": "Query parameter modification operations.",
      "type": "object",
      "properties": {
        "set": {
          "description": "Set (overwrite) query parameters.",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "add": {
          "description": "Add query parameters (appended even if the key already exists).",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "remove": {
          "description": "Remove query parameters by name.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "BodyModifier": {
      "description": "Body replacement configuration for request modifiers.",
      "type": "object",
      "properties": {
        "replace_json": {
          "description": "Replace the request body with this JSON value.",
          "default": null
        },
        "replace": {
          "description": "Replace the request body with this string.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "ResponseModifierConfig": {
      "description": "Response modifier entry.\n\nEach modifier entry can contain one or more of: `headers`, `status`, `body`, `lua_script`, `js_script`, or `rego_module` / `rego_module_path`. Multiple modifier entries in the list are applied in order.",
      "type": "object",
      "properties": {
        "headers": {
          "description": "Header set/add/remove operations.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/HeaderModifiers"
            },
            {
              "type": "null"
            }
          ]
        },
        "status": {
          "description": "Override the response status code and, optionally via `text`, the HTTP/1.x reason phrase.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/StatusOverride"
            },
            {
              "type": "null"
            }
          ]
        },
        "body": {
          "description": "Response body replacement.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/ResponseBodyModifier"
            },
            {
              "type": "null"
            }
          ]
        },
        "lua_script": {
          "description": "Optional Lua script for dynamic response modification.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "js_script": {
          "description": "Optional JavaScript script for dynamic response modification.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "rego_module": {
          "description": "Optional inline Rego module for dynamic response modification (WOR-2482). The module's `data.sbproxy.modify_response` rule evaluates against the same document `lua_script` / `js_script` receive as `resp` and `ctx`, merged into one `input`, and returns `{\"set_headers\": {...}}`, the same shape those scripts return. Mutually exclusive with `rego_module_path`. See `docs/scripting.md`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "rego_module_path": {
          "description": "Filesystem path to a `.rego` file, read once when the config compiles (and again on every reload), in place of an inline `rego_module`. Mutually exclusive with `rego_module`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "rego_budget_ms": {
          "description": "Rego only: evaluation budget in milliseconds. Defaults to 50, the same bound `policy: rego` and `ai_routing_policy`'s Rego form use. Must be greater than zero; a zero budget is refused at config compile.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "rego_v0": {
          "description": "Parse `rego_module` (or the file at `rego_module_path`) as pre-OPA-1.0 Rego v0 (no `if`/`contains` required) instead of the v1 default. A compatibility escape hatch for a module pasted from an older OPA install.",
          "default": false,
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "StatusOverride": {
      "description": "Status code override for response modifiers.",
      "type": "object",
      "required": [
        "code"
      ],
      "properties": {
        "code": {
          "description": "The HTTP status code to set.",
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "text": {
          "description": "Custom reason phrase emitted on the HTTP/1.x status line. Absent means the canonical phrase for `code`. HTTP/2 has no reason phrase on the wire, so the value is ignored there.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "ResponseBodyModifier": {
      "description": "Body replacement configuration for response modifiers.",
      "type": "object",
      "properties": {
        "replace": {
          "description": "Replace the response body with this string.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "replace_json": {
          "description": "Replace the response body with this JSON value.",
          "default": null
        }
      },
      "additionalProperties": false
    },
    "CorsConfig": {
      "description": "CORS configuration.",
      "type": "object",
      "properties": {
        "allowed_origins": {
          "description": "Origins permitted by `Access-Control-Allow-Origin`. Alias: `allow_origins`.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "allowed_methods": {
          "description": "Methods permitted by `Access-Control-Allow-Methods`. Alias: `allow_methods`.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "allowed_headers": {
          "description": "Request headers permitted by `Access-Control-Allow-Headers`. Alias: `allow_headers`.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "expose_headers": {
          "description": "Response headers exposed via `Access-Control-Expose-Headers`.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "max_age": {
          "description": "Optional preflight cache duration in seconds (`Access-Control-Max-Age`).",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "allow_credentials": {
          "description": "When true, sends `Access-Control-Allow-Credentials: true`.",
          "default": false,
          "type": "boolean"
        },
        "enable": {
          "description": "Legacy `enable: true` flag (alias: `enabled`). Accepted but not checked at runtime because the presence of the cors block is sufficient.",
          "default": null,
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "HstsConfig": {
      "description": "HSTS configuration.",
      "type": "object",
      "properties": {
        "max_age": {
          "description": "`max-age` directive of the `Strict-Transport-Security` header, in seconds.",
          "default": 31536000,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "include_subdomains": {
          "description": "When true, emits the `includeSubDomains` HSTS directive.",
          "default": false,
          "type": "boolean"
        },
        "preload": {
          "description": "When true, emits the `preload` HSTS directive.",
          "default": false,
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "CompressionConfig": {
      "description": "Compression configuration.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Master switch for response compression. Alias: `enable`.",
          "default": true,
          "type": "boolean"
        },
        "algorithms": {
          "description": "Allowed algorithms in priority order (e.g. `[\"br\", \"gzip\"]`). The first entry the client's `Accept-Encoding` accepts is the one served, so list your preferred codec first. Valid entries are `zstd`, `br`, and `gzip`; any other name fails config load. Leave the list empty to take the built-in order, best ratio first: `zstd`, then `br`, then `gzip`.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "min_size": {
          "description": "Minimum response size, in bytes, before compression is applied.",
          "default": 0,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "level": {
          "description": "Encoder effort setting, clamped into the negotiated algorithm's native range (gzip 0-9, brotli 0-11, zstd 1-22). Absent keeps each library's default.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "SessionConfig": {
      "description": "Session configuration.",
      "type": "object",
      "properties": {
        "cookie_name": {
          "description": "Name of the session cookie.",
          "type": [
            "string",
            "null"
          ]
        },
        "max_age": {
          "description": "Cookie lifetime in seconds. Alias: `cookie_max_age`.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "http_only": {
          "description": "When true, sets the `HttpOnly` cookie attribute.",
          "default": false,
          "type": "boolean"
        },
        "secure": {
          "description": "When true, sets the `Secure` cookie attribute (HTTPS only).",
          "default": false,
          "type": "boolean"
        },
        "same_site": {
          "description": "`SameSite` cookie attribute. Alias: `cookie_same_site`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "allow_non_ssl": {
          "description": "When true, allow sessions over non-SSL connections.",
          "default": false,
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "PropertiesConfig": {
      "description": "Per-origin properties capture configuration. Defaults match the \"capture on, no echo, no redaction\" baseline; operators tune via `sb.yml`.",
      "type": "object",
      "properties": {
        "capture": {
          "description": "Master switch. When `false`, [`capture_properties`] returns an empty map regardless of headers.",
          "default": true,
          "type": "boolean"
        },
        "echo": {
          "description": "Echo captured properties back as `X-Sb-Property-<key>` response headers. Off by default; opt-in per origin.",
          "default": false,
          "type": "boolean"
        },
        "redact": {
          "description": "Redaction rules applied after capture and before any subscriber observes the event.",
          "default": {
            "keys": [],
            "value_regex": []
          },
          "allOf": [
            {
              "$ref": "#/definitions/RedactConfig"
            }
          ]
        },
        "rollup_keys": {
          "description": "Explicit property keys copied into durable usage-rollup dimensions. Bounded to [`MAX_ROLLUP_PROPERTY_KEYS`] during config compilation.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "RedactConfig": {
      "description": "Redaction rules for captured property values.",
      "type": "object",
      "properties": {
        "keys": {
          "description": "Exact-match keys whose values are replaced with `\"[redacted]\"`. Match is case-insensitive on the lowercased captured key.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "value_regex": {
          "description": "Value-content regex patterns. Any value matching any pattern is replaced wholesale with `\"[redacted]\"`.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "SessionsConfig": {
      "description": "Per-origin sessions configuration.",
      "type": "object",
      "properties": {
        "capture": {
          "description": "Master capture switch. When `false`, callers see no session ID regardless of headers or auto-generation policy.",
          "default": true,
          "type": "boolean"
        },
        "auto_generate": {
          "description": "When to auto-generate a session ID for callers that did not supply one.",
          "default": "anonymous",
          "allOf": [
            {
              "$ref": "#/definitions/AutoGenerate"
            }
          ]
        },
        "ttl_seconds": {
          "description": "Refused at config compile. It described the retention of a sessions index that does not exist: the only thing holding sessions is the in-process request ring, which is bounded by entry count and never consulted this value, so a session survived exactly as long as the ring did whatever was set here.\n\nRetained as a parseable field so the failure explains itself rather than reading as an unknown key.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "budget": {
          "description": "Cap on auto-generated session IDs per workspace per window. `None` disables the gate (default). When the cap is hit, the dimension is dropped silently and `sbproxy_capture_budget_dropped_total{workspace,dimension=\"session\"}` is incremented. Caller-supplied session IDs from `X-Sb-Session-Id` are never gated.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/BudgetConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "AutoGenerate": {
      "description": "Auto-generation policy for session IDs when the caller does not supply one.",
      "oneOf": [
        {
          "description": "Never mint a session ID. Capture caller-supplied IDs only.",
          "type": "string",
          "enum": [
            "never"
          ]
        },
        {
          "description": "Mint a session ID only when the caller is anonymous (no resolved user identity). This is the default for AI gateway origins; bursts of anonymous traffic get one session per burst rather than one per request.",
          "type": "string",
          "enum": [
            "anonymous"
          ]
        },
        {
          "description": "Always mint a session ID when one is not supplied.",
          "type": "string",
          "enum": [
            "always"
          ]
        }
      ]
    },
    "BudgetConfig": {
      "description": "Per-dimension cap that the capture helpers consult before stamping auto-generated session IDs or admitting user IDs onto the request envelope. Counts are kept per workspace and rotate on a fixed window so a hostile burst from one tenant does not poison the shared envelope shape.\n\nWhen the cap is hit the offending dimension is dropped silently (no 4xx); a dedicated metric tracks the drop for ops visibility. See `docs/A30.md` (decision log) for the rationale on why this path drops rather than rejects.",
      "type": "object",
      "required": [
        "max_per_window"
      ],
      "properties": {
        "max_per_window": {
          "description": "Maximum admissions per window. Exceeding this floors further admissions in the same window to drop-with-metric.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "window_seconds": {
          "description": "Window length in seconds. Counters rotate per `floor(now / window_seconds)` slot.",
          "default": 60,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "UserConfig": {
      "description": "Per-origin user-ID configuration.",
      "type": "object",
      "properties": {
        "capture": {
          "description": "Master capture switch. When `false`, [`capture_user_id`] always returns `None`.",
          "default": true,
          "type": "boolean"
        },
        "max_length": {
          "description": "Maximum allowed length of the resolved user ID. Values longer than this are dropped wholesale (no truncation, since truncation collides distinct identities).",
          "default": 256,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "budget": {
          "description": "Cap on captured user IDs per workspace per window. `None` disables the gate (default). When the cap is hit, the dimension is dropped silently and `sbproxy_capture_budget_dropped_total{workspace,dimension=\"user\"}` is incremented.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/BudgetConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "RawForwardRule": {
      "description": "One forward rule on an origin: a set of matcher entries plus the inline child origin to dispatch to when any entry hits.\n\nCompiled at config-load time. The runtime walks the `rules` of each forward rule against the incoming request and uses the first matching entry's `origin`. Within a single entry the present matchers (path, header, query, body, method) are ANDed; across entries they are ORed.",
      "type": "object",
      "required": [
        "origin"
      ],
      "properties": {
        "rules": {
          "description": "Path matchers. The rule fires when any one of these matches the request path.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/ForwardRuleMatcher"
          }
        },
        "origin": {
          "description": "Inline child origin executed when the rule fires.",
          "allOf": [
            {
              "$ref": "#/definitions/ForwardRuleOrigin"
            }
          ]
        },
        "parameters": {
          "description": "Parameter declarations that apply to every matcher in this rule.\n\nMirrors the OpenAPI 3.0 Parameter Object verbatim so emission is a near-direct map. Used by OpenAPI emission to populate `paths.<path>.<method>.parameters[]` and is exposed on the request context as `path_params` after the matcher captures values.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/Parameter"
          }
        },
        "deprecation": {
          "description": "RFC 9745 / RFC 8594 API deprecation announcement scoped to the requests this rule matches. Overrides the origin-level `deprecation:` block for them, so `/v1/*` can be deprecated while `/v2/*` on the same origin is not. See [`DeprecationConfig`].",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/DeprecationConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "ForwardRuleMatcher": {
      "description": "One match entry inside a forward rule's `rules:` list.\n\nEach entry may carry any combination of `path`, `header`, `query`, `body`, and `method` matchers. Within a single entry the matchers are ANDed: every present matcher must succeed for the entry to fire. Across entries in the same rule the semantics are OR: any matching entry triggers the rule. The shorthand `match: <prefix>` is equivalent to `path: { prefix: ... }`.",
      "type": "object",
      "properties": {
        "path": {
          "description": "Structured path matcher.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/PathMatcher"
            },
            {
              "type": "null"
            }
          ]
        },
        "match": {
          "description": "Shorthand for a prefix match. Equivalent to `path: { prefix: <value> }`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "header": {
          "description": "Header equality / prefix matcher.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/HeaderMatcher"
            },
            {
              "type": "null"
            }
          ]
        },
        "query": {
          "description": "Query parameter matcher.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/QueryMatcher"
            },
            {
              "type": "null"
            }
          ]
        },
        "body": {
          "description": "JSON request-body field matcher.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/BodyMatcher"
            },
            {
              "type": "null"
            }
          ]
        },
        "method": {
          "description": "HTTP method matcher. A single method or a list of them; the entry fires when the request method equals any listed one. Methods are normalized to uppercase at config-load time, so `post` and `POST` are the same matcher.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/MethodSpec"
            },
            {
              "type": "null"
            }
          ]
        },
        "when": {
          "description": "CEL predicate ANDed with the structured matchers above.\n\nExists for the conditions the structured fields cannot express: OR, negation, and comparisons across two parts of the request. It is evaluated last, only once every structured matcher in the entry has already passed, so a rule that fails on a cheap path check never pays for it.\n\nThe bindings available here are the request as it arrived, and nothing that a later pipeline pass produces. See `docs/scripting.md`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "PathMatcher": {
      "description": "A path matcher inside a forward rule.\n\nExactly one of `prefix`, `exact`, `template`, or `regex` should be set. Precedence when more than one is provided: `template` > `regex` > `exact` > `prefix`. Templates and regex are evaluated lazily, so origins that only use prefix/exact pay no regex cost.\n\nTemplate syntax (`/users/{id}/posts/{post_id}`) supports named segments, catch-all (`/static/{*rest}`), and optional per-segment regex constraints (`/users/{id:[0-9]+}`). Constraint compilation happens at config-load time; the runtime only re-validates constrained params after the trie match.",
      "type": "object",
      "properties": {
        "prefix": {
          "description": "Matches any path that starts with this prefix.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "exact": {
          "description": "Matches only this exact path string.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "template": {
          "description": "OpenAPI-style path template with named segments. Captured params are exposed on the request context as `path_params` for downstream modifiers, CEL/Lua scripts, and metrics labels.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "regex": {
          "description": "Whole-path regex escape hatch. Use named captures (`(?P<id>...)`) to surface params on the request context.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "HeaderMatcher": {
      "description": "Match a request header by exact value or value prefix.\n\nExactly one of `value` or `prefix` should be set. When both are present `value` wins (exact comparison). Header name matching is case-insensitive per RFC 7230; value comparison is case-sensitive.",
      "type": "object",
      "required": [
        "name"
      ],
      "properties": {
        "name": {
          "description": "Header name (case-insensitive lookup).",
          "type": "string"
        },
        "value": {
          "description": "Required exact value.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "prefix": {
          "description": "Required value prefix. Ignored when `value` is set.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "QueryMatcher": {
      "description": "Match a query string parameter by exact value.\n\nThe query string is parsed as `application/x-www-form-urlencoded`. The matcher succeeds if any occurrence of `name` equals `value`. When `value` is omitted the matcher succeeds whenever the parameter is present at all.",
      "type": "object",
      "required": [
        "name"
      ],
      "properties": {
        "name": {
          "description": "Query parameter name (case-sensitive).",
          "type": "string"
        },
        "value": {
          "description": "Required exact value. When `None`, presence of the parameter is enough.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "BodyMatcher": {
      "description": "Match a field inside a JSON request body, addressed by RFC 6901 JSON Pointer.\n\nThis exists because the field an operator most wants to route on is often in the body rather than the URL. `model`, `stream`, and `tools` are body fields in the OpenAI, Anthropic, and Bedrock request shapes, so without this matcher two models cannot be given different rate limits, different upstream credentials, or different guardrail chains without collapsing them onto one origin.\n\nExactly one of `value` or `prefix` should be set. When both are present `value` wins (exact comparison). When neither is set the matcher succeeds whenever the pointer resolves to any JSON value at all, including `null`, an object, or an array, which is how you route on \"this request uses tools\" without naming a tool.\n\nNumbers and booleans are compared against their JSON text form, so `pointer: /stream` with `value: \"true\"` matches `{\"stream\": true}`.\n\nSelecting a route on a body field means the body has to be buffered before the route is known, so this matcher is the one part of routing with a size limit. `max_bytes` caps it, defaulting to 65536 and never exceeding it, because 65536 is the fixed size of the replay buffer that lets the buffered bytes still be forwarded upstream byte for byte.\n\nFive things make this matcher miss rather than fail the request: a body larger than `max_bytes`, a body that is not JSON, a body that does not parse, a pointer that resolves to nothing, and a pointer that resolves to an object or array while `value` or `prefix` asked for a scalar. In every one of those cases the entry does not fire and routing carries on to the next entry, then the next rule, then the origin's own action, which is the same header-only routing that would have happened without the matcher.",
      "type": "object",
      "required": [
        "pointer"
      ],
      "properties": {
        "pointer": {
          "description": "RFC 6901 JSON Pointer to the field, for example `/model` or `/messages/0/role`. An empty pointer addresses the whole document.",
          "type": "string"
        },
        "value": {
          "description": "Required exact value.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "prefix": {
          "description": "Required value prefix. Ignored when `value` is set.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "max_bytes": {
          "description": "Largest request body this matcher will read, in bytes. Defaults to 65536. Config compilation refuses a larger value.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "MethodSpec": {
      "description": "HTTP method spec for a [`ForwardRuleMatcher`]. Either a single method (`method: POST`) or a list (`method: [POST, PUT]`). Tokens are normalized to uppercase when the matcher compiles, so `post` and `POST` mean the same thing; an empty list or a token that is not a valid HTTP method token fails config load.",
      "anyOf": [
        {
          "description": "Single HTTP method.",
          "type": "string"
        },
        {
          "description": "Multiple methods; any match counts.",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      ]
    },
    "ForwardRuleOrigin": {
      "description": "Inline child origin used when a forward rule fires. Carries the action plus optional request modifiers. Compatibility metadata fields remain parseable but are not copied into the compiled child origin.",
      "type": "object",
      "required": [
        "action"
      ],
      "properties": {
        "id": {
          "description": "Optional identifier used in metrics and logs.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "hostname": {
          "description": "Compatibility-only hostname tag. The parent origin's hostname routes the request; this value is not consumed.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "workspace_id": {
          "description": "Compatibility-only workspace identifier; not consumed.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "version": {
          "description": "Compatibility-only version label; not consumed.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "action": {
          "description": "Action executed when the rule fires. Stays as raw JSON because action types are plugin-extensible (registered via the inventory crate)."
        },
        "request_modifiers": {
          "description": "Optional request modifiers applied before the action runs.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/RequestModifierConfig"
          }
        }
      },
      "additionalProperties": false
    },
    "Parameter": {
      "description": "An OpenAPI 3.0 Parameter Object declared on a forward rule.\n\nField names and shapes mirror the OpenAPI spec exactly so emission is a direct passthrough. The `schema` field is kept as `serde_json::Value` because the OpenAPI Schema Object is large and we forward it verbatim.",
      "type": "object",
      "required": [
        "in",
        "name"
      ],
      "properties": {
        "name": {
          "description": "Parameter name. For path params this must match a `{name}` segment in one of the rule's `template` matchers.",
          "type": "string"
        },
        "in": {
          "description": "Where the parameter appears: `path`, `query`, or `header`.",
          "allOf": [
            {
              "$ref": "#/definitions/ParameterLocation"
            }
          ]
        },
        "required": {
          "description": "Whether the parameter is required. Path params are always required per the OpenAPI spec; emission enforces this even when `false`.",
          "default": false,
          "type": "boolean"
        },
        "description": {
          "description": "Free-form description surfaced in the emitted spec.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "schema": {
          "description": "OpenAPI Schema Object (e.g. `{ \"type\": \"integer\", \"format\": \"int64\" }`). Forwarded verbatim into the emitted spec.",
          "default": null
        }
      },
      "additionalProperties": false
    },
    "ParameterLocation": {
      "description": "Location of an OpenAPI parameter (`in:` field).\n\nMatches the OpenAPI 3.0 enum exactly. `cookie` is intentionally not supported here yet because the gateway has no per-cookie capture story.",
      "oneOf": [
        {
          "description": "A captured `{name}` segment from a `template` matcher.",
          "type": "string",
          "enum": [
            "path"
          ]
        },
        {
          "description": "A query string parameter.",
          "type": "string",
          "enum": [
            "query"
          ]
        },
        {
          "description": "A request header.",
          "type": "string",
          "enum": [
            "header"
          ]
        }
      ]
    },
    "DeprecationConfig": {
      "description": "API deprecation announcement for an origin or a forward rule.\n\nWhen present, matching responses carry the standard deprecation headers: `Deprecation` (RFC 9745, an RFC 9651 structured-field Date such as `@1767225599`), `Sunset` (RFC 8594, an HTTP-date), and the `successor-version` (RFC 5829) and `deprecation` (RFC 9745) Link relations. A block at origin scope covers every route the origin serves; a block on a forward rule covers only requests that rule matches and overrides the origin block for them.\n\nSpecs: <https://www.rfc-editor.org/rfc/rfc9745.html>, <https://www.rfc-editor.org/rfc/rfc8594.html>, <https://www.rfc-editor.org/rfc/rfc5829.html>.",
      "type": "object",
      "properties": {
        "deprecated": {
          "description": "When the resource is (or will be) deprecated. A date (`2026-09-01`) or RFC 3339 timestamp (`2026-09-01T00:00:00Z`) emits `Deprecation: @<unix>`; past and future instants are both valid per RFC 9745. A bare `true` marks the route deprecated for OpenAPI emission and metrics but emits no `Deprecation` header, because RFC 9745 requires a Date value (the draft-era literal `true` did not survive into the RFC); config compile warns and suggests a date. `false` is refused: remove the block instead.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/DeprecatedStamp"
            },
            {
              "type": "null"
            }
          ]
        },
        "sunset": {
          "description": "When the resource is expected to become unresponsive. A date or RFC 3339 timestamp; emits `Sunset: <HTTP-date>` per RFC 8594. Config compile refuses a sunset earlier than `deprecated` (RFC 9745 section 3: the Sunset timestamp MUST NOT be earlier than the Deprecation one).",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "successor": {
          "description": "URL of the successor version of this resource. Emits `Link: <url>; rel=\"successor-version\"` (RFC 5829).",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "link": {
          "description": "URL of human-readable deprecation documentation. Emits `Link: <url>; rel=\"deprecation\"` (RFC 9745).",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "after_sunset": {
          "description": "What happens to requests after the `sunset` instant passes. `serve` (the default) keeps handling them with the headers attached; `gone` refuses them with `410 Gone` and a JSON body naming the successor. Requires `sunset` to be set.",
          "default": "serve",
          "allOf": [
            {
              "$ref": "#/definitions/AfterSunset"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "DeprecatedStamp": {
      "description": "The `deprecated:` field of a [`DeprecationConfig`]: either a bare boolean or a date / RFC 3339 timestamp string.",
      "anyOf": [
        {
          "description": "Bare `true` (deprecated, no announced instant) or `false` (refused at config compile).",
          "type": "boolean"
        },
        {
          "description": "The deprecation instant: `YYYY-MM-DD` (midnight UTC) or an RFC 3339 timestamp.",
          "type": "string"
        }
      ]
    },
    "AfterSunset": {
      "description": "Post-sunset posture for a [`DeprecationConfig`].",
      "oneOf": [
        {
          "description": "Keep serving requests, headers attached. The default, so a forgotten config never takes an API down by surprise.",
          "type": "string",
          "enum": [
            "serve"
          ]
        },
        {
          "description": "Refuse requests with `410 Gone` once the sunset instant passes.",
          "type": "string",
          "enum": [
            "gone"
          ]
        }
      ]
    },
    "ErrorPageEntry": {
      "description": "One per-status custom error page entry. Multiple entries for the same status code are content-negotiated against the inbound request's `Accept` header.",
      "type": "object",
      "required": [
        "body",
        "content_type",
        "status"
      ],
      "properties": {
        "status": {
          "description": "Which HTTP status code(s) this entry covers.",
          "allOf": [
            {
              "$ref": "#/definitions/StatusSpec"
            }
          ]
        },
        "content_type": {
          "description": "`Content-Type` to advertise on the response.",
          "type": "string"
        },
        "body": {
          "description": "Response body. When `template = true`, the proxy substitutes `{{ status_code }}` and `{{ request.path }}` (with or without surrounding whitespace) at request time.",
          "type": "string"
        },
        "template": {
          "description": "When true, treat `body` as a template and run substitution.",
          "default": false,
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "StatusSpec": {
      "description": "Status code spec for an [`ErrorPageEntry`]. Either a single integer (`status: 401`) or a list (`status: [401, 403]`). The list form is the historical authored shape; the single-int form is a sugar.",
      "anyOf": [
        {
          "description": "Single status code.",
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        {
          "description": "Multiple status codes; any match counts.",
          "type": "array",
          "items": {
            "type": "integer",
            "format": "uint16",
            "minimum": 0.0
          }
        }
      ]
    },
    "ProblemDetailsConfig": {
      "description": "RFC 9457 Problem Details default-renderer configuration.\n\nWhen enabled, any proxy-generated error response that is *not* already matched by a custom [`ErrorPageEntry`] is rendered as `application/problem+json` per RFC 9457. The two configs compose: operators can author per-status custom pages and still opt in to problem-details as a structured fallback for everything else.\n\nSpec: <https://www.rfc-editor.org/rfc/rfc9457.html>.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Whether to render unmatched proxy-generated errors as `application/problem+json`. Defaults to `false`; existing operators see no behavior change unless they opt in.",
          "default": false,
          "type": "boolean"
        },
        "type_base_uri": {
          "description": "Base URI for the `type` field. When set to e.g. `https://api.example.com/errors`, status 503 renders as `type: https://api.example.com/errors/503`. When unset, the renderer emits the RFC 9457 default `about:blank`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "include_detail": {
          "description": "When true (the default), the renderer copies the proxy's internal error message into the `detail` field. Operators who route problem responses to external clients can set this to false to avoid leaking upstream error text.",
          "default": true,
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "ProxyStatusConfig": {
      "description": "RFC 9209 `Proxy-Status` response header configuration.\n\nWhen `enabled`, the proxy stamps a structured `Proxy-Status` header on every non-2xx response. The header carries the proxy identity, the upstream status, and an optional `error` parameter derived from the upstream failure mode. Operators consuming the header can diagnose forwarding errors without scraping the body.\n\nSpec: <https://www.rfc-editor.org/rfc/rfc9209.html>.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Whether to stamp the `Proxy-Status` header on non-2xx responses. Defaults to `false`; opt in per origin so existing operator dashboards that match on bare status codes are not surprised.",
          "default": false,
          "type": "boolean"
        },
        "identity": {
          "description": "Proxy identity token used as the first parameter of the header (per RFC 9209's grammar). Defaults to `sbproxy`. Operators running a fleet can override this for branding (e.g. `acme-edge`, `sbproxy-eu-west-1`).",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "MirrorConfig": {
      "description": "Per-origin shadow-traffic configuration.\n\nWhen set on an origin, the proxy fires a fire-and-forget copy of each request at `url` and discards the response. The primary upstream is never blocked by mirror delivery. Useful for safe rollouts of new backends and replay-driven testing.",
      "type": "object",
      "required": [
        "url"
      ],
      "properties": {
        "url": {
          "description": "Mirror upstream URL (http:// or https://). IPv6 hosts must be bracketed in the URL (e.g. `http://[2001:db8::1]:8080`) per RFC 3986.",
          "type": "string"
        },
        "sample_rate": {
          "description": "Sample rate in `[0.0, 1.0]`. `1.0` (default) mirrors every request; `0.1` mirrors ~10%. Sampling is per-request and uses a uniform PRNG; do not rely on exact counts.",
          "default": 1.0,
          "type": "number",
          "format": "float"
        },
        "timeout_ms": {
          "description": "Mirror request timeout in milliseconds. Independent from the primary upstream timeout. Default 5000ms.",
          "default": 5000,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "mirror_body": {
          "description": "Whether to tee the inbound request body into the mirror request. Default `false`: the mirror sees only method, path, query, and headers (sufficient for read endpoints, GET-mostly traffic, and any case where shadow-replaying writes is unsafe). Set to `true` to enable body teeing for shadow-replay of POST/PUT/PATCH endpoints during migrations.",
          "default": false,
          "type": "boolean"
        },
        "max_body_bytes": {
          "description": "Maximum bytes of body to mirror. Bodies larger than this cap are skipped (the mirror is fired without a body) so a single large upload cannot blow up proxy memory. Default `1048576` (1 MiB).",
          "default": 1048576,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "MessageSignaturesConfig": {
      "description": "RFC 9421 HTTP Message Signatures verification configuration.\n\nWhen `verify: true`, the proxy enforces RFC 9421 signature verification on every inbound request to this origin. Requests without a valid `Signature-Input` + `Signature` header pair matching the configured `key_id` are rejected with `401 Unauthorized` and `WWW-Authenticate: Signature` before any downstream auth provider runs.\n\n`algorithm` is `hmac_sha256`, `ed25519`, or `ecdsa_p256_sha256`. `key` carries the shared secret (HMAC), the base64/hex-encoded raw 32-byte public key (Ed25519), or the base64/hex-encoded uncompressed SEC1 public point of 65 bytes (ECDSA P-256). `required_components` is the optional set of canonical components every accepted signature must cover. `clock_skew_seconds` defaults to 30s.\n\nA signature that covers `content-digest` also has its body checked: the proxy buffers the request body, recomputes the digest, and rejects a mismatch. The body must fit the 64 KiB replay buffer, and a larger one is rejected rather than passed unverified.\n\nSpec: <https://www.rfc-editor.org/rfc/rfc9421.html>.",
      "type": "object",
      "required": [
        "algorithm",
        "key",
        "key_id"
      ],
      "properties": {
        "verify": {
          "description": "Whether to enforce signature verification on inbound requests.",
          "default": false,
          "type": "boolean"
        },
        "algorithm": {
          "description": "Required signature algorithm (`hmac_sha256`, `ed25519`, or `ecdsa_p256_sha256`).",
          "type": "string"
        },
        "key_id": {
          "description": "The `keyid` value the signer is expected to advertise.",
          "type": "string"
        },
        "key": {
          "description": "Verification key material.",
          "type": "string"
        },
        "required_components": {
          "description": "Optional canonical components every accepted signature must cover.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "clock_skew_seconds": {
          "description": "Optional clock skew tolerance in seconds. Defaults to 30s.",
          "default": 30,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "OlpConfig": {
      "description": "`/introspect` (RFC 7662) is deferred to a follow-up PR because it requires a revocation / nonce store.",
      "type": "object",
      "required": [
        "issuer",
        "key_id",
        "signing_key"
      ],
      "properties": {
        "enabled": {
          "description": "Master toggle. When false the well-known endpoints 404.",
          "default": false,
          "type": "boolean"
        },
        "signing_key": {
          "description": "Ed25519 signing key, hex-encoded 32-byte seed. Operators generate one with `openssl rand -hex 32` or read it from a secret store. The secret-resolver pass honors provider-specific references at config-load time, mirroring how other key-material fields work.",
          "type": "string"
        },
        "key_id": {
          "description": "`kid` the JWS header advertises. Rotation appends a new key with a new kid and trusts both for the cutover window.",
          "type": "string"
        },
        "issuer": {
          "description": "Issuer URL stamped onto issued tokens (typically the proxy's public base URL such as `https://api.example.com`).",
          "type": "string"
        },
        "default_scope": {
          "description": "Default scope token list (space-separated, per RFC 8693 §2.2.1). Mints with `scope_override == None` use this value.",
          "default": "ai-input",
          "type": "string"
        },
        "default_ttl_secs": {
          "description": "Default TTL applied to issued tokens (seconds). Mints with `ttl_secs_override == None` use this value.",
          "default": 3600,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "content_key_seed": {
          "description": "WOR-808 PR8: optional Encrypted Media Standard content-key seed (hex-encoded). When set, every issued OLP token carries an RFC 7800 `cnf.jwk` claim with a per-token AES-256-GCM key derived via HKDF(seed, salt=jti, info=\"ems-content-key\"). Decryptors that retain the jti can recompute the key without storing the material. Absent leaves the cnf claim off the token so EMS-unaware clients keep working.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "introspect": {
          "description": "WOR-808 PR9: introspect / revoke surface. Absent leaves both `/.well-known/olp/introspect` (RFC 7662) and `/.well-known/olp/revoke` (RFC 7009) 404'd. Set to enable.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/OlpIntrospectConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "token_rate_limit_per_minute": {
          "description": "Tokens one source IP may mint per minute at `POST /.well-known/olp/token`. Defaults to 60.\n\nThat endpoint is unauthenticated by design: an RSL crawler following a `WWW-Authenticate: License` challenge has no credential yet, and a request carrying no `client_credentials` form falls back to an anonymous `sub`. Every call mints a fresh Ed25519-signed bearer license token. Without a bound, one source mints them at whatever rate the CPU allows, and the endpoint answers before authentication and before the policy chain where an origin's own rate limits live, so nothing else on the request path sees it (WOR-2673).\n\nThe budget is a token bucket keyed on the **raw socket peer**, not `X-Forwarded-For`. On an unauthenticated endpoint a forgeable header is not an identity: a caller that picks a new `X-Forwarded-For` per request would get a fresh full bucket every time and grow the tracking map while doing it. A deployment behind a load balancer therefore budgets the balancer, which is the honest reading of who the proxy is actually talking to.\n\nThis value is both the burst and the steady-state rate: the bucket holds this many tokens and refills at one sixtieth of it per second, the same shape [`OlpIntrospectConfig`]'s inactive-response limiter uses.\n\n`0` is refused at config compile. There is deliberately no \"unlimited\" setting: an unauthenticated mint endpoint whose bound is one typo away from being off is not bounded.",
          "default": 60,
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "OlpIntrospectConfig": {
      "description": "WOR-808 PR9: RFC 7662 OAuth Token Introspection + RFC 7009 Token Revocation configuration for the OLP issuer.\n\nWhen this block is present the proxy exposes:\n\n* `POST /.well-known/olp/introspect`: RFC 7662 §2 introspection. Returns `{ \"active\": true, ... }` for valid + un-revoked tokens issued by this origin's signing key, mirroring every OLP claim. Returns `{ \"active\": false }` for any token that does not verify, has expired, or has been revoked (§2.2 forbids leaking the reason). * `POST /.well-known/olp/revoke`: RFC 7009 §2. Writes the token's `jti` to the configured revocation store with a TTL that matches the token's remaining lifetime, so subsequent introspections return `active: false`.\n\nBoth endpoints share one `auth` policy because the same actor that can ask \"is this token active\" should also be able to assert \"this token is no longer trusted.\" Rate-limiting on `active: false` responses (RFC 7662 §2.1 scan-attack defense) and DPoP-bound confirmation checks ship in a follow-up PR.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Master toggle. When false the well-known endpoints 404 even if the rest of the block is configured. Lets an operator wire the auth + store ahead of time and flip it on later.",
          "default": false,
          "type": "boolean"
        },
        "introspect_path": {
          "description": "Path the introspection endpoint binds to. Defaults to `/.well-known/olp/introspect` so the OLP cluster of endpoints stays under one prefix.",
          "default": "/.well-known/olp/introspect",
          "type": "string"
        },
        "revoke_path": {
          "description": "Path the revocation endpoint binds to. Defaults to `/.well-known/olp/revoke`.",
          "default": "/.well-known/olp/revoke",
          "type": "string"
        },
        "auth": {
          "description": "Caller auth policy. Required for both endpoints; RFC 7662 §2.1 MUSTs \"some form of authorization\" to prevent token-scanning. Defaults to `mode: self` which uses the token-being-introspected as its own proof of possession (works without any operator configuration).",
          "default": {
            "mode": "self"
          },
          "allOf": [
            {
              "$ref": "#/definitions/OlpIntrospectAuth"
            }
          ]
        },
        "realm": {
          "description": "`Basic` realm advertised on 401 challenges. Defaults to `\"olp-introspect\"`; operators with multi-tenant deployments often want one realm per tenant for log clarity.",
          "default": "olp-introspect",
          "type": "string"
        },
        "revocation_store": {
          "description": "Revocation-store backend. Without a store, `/revoke` 503s and `/introspect` reports `active: true` for every otherwise-valid token (RFC 7662 §2.2's \"active\" is only signature + exp). The `memory` default is sufficient for a single-process dev box but does NOT survive restart; production deployments should pick `redb` or `redis`.",
          "default": {
            "backend": "memory"
          },
          "allOf": [
            {
              "$ref": "#/definitions/OlpRevocationStoreConfig"
            }
          ]
        },
        "mirror_cnf": {
          "description": "Whether to mirror the token's optional `cnf` (RFC 7800) confirmation claim onto the introspect response. Defaults to true so EMS-bound tokens carry their content key through to the relying party in one round trip; operators concerned about disclosing the key over a shared introspect connection can flip to false to require the RP to fetch the JWS directly.",
          "default": true,
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "OlpIntrospectAuth": {
      "description": "Auth policy for the introspect + revoke endpoints. Three modes:\n\n* `self` (default): the caller proves possession of the token by sending the same value in `Authorization: License <token>` *and* in the `token=` form parameter. Reasonable for the common \"RP introspects tokens it already holds\" case and requires no operator credential management. * `basic`: HTTP Basic with operator-managed credentials. Pass `{ username, password_hash }` pairs in `clients`; passwords are stored as Argon2id hashes. RFC 7662 §2.1's \"client authentication\" path. * `none`: no auth. ONLY appropriate for fully-private deployments behind a service mesh that already authenticates the caller. The proxy logs a `warn!` at startup when this is selected.",
      "oneOf": [
        {
          "description": "Caller proves possession of the token they are introspecting.",
          "type": "object",
          "required": [
            "mode"
          ],
          "properties": {
            "mode": {
              "type": "string",
              "enum": [
                "self"
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "HTTP Basic with operator-managed credentials.",
          "type": "object",
          "required": [
            "clients",
            "mode"
          ],
          "properties": {
            "mode": {
              "type": "string",
              "enum": [
                "basic"
              ]
            },
            "clients": {
              "description": "One entry per authorized caller. Empty list rejects every request with 401 so an operator cannot accidentally deploy `mode: basic` without setting up any credentials.",
              "type": "array",
              "items": {
                "$ref": "#/definitions/OlpIntrospectBasicClient"
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "No auth (private-deployment escape hatch).",
          "type": "object",
          "required": [
            "mode"
          ],
          "properties": {
            "mode": {
              "type": "string",
              "enum": [
                "none"
              ]
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "OlpIntrospectBasicClient": {
      "description": "One `mode: basic` credential.",
      "type": "object",
      "required": [
        "password_hash",
        "username"
      ],
      "properties": {
        "username": {
          "description": "Username sent over Basic auth.",
          "type": "string"
        },
        "password_hash": {
          "description": "Argon2id hash of the password (PHC string format, as produced by `argon2 -t 3 -m 65536 -p 4 -i`). The proxy verifies with the same parameters; supports secret references via the secret-resolver pass.",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "OlpRevocationStoreConfig": {
      "description": "Revocation-store backend selector.",
      "oneOf": [
        {
          "description": "Process-local, lost on restart. Default; appropriate for dev and CI only.",
          "type": "object",
          "required": [
            "backend"
          ],
          "properties": {
            "backend": {
              "type": "string",
              "enum": [
                "memory"
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "On-disk redb file. Single-process; survives restart, ACID. Production default for single-replica deployments.",
          "type": "object",
          "required": [
            "backend",
            "path"
          ],
          "properties": {
            "backend": {
              "type": "string",
              "enum": [
                "redb"
              ]
            },
            "path": {
              "description": "Filesystem path to the redb file. The path is created on first use; the operator MUST ensure the directory is writable by the proxy user.",
              "type": "string"
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Redis (shared across replicas). Use for horizontally-scaled deployments where a token revoked on one replica must be observed by all the others.",
          "type": "object",
          "required": [
            "backend",
            "url"
          ],
          "properties": {
            "backend": {
              "type": "string",
              "enum": [
                "redis"
              ]
            },
            "url": {
              "description": "`redis://` connection URL. Pool size and timeouts inherit the workspace `redis` defaults.",
              "type": "string"
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "CompMarketplaceConfig": {
      "description": "WOR-2673: IAB Content Authorization Marketplace Protocol (CoMP) bridge, on a per-origin basis.\n\nCoMP is the discovery-and-purchase front door to the license tokens the origin's `olp:` block already issues. It publishes a signed catalog of licensing tiers, prices a buyer's requested volume into a signed quote, and converts a signed, paid acceptance of that quote into an OLP license token. Three well-known endpoints:\n\n* `GET  /.well-known/iab-comp/manifest.json` * `POST /.well-known/iab-comp/quote` * `POST /.well-known/iab-comp/redeem`\n\nYAML key: `origins.<host>.comp`. See `docs/comp-marketplace.md`.\n\nThe bridge does not carry its own OLP issuer. It signs the license token it returns with the same key `origins.<host>.olp.signing_key` names, under the same `kid`, so the token verifies against the origin's own `/.well-known/olp/introspect` with no extra trust configuration. `olp.enabled: true` on the same origin is therefore required, and the config compiler refuses the block without it.",
      "type": "object",
      "required": [
        "master_key",
        "publisher",
        "rotation_id"
      ],
      "properties": {
        "enabled": {
          "description": "Master toggle. When false the three well-known endpoints 404.",
          "default": false,
          "type": "boolean"
        },
        "master_key": {
          "description": "Master key the CoMP quote-signing key is derived from, as a hex- or base64-encoded value of at least 32 bytes.\n\nResolved like every other config secret: a provider URI (`secret://backend/name`, `vault://...`), a `file:/path` reference, or a whole-value `${ENV_VAR}`. HKDF-SHA256 expands it into one Ed25519 signing key per [`Self::rotation_id`], so rotation is a label change rather than the minting and distribution of a new raw seed.\n\nThis is not the OLP token key. Quote signatures live in their own `comp-...` kid namespace precisely so a quote signature can never be replayed as a license token.",
          "type": "string"
        },
        "rotation_id": {
          "description": "Rotation label the active quote-signing key is derived under, for example `2026-q3-001`. Bumping it derives and publishes a new `comp-<rotation_id>` kid; the previous one keeps verifying until the process restarts.",
          "type": "string"
        },
        "publisher": {
          "description": "Publisher identity the manifest advertises.",
          "allOf": [
            {
              "$ref": "#/definitions/CompPublisherConfig"
            }
          ]
        },
        "tiers": {
          "description": "Licensing tiers the manifest publishes. At least one is required, and at least one must be `authorization: olp`, since that is the only kind `redeem` can mint a token for.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/CompTierConfig"
          }
        },
        "buyer_keys": {
          "description": "Buyer verification keys, by `kid`. A redeem request is signed by the buyer and refused unless its `kid` resolves here, so this list is the onboarding boundary: a buyer whose key is absent cannot redeem anything at any price.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/CompBuyerKeyConfig"
          }
        },
        "manifest_hash": {
          "description": "SHA-256 of the canonical JSON of the manifest, in `sha256:<hex>` form, published in the manifest itself.\n\nComputed over the manifest with this field cleared when it is omitted, which is what the CoMP field is specified to be. Set it explicitly only when an out-of-band process (a signed catalog feed, a marketplace aggregator) owns the value.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "allow_unknown_quotes": {
          "description": "Honor a redeem whose `quote_id` this process never issued. Defaults to `false`.\n\nThe bridge keeps its issued-quote ledger in memory, matching the no-external-store rule this port ships under, so a reload or a restart forgets every quote it signed. With the default, a buyer holding a quote from before that restart is refused and has to ask for a new one, which costs it one round trip.\n\nTurning this on removes that refusal, and removes with it the only thing standing between an onboarded buyer key and a token per call with a fabricated `quote_id`, no quote, and no price. A deployment that turns it on should be running a shared revocation denylist, because that check is then the only durable one left. See `docs/comp-marketplace.md`.",
          "default": false,
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "CompPublisherConfig": {
      "description": "Publisher identity block of the CoMP manifest.",
      "type": "object",
      "required": [
        "contact",
        "name"
      ],
      "properties": {
        "name": {
          "description": "Legal or trading name of the publisher.",
          "type": "string"
        },
        "contact": {
          "description": "Licensing contact address a buyer can reach a human at.",
          "type": "string"
        },
        "verified_at": {
          "description": "RFC 3339 timestamp of the publisher's marketplace verification, when a marketplace has issued one. Advisory; nothing in the proxy checks it.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "CompTierConfig": {
      "description": "One licensing tier in the CoMP manifest.",
      "type": "object",
      "required": [
        "authorization",
        "description",
        "id",
        "license",
        "name",
        "pricing",
        "route_glob",
        "shape"
      ],
      "properties": {
        "id": {
          "description": "Stable tier identifier a buyer names in a quote request.",
          "type": "string"
        },
        "name": {
          "description": "Display name.",
          "type": "string"
        },
        "description": {
          "description": "One-line description of what the tier licenses.",
          "type": "string"
        },
        "license": {
          "description": "License URN this tier grants, stamped into the minted token's `license_urn` claim.",
          "type": "string"
        },
        "shape": {
          "description": "Content shape: `html`, `json-envelope`, or `bulk-archive`.",
          "type": "string"
        },
        "authorization": {
          "description": "Acquisition flow: `public`, `cap`, or `olp`. Only `olp` tiers can be redeemed for a license token.",
          "type": "string"
        },
        "route_glob": {
          "description": "Route allow-list glob echoed to the buyer. Advisory: the marketplace bridge publishes it, and the origin's own policies enforce access.",
          "type": "string"
        },
        "pricing": {
          "description": "Pricing block.",
          "allOf": [
            {
              "$ref": "#/definitions/CompTierPricingConfig"
            }
          ]
        },
        "rate_caps": {
          "description": "Rate ceiling advertised for the tier.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/CompRateCapsConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "CompTierPricingConfig": {
      "description": "Pricing block of a CoMP tier.",
      "type": "object",
      "required": [
        "currency",
        "model"
      ],
      "properties": {
        "model": {
          "description": "`free`, `per_request`, or `flat_rate`.",
          "type": "string"
        },
        "currency": {
          "description": "ISO 4217 currency code.",
          "type": "string"
        },
        "amount": {
          "description": "Whole-unit amount. For `free` and `flat_rate`.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "amount_micros": {
          "description": "Micro-unit amount (millionths of a currency unit). For `per_request`.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "CompRateCapsConfig": {
      "description": "Rate ceiling advertised for a CoMP tier.",
      "type": "object",
      "required": [
        "max_bytes_per_day",
        "max_rps"
      ],
      "properties": {
        "max_rps": {
          "description": "Maximum requests per second.",
          "type": "number",
          "format": "double"
        },
        "max_bytes_per_day": {
          "description": "Maximum bytes per day.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "CompBuyerKeyConfig": {
      "description": "One onboarded buyer's verification key.",
      "type": "object",
      "required": [
        "kid",
        "public_key"
      ],
      "properties": {
        "kid": {
          "description": "Key id the buyer names in its redeem signature header.",
          "type": "string"
        },
        "public_key": {
          "description": "Ed25519 public key, base64url without padding, 32 bytes decoded. Public by definition, so it carries no secret and is not resolved through the secret layer.",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "WebBotAuthPublishConfig": {
      "description": "WOR-808 PR7: Open License Protocol (OLP) issuer configuration.\n\nWhen set, the proxy stands up two well-known endpoints on the origin:\n\n- `POST /.well-known/olp/token` issues a license token signed with the configured Ed25519 key, body shaped per RFC 6749 (`access_token` + `token_type: \"License\"` + `expires_in`). - `GET /.well-known/olp/key` publishes the verification JWK set (RFC 7517) so external introspectors can verify tokens without contacting the issuer per-token.\n\nWOR-805 AC#4: Web Bot Auth publish config. When enabled the proxy serves two unauthenticated well-known endpoints on this origin:\n\n* `GET /.well-known/http-message-signatures-directory`: JWKS document carrying SBproxy's own Ed25519 signing-key public key. Verifiers (Cloudflare, AWS WAF, any third-party origin that runs a Web Bot Auth verifier) fetch this to verify the `Signature-Input` + `Signature` headers SBproxy attaches to outbound requests. * `GET /.well-known/web-bot-auth/agent-card`: the discovery document that points verifiers at the directory; carries the operator-facing agent name, description, and contact URL.\n\n`public_key_hex` is the 32-byte Ed25519 public key, hex-encoded (the SECRET side never crosses this config; the signer that runs upstream of the proxy holds it). `key_id` is the `kid` the signer advertises and the directory JWK publishes.\n\nOperators who do not configure this block expose neither endpoint; requests to those paths fall through to the upstream proxy (or return 404 if no route matches).",
      "type": "object",
      "required": [
        "agent_name",
        "directory_url",
        "key_id",
        "public_key_hex"
      ],
      "properties": {
        "enabled": {
          "description": "Whether the publish endpoints are enabled.",
          "default": false,
          "type": "boolean"
        },
        "key_id": {
          "description": "`kid` the JWK + Signature Agent Card advertise. Stable across rotations of unrelated keys so an old `keyid=` reference in a signed request still resolves.",
          "type": "string"
        },
        "public_key_hex": {
          "description": "Ed25519 public key, hex-encoded (32 bytes → 64 hex chars).",
          "type": "string"
        },
        "agent_name": {
          "description": "Operator-facing agent name on the Signature Agent Card.",
          "type": "string"
        },
        "directory_url": {
          "description": "`directory_url` the agent card points at. Must be `https://`; every Web Bot Auth verifier rejects plaintext.",
          "type": "string"
        },
        "description": {
          "description": "Optional description shown alongside the agent name.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "contact_url": {
          "description": "Optional contact URL (mailto:, https://, etc.) for misuse reports.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "signing_key_hex": {
          "description": "Optional 32-byte Ed25519 private seed, hex-encoded (64 hex chars). When set, the directory and agent-card HTTP responses are self-signed per RFC 9421 over `(\"content-digest\")` with `tag=\"web-bot-auth\"` and `keyid=key_id`. Verifiers can then confirm the body they fetched was emitted by the holder of the key the directory advertises, closing the trust loop without relying solely on TLS. Absent leaves the responses unsigned; the Web Bot Auth IETF draft permits both shapes and verifiers MAY treat unsigned directories as lower-trust. The secret-resolver pass honors secret references at config load so the raw seed never has to live in the YAML.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "IdempotencyConfig": {
      "description": "`Idempotency-Key` middleware configuration, per `draft-ietf-httpapi-idempotency-key-header`.\n\nWhen `enabled`, the proxy reads an idempotency key from the configured request header (default: `Idempotency-Key`), hashes the request body for conflict detection, and serves cached responses on retries. Workspace-isolated keys mean two workspaces using the same key never collide. The middleware engages only on the listed HTTP methods; defaults to POST, PUT, and PATCH.\n\nBacked by `sbproxy_middleware::idempotency` (the cache backend trait + memory / Redis impls). For Redis-backed clusters set `backend: redis`; the cache binds to the cluster L2 store at compile time. Single-instance deployments leave `backend: memory` (the default).",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Whether to engage the idempotency middleware on this origin. Defaults to false; opt in per origin.",
          "default": false,
          "type": "boolean"
        },
        "header_name": {
          "description": "Request header name carrying the idempotency key. Defaults to `Idempotency-Key`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "ttl_secs": {
          "description": "Time-to-live for cached entries, in seconds. Defaults to 86400 (24 hours).",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "methods": {
          "description": "HTTP methods the middleware engages on. Defaults to `[POST, PUT, PATCH]`. Other methods (idempotent by HTTP spec) pass through unaffected.",
          "default": null,
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        },
        "backend": {
          "description": "Cache backend selector. `memory` (the default) uses an in-process LRU; `redis` binds to the cluster L2 store at compile time. Operators who set `redis` without configuring `proxy.l2_store` get an error at config-validate time.",
          "default": "memory",
          "allOf": [
            {
              "$ref": "#/definitions/IdempotencyBackend"
            }
          ]
        },
        "max_request_body_bytes": {
          "description": "Maximum request body size in bytes that the middleware will buffer for the cache check. Requests larger than this cap gracefully degrade: the middleware skips caching for that request and stamps `x-sbproxy-idempotency: SKIPPED-OVERSIZE-REQUEST` on the response so operators can see the skip. Defaults to 1 MiB.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "max_response_body_bytes": {
          "description": "Maximum response body size in bytes that will be buffered for caching. Responses larger than this cap stream to the client uncached; the next retry with the same key falls through to the upstream. Defaults to 1 MiB.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "max_concurrent_buffers": {
          "description": "Process-wide cap on the number of concurrent buffered idempotency requests *for this origin*. When the pool is exhausted, new requests skip caching and stream normally; `x-sbproxy-idempotency: SKIPPED-POOL-FULL` is stamped so operators can spot pool pressure. Defaults to 256, which at the default per-request cap gives a 256 MiB worst-case memory budget per origin.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        },
        "claim_lease_secs": {
          "description": "How long the request that took an idempotency key holds it before another request may take it over, in seconds. Defaults to 60.\n\nThis is the bound on how long a request that died mid-flight can wedge one key, and nothing else. Raise it above the slowest response this origin produces: a lease that runs out while its owner is still working lets a retry through to the upstream, which is the duplicate call the middleware exists to prevent. An origin fronting an AI completion or a payment with a 3DS step-up wants a larger value. Lowering it makes a crashed request's key free sooner and makes duplicates more likely; a request that ends cleanly releases its key immediately either way, so the lease only governs the requests that did not.\n\nA response is stored under `ttl_secs`, which is hours, not under this. The two lifetimes are unrelated: an upstream slower than the lease still caches its response.\n\nZero is refused at config-validate time rather than silently normalized, because a zero lease expires the instant it is taken and would turn single-flight off without saying so.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "claim_wait_ms": {
          "description": "How long an overlapping request waits for the key holder's response before answering 409 `ledger.idempotency_in_flight`, in milliseconds. Defaults to 3000.\n\nA retry that arrives while the original request is still running waits this long and then replays the original's response, so the client sees its answer rather than an error. Set it to 0 to answer 409 immediately, which is the floor `draft-ietf-httpapi-idempotency-key-header` describes. Raising it past the client's own timeout buys nothing: the client gives up first and the wait is abandoned.\n\nA waiting request holds a slot in a pool sized by `max_concurrent_buffers`, separate from the buffering pool, so a long wait cannot spend the slots other keys need.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "IdempotencyBackend": {
      "description": "Cache backend for [`IdempotencyConfig`].",
      "oneOf": [
        {
          "description": "In-process cache. Default. Suitable for single-instance deployments and per-replica idempotency in clusters where retries land on the same replica.",
          "type": "string",
          "enum": [
            "memory"
          ]
        },
        {
          "description": "Cluster-wide cache backed by the shared L2 store (`proxy.l2_store`). Required for clusters where retries may land on different replicas.",
          "type": "string",
          "enum": [
            "redis"
          ]
        }
      ]
    },
    "ConnectionPoolConfig": {
      "description": "Legacy per-origin connection-pool shape.\n\nOne field is live. `idle_timeout_secs` is the legacy spelling of the upstream idle deadline and feeds the origin's resolved [`UpstreamTimeouts`] when `timeouts.idle_ms` is not set.\n\nThe other two parse and are then refused at config compile. They are retained as `Option` rather than deleted outright so an archived schema-v1 document reaches an explanatory diagnostic instead of an unknown-key error, the same call `proxy.messenger_settings` took.\n\nNeither has a Pingora primitive behind it in this build. `pingora_core::upstreams::peer::PeerOptions`, the per-peer struct `proxy_http.rs` tunes, carries no pool-size and no maximum-lifetime field. The only pool-size knob in the vendored fork is `ConnectorOptions::keepalive_pool_size`, which is set once per connector from the server config and so cannot express a per-origin limit, and `pingora-pool` has no age-based eviction at all. Wiring either one is a change to Pingora, not a change here.",
      "type": "object",
      "properties": {
        "max_connections": {
          "description": "Refused at config compile. Pingora sizes the upstream keepalive pool per connector, not per origin, so this never bounded anything.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0.0
        },
        "idle_timeout_secs": {
          "description": "Maximum idle time before a pooled upstream connection is closed, in seconds.\n\nLegacy spelling of `timeouts.idle_ms`; setting both fails config compile. Default: 90 s.",
          "default": 90,
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "max_lifetime_secs": {
          "description": "Refused at config compile. Pingora's connection pool has no age-based eviction, so no upstream connection was ever retired on this deadline.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "UpstreamTimeoutsConfig": {
      "description": "Per-origin upstream timeout overrides.\n\nEvery field is optional. An absent field resolves to the matching `DEFAULT_UPSTREAM_*` constant at config compile time, so the request path always reads a concrete [`UpstreamTimeouts`]. A value of `0` fails config compile: a zero deadline fails the operation the moment it starts and is never what an operator meant.",
      "type": "object",
      "properties": {
        "connect_ms": {
          "description": "Deadline for one upstream TCP connect attempt, in milliseconds. Default: 5000 (5 s).",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "total_connect_ms": {
          "description": "Deadline across all connect attempts for one upstream selection, including TLS, in milliseconds. Default: 10000 (10 s).",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "read_ms": {
          "description": "Per-read socket deadline on the upstream connection, in milliseconds. Default: 30000 (30 s).",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "write_ms": {
          "description": "Per-write socket deadline on the upstream connection, in milliseconds. Default: 30000 (30 s).",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "idle_ms": {
          "description": "Idle time before a pooled upstream connection is closed, in milliseconds. Service discovery caps the effective value at half the DNS refresh window. Default: 90000 (90 s).",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "AgentSkillEntry": {
      "description": "One entry in an origin's `agent_skills:` advertisement.\n\nThe shape mirrors the v0.2.0 manifest entry described at `https://schemas.agentskills.io/discovery/0.2.0/schema.json`: every entry carries a stable name, a kind discriminator, a human description, and the URL the agent fetches to retrieve the artifact. A `digest` field is computed at config-load time by hashing the resolved artifact bytes; the per-request handler re-hashes the body on every serve and refuses to ship a tampered artifact.\n\nThe optional safety knobs (`max_decompression_ratio`, `max_entries`, `max_expanded_bytes`, `max_clock_skew_secs`) cap archive parsing so a malicious origin cannot zip-bomb a downstream agent. All four have sensible defaults, and v1 configs that omit `agent_skills:` pay nothing for the new schema field.",
      "type": "object",
      "required": [
        "description",
        "name",
        "type",
        "url"
      ],
      "properties": {
        "name": {
          "description": "Stable identifier (used as the manifest `name` and as the audit-event subject). Must be unique within the origin's `agent_skills:` list.",
          "type": "string"
        },
        "type": {
          "description": "Discriminator: `skill-md` for a single Markdown body or `archive` for a `.tar.gz` / `.zip` bundle. The v0.2.0 spec reserves these two values; any other input is rejected at config-load time.",
          "type": "string"
        },
        "description": {
          "description": "Human-readable description surfaced in the manifest. Reads like a one-line capability summary.",
          "type": "string"
        },
        "url": {
          "description": "URL the agent fetches to retrieve the artifact. May be:\n\n- A path-absolute reference (`/skills/foo.md`) re-hosted by the proxy on the same origin. - A relative reference (`skills/foo.md`), which names the same artifact as the path-absolute spelling and resolves the same way. - A fully-qualified URL fetched once at config-load and re-emitted verbatim in the manifest (the proxy does not re-host external artifacts).\n\nThe two re-hosted spellings resolve against the path prefix the serving surface answers on, not against the manifest's own directory: the bare path for this `agent_skills:` block, and `/.well-known/agent-skills/<listing>/` for the same entry shape under a Listing's `spec.skills[]`.",
          "type": "string"
        },
        "visibility": {
          "description": "Visibility gate. `public` (default) returns the entry to every caller. `authenticated` filters the entry out of the manifest served to anonymous callers; the proxy still recomputes the digest per request so caching does not leak filtered entries.",
          "default": "public",
          "type": "string"
        },
        "path": {
          "description": "Local filesystem path to the artifact body, when the operator hosts the file alongside the config. Used for `skill-md` entries with a path-absolute or relative `url`. When neither `path` nor `body` is set and `url` is path-absolute, the compiler resolves the path relative to the workspace root.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "body": {
          "description": "Inline literal body. Useful for short skill files without having to commit a separate Markdown file. Mutually exclusive with `path`; when both are set the compiler prefers `path`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "max_decompression_ratio": {
          "description": "Maximum decompression ratio (compressed:expanded) tolerated for `archive` entries. Default 100. Refuses to extract archives whose total expanded size exceeds the cap.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0.0
        },
        "max_entries": {
          "description": "Maximum entry count per archive. Default 1000.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0.0
        },
        "max_expanded_bytes": {
          "description": "Maximum expanded byte budget per archive. Default 10 MiB.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        },
        "max_clock_skew_secs": {
          "description": "Per-entry clock-skew tolerance in seconds for any time-sensitive header attached to the artifact response. Default 60. Reserved: the v0.2.0 ship attaches no such header today; the field exists so a follow-up that signs each artifact body can wire its own freshness check without a config-schema break.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "AgentsJsonConfig": {
      "description": "Per-origin agents.json manifest configuration (WOR-820). See the [`RawOriginConfig::agents_json`] field and the agents.json v0.1 spec at <https://github.com/wild-card-ai/agents-json>.",
      "type": "object",
      "required": [
        "info"
      ],
      "properties": {
        "info": {
          "description": "`info` block (title, version, description).",
          "allOf": [
            {
              "$ref": "#/definitions/AgentsJsonInfo"
            }
          ]
        },
        "sources": {
          "description": "API sources. When omitted, the proxy emits a single source pointing at this origin's `/.well-known/openapi.json`. Each entry must carry an `id` and a `path` per the spec.",
          "default": null,
          "type": [
            "array",
            "null"
          ],
          "items": true
        },
        "flows": {
          "description": "Operator-authored flow objects, emitted verbatim. Each flow must be schema-valid (`id`, `title`, `description`, `actions`, `fields`); the proxy does not synthesize flows.",
          "default": [],
          "type": "array",
          "items": true
        },
        "overrides": {
          "description": "Optional `overrides` array, emitted verbatim when present.",
          "default": null,
          "type": [
            "array",
            "null"
          ],
          "items": true
        }
      },
      "additionalProperties": false
    },
    "AgentsJsonInfo": {
      "description": "The `info` block of an agents.json manifest.",
      "type": "object",
      "required": [
        "title",
        "version"
      ],
      "properties": {
        "title": {
          "description": "Human-readable manifest title.",
          "type": "string"
        },
        "version": {
          "description": "Manifest version string.",
          "type": "string"
        },
        "description": {
          "description": "Manifest description.",
          "default": "",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "OutboundCredentialSchema": {
      "description": "Schema-only mirror of the deferred outbound credential enum. Runtime parsing remains in `sbproxy-modules`; this keeps generated editor tooling precise without introducing a crate dependency cycle.",
      "oneOf": [
        {
          "type": "object",
          "required": [
            "audience",
            "token_endpoint",
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "token_exchange"
              ]
            },
            "token_endpoint": {
              "type": "string"
            },
            "audience": {
              "type": "string"
            },
            "scope": {
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "subject_token_issuers": {
              "default": [],
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "allowed_audiences": {
              "default": [],
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "act_depth_cap": {
              "default": 4,
              "type": "integer",
              "format": "uint",
              "minimum": 0.0
            },
            "client_id": {
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "client_secret": {
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "dpop": {
              "anyOf": [
                {
                  "$ref": "#/definitions/OutboundDpopSchema"
                },
                {
                  "type": "null"
                }
              ]
            }
          }
        },
        {
          "type": "object",
          "required": [
            "client_id",
            "client_secret",
            "token_endpoint",
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "client_credentials"
              ]
            },
            "token_endpoint": {
              "type": "string"
            },
            "client_id": {
              "type": "string"
            },
            "client_secret": {
              "type": "string"
            },
            "scope": {
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "audience": {
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "dpop": {
              "anyOf": [
                {
                  "$ref": "#/definitions/OutboundDpopSchema"
                },
                {
                  "type": "null"
                }
              ]
            }
          }
        },
        {
          "type": "object",
          "required": [
            "secret",
            "type"
          ],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "vault_secret"
              ]
            },
            "secret": {
              "type": "string"
            },
            "header": {
              "default": "authorization",
              "type": "string"
            },
            "scheme": {
              "default": "Bearer",
              "type": "string"
            },
            "dpop": {
              "anyOf": [
                {
                  "$ref": "#/definitions/OutboundDpopSchema"
                },
                {
                  "type": "null"
                }
              ]
            }
          }
        }
      ]
    },
    "OutboundDpopSchema": {
      "type": "object",
      "required": [
        "alg",
        "jwk",
        "key"
      ],
      "properties": {
        "key": {
          "description": "Existing provider URI or `file:` secret reference. Inline PEM is rejected and SBproxy never generates this key.",
          "type": "string"
        },
        "jwk": {
          "description": "Public-only JWK matching the referenced private key."
        },
        "alg": {
          "description": "Asymmetric signing algorithm accepted for RFC 9449 proofs.",
          "allOf": [
            {
              "$ref": "#/definitions/OutboundDpopAlgorithmSchema"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "OutboundDpopAlgorithmSchema": {
      "type": "string",
      "enum": [
        "ES256",
        "ES384",
        "RS256",
        "RS384",
        "RS512",
        "PS256",
        "PS384",
        "PS512",
        "EdDSA"
      ]
    },
    "OriginAttestationConfig": {
      "description": "`origins.<host>.attestation`: per-origin attestation overrides.\n\nTwo fields, and they are here for different reasons. `role` is an override, because one gateway commonly fronts both a partner API it resells (claims) and its own service (receipts). `agreement_id` has no proxy-wide equivalent at all: it names the commercial agreement the units are billed under, and that is a property of who is on the other end of the connection, never of the proxy.",
      "type": "object",
      "properties": {
        "role": {
          "description": "Narrows or widens `proxy.attestation.role` for this origin. Absent inherits the proxy-wide role.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/AttestationRole"
            },
            {
              "type": "null"
            }
          ]
        },
        "agreement_id": {
          "description": "The commercial agreement this origin's units are billed under. Without it a receipt says how much was consumed but not which contract turns that into money.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "OriginObservabilityConfig": {
      "description": "Origin-scope observability sub-tree. The `log.redact` block, `log.sinks` (origin-scoped fan-out, filtered by the stamped `route`), and `log.custom_fields` (the most-specific access-log fields, which override tenant- and proxy-scope fields of the same name) are all consumed at runtime.",
      "type": "object",
      "properties": {
        "log": {
          "description": "Origin-scope log block. See [`OriginObservabilityLogConfig`].",
          "default": {
            "redact": {},
            "sinks": [],
            "custom_fields": []
          },
          "allOf": [
            {
              "$ref": "#/definitions/OriginObservabilityLogConfig"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "OriginObservabilityLogConfig": {
      "description": "Origin-scope `log:` sub-block. Mirrors the proxy-scope and tenant-scope shape; exposes redaction plus origin-scoped sinks (WOR-1045 PR2). The dispatcher routes every record whose stamped `route` matches this origin's hostname into each declared sink.",
      "type": "object",
      "properties": {
        "decision_audit": {
          "description": "Scoped `decision_audit:` additions, composed per event label against the wider scopes rather than replacing them.\n\nThe same shape as the proxy-scope block. Composition is per key, matching `custom_fields:`: a scope naming one event must not silence the events a wider scope turned on, because that would make enabling one tenant's routing audit quietly disable its cache audit. Precedence for a given event is origin, then tenant, then proxy, and an event no scope names is off.\n\n`enabled:` composes the same way: the most specific scope that sets it wins for the events nobody names explicitly.",
          "anyOf": [
            {
              "$ref": "#/definitions/DecisionAuditConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "redact": {
          "description": "Origin-scope `redact:` sub-block. See [`OriginObservabilityRedactConfig`].",
          "default": {},
          "allOf": [
            {
              "$ref": "#/definitions/OriginObservabilityRedactConfig"
            }
          ]
        },
        "sinks": {
          "description": "Origin-scoped log sinks. Each sink's `name` is unique within this origin; cross-scope collisions with a tenant or proxy `sinks:` entry are intentional (they are different sinks). Sinks at this scope default to the `external` redaction profile.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/ObservabilitySinkConfig"
          }
        },
        "custom_fields": {
          "description": "Origin-scoped custom access-log fields. Same shape as the proxy-scope `custom_fields:`. A field defined here is the most specific: it overrides a tenant- or proxy-scope field with the same `name` for requests routed to this origin.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/CustomLogFieldConfig"
          }
        }
      },
      "additionalProperties": false
    },
    "OriginObservabilityRedactConfig": {
      "description": "Origin-scope `redact:` sub-block. Carries the per-origin overrides for the field-key denylist (WOR-1042 `fields:`, additive), the operator regex pass (WOR-1042 `patterns:` + `disable:`), and the rule-driven PII redactor (WOR-1043 `pii:`).",
      "type": "object",
      "properties": {
        "fields": {
          "description": "WOR-1042: origin-scope additions to the field-key denylist. Additive only on top of the merged proxy + tenant set; an origin cannot disable a parent denylist entry.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "patterns": {
          "description": "WOR-1042: origin-scope additions to the regex pattern set. Additive on top of proxy + tenant. Use `disable:` to opt out of a more-general pattern by name.",
          "type": "array",
          "items": {
            "$ref": "#/definitions/ObservabilityRedactPattern"
          }
        },
        "disable": {
          "description": "WOR-1042: pattern names to opt out of at this origin. Resolved against the merged proxy + tenant pattern set; the built-in field-key denylist is never disable-able.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "pii": {
          "description": "Origin-scope override for the tenant-scope (or proxy-scope when the origin has no tenant) PII pass. Resolution rules: the origin inherits the parent scope's `enabled` flag and rule set, then ADDS its own `rules:` entries and SUBTRACTS its own `disable:` entries. An explicit `enabled: false` opts the origin out even when parent scopes enable PII. See [`ObservabilityPiiConfig`].",
          "anyOf": [
            {
              "$ref": "#/definitions/ObservabilityPiiConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "AccessLogConfig": {
      "description": "Structured-JSON access-log emission, off by default.\n\nWhen `enabled` is true, every completed request emits one JSON line via the tracing `access_log` target after status, method, and sampling filters are applied. The actual record shape is `AccessLogEntry` in `sbproxy-observe`; this struct only governs whether and which records are emitted.\n\nFilter semantics: - `status_codes` empty matches every status; non-empty restricts to the listed codes. - `methods` empty matches every method; non-empty restricts to the listed methods (case-insensitive on emit). - `sample_rate` is applied last and accepts a value in `[0.0, 1.0]`.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Master switch. When false (the default), no access-log lines are emitted regardless of the other fields.",
          "default": false,
          "type": "boolean"
        },
        "sample_rate": {
          "description": "Probability in `[0.0, 1.0]` that a matching request is logged. Defaults to `1.0` (log every match).",
          "default": 1.0,
          "type": "number",
          "format": "double"
        },
        "status_codes": {
          "description": "HTTP status codes to log. Empty (the default) means log all statuses.",
          "default": [],
          "type": "array",
          "items": {
            "type": "integer",
            "format": "uint16",
            "minimum": 0.0
          }
        },
        "methods": {
          "description": "HTTP methods to log. Empty (the default) means log all methods. Comparison is case-insensitive.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "capture_headers": {
          "description": "Header capture configuration. Off by default; opt in by listing header names (or `*`) in `request` / `response`. Captured values land in the `request_headers` / `response_headers` fields of the emitted entry.",
          "default": {
            "request": [],
            "response": [],
            "max_value_bytes": 1024,
            "redact_pii": false,
            "redact_pii_rules": [],
            "redact_pii_other_fields": false
          },
          "allOf": [
            {
              "$ref": "#/definitions/CaptureHeadersConfig"
            }
          ]
        },
        "slow_request_threshold_ms": {
          "description": "Log every request at or above this latency, regardless of `sample_rate`. `None` preserves sampler-only behavior.",
          "default": null,
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "always_log_errors": {
          "description": "Log every 5xx response regardless of `sample_rate`.",
          "default": false,
          "type": "boolean"
        },
        "output": {
          "description": "Output sink. Defaults to stderr/tracing target.",
          "default": {
            "type": "stderr",
            "path": null,
            "max_size_mb": 100,
            "max_backups": 7,
            "compress": false
          },
          "allOf": [
            {
              "$ref": "#/definitions/AccessLogOutputConfig"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "CaptureHeadersConfig": {
      "description": "Allowlist-driven header capture for the access log.\n\nLists are matched after lowercasing both the configured names and the inbound header names. Two pattern shapes are accepted:\n\n* Exact name (`\"user-agent\"`, `\"x-cache\"`). * `\"*\"` to capture every header (subject to the sensitive-header denylist below). * Trailing-glob (`\"x-ratelimit-*\"`) to capture every header whose name starts with the prefix before the `*`. Only one trailing `*` is supported; embedded wildcards are treated as literal.\n\nA hardcoded denylist of sensitive headers (`authorization`, `cookie`, `set-cookie`, `proxy-authorization`, `x-api-key`) is excluded from `*` and glob matches. To capture one of these, list it by exact name; the proxy logs a `WARN` at config load so the choice is visible.",
      "type": "object",
      "properties": {
        "request": {
          "description": "Request-side allowlist. Empty (the default) captures nothing.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "response": {
          "description": "Response-side allowlist. Empty (the default) captures nothing.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "max_value_bytes": {
          "description": "Per-value byte cap. Captured header values longer than this are truncated to the cap with a trailing `\"...\"` (the suffix counts toward the cap). Defaults to 1024.",
          "default": 1024,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "redact_pii": {
          "description": "Run the `sbproxy-security` PII redactor over captured header values. Off by default. Independent from the cheap secret-key scan that already runs over every emitted access-log line.",
          "default": false,
          "type": "boolean"
        },
        "redact_pii_rules": {
          "description": "Optional rule-name filter for `redact_pii`. Empty (the default) uses the full built-in rule set; non-empty restricts to the listed rule names (`email`, `us_ssn`, `credit_card`, `phone_us`, `ipv4`, `openai_key`, `anthropic_key`, `aws_access`, `github_token`).\n\nThe same rule list is shared by [`Self::redact_pii_other_fields`] when that knob is on, so operators only configure one rule list for both the header scope and the non-header scope.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "redact_pii_other_fields": {
          "description": "Run the same `sbproxy-security` PII redactor over the non-header access-log fields that can carry PII: `path`, `user_id`, `properties` values (keys are left untouched), and `model`. Off by default. Independent of [`Self::redact_pii`] so operators can adopt header redaction first and the broader scope later (or either alone). Reuses [`Self::redact_pii_rules`] for the rule filter; the cheap `redact_secrets` pass over the full JSON line still runs regardless of this knob.",
          "default": false,
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "AccessLogOutputConfig": {
      "description": "Access-log output sink.",
      "type": "object",
      "properties": {
        "type": {
          "description": "Sink type: `stderr` (default) or `file`.",
          "default": "stderr",
          "type": "string"
        },
        "path": {
          "description": "File path when `type: file`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "max_size_mb": {
          "description": "Rotate before writing when the file is at least this size.",
          "default": 100,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "max_backups": {
          "description": "Number of rotated backups to retain.",
          "default": 7,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "compress": {
          "description": "Gzip rotated files.",
          "default": false,
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "AgentClassesConfig": {
      "description": "Top-level `agent_classes:` block. Tunes the agent-class resolver the binary constructs at startup and threads through the request pipeline.\n\nThe block is fully optional: when absent the binary builds the resolver from `AgentClassCatalog::defaults()` plus the default resolver tuning. Most operators leave it untouched.",
      "type": "object",
      "properties": {
        "catalog": {
          "description": "Catalog source. `builtin` (default) loads the embedded YAML and `inline` loads `entries`. The compatibility values `hosted-feed` and `merged` currently warn and fall back to the embedded defaults; the OSS runtime does not fetch `hosted_feed.url`.",
          "default": "builtin",
          "type": "string"
        },
        "entries": {
          "description": "Inline catalog entries. Used when `catalog: inline`; each entry is validated by the runtime against the same schema as the embedded catalog.",
          "type": "array",
          "items": true
        },
        "hosted_feed": {
          "description": "Compatibility-only hosted-feed configuration. It remains parseable but the OSS runtime does not fetch or merge it.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/HostedFeedConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "resolver": {
          "description": "Resolver tuning (rDNS toggle, bot-auth toggle, cache size). Each field has a sensible default; this block is rarely needed.",
          "default": {
            "rdns_enabled": true,
            "bot_auth_keyid_enabled": true,
            "cache_size": 10000
          },
          "allOf": [
            {
              "$ref": "#/definitions/AgentClassResolverConfig"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "HostedFeedConfig": {
      "description": "Hosted-feed source for the agent-class catalog.\n\nReserved so YAML written against the `hosted-feed` or `merged` shapes parses cleanly. The OSS runtime does not fetch, refresh, or verify this feed; selecting either catalog value warns and falls back to the embedded defaults.",
      "type": "object",
      "required": [
        "url"
      ],
      "properties": {
        "url": {
          "description": "Reserved feed URL. It is accepted but not fetched or validated by the OSS runtime.",
          "type": "string"
        },
        "bootstrap_keys": {
          "description": "Reserved bootstrap public keys. They are accepted but no signature verification is installed in the OSS runtime.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "AgentClassResolverConfig": {
      "description": "Resolver-tuning knobs for the agent-class chain.\n\nAll fields have sensible defaults: rDNS verification on, bot-auth keyid lookup on, 10 000-entry verdict cache. Operators set fields only when they need to disable a specific signal (typically rDNS in environments without a working PTR resolver).",
      "type": "object",
      "properties": {
        "rdns_enabled": {
          "description": "Run forward-confirmed reverse-DNS as resolver step 2. Default `true`. Disable when the runtime has no working DNS resolver.",
          "default": true,
          "type": "boolean"
        },
        "bot_auth_keyid_enabled": {
          "description": "Honor the verified Web Bot Auth `keyid` as resolver step 1. Default `true`. Off forces the resolver to fall through to rDNS / UA matching even when bot-auth verified the request.",
          "default": true,
          "type": "boolean"
        },
        "cache_size": {
          "description": "Per-process verdict cache capacity (rDNS verdicts only). 10 000 is the default; bump for very high-cardinality IP populations.",
          "default": 10000,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "RateLimitsConfig": {
      "description": "WOR-1130: top-level workspace rate-limit budget configuration.",
      "type": "object",
      "properties": {
        "workspace_default": {
          "description": "Budget applied to the default workspace (the only workspace in the OSS single-tenant build; enterprise multi-tenant resolves a per-tenant budget).",
          "default": {
            "http_rps_sustained": 1000,
            "http_rps_burst": 2000,
            "soft_threshold_rps": null
          },
          "allOf": [
            {
              "$ref": "#/definitions/WorkspaceBudgetConfig"
            }
          ]
        },
        "escalation": {
          "description": "Throttle -> auto-suspend escalation tuning.",
          "default": {
            "abuse_threshold_throttle_to_suspend": 1000,
            "auto_suspend_cooldown_secs": 3600
          },
          "allOf": [
            {
              "$ref": "#/definitions/RateLimitEscalationConfig"
            }
          ]
        },
        "clock": {
          "description": "Clock source for the token-bucket refill + suspend cool-down. `system` (default) uses wall time; `manual` advances only via the `/api/rate_limits/clock/advance` admin endpoint (tests).",
          "default": "system",
          "allOf": [
            {
              "$ref": "#/definitions/RateLimitClockMode"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "WorkspaceBudgetConfig": {
      "description": "WOR-1130: the per-workspace request budget.",
      "type": "object",
      "properties": {
        "http_rps_sustained": {
          "description": "Sustained inbound HTTP requests-per-second ceiling (the token bucket refill rate).",
          "default": 1000,
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "http_rps_burst": {
          "description": "Burst ceiling (the token bucket capacity). Requests above this within one window are throttled.",
          "default": 2000,
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "soft_threshold_rps": {
          "description": "Soft observation threshold. Traffic above this but below the sustained ceiling emits `sbproxy_rate_limit_total{result=\"soft\"}` without throttling, so operators see the climb early.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "RateLimitEscalationConfig": {
      "description": "WOR-1130: throttle -> auto-suspend escalation tuning.",
      "type": "object",
      "properties": {
        "abuse_threshold_throttle_to_suspend": {
          "description": "Consecutive-throttle count that promotes a workspace from `Throttle` to `AutoSuspend`. A2.5 default is 1000.",
          "default": 1000,
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "auto_suspend_cooldown_secs": {
          "description": "Cool-down (seconds) a workspace stays auto-suspended before it drops back to `Throttle`. A2.5 default is 3600.",
          "default": 3600,
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "RateLimitClockMode": {
      "description": "WOR-1130: clock source for the rate-limit budget.",
      "oneOf": [
        {
          "description": "Wall-clock time (production default).",
          "type": "string",
          "enum": [
            "system"
          ]
        },
        {
          "description": "Test clock advanced only via the admin endpoint.",
          "type": "string",
          "enum": [
            "manual"
          ]
        }
      ]
    },
    "AuditConfig": {
      "description": "Where the audit trail is durably recorded.",
      "type": "object",
      "properties": {
        "sink": {
          "description": "Which durable form the audit trail takes. The in-memory ring and the `config_audit` / `security_audit` / `key_audit` tracing targets are unconditional and are not what this selects.",
          "default": "memory",
          "allOf": [
            {
              "$ref": "#/definitions/AuditSinkKind"
            }
          ]
        },
        "path": {
          "description": "Chain file for the `chain` sink. Required when `sink: chain`; refused otherwise, because a path that names no file is a deployment that believes it has a trail. Parent directories are created at boot.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "sign_with": {
          "description": "Signing identity for the `chain` sink. The only value this build resolves is [`ATTESTATION_SIGN_WITH_WEB_BOT_AUTH`], and that block must be present. Required when `sink: chain`; refused otherwise.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "config_path": {
          "description": "Optional path where `config_audit` events are chained. Opt-in; when absent, `config_audit` remains a tracing stream and is not durably recorded, preserving exactly the old behavior. Requires `sink: chain`. Must differ from `path`, `key_path`, and `admin_path`: every audit channel has a different payload format and verifies independently.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "key_path": {
          "description": "Optional path where `key_audit` mutations are chained (WOR-2478). Opt-in, same terms as `config_path`. The chained record is metadata plus a keyed-HMAC fingerprint of each before/after field, never the raw diff `key_audit`'s tracing target carries; see `sbproxy_observe::audit`'s module docs. Requires `sink: chain`. Must differ from `path`, `config_path`, and `admin_path`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "admin_path": {
          "description": "Optional path where authenticated admin-console actions are chained (WOR-2478): mutating admin API calls, logins, and content inspection, the same events the `sbproxy::admin::audit` tracing target and the admin ring's `admin` channel already carry. Opt-in, same terms as `config_path`. Requires `sink: chain`. Must differ from `path`, `config_path`, and `key_path`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "AuditSinkKind": {
      "description": "Accepted audit sink names.",
      "oneOf": [
        {
          "description": "No durable trail. Events reach the bounded in-memory ring behind `/api/audit/events` and the tracing targets, and both are lost when the process is.",
          "type": "string",
          "enum": [
            "memory"
          ]
        },
        {
          "description": "Retained for the error message. `compile_config` refuses this value: emission to the tracing targets is unconditional and always was, so selecting it never selected anything. Use `memory` for the same behavior under an honest name, or `chain` for a trail that survives a restart.",
          "type": "string",
          "enum": [
            "tracing"
          ]
        },
        {
          "description": "Append every `security_audit` event to a SHA-256 hash-chained, Ed25519-signed file at `path`, signed by the identity `sign_with` names. Editing or removing a record breaks the chain, and `sbproxy audit verify` re-derives it from genesis. `config_path`, `key_path`, and `admin_path` opt the `config_audit`, `key_audit`, and admin-console channels into their own chain files under the same signing identity (WOR-2478).",
          "type": "string",
          "enum": [
            "chain"
          ]
        }
      ]
    },
    "EgressTopLevelConfig": {
      "description": "Top-level `egress:` section: operator-authored allowlists that arm the per-purpose egress gates (WOR-2476), plus the OTLP exporter gate (WOR-2481).\n\nReuses the mode/hosts/allow_private vocabulary the per-tool MCP/OpenAPI `egress:` block already ships (`sbproxy_extension::mcp::egress::EgressPolicy`); this crate cannot depend on `sbproxy-extension` (that dependency runs the other way), so the shape is redeclared here rather than shared by type.\n\nEvery sub-block is independently optional. A purpose whose sub-block is omitted stays legacy ungated: `AiClient`'s documented `None` contract, the classifier hooks' legacy ungated dispatch, the usage sinks' unauthenticated dispatch, the model-artifact fetcher's unauthenticated download, the non-MCP token-exchange resolver, and the OTLP exporters all keep behaving exactly as they did before this section existed. `compile_config` compiles each configured sub-block into a [`sbproxy_security::egress::EgressAuthorizer`] once, on [`crate::snapshot::CompiledConfig::egress`]; nothing downstream parses this raw struct directly.",
      "type": "object",
      "properties": {
        "ai_providers": {
          "description": "Arms `EgressPurpose::AiProvider`: every upstream AI provider dispatch the AI gateway's client makes.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/EgressPurposeConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "agent_orchestration": {
          "description": "Arms `EgressPurpose::AgentOrchestration`: HTTP invocations made by configured AI toolkit workflows. Agent endpoints fail closed unless this purpose is configured with `mode: deny_by_default`.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/EgressPurposeConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "classifier_hooks": {
          "description": "Arms `EgressPurpose::ClassifierHook`: the stock intent and prompt-aware provider-quality classifier RPCs.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/EgressPurposeConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "usage_sinks": {
          "description": "Arms Langfuse, Datadog, and object-store usage-sink deliveries under `EgressPurpose::UsageSink`, and webhook deliveries under `EgressPurpose::Webhook` (a separate, pre-existing purpose the webhook sink authorizes under internally): one config knob, two purposes armed from the same allowlist.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/EgressPurposeConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "model_artifacts": {
          "description": "Arms `EgressPurpose::ModelArtifact`: the model-host artifact fetcher's HTTP downloads.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/EgressPurposeConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "token_exchange": {
          "description": "Arms `EgressPurpose::TokenExchange`: every OAuth token-endpoint call this proxy makes, the non-MCP outbound credential resolver's and the MCP run-as-user token exchange's (`sbproxy_extension::mcp::auth`) alike. A per-server `egress:` block gates that server's upstream connects and OpenAPI tool calls and does not reach this purpose, so this sub-block is the only way to arm a token endpoint.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/EgressPurposeConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "federation": {
          "description": "Arms `EgressPurpose::Federation`: the OpenID Federation fetcher's entity-configuration and subordinate-statement GETs (`crates/sbproxy-federation`). Unlike every other sub-block here, omitting this one does not leave the purpose unguarded: the fetcher refuses a peer that resolves to a private, loopback, or link-local address whether or not an allowlist is armed, and it never follows a redirect it has not re-authorized. What this sub-block adds on top is the host, scheme, and port allowlist, so a federation peer outside the trust anchors an operator wrote down cannot be dialed at all.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/EgressPurposeConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "telemetry": {
          "description": "Arms `EgressPurpose::Telemetry` (WOR-2481): the OTLP trace, metric, and log exporter endpoints. Authorized once at boot, where each exporter is constructed. A config reload re-verifies the already-running trace and metric exporters' endpoints against the new allowlist and refuses the reload if either is now denied; the log exporter is rebuilt on every reload and re-authorizes itself then.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/EgressPurposeConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "EgressPurposeConfig": {
      "description": "One purpose's allowlist under the top-level `egress:` section.\n\nShares its vocabulary with the per-tool MCP/OpenAPI `egress:` block; see `sbproxy_extension::mcp::egress::EgressPolicy` for the enforcement semantics this compiles to. `hosts` (exact match), `ports`, and `allow_private` are supported here; the per-tool block's `suffixes` has no equivalent in this section.",
      "type": "object",
      "properties": {
        "mode": {
          "description": "Default behavior for hosts that do not match `hosts`. `allow_by_default` (the default) is inert: the purpose stays legacy ungated even though `hosts` is present, mirroring the per-tool block's `EgressMode::AllowByDefault` short-circuit. Set `deny_by_default` to actually arm the gate.",
          "default": "allow_by_default",
          "allOf": [
            {
              "$ref": "#/definitions/EgressPurposeMode"
            }
          ]
        },
        "hosts": {
          "description": "Exact hostnames, compared case-insensitively. Ignored under `allow_by_default`.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "allow_private": {
          "description": "When true, resolved private/link-local addresses are permitted for hosts on this allowlist (operator opt-in).",
          "default": false,
          "type": "boolean"
        },
        "ports": {
          "description": "Permitted destination ports. Defaults to `[80, 443]`, the scheme-standard HTTP/HTTPS ports most sub-blocks dial. An override is required for a purpose that does not: `telemetry`'s OTLP endpoint is commonly `4317` (gRPC) or `4318` (HTTP), never `80`/`443`, so the default here would refuse every destination that sub-block reaches with `DisallowedPort` and there would be no `hosts:` fix an operator could make to recover. Refused if present but empty, or if it names port `0`: either would silently refuse every destination this purpose reaches with no indication why.",
          "default": [
            80,
            443
          ],
          "type": "array",
          "items": {
            "type": "integer",
            "format": "uint16",
            "minimum": 0.0
          }
        }
      },
      "additionalProperties": false
    },
    "EgressPurposeMode": {
      "description": "Egress behavior when a destination host does not match `hosts` (mirrors `sbproxy_extension::mcp::egress::EgressMode`, minus its `enforce` alias).",
      "oneOf": [
        {
          "description": "Only explicitly listed `hosts` may be contacted; compiles to a real `EgressAuthorizer` that fails closed.",
          "type": "string",
          "enum": [
            "deny_by_default"
          ]
        },
        {
          "description": "All hosts may be contacted. Legacy default: an omitted `mode:`, or an omitted sub-block entirely, compiles to no authorizer at all.",
          "type": "string",
          "enum": [
            "allow_by_default"
          ]
        }
      ]
    },
    "SessionLedgerConfig": {
      "description": "WOR-1186: session-ledger emission configuration.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Turn ledger emission on. When false (the default), the `tools/call` path pays a single atomic load and emits nothing.",
          "default": false,
          "type": "boolean"
        },
        "sink": {
          "description": "Where ledger records go.",
          "default": "logging",
          "allOf": [
            {
              "$ref": "#/definitions/SessionLedgerSinkKind"
            }
          ]
        },
        "path": {
          "description": "NDJSON output path for the `file` sink. Required when `sink: file`; ignored otherwise.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "SessionLedgerSinkKind": {
      "description": "WOR-1186: session-ledger sink kinds.",
      "oneOf": [
        {
          "description": "Emit each record as a structured `session_ledger` tracing line.",
          "type": "string",
          "enum": [
            "logging"
          ]
        },
        {
          "description": "Append each record as one NDJSON line to `path`.",
          "type": "string",
          "enum": [
            "file"
          ]
        }
      ]
    },
    "RequestEventsConfig": {
      "description": "Request-event egress configuration.",
      "type": "object",
      "properties": {
        "sink": {
          "description": "Which backend receives each completed request event.",
          "default": "none",
          "allOf": [
            {
              "$ref": "#/definitions/RequestEventSinkKind"
            }
          ]
        },
        "path": {
          "description": "NDJSON output path for the `file` sink. Required when `sink: file`; ignored otherwise.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "nats": {
          "description": "Broker settings for `sink: nats`. Ignored otherwise.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/RequestEventsNatsConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "clickhouse": {
          "description": "Warehouse settings for `sink: clickhouse`. Ignored otherwise.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/RequestEventsClickHouseConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "watermark_store_path": {
          "description": "Path to an embedded store holding the delivery watermark, so an operator reconciling a broker or a warehouse against the proxy has a checkpoint that survives a restart. Absent means no watermark is kept, which costs nothing and answers nothing.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "queue_capacity": {
          "description": "Bound on the hand-off queue between the request path and the delivery worker, for the `nats` and `clickhouse` sinks. A full queue drops the incoming event and counts the drop rather than making a request wait on a broker.",
          "default": 8192,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "RequestEventSinkKind": {
      "description": "Request-event sink kinds.",
      "oneOf": [
        {
          "description": "Discard every event. The default, and the only behavior the proxy had before the block existed.",
          "type": "string",
          "enum": [
            "none"
          ]
        },
        {
          "description": "Emit each event as a structured `request_event` tracing line.",
          "type": "string",
          "enum": [
            "logging"
          ]
        },
        {
          "description": "Append each event as one NDJSON line to `path`.",
          "type": "string",
          "enum": [
            "file"
          ]
        },
        {
          "description": "Publish one JSON message per event to a NATS subject tree. Requires `request_events.nats`.",
          "type": "string",
          "enum": [
            "nats"
          ]
        },
        {
          "description": "Insert batches into a ClickHouse table over its HTTP interface. Requires `request_events.clickhouse`.",
          "type": "string",
          "enum": [
            "click_house"
          ]
        }
      ]
    },
    "RequestEventsNatsConfig": {
      "description": "Broker settings for `request_events.sink: nats`.\n\nThe address is `host:port`, not a URL: the core NATS protocol this speaks is plain TCP, and a `nats://` string would suggest a URL parser that is not there.",
      "type": "object",
      "required": [
        "address"
      ],
      "properties": {
        "address": {
          "description": "`host:port` of the broker.",
          "type": "string"
        },
        "subject_prefix": {
          "description": "Prefix every subject starts with. The published subject is `<prefix>.<workspace_id>.<event_type>`, with the workspace id sanitized so it cannot add a level or name a wildcard.",
          "default": "sb.events",
          "type": "string"
        },
        "token": {
          "description": "Secret reference for the broker's authentication token, resolved through `proxy.secrets`. A literal here is refused the same way every other credential reference is.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "RequestEventsClickHouseConfig": {
      "description": "Warehouse settings for `request_events.sink: clickhouse`.",
      "type": "object",
      "required": [
        "url"
      ],
      "properties": {
        "url": {
          "description": "HTTP endpoint, for example `http://clickhouse.internal:8123`.",
          "type": "string"
        },
        "database": {
          "description": "Database name. Refused unless it matches `[A-Za-z0-9_]+`.",
          "default": "sbproxy",
          "type": "string"
        },
        "table": {
          "description": "Table name. Refused unless it matches `[A-Za-z0-9_]+`. The proxy never applies DDL; create the table first.",
          "default": "sbproxy_request_events",
          "type": "string"
        },
        "user": {
          "description": "Optional user.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "password": {
          "description": "Secret reference for the password, resolved through `proxy.secrets`.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "EventsConfig": {
      "description": "Egress for the typed proxy events.\n\nDistinct from [`RequestEventsConfig`], and the difference is the stream rather than the transport. `request_events:` carries the capture envelope: one fully populated record per terminating request, for analytics and billing reconciliation. `events:` carries the lifecycle events an operator wants to react to (a policy denial, an auth failure, a config reload) and can filter down to just those, so a SIEM subscription does not cost one delivery per request served.\n\nDelivery never runs on the request path. The publish site puts the event on a bounded queue and returns; a background worker owns the file handle or the HTTP client. When the queue is full the event is dropped and counted on `sbproxy_events_dropped_total`, because the alternative is a slow SIEM adding latency to every denied request.\n\nShutdown does not flush; see `docs/events.md`.",
      "type": "object",
      "properties": {
        "sink": {
          "description": "Which backend receives each selected event.",
          "default": "none",
          "allOf": [
            {
              "$ref": "#/definitions/EventSinkKind"
            }
          ]
        },
        "path": {
          "description": "NDJSON output path for the `file` sink. Required when `sink: file`; refused otherwise, because a path nothing writes to describes a deployment that believes it has an event log.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "url": {
          "description": "Destination URL for the `webhook` sink. Required when `sink: webhook`; refused otherwise. Validated against the SSRF guard at boot and again before every batch.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "signing_secret": {
          "description": "Shared secret the `webhook` sink signs each batch with (HMAC-SHA256, sent as `X-Sbproxy-Signature: v1=<hex>` over `<timestamp>.<body>`, the same construction the alert webhook uses).\n\nAccepts a secret reference (`${VAR}`, `file:`, `secret://`, `vault://`, and the other backend URI schemes) and is the only way to give the sink a credential: there is no plaintext alternative field. Optional, but an unsigned POST is a payload any host that can reach the endpoint can forge.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "types": {
          "description": "Which of the eighteen event types to deliver. Empty or absent means all of them.\n\nNames are the snake_case wire names (`policy_denied`, `auth_denied`, `config_reloaded`, ...). An unrecognized name is refused with the accepted list rather than skipped, so a typo cannot present as a sink that is working and quiet.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "fail_closed": {
          "description": "Event type names for which delivery must never be silently dropped (WOR-2384). Empty by default, meaning every type keeps the best-effort, drop-and-count contract every other event type has.\n\nNames come from the same closed set `types:` accepts, and an unrecognized one is refused the same way, with the accepted list in the message. A name here does not have to also appear in `types:`, but an operator who lists one without also selecting it there has configured every governed call to be refused: nothing would ever be able to deliver that type, and a caller listed here treats \"nothing can deliver this\" the same as \"delivery failed\".\n\nThe only publisher that reads this today is the MCP tool-call funnel, for `mcp_governance_decision`: when the type is named here and the record cannot be queued, the tool call is refused with a JSON-RPC internal error rather than served un-evidenced.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "queue_capacity": {
          "description": "Bound on the hand-off queue between the publish site and the delivery worker. Defaults to 4096. Refused when `sink: none`, and refused at zero, which would drop every event while looking configured.",
          "default": null,
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "EventSinkKind": {
      "description": "Accepted `events.sink` values.\n\nKafka, NATS, and EventBridge are not here. Each needs a client library, a partitioning decision, and a delivery-guarantee story that a bounded queue and a best-effort POST do not have. They are follow-ups, and until they land their names are refused by the deserializer rather than accepted into a sink that would not deliver.",
      "oneOf": [
        {
          "description": "Publish nothing. The default, and the only behavior the proxy had before the block existed.",
          "type": "string",
          "enum": [
            "none"
          ]
        },
        {
          "description": "Append each event as one NDJSON line to `path`.",
          "type": "string",
          "enum": [
            "file"
          ]
        },
        {
          "description": "POST batches of events to `url`.",
          "type": "string",
          "enum": [
            "webhook"
          ]
        }
      ]
    },
    "FeatureFlagConfig": {
      "description": "One process-wide feature flag exposed to CEL.",
      "type": "object",
      "required": [
        "name"
      ],
      "properties": {
        "name": {
          "description": "Unique name passed as the first argument to `flag_enabled`.",
          "type": "string"
        },
        "default": {
          "description": "Value returned when none of the configured rules match.",
          "default": false,
          "type": "boolean"
        },
        "rules": {
          "description": "Allow/block lists and sticky rollout rules.",
          "default": {
            "allow_list": [],
            "block_list": [],
            "rollout_percent": 0
          },
          "allOf": [
            {
              "$ref": "#/definitions/FeatureFlagRuleConfig"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "FeatureFlagRuleConfig": {
      "description": "Rules for a process-wide feature flag.",
      "type": "object",
      "properties": {
        "allow_list": {
          "description": "Bucketing keys that always evaluate to true.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "block_list": {
          "description": "Bucketing keys that always evaluate to false.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "rollout_percent": {
          "description": "Sticky rollout cutoff in the inclusive range 0..=100.",
          "default": 0,
          "type": "integer",
          "format": "uint32",
          "maximum": 100.0,
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "UpdateConfig": {
      "description": "The `update:` block: how `sbproxy update` behaves (WOR-1804).\n\nPinning always wins. A `path` / `brew` / `apt`-managed artifact, or one pinned to an explicit version or digest, is reported but never replaced unless a run explicitly targets it. A background check (see `auto`) only ever reports; applying an update is always an explicit `sbproxy update` run.",
      "type": "object",
      "properties": {
        "channel": {
          "description": "Release stream the binary and managed engines follow.",
          "default": "stable",
          "allOf": [
            {
              "$ref": "#/definitions/UpdateChannel"
            }
          ]
        },
        "auto": {
          "description": "When true, a background freshness check runs every `check_interval` and reports to `sbproxy doctor` and the logs. A background check never replaces an artifact; it only reports.",
          "default": false,
          "type": "boolean"
        },
        "check_interval_secs": {
          "description": "How often the background check runs, in seconds. Accepts a humanized duration (`6h`, `1d`) or bare seconds. Only consulted when `auto` is true. Defaults to once a day.",
          "default": 86400,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "UpdateChannel": {
      "description": "Which release stream `sbproxy update` follows for the binary and the managed inference engines (WOR-1804).",
      "oneOf": [
        {
          "description": "Track the latest stable release. The default.",
          "type": "string",
          "enum": [
            "stable"
          ]
        },
        {
          "description": "Track the newest release, pre-releases included.",
          "type": "string",
          "enum": [
            "latest"
          ]
        },
        {
          "description": "Never move automatically. Every artifact is held; only an `sbproxy update` run that explicitly targets an artifact may replace it.",
          "type": "string",
          "enum": [
            "pinned"
          ]
        }
      ]
    },
    "OriginSourcesConfig": {
      "description": "Top-level `origin_sources:` block: which project repositories the aggregator pulls, and the tier its pinning rule is judged against.\n\nA project repository commits a hostless origin profile ([`crate::origin_profile::OriginProfile`]). It never names a hostname, because a hostname is an environment fact. This block is where the runtime config supplies the facts the project does not have: the hosts each declared profile origin answers on, the values for the inputs the profile declares, and the last word through `overrides:`.\n\nThe whole block is on [`crate::AUTHORITY_DENIED_PATHS`]. `source` is denied because a fragment that can set it redirects the fleet at one repository; this block names N repositories whose Lua, WASM and JS bodies the `{{ }}` interpolator deliberately never reads, so an authority able to write it turns into arbitrary code fetch on every node that trusts it.",
      "type": "object",
      "properties": {
        "tier": {
          "description": "Which tier this runtime config document is. See [`EnvironmentTier`].",
          "default": "development",
          "allOf": [
            {
              "$ref": "#/definitions/EnvironmentTier"
            }
          ]
        },
        "aggregator": {
          "description": "How often the aggregator polls, how long it waits before composing, and how many repositories it reads at once.",
          "default": {
            "poll_interval_secs": 120,
            "debounce_secs": 15,
            "max_deferral_secs": 120,
            "concurrency": 8,
            "deadline_secs": 300
          },
          "allOf": [
            {
              "$ref": "#/definitions/OriginAggregatorConfig"
            }
          ]
        },
        "entries": {
          "description": "The project repositories composed into `origins:`.",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/OriginSourceEntry"
          }
        }
      },
      "additionalProperties": false
    },
    "EnvironmentTier": {
      "description": "The tier of the runtime config document, which is what the production pinning rule keys off.\n\nDeliberately a property of the runtime document rather than of an entry. A rule that read the entry own `environment:` field would be no rule at all: an entry that wants to track a branch would simply write `environment: dev` and be granted what it asked for. A self-declared constraint is not a constraint. The entry `environment:` selects which `environments:` layer of the profile applies, and nothing more.",
      "oneOf": [
        {
          "description": "The default. An entry may follow a branch, or pin nothing at all and follow the default branch.",
          "type": "string",
          "enum": [
            "development"
          ]
        },
        {
          "description": "Every entry must pin an immutable revision: a full commit sha, or a tag spelled `refs/tags/<name>`. A bare name is refused because git does not tell a tag from a branch by spelling, and a rule that guessed would be a rule a branch could walk through.",
          "type": "string",
          "enum": [
            "production"
          ]
        }
      ]
    },
    "OriginAggregatorConfig": {
      "description": "Timings and bounds for `sbproxy aggregate` (WOR-2437, WOR-2438).\n\nInside `origin_sources` rather than under `proxy:` for the same reason [`EnvironmentTier`] is: this block is on [`crate::AUTHORITY_DENIED_PATHS`], so an authority cannot reach in and set a poll interval on a fleet that did not ask for one.\n\nEvery default here is stated in `docs/configuration.md` alongside what it costs in requests per hour per repository, because that number is the one a platform team gets asked about by whoever runs the git server.",
      "type": "object",
      "properties": {
        "poll_interval_secs": {
          "description": "How often each unpinned entry is asked whether its revision moved, in seconds.\n\nOne `git ls-remote` per unpinned entry per interval, so the cost is `3600 / poll_interval_secs` requests per hour per repository. The default of 120 is Argo CD's `--app-resync` default and gives 30 requests per hour per repository. An entry pinned to a full commit sha is polled zero times, because a sha cannot move.",
          "default": 120,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "debounce_secs": {
          "description": "How long a moved entry waits for others to move before the aggregator composes, in seconds.\n\nProject repositories merge on their own cadences and nothing coordinates them, so three teams merging inside one minute would otherwise be three published revisions and three fleet-wide pipeline rebuilds. Zero composes immediately.",
          "default": 15,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "max_deferral_secs": {
          "description": "The ceiling on that wait, in seconds.\n\nA continuously-changing entry would otherwise reset the debounce window forever and never publish at all. Measured from the first movement in the current window, not from the last.",
          "default": 120,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "concurrency": {
          "description": "How many repositories are fetched at once.\n\nSerial resolution with per-entry timeouts means fifty entries can hold one compose open for fifty times one timeout, so the pool is bounded rather than absent and rather than unbounded.",
          "default": 8,
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "deadline_secs": {
          "description": "Hard deadline for all of one compose's fetches, in seconds.\n\nDistinct from the per-entry `timeout_secs`: that one bounds a single repository, this one bounds the whole round, so a pool of slow-but-not-timing-out repositories cannot hold a compose open past it.",
          "default": 300,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "OriginSourceEntry": {
      "description": "One project repository, and the runtime facts that deploy it.\n\nThe git fields are the [`ConfigSource::Git`] set rather than a narrower struct of their own. Omitting `credential` would mean no private project repositories, omitting `verify_signature` would take away the check the whole pinning trust story leans on, and omitting `timeout_secs` would mean one unreachable project repository can hold a compose open.\n\nTwo `ConfigSource::Git` fields are deliberately absent. `refresh_interval_secs` is the aggregator poll cadence and belongs to the aggregator rather than to one entry. `confine` is absent because a project profile is **always** confined: the flag exists so an operator can opt a repository they own into the boundary, and there is no repository here the operator authored.",
      "type": "object",
      "required": [
        "name",
        "path",
        "repo"
      ],
      "properties": {
        "name": {
          "description": "Stable name for this entry, unique within `origin_sources`. Every refusal the composition raises names it, so make it the name an operator would recognize.",
          "type": "string"
        },
        "repo": {
          "description": "Repository URL, in any form `git clone` accepts.",
          "type": "string"
        },
        "revision": {
          "description": "Branch, tag, or full commit sha this entry is pinned to. Absent follows the default branch, which [`EnvironmentTier::Production`] refuses.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "path": {
          "description": "Path inside the repository to the profile document, relative to the repository root. Conventionally `sbproxy/origin.yaml`.",
          "type": "string"
        },
        "credential": {
          "description": "Optional credential reference for a private repository, as `env:NAME`, `${NAME}`, `file:/path`, or `secret://backend/name`. An inline literal is refused, exactly as `source.credential` refuses one: a token in a config file is a token in every copy of that file.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "verify_signature": {
          "description": "Require a valid signature on the resolved tag or commit. Off by default, because most repositories are not signed.",
          "default": false,
          "type": "boolean"
        },
        "timeout_secs": {
          "description": "Hard timeout for one fetch of this repository, in seconds.",
          "default": 60,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "environment": {
          "description": "Which `environments:` layer of the profile applies. Absent means the profile `base:` layer alone. This selects a layer and grants nothing; see [`EnvironmentTier`].",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "hosts": {
          "description": "Hosts each declared profile origin answers on, keyed by the profile origin name.\n\nA map rather than a bare list because a profile may declare more than one origin from day one: an API host plus a webhook host is the common case, and changing this from a list to a map later would break every committed entry.",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "inputs": {
          "description": "Values for the inputs the profile declares.\n\nA value lands in the profile document through `{{vars.NAME}}` and is then checked as document text, so a host-backed secret reference (`env:NAME`, `file:/path`, `vault://env/NAME`) is refused here just as it is refused anywhere else in an externally authored document. Supply a provider URI (`secret://backend/name`) instead: it resolves only against a backend the operator declared under `proxy.secrets`, which is a path no project can write.",
          "default": {},
          "type": "object",
          "additionalProperties": true
        },
        "overrides": {
          "description": "The runtime last word for this entry, layered after everything the project wrote.\n\nSame untyped origin shape as `origin_defaults`, and merged the same way. The runtime bookends the stack, so a project can be given room without being given the last word.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": true
        }
      },
      "additionalProperties": false
    }
  }
}
