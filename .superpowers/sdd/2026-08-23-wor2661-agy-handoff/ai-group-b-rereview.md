# AI Group B independent adversarial re-review

## Verdict

**NOT APPROVED.** There are 0 Blocker, 5 Major, and 0 Minor findings.

## Adversarial review

Reviewer: fresh independent static re-review

Findings: 0 Blocker, 5 Major, 0 Minor

Verification: Read the repository instructions, review rubric, handoff plan, original independent review, and complete review-fix report. Inspected the full frozen Group B diff from `8125538e`, both Group B commits, the current dirty fixes, and the final relevant files. Reconciled every original finding and performed production-only mutation analysis against the exact focused selection. `git diff --check 8125538e` and `git diff --check` both pass. Per the review assignment, no Cargo, Rust, or rustfmt command was run; the fix report's 9/9 GREEN and 1/8 combined-mutation result are historical evidence, not results rerun by this reviewer.

### Blocker

None.

### Major

- Major - `crates/sbproxy-classifier/src/registry.rs:81` - missing or empty tenant ids still emit one release-level warning per public TCP classify request, while the new warning regression exercises only a non-empty unknown id. `handle_classify` passes the wire's optional tenant directly to `Registry::get` at `tcp.rs:443-449`; the `None`/empty branch warns here before returning the already-metered `tenant_not_registered` refusal. However, `repeated_unknown_tenants_return_bounded_sanitized_errors` sets `Some(hostile_tenant)` at `tcp.rs:819-823`, which takes the non-empty lookup branch at `registry.rs:76-79` and can never observe this warning. Failure scenario: an unauthenticated client keeps a public TCP connection open and repeatedly sends small `classify` frames with the tenant omitted or empty, producing one WARN per request and flooding the production log channel even though routine unknown-tenant refusals were meant to move to `debug!`. Cover both omitted and empty tenants through the real MessagePack round trip and make this expected refusal non-warning. Not fixed here; this was a read-only re-review.

- Major - `crates/sbproxy-classifier/src/health.rs:83` - the new HTTP admission, parser, and whole-connection deadline refusals are not scrapeable, so an incident that disables the sidecar's own health and metrics surface leaves no production evidence. Slot exhaustion silently drops the accepted socket at lines 83-85, while parser failures and deadline expiry exist only in `debug!` calls at lines 93-100; the repository rubric records that debug/trace is compiled out of release builds. The same gap remains for an oversized TCP frame, which closes at `tcp.rs:277-279` without `record_error`, unlike frame-budget exhaustion at `tcp.rs:288-298` and malformed MessagePack at `tcp.rs:310-315`. This conflicts with the stable `sbproxy_classifier_errors_total` contract at `sbproxy-observe/src/metric_registry.rs:1036-1044`, which describes rich-sidecar requests that could not complete, and with the dashboard's errors-by-reason panel. Failure scenario: 128 slow HTTP clients occupy every five-second slot and the 129th liveness, readiness, or scrape connection is dropped; after recovery, neither the counter nor release logs distinguish admission saturation, deadline expiry, or malformed/oversized input from a network outage. Record these refusals with closed transport/reason labels and prove counter movement through the actual listeners. Not fixed here; this was a read-only re-review.

- Major - `crates/sbproxy-classifier/src/auth.rs:60` - the security-sensitive auth-file mutation proof does not independently pin `O_NOFOLLOW` or same-descriptor validation. Production currently opens once with `O_NONBLOCK | O_NOFOLLOW`, obtains metadata from that file, and reads the same file, which is correct. The two new regressions cover a regular inode growing after metadata at lines 280-305 and a no-writer FIFO at lines 309-325. The combined mutation removed nonblocking/no-follow opening and regular-descriptor rejection together, so the FIFO blocks before it can distinguish those properties. A production-only mutation that drops only `O_NOFOLLOW` while retaining `O_NONBLOCK`, `file.metadata()`, and the regular-file check follows a final-component symlink yet leaves both tests unchanged. Likewise, a refactor that reopens the pathname after metadata is not distinguished by the growth hook because it writes the same pathname and inode. Add a final-component symlink refusal and a post-open pathname-replacement test that must still read/validate the original descriptor, then kill the no-follow and reopen mutations separately from the FIFO mutation. Not fixed here; this was a read-only re-review.

- Major - `crates/sbproxy-classifier/src/health.rs:79` - defensive HTTP `serve_on` validation is correct in production but is not isolated by any selected regression or mutation. `main::tests::invalid_http_limits_cannot_publish_readiness` at `main.rs:425-448` reaches only `bind_required_listeners`, and the only direct `health::serve_on` regression at `health.rs:398-412` supplies valid limits. The combined mutation also restored both parser defects, so its header-test failures cannot prove this line was killed. The exact production-only mutation that removes only `limits.validate()?` leaves the nine-test selection behavior unchanged: the pre-bind helper still refuses its invalid case, the parser tests do not call `serve_on`, and the slowloris test uses one connection and a nonzero deadline. Failure scenario: a future direct caller passes zero connections and gets an accept-and-drop loop rather than an error, or passes a zero deadline and makes every probe race an immediate expiry, while all current focused tests stay green. Add a direct task-bounded `health::serve_on` test for zero/max-plus-one connection and deadline bounds, analogous to the TCP defensive validation test, and kill this mutation independently. Not fixed here; this was a read-only re-review.

- Major - `crates/sbproxy-classifier/src/tcp.rs:302` - the aggregate-frame regression observes a synthetic declared-byte counter rather than the actual payload allocation, so it does not kill the allocate-before-lease mutation the fix report says it proves. The probe increments by `msg_len` at lines 302-304 and the real `vec![0u8; msg_len]` occurs separately at line 305. Exact surviving production-only mutation: move line 305 above the permit conversion/acquisition at lines 286-300 but leave the probe after acquisition. All five 4 MiB vectors are then allocated before admission; the fifth is dropped when its permit is refused before reaching the probe, while the first four still make `peak_bytes()` report exactly 16 MiB and `declared_frames()` still reports five. The public-listener test at lines 891-956 therefore remains green despite a real transient 20 MiB allocation. Put observation on the allocator call itself, encode allocation behind a lease-owning production type whose ordering the test can exercise, or use an equivalent real allocation boundary; then show that moving the actual allocation above acquisition fails independently. Not fixed here; this was a read-only re-review.

### Minor

None.

## Original findings rechecked

- Aggregate TCP ceiling: the current production ordering is sound. `TcpLimits` accepts only 1 through 128, `tcp::serve_on` validates before accepting, public and admin listeners share one 16 MiB semaphore created in `main.rs`, and each declared frame acquires its byte lease before `vec!`. The proof gap is reported above.
- gRPC backpressure terminal semantics: sound. The bounded verdict channel is awaited, terminal status has a separate one-item-by-construction channel chained after accepted verdicts, and the actual handler regression forbids clean EOF after saturation.
- Exact HTTP parsing: sound. Cap exhaustion returns the exact `InvalidData` error, truncated headers return the exact `UnexpectedEof` error, and only an explicit newline-terminated blank line completes headers. The socket-backed parser tests assert the server result and an empty response rather than accepting a client reset as proof.
- Admin-token loading: sound in current production. Unix opening is nonblocking/no-follow, regular-file type and mode come from the opened descriptor, and the 256 KiB-plus-one read uses that same descriptor. The missing independent security mutations are reported above.
- Pre-bind/readiness/serve validation: the pre-bind helper validates TCP and HTTP limits before its first bind and readiness publication, and TCP `serve_on` calls full validation. HTTP `serve_on` also validates today, but its proof gap is reported above.
- Operator docs: `docs/classifier-sidecar.md:266-306` accurately lists configurable/default maxima, fixed gRPC/TCP/HTTP/auth bounds, the shared 16 MiB frame budget, required blank HTTP terminator, and the Unix regular/nonblocking/no-follow/same-descriptor contract.
- Formatting: both static whitespace checks pass, and the original auth wrapping/blank-line plus health import-order defects are no longer present. Rustfmt itself was intentionally not invoked under this assignment.

## Additional checks and scope boundaries

- The current `auth.rs`, `grpc.rs`, `health.rs`, `main.rs`, `tcp.rs`, and classifier operator-document hashes exactly match the accepted-GREEN hashes recorded in the review-fix report. No mutation marker, conflict marker, or patch helper remains in the reviewed boundary.
- Root `llms.txt` hashes to `7252c3b9ce747fa302c8f099cb1bf788f4fb23ff`, exactly matching `66c0f252^:llms.txt`, and its filesystem/tree mode is the expected `100644`. It remains untracked because HEAD records its deletion, so packaging must explicitly add it; `git add -u` would omit this restoration.
- `crates/sbproxy-classifier/src/quality.rs` hashes to `89a8e2ec1feb953d5e3029f92a94b95e4ea0e1d5`, exactly matching both `337a7816` and the pre-Group-B blob, and has no status or diff entry.
- The mixed Group A portions of `66c0f252` remain excluded exactly as in the original independent review: `sbproxy-core/src/server/ai_dispatch.rs`, the module wiring files, and the `prompt_injection_v2` in-process/sidecar changes were not re-adjudicated here.
- Groups C through F remain unopened and unreviewed. This verdict covers only the complete frozen AI Group B boundary and its directly adjacent production/test seams.
